---
title: "User Status Module Specification"
description: "Complete technical specification including entities, APIs, business rules, and integration points"
---

The user-status module manages business-level availability indicators for users within organizations. This is distinct from real-time presence tracking and focuses on deliberate availability states that affect business operations like lead assignment and customer service routing.

<Note>
This module tracks whether users are ONLINE (available for work) or AWAY (unavailable), along with specific reasons for away status. It supports both manual status changes and automatic transitions based on business rules.
</Note>

## Overview

The module follows standard PropWise patterns with clear separation between manual and automatic status management:

- **Manual Status Changes**: Users can explicitly set themselves away with reasons like breaks or custom messages
- **Automatic Status Tracking**: System automatically transitions users based on inactivity, working hours, session expiry, and scheduled leave
- **Event-Driven Updates**: Listens to session lifecycle events to automatically restore availability when appropriate
- **Scheduled Jobs**: Cron-based background processes enforce time-based business rules

## Entity Structure

### UserStatus Entity

Represents per-user per-organization availability state.

<AccordionGroup>
<Accordion title="Entity Fields">

| Field | Type | Description |
|-------|------|-------------|
| id | UUID | Primary key |
| user | User | ManyToOne relationship to User entity |
| organization | Organization | ManyToOne relationship to Organization entity |
| status | UserStatusType | Current status (ONLINE/AWAY) |
| awayReason | AwayReason | Reason for away status (nullable) |
| customReason | string | Free-text reason when awayReason is CUSTOM (max 500 chars) |
| awayUntil | Date | Optional end time for away status |
| createdAt | Date | Record creation timestamp |
| updatedAt | Date | Last modification timestamp |

</Accordion>

<Accordion title="Database Indexes">

- Composite unique index on (user, organization)
- Index on (organization, status) for efficient filtering

</Accordion>
</AccordionGroup>

## API Endpoints

### List Organization User Statuses

<CodeGroup>
```http GET /user-status/:orgId
GET /user-status/{orgId}?status=ONLINE&page=1&limit=50
```

```typescript Response
{
  data: UserStatusResponseDto[],
  total: number,
  page: number,
  limit: number
}
```
</CodeGroup>

**Parameters:**
- `orgId` (path): Organization UUID
- `status` (query): Filter by UserStatusType
- `page` (query, default 1): Page number
- `limit` (query, default 50): Items per page

<Info>Requires MANAGE_TEAM or ASSIGN_LEADS permissions</Info>

### Bulk Update User Statuses

<CodeGroup>
```http PUT /user-status/:orgId/bulk
PUT /user-status/{orgId}/bulk
```

```typescript Request
{
  updates: [
    {
      userId: string,
      status: UserStatusType,
      awayReason?: AwayReason,
      customReason?: string,
      awayUntil?: Date
    }
  ]
}
```

```typescript Response
UserStatusResponseDto[]
```
</CodeGroup>

<Warning>Requires MANAGE_TEAM permission</Warning>

### Update Current User Status

<CodeGroup>
```http PUT /user-status/:orgId/me
PUT /user-status/{orgId}/me
```

```typescript Request
{
  status: UserStatusType,
  awayReason?: AwayReason,
  customReason?: string,
  awayUntil?: Date
}
```

```typescript Response
UserStatusResponseDto
```
</CodeGroup>

<Check>User must be member of the organization</Check>

### Remove User Status

```http
DELETE /user-status/{orgId}/{userId}
```

Sets user back to default ONLINE state by removing their status record.

<Warning>Requires MANAGE_TEAM permission</Warning>

## Enums and Types

### UserStatusType

<Tabs>
<Tab title="Values">
- `ONLINE`: User is available for work assignments
- `AWAY`: User is unavailable
</Tab>

<Tab title="Usage">
```typescript
enum UserStatusType {
  ONLINE = 'ONLINE',
  AWAY = 'AWAY'
}
```
</Tab>
</Tabs>

### AwayReason

<AccordionGroup>
<Accordion title="Manual Reasons">
- `ON_LEAVE`: User explicitly set themselves away
- `CUSTOM`: User provided custom reason text
- `VACATION`: Scheduled vacation period is active
</Accordion>

<Accordion title="Automatic Reasons">
- `INACTIVITY`: No activity for 24+ hours (automatic)
- `OUTSIDE_HOURS`: Outside configured working hours (automatic)
- `SESSION_EXPIRED`: All user sessions expired or revoked (automatic)
</Accordion>
</AccordionGroup>

## Business Rules

### Status Transitions

<Steps>
<Step title="Default State">
New users start as ONLINE when they join an organization
</Step>

<Step title="Manual Away">
Users can set themselves away with any reason except automatic ones (INACTIVITY, OUTSIDE_HOURS, SESSION_EXPIRED)
</Step>

<Step title="Automatic Away">
System sets users away for business rule violations
</Step>

<Step title="Restoration Priority">
Manual reasons persist until manually changed; automatic reasons can be overridden by new sessions or manual updates
</Step>
</Steps>

### Automatic Status Management

<CardGroup cols={2}>
<Card title="Inactivity Detection" icon="clock">
Users are marked away after 24 hours of no activity (checked every 5 minutes)
</Card>

<Card title="Working Hours" icon="calendar">
Users outside configured hours are automatically set to away (if working hours are configured)
</Card>

<Card title="Session Management" icon="shield">
When all sessions expire or are revoked, users are marked away
</Card>

<Card title="Session Restoration" icon="refresh">
When users create new sessions, they're restored to ONLINE if previously away for automatic reasons only
</Card>
</CardGroup>

### Validation Rules

<Note>
The following validation rules are enforced by the system:

1. `customReason` is required when `awayReason` is CUSTOM
2. `customReason` is forbidden when `awayReason` is not CUSTOM
3. `awayReason` is required when status is AWAY
4. `awayReason` is forbidden when status is ONLINE
5. Users cannot manually set automatic away reasons (INACTIVITY, OUTSIDE_HOURS, SESSION_EXPIRED)
6. Custom reasons are limited to 500 characters
</Note>

## Integration Points

### Event System

<Tabs>
<Tab title="Listens To">
- Session creation/revocation events from auth module
</Tab>

<Tab title="Emits">
- Status change events for downstream consumers (lead assignment, notifications)
</Tab>
</Tabs>

### Scheduled Jobs

<CodeGroup>
```javascript Cron Schedule
@Cron(CronExpression.EVERY_5_MINUTES)
```

```typescript Functions
- Inactivity detection
- Working hours enforcement  
- Vacation window management
```
</CodeGroup>

### Access Control

<AccordionGroup>
<Accordion title="RBAC Integration">
- Integrates with RBAC module for permission checks
- Uses organization membership guards
- Supports both team management and self-service operations
</Accordion>

<Accordion title="Database">
- Uses tenant context for multi-tenant data isolation
- Follows standard PropWise entity patterns with UUIDs and timestamps
- Implements soft business rules rather than hard database constraints for flexibility
</Accordion>
</AccordionGroup>

## Patterns and Conventions

The module follows standard PropWise architectural patterns:

<Tip>
**Key Implementation Patterns:**
- **DTO Validation**: Comprehensive class-validator rules with conditional validation
- **Service Layer**: Business logic centralized in UserStatusService
- **Event-Driven Architecture**: Loose coupling via event emitter patterns
- **Multi-Tenant**: Proper tenant context isolation
- **Error Handling**: Standard HTTP exceptions and logging
- **API Documentation**: Full Swagger/OpenAPI specification
- **Background Processing**: NestJS scheduler for automated tasks
</Tip>

## Module Status

<Info>
**Last Updated:** 2024-12-19  
**Status:** Active
</Info>