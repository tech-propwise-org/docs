---
title: "User Status Module Specification v21"
description: "Complete specification for the User Status module including business-level availability management, scheduled vacation windows, working hours overrides, and scoped privacy controls"
---

<Info>
**Last Updated:** 2026-05-25  
**Status:** Active
</Info>

## Overview

The user-status module manages **business-level availability** for users within organizations. This is distinct from real-time presence (Session + WebSocket heartbeats) and instead drives operational behavior such as lead distribution eligibility, sidebar availability toggles, and staff profile availability display.

Users are **ONLINE** (available for work) or **AWAY** (unavailable), with explicit or automatic away reasons, optional custom notes, scheduled vacation windows, and per-user working-hours overrides.

## Entity Structure

### UserStatus

Per-user, per-organization availability record.

| Field | Type | Description |
|-------|------|-------------|
| `id` | UUID | Primary key |
| `user` | User | ManyToOne relationship |
| `organization` | Organization | ManyToOne relationship |
| `status` | UserStatusType | ONLINE or AWAY |
| `awayReason` | AwayReason | Nullable; required semantically when AWAY |
| `customReasonText` | string | Free text for CUSTOM away or vacation note (max 255 / 2000 for scheduled note) |
| `lastActiveAt` | Date | Updated on API activity and explicit status changes |
| `scheduledAwayFrom` | Date | Start of scheduled away window |
| `scheduledAwayUntil` | Date | End of scheduled away window |
| `workingHoursTimezone` | string | IANA timezone override |
| `workingHoursSchedule` | JSONB | Weekly schedule entries `{ dayOfWeek, startTime, endTime }` |
| `createdAt` | Date | Record creation |
| `updatedAt` | Date | Last modification |

<Note>
**Indexes:** unique `(user, organization)`; index on `(organization, status)`.

**Default behavior:** Distribution and scoped read APIs treat missing records as **ONLINE** (same as `findOrCreate` default on self endpoints).
</Note>

### Calendar Roster Endpoint

#### GET /user-status/calendar-roster

Retrieves scheduled vacation windows for users shown on organization or team stacked calendars (red diagonal stripe overlay in the web app).

**Parameters:**
- `userIds` (query, required): Comma-separated subject user UUIDs (1–10, **excluding** the authenticated user)
- `teamId` (query, optional): When set, caller must have team calendar access for that org team

**Response:**
```json
{
  "byUserId": {
    "user-uuid-1": { 
      "scheduledAwayFrom": "2026-06-01T00:00:00Z", 
      "scheduledAwayUntil": "2026-06-15T23:59:59Z" 
    },
    "user-uuid-2": null
  }
}
```

**Access Control:**
- Without `teamId`: Same gate as `GET /v1/calendar/org` (organization CRM/sales/admin roles only)
- With `teamId`: Same gate as `GET /v1/calendar/teams/:teamId` (org admin/owner or team visibility permissions)

## Enums

### UserStatusType

<Tabs>
  <Tab title="Values">
    - `ONLINE` — available for assignment / shown as online
    - `AWAY` — unavailable
  </Tab>
</Tabs>

### AwayReason

| Value | Set by | Notes |
|-------|--------|--------|
| `ON_LEAVE` | User / admin | Manual |
| `CUSTOM` | User / admin | Requires `customReasonText` |
| `VACATION` | Scheduler / scheduled-away | Active vacation window |
| `INACTIVITY` | Scheduler | Auto after inactivity threshold |
| `OUTSIDE_HOURS` | Scheduler | Outside working hours |
| `SESSION_EXPIRED` | Session listener | All org sessions revoked/expired |

### UserStatusViewScope

<Warning>
Service-layer only enum used by `UserStatusService.resolveViewScope()` — not exposed on the wire.
</Warning>

- `FULL` — all DTO fields including `customReasonText`
- `BASIC` — availability fields only; `customReasonText` redacted in `UserStatusDto`

## API Endpoints

All routes are under `/v1` and require JWT auth unless noted.

### Self Management Endpoints

<AccordionGroup>
  <Accordion title="Self Status Management">
    | Method | Path | Description |
    |--------|------|-------------|
    | GET | `/user-status/me` | Own status; **creates** row if missing |
    | PUT | `/user-status/online` | Set self ONLINE; clears away + scheduled window |
    | PUT | `/user-status/away` | Set self AWAY with reason |
    | PUT | `/user-status/scheduled-away` | Schedule vacation/leave window |
    | DELETE | `/user-status/scheduled-away` | Clear scheduled window |
    | PUT | `/user-status/working-hours` | Personal timezone + schedule overrides |
  </Accordion>
</AccordionGroup>

### Administrative Endpoints

| Method | Path | Access | Description |
|--------|------|--------|-------------|
| GET | `/organizations/:orgId/user-status` | `system.admin` | Paginated org list (staff table) |
| PUT | `/organizations/:orgId/users/:userId/status` | `system.admin` | Force status on any org member |

### Scoped Per-User Read

#### GET /organizations/:orgId/users/:userId/status

**Guards:** `AuthGuard`, `OrgMembershipGuard` (`:orgId` must match JWT org). No `@CheckAccess` — authorization is enforced in `UserStatusService.getForViewer()`.

<Steps>
  <Step title="Authentication">
    Verify JWT token and organization membership
  </Step>
  <Step title="Authorization">
    Apply view scope rules based on viewer relationship to target user
  </Step>
  <Step title="Response">
    Return `UserStatusDto` with appropriate privacy redaction
  </Step>
</Steps>

#### View Scope Rules

| Viewer | Scope | `customReasonText` |
|--------|-------|-------------------|
| Self (`viewer.id === targetUserId`) | FULL | visible |
| `system.admin` | FULL | visible |
| `team.admin` on a team shared with target | FULL | visible |
| Any other active org member | BASIC | **omitted** |
| Target not in org / inactive user | 404 | — |
| Viewer not in org | 403 | — |

<Note>
**Read behavior:** Does **not** call `findOrCreate`. Missing `user_status` row returns synthetic ONLINE via `UserStatusDto.forDefaultOnline()` (no `id` field).
</Note>

#### Frontend Usage

<CardGroup cols={2}>
  <Card title="Staff Profile Modal" icon="user">
    Use `UserStatusApi.getByUser` for individual user status display
  </Card>
  <Card title="Personal Settings" icon="gear">
    Availability tab uses scheduled away endpoints for vacation management
  </Card>
</CardGroup>

Staff profile modal and other per-user surfaces should call this endpoint instead of loading the admin list to avoid over-fetching and to respect note redaction.

**Self-service scheduled away:** Personal settings → **Availability** tab and the sidebar profile menu **On Vacation** shortcut both use scheduled away endpoints via shared `ScheduledAwayDialog`.

## Business Rules

### Manual vs Automatic Away

<Tabs>
  <Tab title="Manual Reasons">
    - `ON_LEAVE`, `CUSTOM`, `VACATION`
    - Persist across session restore unless user toggles online or admin overrides
  </Tab>
  <Tab title="Automatic Reasons">
    - `INACTIVITY`, `OUTSIDE_HOURS`, `SESSION_EXPIRED`
    - Cleared when user creates new org session (`restoreOnlineIfAutoAway`)
  </Tab>
</Tabs>

### Distribution Integration

<AccordionGroup>
  <Accordion title="Online Agent Filtering">
    `getOnlineAgentIds()` — agents without a row or with `ONLINE` pass the pre-filter.
  </Accordion>
  <Accordion title="Working Hours Filtering">
    `filterByWorkingHours()` — per-user schedule/timezone overrides fall back to org defaults.
  </Accordion>
</AccordionGroup>

### Scheduled Jobs

<Info>
Cron handlers in `UserStatusScheduler` manage:
- Inactivity marking
- Scheduled-away activation/expiry
- Working-hours enforcement
</Info>

## Integration Points

<CardGroup cols={2}>
  <Card title="Auth & Sessions" icon="shield-check">
    - `UserStatusListener` on session create → `restoreOnlineIfAutoAway`
    - Session expiry → `setAwayOnSessionExpiry`
  </Card>
  <Card title="Activity Tracking" icon="clock">
    `SessionActivityInterceptor` → `touchLastActive`
  </Card>
  <Card title="Distribution" icon="share-nodes">
    - `UserStatusService.getOnlineAgentIds`
    - `filterByWorkingHours`
  </Card>
  <Card title="Access Control" icon="lock">
    - `AccessService.hasPermission`
    - `getPermissionedTeamIdsFromUser` for scoped read
  </Card>
</CardGroup>

### Database Integration

<Tip>
Tenant-scoped reads/writes use `TenantContext`. Missing status rows remain a soft default rather than a hard database requirement.
</Tip>

## Testing

### Test Coverage

<AccordionGroup>
  <Accordion title="Unit Tests">
    `src/modules/user-status/user-status.service.spec.ts`
    - `resolveViewScope` logic
    - `getForViewer` redaction behavior
  </Accordion>
  <Accordion title="E2E Tests">
    `test/e2e/distribution/distribution-user-status.e2e-spec.ts`
    - Self/admin flows
    - Scoped GET privacy matrix
  </Accordion>
</AccordionGroup>

## Module Layout

```
src/modules/user-status/
├── entities/user-status.entity.ts
├── dto/user-status.dto.ts
├── types/
│   ├── user-status-type.enum.ts
│   ├── away-reason.enum.ts
│   └── user-status-view-scope.enum.ts
├── user-status.controller.ts
├── user-status.service.ts
├── user-status.scheduler.ts
└── user-status.listener.ts
```

<Check>
The User Status module provides comprehensive availability management with proper privacy controls and integration points for business-critical functions like lead distribution and team coordination.
</Check>