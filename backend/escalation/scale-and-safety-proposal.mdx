---
title: "Scale and safety proposal"
description: "Production readiness proposal for escalation and distribution engines covering connection pool constraints, scaling risks, and correctness gaps"
---

<Note>
**Status:** Proposed (not yet implemented)  
**Scope:** `src/modules/crm/escalation/`, `src/modules/crm/distribution/`, pg-boss workers, connection pool  
**Audience:** Engineering / DevOps / product owners configuring distribution and escalation rules
</Note>

## Executive summary

The escalation and distribution engines are **architecturally sound for production** at ~100 orgs: two-stage escalation dispatch, `singletonKey` dedup, bounded trigger scans, and distribution batch caps prevent runaway crashes. The real constraints are **connection-pool budget**, **unbounded due-tracker execution per org job**, **scan query cost at large lead volume**, and **two correctness gaps** (silent dropped distribution enqueues and destructive `REDISTRIBUTE` when distribution is off).

This document proposes a prioritized fix plan: pool-safe throughput bounds, self-healing reconciliation, observability, and redistribution safety — without increasing concurrent transactions beyond what `DB_POOL_MAX` can sustain.

## Current architecture (reference)

### Escalation (two-stage pg-boss model)

| Stage | Queue / cron | Behavior |
|-------|-------------|----------|
| **Stage 1 — Dispatcher** | `escalation-scheduler`, cron `* * * * *` (~60s) | Cluster-wide tick: `UNION` of orgs with active rules or active trackers → one `escalation-org` job per org (`singletonKey: esc-org:<orgId>`) |
| **Stage 2 — Per-org worker** | `escalation-org`, `batchSize: 5`, `stately` policy | Up to 5 orgs in parallel; per org: `TriggerDetector.scan()` (LIMIT 500/rule) → due trackers → sequential `executeTrackerTier()` |

<Check>
**Safeguards already in place:** partial unique index + `ON CONFLICT DO NOTHING` on tracker insert; `singletonKey` drops duplicate org jobs while one is in-flight; per-tracker try/catch; `expireInSeconds: 120` on org jobs.
</Check>

### Distribution (event-driven + pg-boss)

| Path | Trigger | Worker |
|------|---------|--------|
| **Single lead** | `LEAD_CREATED` → `DistributionListener` → `enqueueSingle` (priority 10) | `distribution` queue, `batchSize: 10` |
| **Bulk / backfill** | Import or toggle-on backfill → `enqueueBatch` (priority 0) | Same worker |
| **Escalation REDISTRIBUTE** | `TierExecutor` → `DistributionEngineService.redistribute()` | Direct call (no toggle check) |

<Check>
**Safeguards already in place:** `singletonKey: lead:<leadId>`; idempotency in engine; two-phase capacity (advisory lock + re-count); post-commit `LEAD_ASSIGNED` notifications; `FAILED` `distribution_log` on exhausted worker retries.
</Check>

### What "noisy" local dev usually is

<Info>
- Cron dispatcher every ~60s + per-org `.log()` even when `+0 trackers, 0 tiers`.
- `EscalationAutoResolver` logging on every activity/stage/stakeholder event.
- Not extra load — **log volume**, not a runaway loop.
</Info>

## Governing constraint: connection pool budget

Production default: **`DB_POOL_MAX = 20` per app instance** (`app.config.ts` → `mikro-orm.config.ts`). Every `TenantContext.executeInOrg()` holds a connection for the transaction duration.

### Concurrent consumers (worst-case ceiling)

| Consumer | Concurrent transactions | Notes |
|----------|-------------------------|-------|
| Distribution worker | **Up to 10** | `processJobs` uses `Promise.allSettled` over full batch (`distribution-job.handler.ts`) |
| Escalation org worker | **Up to 5** | One connection per org in batch; tracker tiers run **sequentially** per org |
| HTTP / other crons | Remainder | Auth, messaging, search maintenance, user-status, etc. |

<Warning>
**Worst case on one instance:** ~**15 of 20** slots during overlap (bulk import + escalation cycle). This is why **parallelizing the inner escalation tracker loop is the wrong lever** — it would multiply against the 5-org batch and blow the pool.
</Warning>

### Multi-instance note

pg-boss **cron dispatchers** are cluster-wide (one tick), but **workers** (`escalation-org`, `distribution`) run on **every** app instance. Total pressure on Neon ≈ **`N_instances × DB_POOL_MAX`**, plus pg-boss's own pool (`DATABASE_URL`). Size pools before scaling horizontally.

<Tip>
**Implication:** Treat "total concurrent transactions" as a **fixed budget**. Prefer bounds, sequential inner loops, and horizontal scale with **lower** per-instance `DB_POOL_MAX` — not larger batches.
</Tip>

## Scaling risks and mitigations

### Risk 1 — Scan query cost (steady-state, large lead tables)

`TriggerDetector.scan()` runs **one multi-table join per active rule per org every ~60s** (`lead ⋈ entity_stakeholder ⋈ activity`, `LIMIT 500` on results only).

At 100 orgs × 3 rules ≈ **300 scan queries/minute**. Unindexed paths degrade before the pool saturates.

| Mitigation | Priority |
|------------|----------|
| `EXPLAIN (ANALYZE, BUFFERS)` on NFC + WENT_COLD scans against production-sized data | **Gate (P0)** |
| Confirm indexes on `entity_stakeholder`, `activity`, `lead`, `escalation_tracker.next_escalation_at` | **Gate (P0)** |

### Risk 2 — Unbounded due-tracker loop (concrete bug)

Per org, **all** due trackers are selected (no `LIMIT`) and processed sequentially. A heavy org (e.g. business-hours backlog) can exceed **`expireInSeconds: 120`** → pg-boss expiry → retries → wasted work.

Heavy tiers (`REDISTRIBUTE`, `NOTIFY_ADMIN`) can exceed naive ~100ms/tracker estimates.

<AccordionGroup>
<Accordion title="Mitigation strategy">

| Mitigation | Priority |
|------------|----------|
| **Phase 1a:** `ORDER BY next_escalation_at ASC LIMIT $DUE_TRACKER_BATCH_LIMIT` (~50–100, or wall-clock budget ~60s) on due-tracker query | **P1 — do first** |
| Keep inner loop **sequential** (pool-safe) | Required |
| Drain remainder on next cron tick (~5 min for 1000 trackers @ 200/batch) | Default |
| Optional **Phase 1b:** continuation job with distinct `singletonKey` (`esc-org-cont:<orgId>`) only if drain latency is unacceptable | **P5 — defer** |

Mirror precedent: `SCAN_BATCH_LIMIT = 500` in `trigger-detector.ts`.

</Accordion>
</AccordionGroup>

### Risk 3 — Silent dropped distribution enqueue

`DistributionListener.enqueueWithRetry` retries 3×, then **only logs** — lead is created (HTTP 201) but **no job** and **no `distribution_log`**.

<Warning>
This creates a silent failure mode where leads are created but never distributed to agents.
</Warning>

| Mitigation | Priority |
|------------|----------|
| **Phase 2:** Stuck-lead reconciliation cron (~10–15 min), re-enqueue via `enqueueBatch` | **P2 — highest silent-failure value** |
| **Phase 3a:** On enqueue exhaustion, write `distribution_log` with `outcome: FAILED`, `failureReason: enqueue_failed` | **P3** |
| **Phase 3b:** Metrics: failed logs window, `escalation-org` retry exhaustion, pg-boss failed-job depth | **P3** |

#### Stuck-lead definition (all must hold)

<Steps>
<Step title="Distribution enabled">
Distribution enabled for org/team
</Step>

<Step title="Grace period exceeded">
Created > grace period (e.g. 10 min)
</Step>

<Step title="Not archived/deleted">
Not archived / not deleted
</Step>

<Step title="No stakeholder assigned">
**No** user or team stakeholder
</Step>

<Step title="No terminal log">
**No** terminal `distribution_log` outcome: `ASSIGNED | FALLBACK_POOL | FALLBACK_TEAM | QUEUED | ALREADY_ASSIGNED`  
(zero logs, or only `FAILED` logs)
</Step>
</Steps>

<Info>
**Critical filter:** `FALLBACK_POOL` is terminal — leads intentionally in the pool must not be re-enqueued.

**False-positive guard:** Manual unassignment leaves no terminal log. Sweeper should target **never-attempted** leads (e.g. no `distribution_log` at all, or only `enqueue_failed` / worker `FAILED`) — not leads an admin deliberately left unassigned.

**Module boundary:** No cross-module raw SQL. Expose e.g. `LeadService.findUndistributedLeadIdsInTransaction(...)` + compose with `EntityStakeholderService`; join `distribution_log` in distribution module only.
</Info>

### Risk 4 — Pool saturation under burst

Bulk import (`priority: 0`, batch 10) overlapping escalation cycle.

| Mitigation | Priority |
|------------|----------|
| Size `DB_POOL_MAX` so `N × max` < Neon ceiling | **Gate** |
| Optional: env-configurable `distribution` `batchSize` | **P5** |
| Horizontal scale: pg-boss `SKIP LOCKED` — safe multi-instance; reduce per-instance pool | When volume demands |

### Risk 5 — Observability gap

Operators cannot see queue depth, per-org cycle duration, or enqueue failures without log diving.

| Mitigation | Priority |
|------------|----------|
| Wire `PgBossHealthIndicator` into readiness | **P3** |
| Reduce per-cycle `.log()` to `.debug()` when `+0 trackers, 0 tiers` | Low effort |
| Optional prod kill-switch env for escalation cron (test has `ESCALATION_SKIP_PGBOSS_WORKERS`) | Nice-to-have |

## Correctness gap: REDISTRIBUTE vs distribution toggle

### Investigation summary

We analyzed escalation `REDISTRIBUTE` when org-level distribution is **off**.

### What we found (verified in code)

<Tabs>
<Tab title="Toggle behavior">

**`distributionEnabled` is a trigger gate, not an engine kill-switch**

Checked in `DistributionListener.handleLeadCreated` and team Path B routing only. Org pipeline (`runDistribution`) still runs when off — **"off" = manual assignment mode**, not "engine disabled".

</Tab>

<Tab title="Bypass issue">

**Escalation `REDISTRIBUTE` bypasses the toggle**

`TierExecutor.handleRedistribute` → `DistributionEngineService.redistribute()` with **no** `distributionEnabled` check (`tier-executor.ts`).

</Tab>

<Tab title="Removal timing">

**`redistribute()` removes the current agent up-front**

`removeAllForEntityInTransaction` runs **before** the pipeline (`distribution-engine.service.ts`), with `excludeAgentIds: [previousAgent]`.

</Tab>

<Tab title="Default behavior">

**Default off state → `POOL` fallback**

No matching rule + `defaultMethod = POOL` → `FALLBACK_POOL` log, **no** new stakeholder, agent availability ignored.

</Tab>

<Tab title="Auto-create">

**Auto-create side effect**

Missing `DistributionSettings` row is created with `distributionEnabled = false`, `defaultMethod = POOL` — then pipeline proceeds anyway.

</Tab>
</Tabs>

### The gap

<Warning>
`REDISTRIBUTE` is **destructive** when distribution is off: removes Agent X, fails to reassign, leaves lead **orphaned in pool**. Escalation logs tier action `FAILED`; un-assignment is already committed.
</Warning>

| Problem | Description |
|---------|-------------|
| **A — Intent mismatch** | Escalation overrides operator's "distribution off / manual mode". |
| **B — Decision ordering** | Agent removed **before** placement confirmed. Removal + redistribution share one transaction (atomic by design); non-`ASSIGNED` outcomes (`FALLBACK_POOL`, `NO_ELIGIBLE_AGENTS`, `FAILED`) **commit** with agent gone. Also occurs when distribution is **on** but no one is eligible. |

### Proposed fix (redistribution safety)

<Steps>
<Step title="Part 1 — Toggle guard">

In `handleRedistribute`, before any removal: `getDistributionFlags(orgId, teamId)`. If both org and team off → `ActionResult.SKIPPED`, `reason: distribution_disabled`. Other tier actions still run.

**Fixes:** Intent mismatch (A)

</Step>

<Step title="Part 2 — Confirm before remove">

In `redistribute()`, only remove stakeholders **after** confirmed replacement (`ASSIGNED`). On pool / no eligible / failed → retain assignment, return non-assigned outcome.

**Fixes:** Safety issue (B), all failure modes

</Step>

<Step title="Part 3 — UI warning (optional)">

Warn when saving escalation rule with `REDISTRIBUTE` tier while distribution disabled.

**Fixes:** UX only

</Step>
</Steps>

## Proposed fixes (prioritized)

<CardGroup cols={1}>
<Card title="Phase 0: Go-live gates" icon="shield-check">

| # | Change | Addresses | Risk | When |
|---|--------|-----------|------|------|
| **0** | `EXPLAIN ANALYZE` + index verification on escalation scans | Steady-state DB load at scale | Low | **Go-live gate** |
| **0** | Size `DB_POOL_MAX` for `N_instances × max` vs Neon limit | Pool exhaustion | Low | **Go-live gate** |

</Card>

<Card title="Phase 1: Bounded execution" icon="gauge-high">

| # | Change | Addresses | Risk | When |
|---|--------|-----------|------|------|
| **1a** | `LIMIT` (+ `ORDER BY next_escalation_at`) on due-tracker query; sequential processing | Unbounded org job duration | Medium | **P1** |
| **1b** | Redistribution safety (toggle guard + confirm-before-remove) | Destructive REDISTRIBUTE | High (correctness) | **P1** |

</Card>

<Card title="Phase 2: Self-healing" icon="rotate">

| # | Change | Addresses | Risk | When |
|---|--------|-----------|------|------|
| **2** | Stuck-lead reconciliation cron (~10–15 min interval) | Silent dropped enqueues | Medium (silent failure) | **P2** |

</Card>

<Card title="Phase 3: Observability" icon="chart-line">

| # | Change | Addresses | Risk | When |
|---|--------|-----------|------|------|
| **3a** | Write `distribution_log` on enqueue exhaustion (`outcome: FAILED`, `reason: enqueue_failed`) | Audit trail gap | Low | **P3** |
| **3b** | Metrics + health checks (pg-boss queue depth, failed jobs, retry exhaustion) | Operator blind spots | Low | **P3** |

</Card>

<Card title="Phase 4: Tuning (optional)" icon="sliders">

| # | Change | Addresses | Risk | When |
|---|--------|-----------|------|------|
| **4** | Env-configurable `distribution` `batchSize`, escalation cron kill-switch | Operational flexibility | None | **P5** |

</Card>
</CardGroup>

## Pre-go-live checklist

<Note>
Complete all **Gate (P0)** and **P1** items before production launch.
</Note>

<Steps>
<Step title="Database readiness">

- [ ] Run `EXPLAIN (ANALYZE, BUFFERS)` on NFC and WENT_COLD trigger scans with production-sized dataset
- [ ] Verify indexes exist on:
  - `entity_stakeholder` (join columns)
  - `activity` (join columns, timestamps)
  - `lead` (status, timestamps, foreign keys)
  - `escalation_tracker.next_escalation_at`
- [ ] Document query plans and expected execution times

</Step>

<Step title="Connection pool sizing">

- [ ] Calculate total connection demand: `N_instances × DB_POOL_MAX`
- [ ] Verify Neon connection limit > calculated demand × 1.5 (safety margin)
- [ ] Configure `DB_POOL_MAX` per instance accordingly
- [ ] Document pool allocation per consumer type

</Step>

<Step title="Bounded execution (P1)">

- [ ] Implement `LIMIT` on due-tracker query with `ORDER BY next_escalation_at ASC`
- [ ] Set `DUE_TRACKER_BATCH_LIMIT` env var (recommended: 50–100)
- [ ] Verify sequential processing maintained in inner loop
- [ ] Test heavy-org scenario (>200 due trackers)

</Step>

<Step title="Redistribution safety (P1)">

- [ ] Implement toggle guard in `handleRedistribute`
- [ ] Implement confirm-before-remove in `redistribute()`
- [ ] Test scenarios:
  - Distribution off + REDISTRIBUTE tier
  - Distribution on + no eligible agents
  - Distribution on + successful reassignment
- [ ] Verify no orphaned leads in failure cases

</Step>

<Step title="Load testing">

- [ ] Simulate 100 orgs with active escalation rules
- [ ] Test bulk import (1000+ leads) during escalation cycle
- [ ] Monitor connection pool utilization (target: <80% of `DB_POOL_MAX`)
- [ ] Verify no pg-boss job expiration under load
- [ ] Document maximum sustainable throughput

</Step>

<Step title="Monitoring and alerting">

- [ ] Set up alerts for:
  - Connection pool exhaustion (>90%)
  - pg-boss failed job depth threshold
  - `escalation-org` retry exhaustion
  - Distribution enqueue failures
- [ ] Configure health check endpoints
- [ ] Document runbook for common failure modes

</Step>
</Steps>

## Testing and documentation requirements

### Load testing scenarios

<CodeGroup>

```typescript Escalation heavy load
// Scenario: 100 orgs, 3 rules each, varied lead volumes
const testConfig = {
  orgs: 100,
  rulesPerOrg: 3,
  leadVolumes: [100, 1000, 10000], // per org
  dueTrackerBacklog: [0, 50, 200], // per org
  concurrentImports: true,
};

// Expected outcomes:
// - All org jobs complete within expireInSeconds (120s)
// - Pool utilization < 80%
// - No tracker processing skipped
// - Scan queries < 500ms p95
```

```typescript Distribution burst
// Scenario: Bulk import during active escalation
const testConfig = {
  bulkImportSize: 1000,
  prioritySingle: 10, // normal creates
  priorityBulk: 0, // imports
  concurrentEscalationOrgs: 5,
  expectedBehavior: {
    poolSaturation: '<80%',
    enqueueSuccessRate: '>99%',
    assignmentLatencyP95: '<30s',
  },
};
```

```typescript Failure recovery
// Scenario: Enqueue failure + reconciliation
const testConfig = {
  simulateEnqueueFailure: true,
  stuckLeadGracePeriod: 600, // 10 min
  reconciliationInterval: 900, // 15 min
  expectedOutcomes: [
    'distribution_log with FAILED outcome',
    're-enqueue on next sweep',
    'no duplicate assignments',
  ],
};
```

</CodeGroup>

### Performance benchmarks

Document baseline performance for production sizing:

| Metric | Target | Measurement method |
|--------|--------|-------------------|
| Scan query p95 | <500ms | `EXPLAIN ANALYZE` on production-sized tables |
| Due-tracker processing | <100ms/tracker | Tier executor instrumentation |
| Distribution assignment | <30s p95 | Job completion time - queue wait |
| Connection pool utilization | <80% peak | MikroORM pool metrics |
| Escalation cycle completion | <120s per org | Org job duration |

### Documentation requirements

<AccordionGroup>

<Accordion title="Operator runbook">

**Required sections:**

1. **Pool exhaustion response**
   - Symptoms: slow queries, job timeouts
   - Triage: check pool metrics, active connections
   - Mitigation: scale instances with lower per-instance pool, or increase Neon limit

2. **Stuck lead reconciliation**
   - How to identify stuck leads
   - Manual re-enqueue procedure
   - When to escalate vs wait for sweeper

3. **Escalation backlog management**
   - Understanding due-tracker batch limits
   - Expected drain time calculation
   - When heavy-org pattern indicates config issue

4. **Distribution toggle + REDISTRIBUTE conflict**
   - Expected behavior when distribution disabled
   - How to audit for orphaned leads
   - Safe escalation rule configuration

</Accordion>

<Accordion title="Configuration guide">

**Environment variables:**

```bash
# Connection pool
DB_POOL_MAX=20                    # per instance
DATABASE_URL=postgres://...       # pg-boss pool (separate)

# Escalation bounds
DUE_TRACKER_BATCH_LIMIT=100      # per org per cycle
SCAN_BATCH_LIMIT=500             # per rule (existing)

# Distribution
DISTRIBUTION_BATCH_SIZE=10        # optional override

# Reconciliation
STUCK_LEAD_GRACE_PERIOD_MS=600000    # 10 min
STUCK_LEAD_SWEEP_INTERVAL_MS=900000  # 15 min

# Observability (optional)
ESCALATION_SKIP_PGBOSS_WORKERS=false # kill-switch
```

**Scaling guidelines:**

- Start with 2 instances × `DB_POOL_MAX=20` = 40 connections
- Neon minimum: 60 connections (40 + 50% margin)
- Scale horizontally, not vertically (lower pool per instance)
- Monitor queue depth before adding workers

</Accordion>

<Accordion title="Developer guide">

**Module boundaries:**

- Escalation: `src/modules/crm/escalation/`
- Distribution: `src/modules/crm/distribution/`
- Cross-module: use service interfaces, not raw SQL

**Pool-safe patterns:**

```typescript
// ✅ GOOD: Sequential processing
for (const tracker of dueTrackers) {
  await executeTrackerTier(tracker);
}

// ❌ BAD: Parallel inner loop
await Promise.all(
  dueTrackers.map(t => executeTrackerTier(t))
); // multiplies pool pressure

// ✅ GOOD: Bounded query
const dueTrackers = await em.find(EscalationTracker, 
  { next_escalation_at: { $lte: now } },
  { 
    orderBy: { next_escalation_at: 'ASC' },
    limit: DUE_TRACKER_BATCH_LIMIT 
  }
);
```

**Testing requirements:**

- Unit tests for all new limits and guards
- Integration tests for reconciliation logic
- Load tests before production deployment
- Chaos engineering: simulate pg-boss failures, pool exhaustion

</Accordion>

</AccordionGroup>

### Metrics and monitoring

<CardGroup cols={2}>

<Card title="Critical metrics" icon="triangle-exclamation">

- Connection pool utilization (%)
- pg-boss failed job count
- Distribution enqueue retry exhaustion rate
- Escalation org job expiration count
- Stuck lead count (real-time)

</Card>

<Card title="Performance metrics" icon="gauge">

- Scan query duration (p50, p95, p99)
- Due-tracker processing time per org
- Distribution assignment latency
- Escalation cycle completion time
- Queue depth per consumer type

</Card>

<Card title="Business metrics" icon="chart-mixed">

- Leads distributed per hour
- Escalation tiers executed per hour
- REDISTRIBUTE success rate
- Manual interventions required
- Fallback-to-pool rate

</Card>

<Card title="Health indicators" icon="heart-pulse">

- pg-boss worker connectivity
- Database replication lag
- Worker queue processing rate
- Failed job drain rate
- Reconciliation sweep effectiveness

</Card>

</CardGroup>

---

<Tip>
**Next steps:** Review this proposal with DevOps and product stakeholders, prioritize P0/P1 items for immediate implementation, and schedule load testing before go-live.
</Tip>