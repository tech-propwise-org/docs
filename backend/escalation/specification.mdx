---
title: "Escalation Module Specification"
description: "Detailed technical specification including entities, business rules, and implementation details"
---

<Note>
The Escalation Engine focuses on **reassignment workflows** for leads requiring ownership changes.
For lead activity monitoring with **agent notifications** (without forced reassignment), see the
separate **Activity Monitor module**.

Both systems complement each other:
- **Escalation Engine**: Handles reassignment when agents fail to make first contact
- **Activity Monitor**: Handles notification alerts for ongoing lead activity issues
</Note>

## Overview

The escalation module provides automated **reassignment workflows** for leads that were never
contacted after assignment (`NO_FIRST_CONTACT` trigger only). It enables organizations to define
tiered escalation rules that progressively take actions (notifications, reassignments, etc.) when
an agent fails to make first contact with a newly assigned lead.

<Info>
The Escalation Engine handles `NO_FIRST_CONTACT` only. For leads with existing activity
that go quiet, use the Activity Monitor module which provides notification-only alerts without
forced reassignment.
</Info>

The module consists of three main components:
- **Escalation Rules** - Define when and how escalations should trigger
- **Escalation Trackers** - Track active escalations and their execution history
- **Escalation Tiers** - Multi-level actions that execute in sequence with configurable delays

## Architecture

The escalation system follows an event-driven architecture with scheduled evaluation of trigger conditions:

<Steps>
<Step title="Rule Definition">
Organizations create escalation rules with specific triggers and multi-tier actions
</Step>

<Step title="Condition Evaluation">
Background jobs periodically evaluate leads against active escalation rules
</Step>

<Step title="Tracker Creation">
When conditions are met, an escalation tracker is created to manage execution
</Step>

<Step title="Tier Execution">
Actions are executed in sequence based on tier delays and configurations
</Step>

<Step title="Resolution">
Escalations resolve automatically when conditions change or manually by users
</Step>
</Steps>

## Entities

### EscalationRule

Defines the conditions under which escalations should trigger and the actions to take.

| Field | Type | Description |
|-------|------|-------------|
| `id` | UUID | Primary key |
| `organization` | Organization | RLS boundary |
| `createdBy` | User | Rule creator |
| `name` | string | Human-readable rule name |
| `description` | string | Optional rule description |
| `isActive` | boolean | Whether rule is currently enabled |
| `triggerType` | TriggerType | Type of condition that triggers escalation |
| `triggerConfig` | JSON | Threshold configuration (format depends on trigger type) |
| `applyTo` | EscalationApplyTo | Scope of leads this rule applies to |
| `applyToConfig` | JSON | Additional configuration for scope (stages, temperatures, etc.) |
| `tiers` | EscalationTier[] | Ordered list of escalation actions |

#### Supported Trigger Types

<AccordionGroup>
<Accordion title="Active Triggers">
| Type | Status | Description |
|---|---|---|
| `NO_FIRST_CONTACT` | ✅ **Active** | Fires when an agent never makes first contact after assignment |
</Accordion>

<Accordion title="Deprecated Triggers">
| Type | Status | Description |
|---|---|---|
| `NO_ACTIVITY` | ❌ Deprecated — moved to Activity Monitor | |
| `STALE_LEAD` | ❌ Deprecated — moved to Activity Monitor | |
| `STAGE_STUCK` | ❌ Deprecated — moved to Activity Monitor | |
| `CUSTOM` | ❌ Deprecated | |
</Accordion>
</AccordionGroup>

#### Trigger Configuration

<CodeGroup>
```json NO_FIRST_CONTACT
{
  "thresholdMinutes": 60
}
```
</CodeGroup>

<Warning>
The `thresholdHours`, `thresholdDays`, and `stageIds` fields in `TriggerConfig` are kept for
historical data readability only — they are not used for new rule evaluation.
</Warning>

### EscalationTier

Individual action levels within an escalation rule, executed in sequence.

| Field | Type | Description |
|-------|------|-------------|
| `id` | UUID | Primary key |
| `organization` | Organization | RLS boundary |
| `escalationRule` | EscalationRule | Parent rule |
| `tierOrder` | number | Execution order (1, 2, 3, etc.) |
| `delayHours` | number | Hours to wait before executing this tier |
| `actions` | JSON[] | Array of actions to execute |

#### Supported Action Types

| Type | Parameters | Description |
|------|-----------|-------------|
| `NOTIFY_AGENT` | `message?` | Notify the lead's currently assigned agent |
| `NOTIFY_ADMIN` | `message?` | Self-resolving — notifies **all** org users with `system.admin` permission |
| `NOTIFY_TEAM_LEAD` | `message?` | Self-resolving — notifies **all** team members with `team.admin` permission in the lead's team |
| `REDISTRIBUTE` | _(no params)_ | Delegates to the DistributionEngine; removes current stakeholders and re-runs the full pipeline, excluding the current assignee. **Must be in the last tier.** |
| `CHANGE_TEMPERATURE` | `temperature: 'hot' \| 'warm' \| 'cold'` | Change lead temperature |
| `ADD_TAG` | `tagIds: string[]` | Add tags to the lead |

#### Action Configuration Examples

<CodeGroup>
```json Notify Agent
{
  "type": "NOTIFY_AGENT",
  "message": "Lead needs attention"
}
```

```json Notify Admin
{
  "type": "NOTIFY_ADMIN",
  "message": "Escalation alert"
}
```

```json Notify Team Lead
{
  "type": "NOTIFY_TEAM_LEAD"
}
```

```json Redistribute
{
  "type": "REDISTRIBUTE"
}
```

```json Change Temperature
{
  "type": "CHANGE_TEMPERATURE",
  "temperature": "hot"
}
```

```json Add Tags
{
  "type": "ADD_TAG",
  "tagIds": ["VIP", "Hot Buyer"]
}
```
</CodeGroup>

### EscalationTracker

Tracks active escalations and their execution history for specific leads.

| Field | Type | Description |
|-------|------|-------------|
| `id` | UUID | Primary key |
| `organization` | Organization | RLS boundary |
| `lead` | Lead | Lead being escalated |
| `escalationRule` | EscalationRule | Rule that triggered escalation |
| `status` | EscalationStatus | Current escalation state |
| `triggeredAt` | Date | When escalation was first triggered |
| `resolvedAt` | Date | When escalation was resolved (if applicable) |
| `resolvedBy` | ResolvedBy | How escalation was resolved |
| `currentTier` | number | Currently executing tier (1-based) |
| `nextTierAt` | Date | When next tier should execute |
| `executionHistory` | JSON[] | Log of all tier executions |

#### Execution History Entry

```json
{
  "tier": 1,
  "executedAt": "2024-01-15T10:30:00Z",
  "actions": [
    {
      "type": "NOTIFY_AGENT",
      "status": "SUCCESS", 
      "details": { "notificationId": "uuid" }
    }
  ]
}
```

## Business Rules

### Trigger Evaluation

- Rules are evaluated periodically by background jobs (typically every 15-30 minutes)
- Only active rules (`isActive = true`) are considered
- Leads with existing active escalations are skipped to prevent duplicates
- Trigger conditions must account for business hours and timezone settings

### Tier Execution

- Tiers execute in order based on `tierOrder` field
- `delayHours` is calculated from the escalation's `triggeredAt` timestamp
- If a tier fails partially, successful actions are not retried
- Execution continues to next tier regardless of individual action failures

### Resolution Conditions

<CardGroup cols={2}>
<Card title="Manual Resolution" icon="hand">
User explicitly resolves via UI/API
</Card>

<Card title="Auto Activity" icon="activity">
New activity added to lead
</Card>

<Card title="Auto Stage Change" icon="arrows-rotate">
Lead moves to different stage
</Card>

<Card title="Auto Reassignment" icon="user-check">
Lead reassigned to different agent
</Card>

<Card title="Redistributed" icon="share-nodes">
Scheduler resolved tracker after successful REDISTRIBUTE action
</Card>
</CardGroup>

<Tip>
When a `REDISTRIBUTE` action successfully assigns a lead to a new agent (`ASSIGNED` outcome), 
the tracker is immediately resolved with `resolvedBy = REDISTRIBUTED`. No further tiers fire. 
A new tracker will be created by TriggerDetector if the new agent also fails to act.
</Tip>

### Scope Application

- **ALL** - Applies to all leads in organization
- **SPECIFIC_STAGES** - Only leads in configured stages
- **SPECIFIC_TEMPERATURES** - Only leads with configured temperatures

## Integration Points

### CRM Module

- **Lead Entity** - Escalations track specific leads and can modify lead properties
- **Activity Tracking** - Used to determine "last activity" for NO_ACTIVITY triggers
- **Stage Management** - Stage changes can trigger or resolve escalations

### Notification Module

- **Agent Notifications** - NOTIFY_AGENT actions create notifications for assigned users
- **Admin Notifications** - NOTIFY_ADMIN actions route to specified admins
- **Team Notifications** - NOTIFY_TEAM_LEAD actions notify team leadership

### Distribution Module

<Check>
**REDISTRIBUTE** calls `DistributionEngineService.redistribute()` which runs the full distribution 
pipeline (rule matching, eligibility, capacity, language) excluding the current agent. A 
`distribution_log` entry with method `REDISTRIBUTION` is written for observability.
</Check>

### User Module

- **Admin Resolution** — `NOTIFY_ADMIN` self-resolves by querying all org members with the `system.admin` permission key
- **Team Leadership Resolution** — `NOTIFY_TEAM_LEAD` self-resolves by querying all team members with the `team.admin` permission key in the lead's assigned team

## API Endpoints

<Note>
API endpoints are not yet implemented based on the provided source files. This section will be updated when controllers are added.
</Note>

### Escalation Rules Management

#### GET /escalation-rules
List escalation rules for the organization.

**Authorization:** `MANAGE_ESCALATION` permission  
**Response:** Array of `EscalationRuleDto`

#### POST /escalation-rules
Create a new escalation rule.

**Authorization:** `MANAGE_ESCALATION` permission  
**Request Body:** `CreateEscalationRuleDto`  
**Response:** `EscalationRuleDto`

#### PUT /escalation-rules/:id
Update an existing escalation rule.

**Authorization:** `MANAGE_ESCALATION` permission  
**Request Body:** `UpdateEscalationRuleDto`  
**Response:** `EscalationRuleDto`

#### DELETE /escalation-rules/:id
Soft-delete an escalation rule. All ACTIVE trackers for this rule are immediately cancelled.

**Authorization:** `MANAGE_ESCALATION` permission  
**Response:** `{ success: boolean }`

### Escalation Analytics

#### GET /escalation-analytics
Retrieve escalation metrics and analytics.

**Authorization:** `VIEW_DISTRIBUTION_ANALYTICS` permission

**Query Parameters:**
- `dateFrom` (required): Start date (ISO 8601)
- `dateTo` (required): End date (ISO 8601, max 90 days from dateFrom)
- `ruleId` (optional): Filter by specific rule

**Response:** `EscalationAnalyticsDto` with trigger breakdown, resolution breakdown, and performance metrics

## Patterns and Conventions

### Naming Conventions

- Entity names use singular form: `EscalationRule`, `EscalationTier`
- Enum values use UPPER_SNAKE_CASE: `NO_FIRST_CONTACT`, `NOTIFY_AGENT`
- Database indexes follow pattern: `idx_{table}_{columns}`

### JSON Configuration

- All configuration objects use camelCase field names
- Optional fields are omitted rather than set to null
- Action configurations include `type` field for polymorphic handling

### Error Handling

- Escalation execution failures are logged but do not prevent progression
- Invalid configurations are validated at rule creation time
- Orphaned escalations (deleted leads/rules) are cleaned up by maintenance jobs

### Performance Considerations

- Escalation evaluation is optimized with database indexes on trigger-relevant fields
- Large organizations may require partitioning of tracker history
- Tier delays are calculated using database timestamps to handle server restarts