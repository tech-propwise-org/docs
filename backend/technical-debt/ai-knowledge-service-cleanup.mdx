---
title: "AI Knowledge Service Cleanup"
description: "Technical debt item to remove the temporary AI Assistant knowledge-base glue and migrate to the canonical AiAgentKnowledgeBaseService"
---

<Warning>
**Status:** Open — interim structural alignment DONE (2026-05-30, on `ai-assistant`); final deletion still pending the PCD-64 merge
</Warning>

## Overview

This technical debt item involves removing temporary glue code that was created to bridge the AI Assistant's knowledge-base functionality while the full AI Agent runtime was being developed. The interim service needs to be replaced with the canonical `AiAgentKnowledgeBaseService`.

<Info>
**Estimated effort:** ~2 hours + verification (alignment spent; deletion remains)  
**Risk:** Low — pure refactor, no schema/contract change  
**Created:** 2026-05-28 (right after merging `dev` → `PCD-64-ai-agents-and-workflows`)
</Info>

## Background

When `dev` cherry-picked PCD-64's shared knowledge-base infrastructure (`KnowledgeBase`, `KnowledgeChunk`, `OrganizationLlmKey`, `EmbeddingService`, `KnowledgeBaseService`, `AiAgentKnowledgeBaseController`, BYOK provisioning), it could not yet adopt PCD-64's `AiAgentKnowledgeBaseService` because the full AI Agent runtime was out of scope for that PR.

To keep the internal AI assistant's `searchKnowledge` tool working in the interim, dev shipped a thin, dev-only glue layer:

- `src/modules/ai/services/ai-knowledge.service.ts` — slim search-only service that wraps `EmbeddingService.generateEmbedding(...)` + raw pgvector SQL against `knowledge_chunks JOIN knowledge_base`
- `src/modules/ai/ai-knowledge-tools.provider.ts` — wraps the above into the `searchKnowledge` AI tool consumed by `ai-assistant/`
- Three lines at the bottom of `src/modules/ai/ai.module.ts` (`providers` + `exports`) wiring the glue + the `provideAiTool(...) as never` registration

### Interim Alignment (Completed)

<Check>
**Done — 2026-05-30, `ai-assistant` branch**
</Check>

To keep the eventual deletion + PCD-64 merge low-diff, `AIKnowledgeService` was refactored **in place** to mirror the canonical `AiAgentKnowledgeBaseService.searchAgentKnowledge` structure:

- `tenantContext.executeInOrg` + `em.createQueryBuilder(KnowledgeChunk)` + `raw('(kc.embedding <=> ?::vector)')` distance
- Returns flat `KnowledgeChunkHit[]` (`chunkId`, `content`, `sourceId`, `sourceTitle`, `sourceType`, `distance`)
- Adopted canonical `status = READY` filter and owner-scoping (`owner_user_id IS NULL OR = :requestingUserId`)
- Skips the `aiUsed` write (assistant is `accessMode: 'read'`)

Class name (`AIKnowledgeService`), method name (`searchKnowledge`), and `ai.module.ts` wiring were intentionally left unchanged so this branch's edits stay conflict-free against PCD-64's `ai.module.ts` rewrite.

## Goal

Migrate `AiKnowledgeToolsProvider` to call `AiAgentKnowledgeBaseService` directly, then delete `AIKnowledgeService` and remove the temporary registration lines from `ai.module.ts`.

<Note>
The `searchKnowledge` tool's external contract (name, schema, accessMode, contextScopes, audit category, untrusted-content treatment) must remain identical so no eval suite or AI assistant test breaks.
</Note>

## Implementation Steps

<Steps>
<Step title="Update the tools provider">
Modify `src/modules/ai/ai-knowledge-tools.provider.ts`:
- Change the constructor to inject `AiAgentKnowledgeBaseService` instead of `AIKnowledgeService`
- Call `searchAgentKnowledge(query, organizationId, agent, topK, apiKey, skipAiUsedUpdate)` instead of `searchKnowledge`
- Note: The canonical method requires an `AiAgent` and explicit `apiKey`, and performs an `aiUsed` write unless `skipAiUsedUpdate=true`
- The agent-less assistant must obtain/construct a suitable `AiAgent`, resolve the BYOK key, and pass `skipAiUsedUpdate=true`
</Step>

<Step title="Clean up the AI module">
Update `src/modules/ai/ai.module.ts`:
- Remove the `AIKnowledgeService` import
- Remove `AIKnowledgeService` from `providers`
- Remove `AIKnowledgeService` from `exports`
- Keep `AiKnowledgeToolsProvider` and `provideAiTool(AiKnowledgeToolsProvider) as never`
- Keep `AI_TOOL_PROVIDER_TOKEN` in `exports`
- Update the module-level JSDoc to drop the "dev-only glue" paragraph
</Step>

<Step title="Delete the temporary service">
Remove `src/modules/ai/services/ai-knowledge.service.ts` entirely — no external module references it after the migration.
</Step>

<Step title="Update documentation">
Modify `Docs/AI_MODULE_SPECIFICATION.md`:
- Remove the `AIKnowledgeService` section under "## Services"
- Update any reference to "dev-only glue" in the overview
- Add a one-line note in the changelog saying the assistant now consumes `AiAgentKnowledgeBaseService` directly
</Step>
</Steps>

## Files Affected

<AccordionGroup>
<Accordion title="Files to Delete">
- `src/modules/ai/services/ai-knowledge.service.ts` — the slim search-only glue service
</Accordion>

<Accordion title="Files to Modify">
- `src/modules/ai/ai-knowledge-tools.provider.ts` — update dependency injection and method calls
- `src/modules/ai/ai.module.ts` — remove temporary service registration
- `Docs/AI_MODULE_SPECIFICATION.md` — update documentation
</Accordion>
</AccordionGroup>

## Verification Process

After implementing the changes, run these verification steps in order:

<Steps>
<Step title="Type checking">
```bash
pnpm tsc --noEmit
```
Must return zero errors.
</Step>

<Step title="Notification allowlist check">
```bash
pnpm check:notification-allowlist
```
Must be OK (no enum changes, should be trivially green).
</Step>

<Step title="AI assistant unit tests">
```bash
pnpm test src/modules/ai-assistant
```
All unit suites (orchestrator, tool registry, tool executor, tool bridge, etc.) must pass.
</Step>

<Step title="E2E tests">
```bash
pnpm test:e2e:core
```
`assistant-tools.e2e-spec.ts` and `diagnostic-registry.e2e-spec.ts` must pass. They assert `searchKnowledge` is in the registered tools list.
</Step>

<Step title="AI evaluation suite">
```bash
pnpm eval:run
```
The `routing-knowledge-base-001` case in `evals/suites/tool-selection.yaml` pins `must_call_tools: [searchKnowledge]` and must still pass.
</Step>

<Step title="Manual smoke test">
With a real org that has an active `organization_llm_key` row and at least one `knowledge_base` row + indexed chunks, send the AI assistant a "what is our commission split policy" prompt and confirm it routes through `searchKnowledge` and returns a snippet.
</Step>
</Steps>

## Safety Considerations

<CardGroup cols={2}>
<Card title="Contract Preservation" icon="shield-check">
The tool's external contract (name, input schema, output projection fields) is preserved
</Card>

<Card title="Security Posture" icon="lock">
`AiAgentKnowledgeBaseService` runs inside `tenantContext.executeInOrg` — same RLS posture as the glue
</Card>

<Card title="Vector Consistency" icon="chart-line">
The vector space (OpenAI `text-embedding-3-small`, 768 dims) and pgvector cosine distance operator are unchanged
</Card>

<Card title="BYOK Enforcement" icon="key">
Both services resolve through `OrganizationLlmKeyService.getActiveKey` for consistent enforcement
</Card>
</CardGroup>

## Why This Matters

<Tip>
**Value of this cleanup:**
- Eliminates duplicate services doing the same thing
- Removes inline pgvector SQL that would drift from the canonical service over time
- Decouples AI assistant from dev-period workarounds
- Simplifies the `ai.module.ts` providers list
</Tip>

## Out of Scope

The following items are explicitly **not** part of this cleanup:

- The `searchKnowledge` tool name itself (PCD-64 runtime has its own tool-registry layer)
- The `/v1/ai-knowledge-bases` HTTP surface
- The `KnowledgeBase` / `KnowledgeChunk` / `OrganizationLlmKey` entities
- The `KnowledgeBaseService` ingestion flows
- BYOK provisioning functionality