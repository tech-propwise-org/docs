---
title: "Messaging Module Specification"
description: "Complete specification for the unified messaging module supporting WhatsApp, Instagram, and Facebook Messenger with shared entities, queue system, and WebSocket architecture"
---

## Overview

The Messaging module provides a unified, channel-agnostic messaging system for WhatsApp, Instagram, and Facebook Messenger. It replaces the separate per-channel modules with shared entities, a shared queue, and a single WebSocket namespace.

<Note>
**Status:** Active | **Last Updated:** April 15, 2026
</Note>

### Problem → Solution

| Problem | Solution |
| --- | --- |
| Duplicated logic across WhatsApp and Instagram modules | Single `MessagingModule` with channel providers |
| No webhook signature validation (security gap) | Shared `MetaWebhookGuard` validates `X-Hub-Signature-256` |
| Inconsistent WebSocket auth (Instagram gateway has no JWT) | Single `/messaging` gateway with JWT auth |
| No Facebook Messenger support | Third channel provider |
| Separate entity schemas per channel | Unified entities: `Conversation`, `Message`, `ChannelAccount` |
| No shared queue infrastructure | Shared `PgBossQueueService` for messaging + notifications |

### Key Design Decisions

<AccordionGroup>
  <Accordion title="pg-boss over BullMQ">
    Project already uses pg-boss for notifications. No new Redis dependency. Interface-based design (`IQueueService`) allows swapping later.
  </Accordion>

  <Accordion title="Direct PersonChannel FK on Conversation">
    Conversations link directly to the CRM's `PersonChannel` via FK. Simpler model, no bidirectional sync overhead. The lead FK was moved from Conversation to Lead (`Lead.sourceConversation`).
  </Accordion>

  <Accordion title="Archive as boolean, not status">
    `Conversation.isArchived` is orthogonal to `status` (OPEN/CLOSED), following `ARCHIVE_SYSTEM_SPECIFICATION.md`.
  </Accordion>

  <Accordion title="ConversationAssignment entity">
    Conversations use a dedicated `conversation_assignment` table instead of the CRM `entity_stakeholder` pattern. Each assignment is one row with nullable `user_id` and `team_id`.
  </Accordion>

  <Accordion title="Transactional outbox">
    Outbound messages use an outbox table written in the same DB transaction as the Message entity, guaranteeing at-least-once delivery.
  </Accordion>

  <Accordion title="Per-conversation AI mode with cascade">
    Each conversation has an `aiMode` field (OFF, AUTO_REPLY, SUGGEST_ONLY, DRAFT). Default cascades: ChannelAccount.defaultAiMode → Organization default → OFF.
  </Accordion>

  <Accordion title="Three-tier template system">
    `MessageTemplate` supports three types: `META_APPROVED` (platform-approved), `QUICK_REPLY` (agent shortcuts), and `AI_PROMPT` (AI system prompts).
  </Accordion>

  <Accordion title="Personal accounts share org WABA token">
    WhatsApp personal accounts reuse the organization's WABA access token. Instagram and Messenger personal accounts use their own Page Access Token via OAuth.
  </Accordion>
</AccordionGroup>

## Architecture & Module Structure

```mermaid
graph TD
    A[Meta Platform Webhooks] --> B[POST /messaging/webhook]
    B --> C[@PublicEndpoint + MetaWebhookGuard]
    C --> D[WebhookEventLog]
    D --> E[pg-boss Queue]
    E --> F[Queue Worker]
    F --> G[Channel Provider]
    G --> H[PersonChannel Match/Create]
    H --> I[Conversation Find/Create]
    I --> J[Message Creation]
    J --> K[WebSocket Events]
```

### Module Structure

```
src/modules/meta-platform/    ← Top-level infra module
  meta-platform.module.ts
  meta-graph-api.service.ts
  meta-api.error.ts
  meta-webhook.guard.ts
  meta-oauth.service.ts
  webhook-event-log.entity.ts

src/modules/queue/            ← Top-level infra module

src/modules/messaging/
  messaging.module.ts
  entities/               ← Core entities
  enums/                  ← Message types, statuses, etc.
  services/               ← Core services + providers/
    providers/            ← WhatsApp, Instagram, Messenger
  controllers/            ← API endpoints
  gateways/               ← WebSocket gateway
  queues/                 ← Workers
  dto/                    ← Request/response DTOs
  utils/                  ← Utilities
```

## Multi-Tenancy Patterns

The messaging module introduces unique multi-tenancy challenges because webhooks arrive without org context.

<Warning>
Webhooks arrive for ALL organizations from a single Meta App. Organization context is unknown at arrival time.
</Warning>

### Two-Step RLS Bypass

<Steps>
  <Step title="Find organization">
    Bypass RLS to identify which org owns the account
    
    ```typescript
    const account = await this.tenantContext.executeReadOnlyWithBypass(async (em) => {
      return em.findOne(ChannelAccount, { externalAccountId: job.data.accountId });
    });
    ```
  </Step>

  <Step title="Process within org context">
    Execute the webhook processing within the organization's context
    
    ```typescript
    await this.tenantContext.executeInOrg(
      account.organization.id,
      async (em) => {
        await this.processMessageInTransaction(em, job.data);
      },
      { userId: undefined }, // system action
    );
    ```
  </Step>
</Steps>

### Composable Transaction Pattern

Services expose `*InTransaction` methods for participation in existing transactions:

```typescript
// Public API — wraps TenantContext
async matchOrCreate(channel, identifier, profileData, orgId): Promise<MatchResult>;

// Composable — accepts EntityManager from caller's transaction
async matchOrCreateInTransaction(em, channel, identifier, profileData, orgId): Promise<MatchResult>;
```

<Check>
The `em` parameter must always be the one provided by the TenantContext callback — never `this.em`.
</Check>

### Read-Only vs Mutation Methods

<Tabs>
  <Tab title="Read-Only">
    Use `executeReadOnly()` for queries and lookups:
    
    ```typescript
    return this.tenantContext.executeReadOnly(organizationId, async (em) => {
      // findById, listConversations, getChannelCounts, etc.
    });
    ```
  </Tab>

  <Tab title="Mutation">
    Use `executeInOrg()` with userId for mutations:
    
    ```typescript
    return this.tenantContext.executeInOrg(organizationId, async (em) => {
      // updateConversation, archiveConversation, etc.
    }, { userId });
    ```
  </Tab>
</Tabs>

## Entities

### Core Entities

<CardGroup cols={2}>
  <Card title="ChannelAccount" icon="user">
    Represents a connected social media account (WhatsApp, Instagram, Messenger)
  </Card>
  <Card title="Conversation" icon="message">
    Groups messages between a person and an organization on a specific channel
  </Card>
  <Card title="Message" icon="envelope">
    Individual message within a conversation (inbound or outbound)
  </Card>
  <Card title="MessageTemplate" icon="template">
    Reusable message templates (Meta approved, quick replies, AI prompts)
  </Card>
</CardGroup>

### ChannelAccount Entity

```typescript
@Entity('channel_account')
export class ChannelAccount extends BaseEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @ManyToOne(() => Organization)
  @JoinColumn({ name: 'organization_id' })
  organization: Organization;

  @Column({ type: 'enum', enum: Channel })
  channel: Channel; // WHATSAPP, INSTAGRAM, MESSENGER

  @Column({ name: 'external_account_id' })
  externalAccountId: string; // Phone number, IG username, Page ID

  @Column({ name: 'display_name', nullable: true })
  displayName?: string;

  @Column({ name: 'profile_picture_url', nullable: true })
  profilePictureUrl?: string;

  @Column({ name: 'access_token', nullable: true })
  accessToken?: string; // Encrypted

  @Column({ name: 'is_personal', default: false })
  isPersonal: boolean;

  @Column({ name: 'personal_user_id', nullable: true })
  personalUserId?: string;

  @Column({ name: 'default_ai_mode', type: 'enum', enum: AiMode, default: AiMode.OFF })
  defaultAiMode: AiMode;

  @Column({ name: 'page_id', nullable: true })
  pageId?: string; // For Instagram outbound messaging

  @OneToMany(() => Conversation, conversation => conversation.channelAccount)
  conversations: Conversation[];
}
```

### Conversation Entity

```typescript
@Entity('conversation')
export class Conversation extends BaseEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @ManyToOne(() => Organization)
  @JoinColumn({ name: 'organization_id' })
  organization: Organization;

  @ManyToOne(() => ChannelAccount)
  @JoinColumn({ name: 'channel_account_id' })
  channelAccount: ChannelAccount;

  @ManyToOne(() => PersonChannel, { nullable: true })
  @JoinColumn({ name: 'person_channel_id' })
  personChannel?: PersonChannel;

  @Column({ name: 'external_conversation_id' })
  externalConversationId: string;

  @Column({ type: 'enum', enum: ConversationStatus, default: ConversationStatus.OPEN })
  status: ConversationStatus;

  @Column({ name: 'is_archived', default: false })
  isArchived: boolean;

  @Column({ name: 'ai_mode', type: 'enum', enum: AiMode })
  aiMode: AiMode;

  @Column({ name: 'last_message_at', type: 'timestamp with time zone' })
  lastMessageAt: Date;

  @Column({ name: 'last_inbound_message_at', type: 'timestamp with time zone', nullable: true })
  lastInboundMessageAt?: Date;

  @OneToMany(() => Message, message => message.conversation)
  messages: Message[];

  @OneToMany(() => ConversationAssignment, assignment => assignment.conversation)
  assignments: ConversationAssignment[];
}
```

### Message Entity

```typescript
@Entity('message')
export class Message extends BaseEntity {
  @PrimaryGeneratedColumn('uuid')
  id: string;

  @ManyToOne(() => Organization)
  @JoinColumn({ name: 'organization_id' })
  organization: Organization;

  @ManyToOne(() => Conversation)
  @JoinColumn({ name: 'conversation_id' })
  conversation: Conversation;

  @Column({ name: 'external_message_id' })
  externalMessageId: string;

  @Column({ type: 'enum', enum: MessageDirection })
  direction: MessageDirection; // INBOUND, OUTBOUND

  @Column({ type: 'enum', enum: MessageType })
  type: MessageType; // TEXT, IMAGE, VIDEO, AUDIO, DOCUMENT, LOCATION

  @Column({ type: 'text', nullable: true })
  text?: string;

  @Column({ type: 'jsonb', nullable: true })
  media?: MessageMedia;

  @Column({ type: 'enum', enum: MessageStatus, default: MessageStatus.PENDING })
  status: MessageStatus;

  @Column({ name: 'sent_by_user_id', nullable: true })
  sentByUserId?: string;

  @Column({ name: 'sent_at', type: 'timestamp with time zone' })
  sentAt: Date;

  @Column({ name: 'delivered_at', type: 'timestamp with time zone', nullable: true })
  deliveredAt?: Date;

  @Column({ name: 'read_at', type: 'timestamp with time zone', nullable: true })
  readAt?: Date;
}
```

## Enums

### Channel Types

```typescript
export enum Channel {
  WHATSAPP = 'WHATSAPP',
  INSTAGRAM = 'INSTAGRAM',
  MESSENGER = 'MESSENGER',
}
```

### Message Enums

<Tabs>
  <Tab title="MessageType">
    ```typescript
    export enum MessageType {
      TEXT = 'TEXT',
      IMAGE = 'IMAGE',
      VIDEO = 'VIDEO',
      AUDIO = 'AUDIO',
      DOCUMENT = 'DOCUMENT',
      LOCATION = 'LOCATION',
      CONTACT = 'CONTACT',
      STICKER = 'STICKER',
      REACTION = 'REACTION',
    }
    ```
  </Tab>

  <Tab title="MessageDirection">
    ```typescript
    export enum MessageDirection {
      INBOUND = 'INBOUND',
      OUTBOUND = 'OUTBOUND',
    }
    ```
  </Tab>

  <Tab title="MessageStatus">
    ```typescript
    export enum MessageStatus {
      PENDING = 'PENDING',
      SENT = 'SENT',
      DELIVERED = 'DELIVERED',
      READ = 'READ',
      FAILED = 'FAILED',
    }
    ```
  </Tab>
</Tabs>

### Conversation Enums

```typescript
export enum ConversationStatus {
  OPEN = 'OPEN',
  CLOSED = 'CLOSED',
}

export enum AiMode {
  OFF = 'OFF',
  AUTO_REPLY = 'AUTO_REPLY',
  SUGGEST_ONLY = 'SUGGEST_ONLY',
  DRAFT = 'DRAFT',
}
```

## Message Flows

### Inbound Message Flow

<Steps>
  <Step title="Webhook received">
    Meta sends webhook to `/messaging/webhook`
  </Step>

  <Step title="Signature validation">
    `MetaWebhookGuard` validates `X-Hub-Signature-256`
  </Step>

  <Step title="Event logging">
    Store raw webhook payload in `WebhookEventLog`
  </Step>

  <Step title="Queue processing">
    `webhook-processor` job processes the event:
    - Find organization by account ID
    - Match/create PersonChannel
    - Find/create Conversation
    - Create Message entity
    - Emit WebSocket events
  </Step>

  <Step title="Notification">
    Send notifications to assigned agents/teams
  </Step>
</Steps>

### Outbound Message Flow

<Steps>
  <Step title="API request">
    Agent sends message via `/conversations/:id/messages`
  </Step>

  <Step title="Permission check">
    Verify user can reply to conversation
  </Step>

  <Step title="Transactional outbox">
    Create Message and MessageOutbox in same transaction
  </Step>

  <Step title="Queue sending">
    `message-sender` job processes outbox entry
  </Step>

  <Step title="Platform delivery">
    Send to Meta API via channel provider
  </Step>

  <Step title="Status update">
    Update message status based on platform response
  </Step>
</Steps>

## Business Rules

### Conversation Assignment Rules

<Info>
Each assignment supports nullable `user_id` and `team_id`:
- `user + null` = direct assignment
- `user + team` = agent on behalf of team  
- `null + team` = team pool
</Info>

### AI Mode Cascade

Default AI mode cascades through:
1. ChannelAccount.defaultAiMode
2. Organization default setting
3. OFF (fallback)

### Personal Account Access

<Warning>
Personal accounts can only be accessed by:
- The connecting user (`personalUserId`)
- Users with `MESSAGING_MANAGE` permission
- System processes (webhooks, automation)
</Warning>

## RBAC Permissions & Access Control

### Module Permissions

| Permission | Description |
| --- | --- |
| `MESSAGING_MANAGE` | Full access to messaging module |
| `MESSAGING_WRITE` | Can view and reply to conversations |
| `team_messaging.manage` | Can manage team assignments |

### Resource Permissions

Conversations return `ResourcePermissionsDto` with:

```typescript
interface ConversationPermissions {
  canView: boolean;
  canReply: boolean;
  canAssign: boolean;
  canEdit: boolean;
  canTransfer: boolean;
  canArchive: boolean;
}
```

<Check>
Permission calculation is done in-memory by `ConversationPermissionService` — no extra DB queries.
</Check>

## API Endpoints

### Conversation Endpoints

<CodeGroup>
```typescript GET /conversations
// List conversations with filtering
GET /conversations?status=OPEN&assigned=true&channel=WHATSAPP
```

```typescript GET /conversations/:id
// Get conversation details with permissions
```

```typescript PATCH /conversations/:id
// Update conversation (archive, status, AI mode)
```

```typescript POST /conversations/:id/messages
// Send message to conversation
```

```typescript POST /conversations/:id/assign
// Assign conversation to user/team
```
</CodeGroup>

### Channel Account Endpoints

<CodeGroup>
```typescript GET /channel-accounts
// List connected accounts
```

```typescript POST /channel-accounts/connect
// Connect new account (organization level)
```

```typescript POST /channel-accounts/connect-personal
// Connect personal account
```

```typescript DELETE /channel-accounts/:id
// Disconnect account
```
</CodeGroup>

### Template Endpoints

<CodeGroup>
```typescript GET /templates
// List message templates
```

```typescript POST /templates
// Create new template
```

```typescript PUT /templates/:id
// Update template
```

```typescript DELETE /templates/:id
// Delete template
```
</CodeGroup>

## WebSocket Events & Room Architecture

### Room Structure

Users join rooms based on their access:

```typescript
// Organization-wide room (for managers)
`messaging:${orgId}`

// Personal account rooms (for account owners)
`messaging:${orgId}:personal:${userId}`

// Team rooms (for team members)
`messaging:${orgId}:team:${teamId}`
```

### Event Types

<Tabs>
  <Tab title="Message Events">
    ```typescript
    // New inbound message
    'message-received': {
      conversationId: string;
      message: MessageDto;
    }

    // Outbound message status update
    'message-status-updated': {
      conversationId: string;
      messageId: string;
      status: MessageStatus;
    }
    ```
  </Tab>

  <Tab title="Conversation Events">
    ```typescript
    // Conversation updated
    'conversation-updated': {
      conversation: ConversationDetailDto;
      changes: string[];
    }

    // Assignment changed
    'conversation-assigned': {
      conversationId: string;
      assignments: ConversationAssignmentDto[];
    }
    ```
  </Tab>

  <Tab title="Typing Events">
    ```typescript
    // User typing indicator
    'user-typing': {
      conversationId: string;
      userId: string;
      isTyping: boolean;
    }
    ```
  </Tab>
</Tabs>

## Queue System

### Queue Jobs

<AccordionGroup>
  <Accordion title="webhook-processor">
    Processes incoming Meta webhook events
    - Validates event idempotency
    - Routes to appropriate channel provider
    - Creates/updates conversation and message entities
    - Emits WebSocket and notification events
  </Accordion>

  <Accordion title="message-sender">
    Sends outbound messages via Meta APIs
    - Processes MessageOutbox entries
    - Retries with exponential backoff
    - Updates message status
    - Cleans up outbox on success
  </Accordion>

  <Accordion title="media-downloader">
    Downloads and stores media files
    - Fetches media from Meta CDN
    - Uploads to organization's file storage
    - Updates message with local URLs
  </Accordion>
</AccordionGroup>

### Retry Strategy

<Warning>
Failed jobs use exponential backoff: 1min, 5min, 15min, 1hr, 6hr, 24hr (6 attempts max)
</Warning>

## Error Handling

### Webhook Errors

| Error Type | Response | Action |
| --- | --- | --- |
| Invalid signature | 401 Unauthorized | Log security event |
| Parsing error | 200 OK | Log error, don't retry |
| Processing error | 200 OK | Queue for retry |

### API Errors

Common error responses follow standard format:

```typescript
{
  "error": "CONVERSATION_NOT_FOUND",
  "message": "Conversation not found or access denied",
  "statusCode": 404
}
```

## Testing Strategy

### Unit Tests

- Entity validation and relationships
- Service business logic
- Permission calculations
- Queue job processing

### Integration Tests

- API endpoint functionality
- WebSocket event emission
- Database transactions
- Queue processing end-to-end

### E2E Tests

- Complete message flows (inbound/outbound)
- Multi-tenant isolation
- Personal account access control
- Webhook signature validation

## Migration & Legacy Removal

<Steps>
  <Step title="Deploy unified module">
    Deploy Messaging module alongside existing WhatsApp/Instagram modules
  </Step>

  <Step title="Migrate webhooks">
    Update Meta App webhook URL to new `/messaging/webhook` endpoint
  </Step>

  <Step title="Data migration">
    Run migration scripts to move data to unified entities
  </Step>

  <Step title="Frontend updates">
    Update frontend to use new WebSocket namespace and API endpoints
  </Step>

  <Step title="Remove legacy">
    Remove old WhatsApp and Instagram modules after validation
  </Step>
</Steps>

## Future Enhancements

<CardGroup cols={2}>
  <Card title="Additional Channels" icon="plus">
    Support for SMS, email, and other messaging platforms
  </Card>
  <Card title="Advanced AI Features" icon="brain">
    Sentiment analysis, intent detection, automated workflows
  </Card>
  <Card title="Analytics & Reporting" icon="chart-line">
    Message volume, response times, agent performance metrics
  </Card>
  <Card title="Bulk Operations" icon="layer-group">
    Bulk message sending, conversation management
  </Card>
</CardGroup>

## Related Documentation

- [Multi-Tenancy Specification](../core/multi-tenancy.md)
- [Archive System Specification](../core/archive-system.md)
- [Queue System Specification](../queue/queue-specification.md)
- [WebSocket Architecture](../websocket/websocket-architecture.md)
- [CRM Integration Guide](../crm/crm-integration.md)