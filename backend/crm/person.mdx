---
title: "Person"
description: "Central identity layer — every individual (lead, contact) links to a Person entity for deduplication and unified communication history."
---

<Note>
**Vocabulary note**: This documentation uses "stakeholder" because it describes the engineering subsystem (entity name, table, services). The **user-facing and AI-facing** terms are "Assignment" (the concept) and "Assignee" (the assigned user or team). See `Docs/STAKEHOLDER_SYSTEM.md` → "Vocabulary" for the full policy and which surfaces switched.
</Note>

<Note>
**Unified inbound lead capture**: Leads arriving from external sources (Property Finder, Bayut/dubizzle, and future Meta/website) are ingested through the source-agnostic `crm/lead-capture` module — `LeadCaptureService.capture()` reuses `PersonService`, `LeadService.createLeadInTransaction`/`findDuplicateLeadMatchInTransaction`, `EntityStakeholderService`, and the `DistributionEngine`. The lead-capture module owns the `CapturedLeadInput` contract, `LeadCaptureSourceRegistry` for adapter registration, org-default `LeadCaptureSettings`, the `CapturedLead` idempotency ledger, and the source-agnostic `lead-ingestion` pg-boss queue + `LeadIngestionWorker`. Full design: `Docs/LEAD_CAPTURE_SPECIFICATION.md`.
</Note>

## Architecture overview

### Design principles

1. **Person + Contact Model**:
   - `Person` is the hidden identity layer (single source of truth for personal details)
   - `Contact` is the business relationship layer (qualified customers)
   - `Lead` is the sales opportunity layer (unqualified inquiries)
   - `Deal` links to `Contact`, not `Person` directly

2. **Unified Stakeholder Model**: Single table for assignment and commission across leads/deals

3. **Polymorphic Patterns**: Notes, tags, and activities use entity_type/entity_id patterns

4. **Channel Separation**: Activity table indexes timeline; channel tables store full data

5. **Modular Design**: CRM core is independent; Real Estate, Marketing, Channels are optional modules

6. **Company via Contact**: Companies associate with `Contact` via `ContactCompanyRole` (not Person)

7. **Organization Membership Display**: CRM DTOs batch-resolve organization membership status for user references, showing removed org members with `isActiveOrgMember: false` badges while preserving historical assignment data

### Module boundaries

```
┌─────────────────────────────────────────────────────────────────┐
│                         CRM CORE                                │
│  Person, Lead, Contact, Company, Deal, DealContact             │
│  person_email, person_phone, person_address, person_channel    │
│  person_not_duplicate, contact_company_role                    │
│  entity_stakeholder, entity_transfer, commission_payment       │
│  activity, note, task, event, tag                              │
└─────────────────────────────────────────────────────────────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ REAL ESTATE  │    │ LEAD CAPTURE │    │   CHANNELS   │
│ development  │    │ captured_lead│    │  whatsapp    │
│ unit         │    │ ingestion    │    │  instagram   │
│ site_visit   │    │ settings     │    │  (linked via │
│ lead_property│    │              │    │  person_     │
│ _interest    │    │              │    │  channel)    │
│ unit_owner-  │    │              │    │              │
│ ship→Person  │    │              │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

### AI module integration

The CRM module integrates with the fully implemented and operational AI module (`AiModule`) for comprehensive automated conversations, lead processing, and workflow-assisted follow-up. CRM owns the business entities and lifecycle rules; the AI module owns agent configuration, runtime execution, queueing, LLM integration, security controls, and activity logging.

The AI module provides:

- **AI Agent Templates**: 10 pre-configured agent types (Receptionist, Sales Qualification, Listing Inquiry, Off-Plan Inquiry, Appointment Booking, After-Hours, FAQ & Support, Campaign Lead Capture, Spam Handler, Human Handoff) seeded via AiAgentTemplateSeeder
- **Knowledge Base Integration**: FAQ, SNIPPET, DOCUMENT, PAGE types with chunking, embedding, and RAG capabilities through KnowledgeBaseService
- **Credit Management**: Usage tracking with organizational and personal budgets via AiCreditUsageService
- **Queue-based Execution**: Reliable agent processing via AiAgentExecuteWorker with retry logic and error handling
- **Tool Registry**: Extensible system through AiAgentToolRegistryService and AiAgentActionService
- **OpenAI Integration**: Project provisioning and encrypted key management through OpenAiProjectProvisioner and OpenAiEncryptionService
- **Activity Logging**: Comprehensive audit trail via AiActivityLogService with filtering and analytics
- **Workflow Integration**: Bidirectional integration - AI agents can trigger workflows through the `trigger_workflow` action AND workflows can activate AI agents via AI_AGENT steps
- **Media Processing**: Audio/image processing via AiAgentMediaProcessorService

## Core entities

### Person entity specification

**Purpose**: Single source of truth for human identity and preferences.

```
Person
├── public_id (coded org-scoped human ID, e.g. PERS-ADNS-001)
├── Identity: first_name, last_name, avatar_url, title
├── Demographics: date_of_birth, nationality
├── Links: links jsonb [{ type, url }] where type is website/facebook/instagram/linkedIn/twitter/tiktok
├── Preferences: timezone
├── Languages: languages (unified array with code and proficiency per entry)
├── Communication Flags: do_not_call, do_not_email
├── Source Tracking: original_source
├── Merge Tracking: merged_into_id, merged_at, merged_by
├── Computed: full_name (getter: first_name + last_name)
└── Related Tables:
    ├── person_email (multiple emails, one primary)
    ├── person_phone (multiple phones, one primary)
    ├── person_address (multiple addresses, one primary)
    ├── person_channel (WhatsApp, Instagram, etc. identities)
    └── person_not_duplicate (deduplication override pairs)
```

<Note>
**Key Rules**: Every Lead, Contact must link to a Person. `publicId` is generated once at creation using the shared CRM public ID allocator (`PERS-*`). Person preferences apply across all contexts (leads, deals, contacts). The `nationality` is stored on Person so leads and contacts share the same identity profile value. The `original_source` is set once when person first enters system.
</Note>

### Contact entity

**Purpose**: Business relationship layer representing qualified customers.

```
Contact
├── public_id (coded org-scoped contact ID, e.g. CONT-ADNS-001)
├── person_id → Person
├── contact_source
├── qualified_at
├── qualification_score
├── status (active, inactive, archived)
├── customer_segment
├── lifetime_value
├── contact_owner → User
└── qualification_criteria (JSON)
```

<Info>
Contacts are created when leads are qualified or external customers are imported. Each contact must link to exactly one Person entity for identity management.
</Info>

### Lead entity

**Purpose**: Sales opportunity layer for unqualified inquiries.

```
Lead
├── public_id (coded org-scoped lead ID, e.g. LEAD-ADNS-001)
├── person_id → Person
├── lead_source
├── stage → LeadStage
├── score
├── status (active, converted, disqualified, archived)
├── assigned_to → User
├── source_channel_id → PersonChannel
├── converted_to_contact_id → Contact
└── conversion_metadata (JSON)
```

### Company entity

**Purpose**: Business entity associated with contacts.

```
Company
├── public_id (coded org-scoped company ID, e.g. COMP-ADNS-001)
├── company_name
├── industry
├── company_size
├── annual_revenue
├── website
├── description
├── headquarters_address
├── company_status (active, inactive, acquired, defunct)
└── Related Tables:
    └── contact_company_role (contact-company relationships)
```

### Deal entity

**Purpose**: Transaction records linked to qualified contacts.

```
Deal
├── public_id (coded org-scoped deal ID, e.g. DEAL-ADNS-001)
├── deal_name
├── stage → DealStage
├── value, currency
├── close_date, expected_close_date
├── probability
├── deal_source
├── assigned_to → User
├── status (open, closed_won, closed_lost)
└── Related Tables:
    └── deal_contact (many-to-many with Contact)
```

## Assignment & commission system

The CRM module uses a unified stakeholder model for assignment and commission tracking across leads and deals.

### Entity stakeholder

**Purpose**: Unified assignment and commission table for leads/deals.

```
entity_stakeholder
├── entity_type (lead, deal)
├── entity_id (lead.id or deal.id)
├── stakeholder_type (agent, team_lead, split_agent, broker)
├── stakeholder_id → User
├── role_percentage (0-100, commission split)
├── assigned_at, assigned_by
├── transferred_from_id → EntityStakeholder (for tracking transfers)
└── is_active (boolean)
```

<Note>
Stakeholders can have overlapping assignments (e.g., agent + team lead on same entity). Role percentages are for commission calculation and should sum to 100% per entity.
</Note>

### Commission payment

**Purpose**: Tracks commission payments to stakeholders.

```
commission_payment
├── public_id (coded org-scoped commission ID, e.g. COMM-ADNS-001)
├── stakeholder_id → EntityStakeholder
├── amount, currency
├── payment_date, due_date
├── status (pending, paid, cancelled)
├── reference_number
├── notes
└── paid_by → User
```

## Transfer system

The CRM implements a comprehensive transfer system for moving leads and deals between stakeholders.

### Entity transfer

**Purpose**: Records all transfer requests and approvals.

```
entity_transfer
├── entity_type (lead, deal)
├── entity_id
├── from_stakeholder_id → EntityStakeholder
├── to_stakeholder_id → EntityStakeholder
├── transfer_reason
├── requested_by → User
├── requested_at
├── status (pending, approved, rejected)
├── approved_by → User
├── approved_at
└── rejection_reason
```

<Note>
Self-assignments and transfers to the same organization do not require approval. Cross-organization transfers require explicit approval from both sides.
</Note>

## Activity & communication system

All communication activities are tracked through a unified activity system that references channel-specific data.

### Activity entity

**Purpose**: Central timeline for all person interactions.

```
activity
├── entity_type (person, lead, contact, deal)
├── entity_id
├── activity_type (call, email, sms, whatsapp, meeting)
├── direction (inbound, outbound)
├── subject, content
├── channel_reference_id (links to specific channel message)
├── performed_by → User
├── performed_at
├── metadata (JSON for channel-specific data)
└── engagement_score
```

## Notes system

The notes system provides contextual annotations for any CRM entity.

### Note entity

**Purpose**: Polymorphic notes for all CRM entities.

```
note
├── entity_type (person, lead, contact, deal, company)
├── entity_id
├── content
├── note_type (general, follow_up, important, private)
├── visibility (public, private, team)
├── created_by → User
├── created_at, updated_at
├── pinned (boolean)
└── mentioned_users (array of User IDs)
```

<Note>
Private notes are only visible to the creator. Team notes are visible to all team members. Public notes are visible to all users with entity access.
</Note>

## Stage history & analytics

The system tracks detailed progression through sales stages for analytics and reporting.

### Stage history

**Purpose**: Tracks entity progression through sales stages.

```
stage_history
├── entity_type (lead, deal)
├── entity_id
├── from_stage, to_stage
├── changed_by → User
├── changed_at
├── duration_in_stage (computed)
├── change_reason
└── automated (boolean)
```

<Info>
Duration calculations help identify bottlenecks in the sales process and optimize conversion workflows.
</Info>

## Query patterns

### Performance optimization queries

<AccordionGroup>
<Accordion title="Person deduplication queries">
```sql
-- Find potential duplicate persons by email
SELECT p1.id, p2.id FROM person p1
JOIN person_email pe1 ON pe1.person_id = p1.id
JOIN person_email pe2 ON pe2.person_id = p2.id
WHERE pe1.email = pe2.email AND p1.id < p2.id
AND NOT EXISTS (
  SELECT 1 FROM person_not_duplicate pnd
  WHERE (pnd.person1_id = p1.id AND pnd.person2_id = p2.id)
     OR (pnd.person1_id = p2.id AND pnd.person2_id = p1.id)
);
```
</Accordion>

<Accordion title="Channel lookup queries">
```sql
-- Find person by channel identifier
SELECT p.* FROM person p
JOIN person_channel pc ON pc.person_id = p.id
WHERE pc.channel_type = :channelType 
AND pc.channel_identifier = :identifier
AND pc.status = 'active';

-- Get all active channels for person
SELECT * FROM person_channel
WHERE person_id = :personId AND status = 'active'
ORDER BY is_primary DESC, created_at ASC;
```
</Accordion>

<Accordion title="Contact information queries">
```sql
-- Get primary contact information
SELECT 
  p.id, p.first_name, p.last_name,
  pe.email, pp.phone_number, pa.city
FROM person p
LEFT JOIN person_email pe ON pe.person_id = p.id AND pe.is_primary = true
LEFT JOIN person_phone pp ON pp.person_id = p.id AND pp.is_primary = true
LEFT JOIN person_address pa ON pa.person_id = p.id AND pa.is_primary = true
WHERE p.id = :personId;
```
</Accordion>
</AccordionGroup>

## Business rules

### Data integrity rules

<AccordionGroup>
<Accordion title="Person contact info rules">
- Each person must have at least one email or phone number
- Only one primary email/phone/address per person per organization
- Email format validation with domain checking
- Phone number normalization to E.164 format
</Accordion>

<Accordion title="Assignment rules">
- Every lead/deal must have at least one active stakeholder
- Commission percentages must sum to 100% per entity
- Transfers require active stakeholder status on source assignment
- Self-assignment transfers do not require approval
</Accordion>

<Accordion title="Channel rules">
- Channel belongs to Person, not Lead (Person-centric architecture)
- One Person can have multiple channels of same type
- All channels must have valid identifiers for their type
- Channel status affects message delivery capabilities
</Accordion>
</AccordionGroup>

### Validation patterns

<AccordionGroup>
<Accordion title="Email validation">
```typescript
// Email format validation with domain checking
const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const isValidEmail = emailRegex.test(email) && !isDisposableEmail(email);

// Bounce status checking before sending
if (personEmail.bounce_status === 'hard_bounce') {
  throw new Error('Cannot send to hard bounced email');
}
```
</Accordion>

<Accordion title="Phone validation">
```typescript
// International phone number validation
import { parsePhoneNumber } from 'libphonenumber-js';

function validatePhone(phone: string, countryCode?: string) {
  const phoneNumber = parsePhoneNumber(phone, countryCode);
  return phoneNumber?.isValid() ?? false;
}
```
</Accordion>

<Accordion title="Channel validation">
```typescript
// Channel-specific identifier validation
function validateChannelIdentifier(type: string, identifier: string) {
  switch(type) {
    case 'whatsapp':
      return parsePhoneNumber(identifier)?.isValid();
    case 'instagram':
      return /^[a-zA-Z0-9_.]+$/.test(identifier);
    case 'facebook':
      return /^\d+$/.test(identifier); // PSID
    default:
      return identifier.length > 0;
  }
}
```
</Accordion>
</AccordionGroup>

## Entity relationship diagram

The Person entity sits at the center of the CRM system with the following key relationships:

```
Person
├── OneToMany: PersonEmail (person.emails)
├── OneToMany: PersonPhone (person.phones) 
├── OneToMany: PersonAddress (person.addresses)
├── OneToMany: PersonChannel (person.channels)
├── OneToMany: Lead (person.leads)
├── OneToOne: Contact (person.contact)
├── OneToMany: UnitOwnership (person.ownerships) [Real Estate]
├── OneToMany: SiteVisit (person.siteVisits) [Real Estate]
├── OneToMany: Activity (polymorphic via entity_type/entity_id)
├── OneToMany: Note (polymorphic via entity_type/entity_id)
└── ManyToMany: PersonNotDuplicate (deduplication overrides)
```

## Events & integration

### Domain events

The Person entity publishes domain events for external systems and internal automation:

```typescript
// Person events
PersonCreated {
  personId: string;
  organizationId: string;
  source: string;
  createdAt: Date;
}

PersonMerged {
  primaryPersonId: string;
  mergedPersonIds: string[];
  mergedBy: string;
  mergedAt: Date;
}

PersonChannelAdded {
  personId: string;
  channelType: string;
  channelIdentifier: string;
  isPrimary: boolean;
}

PersonContactInfoUpdated {
  personId: string;
  contactType: 'email' | 'phone' | 'address';
  isPrimary: boolean;
  updatedBy: string;
}
```

### External integrations

<CardGroup cols={2}>
  <Card title="Marketing automation" icon="envelope">
    Sync person data and communication preferences to marketing platforms for automated campaigns.
  </Card>
  <Card title="Communication channels" icon="comments">
    Bi-directional sync with WhatsApp, Instagram, Email, and SMS platforms for unified messaging.
  </Card>
  <Card title="Real estate systems" icon="home">
    Integration with property management, inventory, and transaction systems via Person entity.
  </Card>
  <Card title="Analytics platforms" icon="chart-line">
    Stream person events for customer analytics, segmentation, and lifetime value calculations.
  </Card>
</CardGroup>

## Data consistency guarantees

The CRM module enforces strict data consistency through multiple mechanisms:

### Transaction boundaries

<AccordionGroup>
<Accordion title="Person merge consistency">
All person merges are wrapped in database transactions to ensure atomicity. If any step fails during merge (field updates, contact info reassignment, lead reassignment), the entire operation is rolled back.
</Accordion>

<Accordion title="Channel assignment consistency">
When assigning channels to persons, the system ensures no duplicate channel identifiers exist within the same organization context. Unique constraints prevent data corruption.
</Accordion>

<Accordion title="Commission calculation consistency">
Commission percentage validation ensures that all stakeholder percentages sum to exactly 100% per entity. Database triggers prevent invalid commission splits.
</Accordion>
</AccordionGroup>