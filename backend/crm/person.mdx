---
title: "Person"
description: "Central identity layer — every individual (lead, contact) links to a Person entity for deduplication and unified communication history."
---

This document contains the complete specification for the CRM module, including entity relationships, implementation logic, query patterns, and business rules.

## Table of contents

1. [Architecture Overview](#architecture-overview)
2. [Core Entities](#core-entities)
3. [Assignment & Commission System](#assignment--commission-system)
4. [Transfer System](#transfer-system)
5. [Activity & Communication System](#activity--communication-system)
6. [Notes System](#notes-system)
7. [Stage History & Analytics](#stage-history--analytics)
8. [Query Patterns](#query-patterns)
9. [Business Rules](#business-rules)
10. [Entity Relationship Diagram](#entity-relationship-diagram)
11. [Events & Integration](#events--integration)
12. [Data Consistency Guarantees](#data-consistency-guarantees)

---

## Architecture overview

### Design principles

1. **Person + Contact Model**:
   - `Person` is the hidden identity layer (single source of truth for personal details)
   - `Contact` is the business relationship layer (qualified customers)
   - `Lead` is the sales opportunity layer (unqualified inquiries)
   - `Deal` links to `Contact`, not `Person` directly

2. **Unified Stakeholder Model**: Single table for assignment and commission across leads/deals

3. **Polymorphic Patterns**: Notes, tags, and activities use entity_type/entity_id patterns

4. **Channel Separation**: Activity table indexes timeline; channel tables store full data

5. **Modular Design**: CRM core is independent; Real Estate, Marketing, Channels are optional modules

6. **Company via Contact**: Companies associate with `Contact` via `ContactCompanyRole` (not Person)

### Module boundaries

```
┌─────────────────────────────────────────────────────────────────┐
│                         CRM CORE                                │
│  Person, Lead, Contact, Company, Deal, DealContact             │
│  person_email, person_phone, person_address, person_channel    │
│  person_not_duplicate, contact_company_role                    │
│  entity_stakeholder, entity_transfer, commission_payment       │
│  activity, note, task, tag                                     │
└─────────────────────────────────────────────────────────────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ REAL ESTATE  │    │  MARKETING   │    │   CHANNELS   │
│ development  │    │  campaign    │    │  whatsapp    │
│ unit         │    │  campaign_   │    │  instagram   │
│ site_visit   │    │  lead        │    │  (linked via │
│ lead_property│    │              │    │  person_     │
│ _interest    │    │              │    │  channel)    │
│ unit_owner-  │    │              │    │              │
│ ship→Person  │    │              │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

### Real Estate → CRM integration

Real Estate entities link to **Person** (not Contact) for identity:

| Real Estate Entity       | Links To    | Rationale                                   |
| ------------------------ | ----------- | ------------------------------------------- |
| `unit_ownership`         | `person_id` | Ownership is about identity, not CRM status |
| `unit_transaction`       | `person_id` | Transaction party is an individual          |
| `site_visit`             | `person_id` | Who visited the property                    |
| `lead_property_interest` | `lead_id`   | Links to Lead for sales context             |
| `deal_property_interest` | `deal_id`   | Links to Deal for transaction context       |

**Deal Property Interest Workflow**:

```
// Deal created FROM Lead:
// Copy primary LeadPropertyInterest → DealPropertyInterest (1:1)
deal.propertyInterest.originatingInterest = leadPropertyInterest

// Deal created directly (walk-in):
// Create DealPropertyInterest with no originating interest
deal.propertyInterest.originatingInterest = null
```

**Access Pattern for Contact**:

```
// Contact page showing ownership
contact.person.unitOwnerships  // All ownerships
contact.person.unitOwnerships.filter(o => o.isActive)  // Current
```

---

## Core entities

### Person (Central Identity)

**Purpose**: Single source of truth for human identity and preferences.

```
Person
├── Identity: first_name, last_name, avatar_url, title
├── Demographics: date_of_birth
├── Social: website, linkedin_url, twitter_url
├── Preferences: preferred_contact_method, timezone, preferred_language
├── Language Proficiency: languageProficiency (per-language skill levels for distribution matching)
├── Communication Flags: do_not_call, do_not_email
├── Source Tracking: original_source, languages[]
├── Merge Tracking: merged_into_id, merged_at, merged_by
├── Computed: full_name (getter: first_name + last_name)
└── Related Tables:
    ├── person_email (multiple emails, one primary)
    ├── person_phone (multiple phones, one primary)
    ├── person_address (multiple addresses, one primary)
    ├── person_channel (WhatsApp, Instagram, etc. identities)
    └── person_not_duplicate (deduplication override pairs)
```

<Note>
  **Key Rules**: Every Lead, Contact must link to a Person. Person preferences apply across all contexts (leads, deals, contacts). The `original_source` is set once when person first enters system. Language proficiency is used for distribution matching to assign leads to agents with appropriate language skills.
</Note>

#### PersonEmail, PersonPhone, PersonAddress

These related tables follow similar patterns for storing multiple contact details per person:

<Tabs>
<Tab title="PersonEmail">
```
person_email
├── person_id → Person
├── email
├── is_primary (boolean)
├── is_verified
├── verification_token
├── bounce_status
├── created_at, updated_at
└── organization_id
```
</Tab>
<Tab title="PersonPhone">
```
person_phone
├── person_id → Person
├── phone_number
├── phone_type (mobile, home, work, fax)
├── is_primary (boolean)
├── is_verified
├── country_code
├── created_at, updated_at
└── organization_id
```
</Tab>
<Tab title="PersonAddress">
```
person_address
├── person_id → Person
├── address_line_1, address_line_2
├── city, state, postal_code, country
├── address_type (home, work, billing, shipping)
├── is_primary (boolean)
├── coordinates (latitude, longitude)
├── created_at, updated_at
└── organization_id
```

Each person can have multiple addresses for different purposes. The system maintains one primary address per person for default communication and billing purposes. Coordinates are stored for location-based queries and mapping functionality. Address validation and geocoding services ensure data accuracy.
</Tab>
</Tabs>

#### PersonChannel (Communication Channels)

**Purpose**: Stores a person's communication channel identities (WhatsApp, Instagram, etc.).

```
person_channel
├── person_id → Person
├── channel_type (whatsapp, instagram, facebook, telegram, sms, webchat)
├── channel_identifier (phone number, username, PSID, etc.)
├── display_name, avatar_url
├── channel_identity_id → WhatsAppUser.id, InstagramUser.id, etc.
├── status (active, inactive, blocked, unsubscribed)
├── is_primary
├── Opt-in: marketing_opt_in, transactional_opt_in
├── Engagement: first_contact_at, last_message_at, message_count
└── Verification: is_verified, verified_at
```

<Info>
  **Key Rules**: Channel belongs to Person, not Lead (Person-centric architecture). Lead can reference `source_channel_id` for attribution (which channel it came through). The `channel_identity_id` links to detailed channel entities (WhatsAppUser, InstagramUser). One Person can have multiple channels of same type (e.g., multiple WhatsApp numbers).
</Info>

#### PersonNotDuplicate (Deduplication Overrides)

**Purpose**: Records pairs of persons that have been manually confirmed as NOT duplicates. Prevents the deduplication system from repeatedly flagging the same pair.

```
person_not_duplicate
├── person1_id → Person
├── person2_id → Person
├── marked_by → User (who made the decision)
├── marked_at (when the decision was made)
├── organization_id → Organization
├── Unique constraint: (person1, person2, organization)
```

<Note>
  **Key Rules**: The relationship is symmetric — if (A, B) is marked as not-duplicate, the system treats (B, A) equivalently. Each organization maintains its own override decisions. Used by `PersonNotDuplicateService` to exclude pairs from duplicate detection.
</Note>

### Lead

**Purpose**: Sales opportunity layer for unqualified inquiries.

```
Lead
├── person_id → Person
├── lead_source, source_channel_id
├── status, stage, priority
├── budget_min, budget_max
├── expected_close_date
├── qualification_notes
├── converted_at, converted_to_contact_id
└── stakeholder assignments and commission
```

### Contact

**Purpose**: Business relationship layer for qualified customers.

```
Contact
├── person_id → Person (OneToOne)
├── contact_status (active, inactive, archived)
├── relationship_type (client, prospect, partner)
├── customer_since
├── lifetime_value
├── last_interaction_at
└── company associations via ContactCompanyRole
```

### Company

**Purpose**: Business entities that contacts can be associated with.

```
Company
├── company_name, legal_name
├── company_type, industry
├── website, description
├── employee_count, annual_revenue
├── headquarters location
└── associations to Contact via ContactCompanyRole
```

### Deal

**Purpose**: Transaction records linked to qualified contacts.

```
Deal
├── contact_id → Contact (not Person directly)
├── deal_name, description
├── deal_value, currency
├── probability, stage
├── expected_close_date, actual_close_date
├── deal_source, campaign_id
├── created_by, assigned_to
└── DealContact for multiple contacts per deal
```

---

## Assignment & commission system

Uses `entity_stakeholder` table for unified stakeholder management across leads and deals:

```
entity_stakeholder
├── entity_type (lead, deal, contact)
├── entity_id
├── stakeholder_id → User
├── stakeholder_type (owner, collaborator, observer)
├── assignment_date
├── commission_percentage
├── is_primary
└── organization_id
```

<Note>
  This unified approach allows consistent assignment and commission tracking across all CRM entities, simplifying the stakeholder management system.
</Note>

---

## Transfer system

The `entity_transfer` table tracks ownership changes:

```
entity_transfer
├── entity_type, entity_id
├── from_user_id, to_user_id
├── transfer_reason, notes
├── transferred_at, transferred_by
├── is_approved, approved_by, approved_at
└── organization_id
```

<Note>
  Transfers can be configured to require approval workflows, ensuring proper oversight of lead/deal reassignments.
</Note>

---

## Activity & communication system

Polymorphic activity tracking across all CRM entities:

```
activity
├── entity_type, entity_id
├── activity_type (call, email, meeting, note, task)
├── title, description
├── scheduled_at, completed_at
├── created_by, assigned_to
├── channel_type, channel_id
└── organization_id
```

<Info>
  The activity table provides timeline indexing while detailed data remains in specific channel tables, balancing query performance with data richness.
</Info>

---

## Notes system

Polymorphic notes attached to any CRM entity:

```
note
├── entity_type, entity_id
├── content, note_type
├── is_private, is_pinned
├── created_by, updated_by
├── created_at, updated_at
└── organization_id
```

<Note>
  Notes can be marked as private for internal team use or pinned for important information that should remain visible.
</Note>

---

## Stage history & analytics

Track status/stage changes over time:

```
stage_history
├── entity_type, entity_id
├── from_stage, to_stage
├── changed_at, changed_by
├── duration_in_previous_stage
├── notes, reason
└── organization_id
```

<Note>
  Stage history enables analytics on conversion rates, stage duration, and bottleneck identification in the sales pipeline.
</Note>

---

## Query patterns

### Lead with person details

```sql
SELECT l.*, p.first_name, p.last_name, pe.email, pp.phone_number
FROM lead l
JOIN person p ON l.person_id = p.id
LEFT JOIN person_email pe ON p.id = pe.person_id AND pe.is_primary = true
LEFT JOIN person_phone pp ON p.id = pp.person_id AND pp.is_primary = true
WHERE l.organization_id = ?
```

### Contact with company roles

```sql
SELECT c.*, p.first_name, p.last_name, 
       comp.company_name, ccr.role_title
FROM contact c
JOIN person p ON c.person_id = p.id
LEFT JOIN contact_company_role ccr ON c.id = ccr.contact_id
LEFT JOIN company comp ON ccr.company_id = comp.id
WHERE c.organization_id = ?
```

### Deal pipeline analytics

```sql
SELECT stage, COUNT(*) as deal_count, SUM(deal_value) as total_value
FROM deal
WHERE organization_id = ? AND stage != 'closed_lost'
GROUP BY stage
ORDER BY total_value DESC
```

---

## Business rules

### Lead to Contact conversion

<Steps>
<Step title="Qualification check">
Lead status must be 'qualified' or 'hot'
</Step>
<Step title="Contact info validation">
Person must have primary email and phone
</Step>
<Step title="Contact creation">
Creates Contact with OneToOne relationship to Person
</Step>
<Step title="Lead marking">
Marks Lead as converted with conversion timestamp
</Step>
<Step title="Stakeholder transfer">
Transfers all stakeholder assignments to new Contact
</Step>
</Steps>

### Deal creation requirements

- Must be linked to existing Contact (not Lead directly)
- Deal value must be positive
- Expected close date cannot be in the past
- Primary stakeholder assignment is required

### Person deduplication rules

<AccordionGroup>
<Accordion title="Matching hierarchy">
1. Exact email match takes precedence
2. Phone number match (normalized format)
3. Name + organization combination
4. Manual overrides via PersonNotDuplicate table
</Accordion>
</AccordionGroup>

---

## Person merge system

Consolidates duplicate persons into a single primary record, reassigning all related data.

**API endpoint:** `POST /persons/:primaryPersonId/merge`

<Steps>
<Step title="Validation">
Verify the primary person and all secondary persons exist, are not deleted, and belong to the same organization.
</Step>
<Step title="Field selection">
Accept `fieldSelections: Record<string, string>` (e.g., `{ "firstName": "primary", "lastName": "person-B-id" }`). For each field, pick the value from the specified source person. Fields not listed default to the primary person's values.
</Step>
<Step title="Contact info merge (opt-in per type)">
Three boolean flags control which contact info is reassigned from secondary persons to the primary:

- `mergeAllEmails` — reassign all secondary emails (`isPrimary` reset to `false`)
- `mergeAllPhones` — reassign all secondary phones (`isPrimary` reset to `false`)  
- `mergeAllAddresses` — reassign all secondary addresses (`isPrimary` reset to `false`)
</Step>
<Step title="Channel merge (always)">
All channels from secondary persons are reassigned to the primary person (`isPrimary` reset to `false`). This is not optional since channels represent unique identities.
</Step>
<Step title="Lead and ownership reassignment">
All leads pointing to secondary persons are bulk-reassigned to the primary person via `nativeUpdate`. All `InventoryUnitOwnership` records are similarly reassigned.
</Step>
<Step title="Mark secondaries">
Each secondary person is marked with `mergedIntoId`, `mergedAt`, `mergedBy`, and soft-deleted (`isDeleted = true`).
</Step>
</Steps>

<Warning>
  Merge is **irreversible** — secondary persons are soft-deleted. Contact entities are NOT directly reassigned; since Contact has a `OneToOne` to Person, the primary person retains its contact if one exists.
</Warning>

### Import services

- **`LeadImportService`** — Handles bulk lead import with person matching/creation, deduplication by `referenceId + leadSource`, and stakeholder assignment.
- **`ContactImportService`** — Handles bulk contact import with person matching, uniqueness validation (one contact per person per organization), and activity backfill.

### Person creation and deduplication

When a lead is created, the system resolves or creates a Person:

```typescript
async function findOrCreatePerson(data: CreateLeadDto) {
  // 1. Check by email
  const existingByEmail = await personEmailRepo.findOne({
    email: data.email,
    organization: data.organizationId,
  });
  if (existingByEmail) return existingByEmail.person;

  // 2. Check by phone
  const existingByPhone = await personPhoneRepo.findOne({
    phone_number: data.phone,
    organization: data.organizationId,
  });
  if (existingByPhone) return existingByPhone.person;

  // 3. Create new person + contact info
  const person = await personRepo.create({ ... });
  await personEmailRepo.create({ person, email: data.email, is_primary: true });
  await personPhoneRepo.create({ person, phone_number: data.phone, is_primary: true });

  return person;
}
```

<Info>
  If `personId` is provided directly in the lead creation request, the system uses that existing person instead of searching.
</Info>

---

## Entity relationship diagram

The CRM module follows a hierarchical structure with Person at the center:

```
Person (Identity Layer)
├── PersonEmail
├── PersonPhone  
├── PersonAddress
├── PersonChannel
└── PersonNotDuplicate

Person → Lead (1:N)
Person → Contact (1:1)
Contact → Deal (1:N)
Contact → ContactCompanyRole ← Company

Polymorphic Entities:
├── EntityStakeholder (leads, deals, contacts)
├── EntityTransfer (ownership changes)
├── Activity (communications timeline)
├── Note (annotations)
└── StageHistory (status changes)
```

<CardGroup cols={2}>
  <Card title="Lead" icon="bullseye" href="/backend/crm/lead">
    Sales opportunities linked to persons in the system.
  </Card>
  <Card title="Contact" icon="address-book" href="/backend/crm/contact">
    Business relationships created when leads convert.
  </Card>
  <Card title="Deal" icon="handshake" href="/backend/crm/deal">
    Transaction records linked to qualified contacts.
  </Card>
  <Card title="Company" icon="building" href="/backend/crm/company">
    Business entities associated with contacts.
  </Card>
</CardGroup>

---

## Events & integration

The CRM module publishes domain events for external systems and internal automation:

### Domain Events

```typescript
PersonCreated {
  personId: string;
  organizationId: string;
  source: string;
  createdAt: Date;
}

PersonMerged {
  primaryPersonId: string;
  mergedPersonIds: string[];
  mergedBy: string;
  mergedAt: Date;
}

LeadCreated {
  leadId: string;
  personId: string;
  source: string;
  assignedTo?: string;
}

ContactConverted {
  contactId: string;
  fromLeadId: string;
  personId: string;
  convertedBy: string;
}

DealStageChanged {
  dealId: string;
  fromStage: string;
  toStage: string;
  changedBy: string;
  probability: number;
}
```

### Integration Points

<Tabs>
<Tab title="Marketing Automation">
- Lead creation triggers email sequences
- Contact conversion updates marketing lists
- Deal closure triggers fulfillment workflows
</Tab>
<Tab title="Real Estate Module">
- Person ownership records sync to CRM
- Property interests link to deals
- Site visits create activities
</Tab>
<Tab title="Communication Channels">
- WhatsApp/Instagram messages create activities
- Channel engagement updates person records
- Opt-out preferences sync across channels
</Tab>
</Tabs>

---

## Data consistency guarantees

### ACID Properties

<AccordionGroup>
<Accordion title="Atomicity">
- Person merge operations are wrapped in database transactions
- Lead-to-Contact conversion maintains referential integrity
- Stakeholder assignments are created/updated atomically with entities
</Accordion>

<Accordion title="Consistency">
- Foreign key constraints prevent orphaned records
- Check constraints validate enum values and business rules
- Unique constraints prevent duplicate primary contact info per person
</Accordion>

<Accordion title="Isolation">
- Read-committed isolation level for most operations
- Serializable isolation for person merge to prevent race conditions
- Row-level locking for high-contention updates (assignment changes)
</Accordion>

<Accordion title="Durability">
- All CRM operations are immediately committed to disk
- Soft deletes preserve audit trails for compliance
- Archive processes maintain historical data integrity
</Accordion>
</AccordionGroup>

### Eventual Consistency

Some operations may have eventual consistency guarantees:

- **Search Index Updates**: Elasticsearch indexing may lag behind database writes by seconds
- **Analytics Aggregations**: Pipeline metrics are updated via scheduled jobs
- **External System Sync**: Third-party integrations use retry mechanisms with exponential backoff

### Data Quality Assurance

<Steps>
<Step title="Input validation">
All API endpoints validate data formats, required fields, and business rules
</Step>
<Step title="Background verification">
Email and phone verification processes run asynchronously
</Step>
<Step title="Duplicate detection">
Automated duplicate detection runs daily with manual review workflows
</Step>
<Step title="Data cleanup">
Scheduled jobs clean up orphaned records and normalize data formats
</Step>
</Steps>

<Note>
  The CRM module prioritizes data consistency and integrity through comprehensive validation, constraints, and monitoring to ensure reliable business operations.
</Note>