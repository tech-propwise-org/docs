---
title: "Lead"
description: "Sales opportunity entity — creation behavior, person resolution, deduplication, assignment authorization, property interests, and import."
---

A lead represents a sales opportunity or inquiry from a person. One person can have multiple leads — each inquiry is tracked separately.

## Lead entity

```
Lead
├── person_id (required) → Person
├── stage_id → lead_stage (pipeline position)
├── intent (buyer | seller | renter | landlord | investor) — single enum
├── reference_id (external reference)
├── source_channel_id → PersonChannel (channel attribution)
├── companyName (optional string for display — NO FK)
├── lead_source (enum, nullable), source_detail (text)
├── score (lead scoring), temperature (cold | warm | hot)
├── budget, budget_currency (default: AED)
├── industry, timeline
├── Pipeline dates: converted_at, disqualified_at
├── Disqualification: disqualification_reason (enum), disqualification_notes
└── Notes: use polymorphic 'note' table
```

<Warning>
  `intent` is a **single enum value** (buyer, seller, renter, landlord,
  investor), not an array. `companyName` is display-only with no foreign key to
  Company — company associations happen at the Contact level.
</Warning>

**Key rules:**

- One person can have multiple leads (each inquiry tracked separately)
- Assignment managed via `entity_stakeholder`, not direct fields
- Stage changes recorded in `lead_stage_history`
- `leadSource` and `sourceDetail` track lead origin separately from Person's `originalSource`
- `temperature` (cold/warm/hot) complements numeric `score`

---

## Lead creation behavior

<AccordionGroup>
  <Accordion title="Person resolution">
    If `personId` is provided, the system uses that existing person. Otherwise
    it searches by email/phone via `PersonService.findOrCreatePerson`, or
    creates a new person (requires at least `firstName`).
  </Accordion>
  <Accordion title="Deduplication">
    If both `referenceId` and `leadSource` are provided, the system checks for
    an existing lead with the same combination to prevent duplicate imports from
    external sources (e.g., Bayut, PropertyFinder). A unique DB constraint
    (error code 23505) catches race conditions.
  </Accordion>
  <Accordion title="Stage assignment">
    New leads are assigned to the "New" stage by default. A custom `stageId` can
    override this.
  </Accordion>
  <Accordion title="Stakeholder assignment">
    If `assignedUserId` is provided, a primary stakeholder is created with 100%
    commission. If `teamId` is also provided, the stakeholder is linked to that
    team. If no `assignedUserId`, the lead goes to the Pool (no stakeholders =
    unassigned).
  </Accordion>
  <Accordion title="Property interests">
    Optional property interests can be attached at creation. At least one must
    be marked `isPrimary`. If none is marked primary, the first one is auto-set
    as primary.
  </Accordion>
</AccordionGroup>

---

## Assignment authorization

When creating a lead via `POST /leads`, the endpoint has **no `@CheckAccess` decorator** (any authenticated user can create leads), but server-side validation enforces assignment rules based on permissions.

**Authorization matrix:**

| Assignment scenario       | Required permission                 | Notes                                           |
| ------------------------- | ----------------------------------- | ----------------------------------------------- |
| Assign to self            | None (always allowed)               | Most common case for normal agents              |
| Pool lead (global)        | `crm.manage`                        | No `assignedUserId`, no `teamId`                |
| Pool lead (team-specific) | `team_crm.manage` in target team    | Only `teamId` provided — lead goes to team pool |
| Assign to another user    | `crm.manage`                        | `assignedUserId ≠ userId`                       |
| Team selection            | User must be member OR `crm.manage` | Validates `teamId` if specified                 |

**Assignment behavior:**

<Steps>
  <Step title="User + team">
    If both `assignedUserId` and `teamId` are provided, a stakeholder record is
    created with both fields populated. The lead is assigned to the user and
    associated with the team.
  </Step>
  <Step title="Team only">
    If only `teamId` is provided (no `assignedUserId`), a team-only stakeholder
    record is created with `user_id = NULL`. The lead appears in the **team
    pool**.
  </Step>
  <Step title="User only">
    If only `assignedUserId` is provided (no `teamId`), a stakeholder record is
    created with only the user. The lead is assigned without team association.
  </Step>
  <Step title="No assignment">
    If neither is provided, no stakeholder record is created. The lead appears
    in the **organization pool**.
  </Step>
</Steps>

**Frontend dynamic behavior:**

| User type                   | Assigned to field                             | Team selection           | Pool lead?             |
| --------------------------- | --------------------------------------------- | ------------------------ | ---------------------- |
| Normal agent                | Self (disabled)                               | Optional (my teams)      | No                     |
| `team_crm.manage` in Team X | Self (default, unlocked when Team X selected) | Optional (my teams)      | Yes (Team X pool only) |
| `crm.manage`                | Anyone (enabled)                              | Any team (all org teams) | Yes (any pool)         |

---

## Business rules

| Rule                       | Description                                                             |
| -------------------------- | ----------------------------------------------------------------------- |
| Multiple leads per person  | One person can have multiple leads (each inquiry tracked separately)    |
| Lead requires person       | `person_id` is required                                                 |
| Intent is single enum      | Lead has one intent (buyer, seller, renter, landlord, investor)         |
| Stage changes tracked      | Every stage change creates a `lead_stage_history` record                |
| Conversion creates contact | When lead converts, a Contact is created (or unarchived) for the person |

<CardGroup cols={2}>
  <Card title="Person" icon="user" href="/backend/crm/person">
    Identity layer that every lead links to.
  </Card>
  <Card title="Stages" icon="diagram-project" href="/backend/crm/stages">
    Lead pipeline stages, conversion, and disqualification behavior.
  </Card>
  <Card
    title="Stakeholder data model"
    icon="database"
    href="/backend/crm/stakeholder-model"
  >
    Stakeholder model for lead assignment and commission splits.
  </Card>
  <Card title="Activities" icon="timeline" href="/backend/crm/activities">
    Activity timeline and communication logging on leads.
  </Card>
</CardGroup>
