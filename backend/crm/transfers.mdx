---
title: "Transfers"
description: "Complete CRM transfer system specification including entity transfer workflows, approval processes, commission splitting, stakeholder management, and integration with core CRM entities for real estate sales operations."
---

The CRM transfer system manages formal transfers of leads and deals between agents or teams, with comprehensive approval workflows, commission splitting, and integration with the broader stakeholder management system.

<Note>
**Vocabulary note:** This doc uses "stakeholder" because it describes the engineering subsystem (entity name, table, services). The **user-facing and AI-facing** terms are "Assignment" (the concept) and "Assignee" (the assigned user or team). See `Docs/STAKEHOLDER_SYSTEM.md` → "Vocabulary" for the full policy and which surfaces switched (modal copy, notification copy, audit timeline labels, AI tool descriptions, AI tool boundary JSON keys).
</Note>

<Info>
**Unified inbound lead capture:** Leads arriving from external sources (Property Finder, Bayut/dubizzle, and future Meta/website) are ingested through the source-agnostic `crm/lead-capture` module — `LeadCaptureService.capture()` reuses `PersonService`, `LeadService.createLeadInTransaction`/`findDuplicateLeadMatchInTransaction`, `EntityStakeholderService`, and the `DistributionEngine`. The lead-capture module owns the `CapturedLeadInput` contract, `LeadCaptureSourceRegistry` for adapter registration, org-default `LeadCaptureSettings`, the `CapturedLead` idempotency ledger, and the source-agnostic `lead-ingestion` pg-boss queue + `LeadIngestionWorker`.
</Info>

## Architecture overview

The CRM module follows a person-centric architecture with sophisticated transfer capabilities:

<Tabs>
<Tab title="Core design principles">
**Person + Contact model:**
- `Person` is the hidden identity layer (single source of truth for personal details)
- `Contact` is the business relationship layer (qualified customers)  
- `Lead` is the sales opportunity layer (unqualified inquiries)
- `Deal` links to `Contact`, not `Person` directly

**Unified stakeholder model:**
- Single table for assignment and commission across leads/deals
- Polymorphic patterns for notes, tags, and activities using entity_type/entity_id
- Channel separation with activity table indexing timeline
- Modular design with CRM core independent of Real Estate, Marketing, Channels
- Company via Contact: Companies associate with `Contact` via `ContactCompanyRole` (not Person)
- Organization Membership Display: CRM DTOs batch-resolve organization membership status for user references, showing removed org members with `isActiveOrgMember: false` badges while preserving historical assignment data

**Transfer system integration:**
- Formal ownership transfer with sophisticated commission splitting
- Integrated approval workflow with configurable authorization
- Atomic operations ensuring data consistency during transfers
- Comprehensive audit trail for compliance reporting
</Tab>

<Tab title="Module boundaries">
```
┌─────────────────────────────────────────────────────────────────┐
│                         CRM CORE                                │
│  Person, Lead, Contact, Company, Deal, DealContact             │
│  person_email, person_phone, person_address, person_channel    │
│  person_not_duplicate, contact_company_role                    │
│  entity_stakeholder, entity_transfer, commission_payment       │
│  activity, note, task, event, tag                              │
└─────────────────────────────────────────────────────────────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ REAL ESTATE  │    │ LEAD CAPTURE │    │   CHANNELS   │
│ development  │    │ captured_lead│    │  whatsapp    │
│ unit         │    │ ingestion    │    │  instagram   │
│ site_visit   │    │ settings     │    │  (linked via │
│ lead_property│    │              │    │  person_     │
│ _interest    │    │              │    │  channel)    │
│ unit_owner-  │    │              │    │              │
│ ship→Person  │    │              │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

**Real estate → CRM integration:**

| Real Estate Entity       | Links To     | Rationale                                           |
| ------------------------ | ------------ | --------------------------------------------------- |
| `unit_ownership`         | `Person`     | Ownership is represented by the individual Person  |
| `unit_transaction`       | `person_id`  | Transaction party is an individual                  |
| `site_visit`             | `person_id`  | Who visited the property                            |
| `lead_property_interest` | `lead_id`    | Links to Lead for sales context                     |
| `deal_property_interest` | `deal_id`    | Links to Deal for transaction context               |
</Tab>
</Tabs>

## Core entities

### Person (Central identity)

**Purpose**: Single source of truth for human identity and preferences.

```
Person
├── public_id (coded org-scoped human ID, e.g. PERS-ADNS-001)
├── Identity: first_name, last_name, avatar_url, title
├── Demographics: date_of_birth, nationality
├── Links: links jsonb [{ type, url }] where type is website/facebook/instagram/linkedIn/twitter/tiktok
├── Preferences: timezone
├── Languages: languages (unified array with code and proficiency per entry)
├── Communication Flags: do_not_call, do_not_email
├── Source Tracking: original_source
├── Merge Tracking: merged_into_id, merged_at, merged_by
├── Computed: full_name (getter: first_name + last_name)
└── Related Tables:
    ├── person_email (multiple emails, one primary)
    ├── person_phone (multiple phones, one primary)
    ├── person_address (multiple addresses, one primary)
    ├── person_channel (WhatsApp, Instagram, etc. identities)
    └── person_not_duplicate (deduplication override pairs)
```

### Lead entity

```
Lead
├── public_id (LEAD-ADNS-001)
├── person_id → Person (required)
├── Business Context: source, campaign_id, lead_value, priority
├── Classification: qualification_status, quality_score, category
├── Stage Management: stage_id → LeadStage, stage_changed_at, stage_changed_by_id
├── Assignment: assigned_to_id → User, assigned_at, assigned_by_id
├── Status: status (new, working, qualified, disqualified, converted, archived)
├── Conversion: converted_to_contact_id, converted_to_deal_id, converted_at, converted_by_id
├── Organization: organization_id
└── Audit: created_at, updated_at, created_by_id
```

### Contact entity

```
Contact
├── public_id (CONT-ADNS-001)
├── person_id → Person (required - underlying identity)
├── Business Context: contact_type, qualification_level, relationship_strength
├── Company Association: via ContactCompanyRole table
├── Lead Conversion: originated_from_lead_id, converted_at, converted_by_id
├── Communication: preferred_contact_method, best_time_to_contact
├── Organization: organization_id
└── Audit: created_at, updated_at, created_by_id
```

### Deal entity

```
Deal
├── public_id (DEAL-ADNS-001)
├── contact_id → Contact (required - qualified customer)
├── Business Details: value, currency, probability, close_date
├── Product Context: product_type, deal_type
├── Stage Management: stage_id → DealStage, stage_changed_at, stage_changed_by_id
├── Assignment: assigned_to_id → User, assigned_at, assigned_by_id
├── Status: status (open, won, lost, archived)
├── Source Tracking: originating_lead_id, source
├── Organization: organization_id
└── Audit: created_at, updated_at, created_by_id
```

### Company entity

```
Company
├── public_id (COMP-ADNS-001)
├── Business Identity: name, legal_name, company_type, industry
├── Identifiers: tax_id, registration_number, website
├── Address: primary_address_id → person_address
├── Contact Details: primary_email, primary_phone
├── Business Details: employee_count, annual_revenue, founded_year
├── Organization: organization_id
└── Audit: created_at, updated_at, created_by_id
```

## Assignment and commission system

The unified stakeholder system manages assignments and commission structures across leads and deals with sophisticated splitting capabilities.

### Entity stakeholder model

```
entity_stakeholder
├── Entity Reference:
│   ├── entity_type (lead | deal)
│   └── entity_id
├── Stakeholder Identity:
│   ├── user_id (nullable)
│   └── team_id (nullable) [Future: team-based assignments]
├── Commission & Role:
│   ├── commission_percentage (0-100)
│   ├── role (owner, collaborator, observer)
│   ├── is_primary (boolean - main responsible party)
│   └── commission_basis (percentage, fixed_amount, hybrid)
├── Assignment Tracking:
│   ├── assigned_at, assigned_by_id
│   ├── status (active, inactive, transferred)
│   └── assignment_notes
├── Payment Details:
│   ├── payment_schedule (immediate, monthly, quarterly, deal_close)
│   ├── payment_terms
│   └── payment_account_id
└── Audit: organization_id, created_at, updated_at
```

<Info>
**Key principles:**
- Polymorphic entity reference supports leads and deals uniformly
- Commission percentages must total ≤ 100% per entity
- Primary stakeholder designation indicates main responsibility
- Payment schedules can be customized per stakeholder
</Info>

### Commission payment processing

```
commission_payment
├── public_id (COMM-ADNS-001)
├── stakeholder_id → entity_stakeholder
├── Financial Details: amount, currency, exchange_rate
├── Source Reference: source_entity_type, source_entity_id
├── Calculation: calculation_note, calculation_date, calculated_by_id
├── Payment Processing: payment_method, payment_account_id, payment_reference
├── Status Tracking: status, processed_at, failed_at, failure_reason
├── Schedule: due_date, payment_schedule
└── Audit: organization_id, created_at, updated_at
```

**Payment lifecycle:**
- `pending` - Calculated but not yet processed
- `processing` - Payment initiated
- `completed` - Successfully processed
- `failed` - Processing failed (with reason)
- `cancelled` - Cancelled before processing

## Transfer system

The transfer system enables formal ownership transfers of leads and deals with sophisticated commission splitting and approval workflows.

### Entity transfer schema

The `entity_transfer` table manages transfer requests with comprehensive tracking and validation:

```sql
entity_transfer
├── Entity reference:
│   ├── entity_type (lead | deal)
│   └── entity_id
├── Transfer parties:
│   ├── from_user_id (nullable), from_team_id (nullable)
│   └── to_user_id (nullable), to_team_id (nullable)
├── Commission handling:
│   ├── from_commission_total (snapshot of sender's commission at request time)
│   └── from_keeps_percentage (e.g., 30%)
├── Workflow tracking:
│   ├── status (pending, approved, rejected, cancelled)
│   ├── reason, reject_reason
│   ├── requested_by_id, requested_at
│   ├── approved_by, approved_at, rejected_by, rejected_at
│   ├── cancelled_at, cancel_reason, cancelled_by_system
├── Audit fields:
│   ├── created_at, updated_at
│   └── organization_id
└── Database constraints:
    ├── Unique partial index: (entity_type, entity_id) WHERE status = 'pending'
    ├── Check: Either from_user_id OR from_team_id (not both)
    ├── Check: Either to_user_id OR to_team_id (not both)
    ├── Check: from_keeps_percentage BETWEEN 0 AND 100
    └── Check: from_commission_total >= 0
```

<Note>
**Polymorphic entity pattern:**
- `entity_type` + `entity_id` references any transferable entity
- Unified transfer logic works across leads and deals
- Future entity types (contacts, projects) can use same system
- Foreign key relationships maintained through application logic
</Note>

### Transfer workflow states

<Warning>
**Auto-cancellation triggers:**
- Entity deleted or archived
- Transferor loses stakeholder status
- Commission percentage changes significantly
- Entity status becomes non-transferable
</Warning>

### Commission split mechanics

**Calculation formula:**
```
to_commission = from_commission_total × (100% - from_keeps_percentage) / 100%
```

**Transfer scenarios:**
- 100% commission, 30% retention → 30% kept, 70% transferred
- 60% commission, 20% retention → 48% kept, 12% transferred  
- 50% commission, 0% retention → 0% kept, 50% transferred (primary status only)

**Primary stakeholder rules:**
- 0% retention → Primary status transfers to recipient
- >0% retention → Primary status remains with transferor (unless recipient has higher commission)
- Commission amounts rounded to 2 decimal places for consistency
- Total stakeholder commission cannot exceed 100%

### Transfer execution workflow

<Steps>
<Step title="Pre-execution validation">
Transfer eligibility validation:

```javascript
const validateTransferEligibility = async (transferRequest) => {
  const { entityType, entityId, fromUserId, toUserId, fromKeepsPercentage } = transferRequest;
  
  // Check self-transfer
  if (fromUserId === toUserId) {
    throw new ValidationError('Cannot transfer to yourself');
  }
  
  // Validate entity type
  if (!['lead', 'deal'].includes(entityType)) {
    throw new ValidationError('Only leads and deals can be transferred');
  }
  
  // Check existing pending transfers
  const existingPending = await EntityTransfer.findOne({
    where: { 
      entity_type: entityType, 
      entity_id: entityId, 
      status: 'pending' 
    }
  });
  
  if (existingPending) {
    throw new ValidationError('Entity already has a pending transfer');
  }
  
  // Validate stakeholder status and commission
  const fromStakeholder = await EntityStakeholder.findOne({
    where: { 
      entity_type: entityType, 
      entity_id: entityId, 
      user_id: fromUserId,
      status: 'active'
    }
  });
  
  if (!fromStakeholder || fromStakeholder.commission_percentage === 0) {
    throw new ValidationError('Transferor must have commission stake in entity');
  }
  
  return true;
};
```
</Step>

<Step title="Atomic execution">
Transfer approval transaction:

```sql
BEGIN;
  -- 1. Update transferor stakeholder
  UPDATE entity_stakeholder 
  SET 
    commission_percentage = :from_commission_total * :from_keeps_percentage / 100,
    is_primary = CASE WHEN :from_keeps_percentage = 0 THEN false ELSE is_primary END,
    updated_at = NOW()
  WHERE entity_type = :entity_type 
    AND entity_id = :entity_id 
    AND user_id = :from_user_id;
    
  -- 2. Create or update recipient stakeholder
  INSERT INTO entity_stakeholder (
    entity_type, entity_id, user_id, commission_percentage, 
    is_primary, role, status, assigned_at, assigned_by_id, organization_id
  ) VALUES (
    :entity_type, :entity_id, :to_user_id,
    :from_commission_total * (100 - :from_keeps_percentage) / 100,
    CASE WHEN :from_keeps_percentage = 0 THEN true ELSE false END,
    'owner', 'active', NOW(), :approved_by_id, :organization_id
  )
  ON CONFLICT (entity_type, entity_id, user_id) 
  DO UPDATE SET 
    commission_percentage = commission_percentage + EXCLUDED.commission_percentage,
    is_primary = CASE 
      WHEN EXCLUDED.commission_percentage > entity_stakeholder.commission_percentage 
      THEN EXCLUDED.is_primary 
      ELSE entity_stakeholder.is_primary 
    END;
    
  -- 3. Update transfer status
  UPDATE entity_transfer 
  SET 
    status = 'approved',
    approved_by = :approved_by_id,
    approved_at = NOW(),
    updated_at = NOW()
  WHERE id = :transfer_id;
  
COMMIT;
```

<Info>
The atomic transaction ensures either complete success or complete rollback. No partial states are possible, maintaining data integrity even during system failures.
</Info>
</Step>

<Step title="Post-execution integration">
System integration updates:

```javascript
const executePostTransferIntegration = async (transfer) => {
  // 1. Update commission payments for closed deals
  if (transfer.entity_type === 'deal') {
    await CommissionPaymentService.recalculateForEntity('deal', transfer.entity_id, {
      reason: `Transfer approved: ${transfer.id}`,
      recalculated_by: transfer.approved_by
    });
  }
  
  // 2. Create activity record
  await Activity.create({
    entity_type: transfer.entity_type,
    entity_id: transfer.entity_id,
    activity_type: 'transfer',
    activity_subtype: 'approved',
    subject: `Ownership transferred`,
    description: `Transfer approved. Commission split: ${transfer.from_keeps_percentage}% retained, ${100 - transfer.from_keeps_percentage}% transferred.`,
    created_by: transfer.approved_by,
    activity_data: {
      transfer_id: transfer.id,
      commission_split: {
        from_keeps: transfer.from_keeps_percentage,
        to_receives: 100 - transfer.from_keeps_percentage,
        total_amount: transfer.from_commission_total
      }
    }
  });
  
  // 3. Update entity assignment if primary stakeholder changed
  if (transfer.from_keeps_percentage === 0) {
    const entity = await getEntity(transfer.entity_type, transfer.entity_id);
    await entity.update({
      assigned_to_id: transfer.to_user_id,
      assigned_at: new Date(),
      assigned_by_id: transfer.approved_by
    });
  }
  
  // 4. Send notifications
  await notifyTransferCompletion(transfer);
};
```
</Step>
</Steps>

### Transfer approval system

<AccordionGroup>
<Accordion title="Approval authority levels">
**Authorization hierarchy:**

```javascript
const getRequiredApprovalLevel = (entityType, entityValue, commissionTransferred) => {
  // High-value transfers require senior approval
  if (entityType === 'deal' && entityValue > 500000) {
    return 'senior_manager';
  }
  
  // Large commission transfers require manager approval
  if (commissionTransferred > 10000) {
    return 'manager';
  }
  
  // Cross-team transfers require team lead approval
  return 'team_lead';
};
```
</Accordion>

<Accordion title="Business rules and constraints">
**Transfer validation rules:**

```javascript
const validateTransferBusinessRules = async (entityType, entityId, transferRequest) => {
  // Rule 1: Entity must be in transferable state
  const entity = await getEntity(entityType, entityId);
  const nonTransferableStates = {
    lead: ['archived', 'converted', 'deleted'],
    deal: ['won', 'lost', 'cancelled', 'closed']
  };
  
  if (nonTransferableStates[entityType].includes(entity.status)) {
    throw new BusinessRuleError(`Cannot transfer ${entityType} in ${entity.status} state`);
  }
  
  // Rule 2: Maximum transfer frequency (prevents abuse)
  const recentTransfers = await EntityTransfer.count({
    where: {
      entity_type: entityType,
      entity_id: entityId,
      status: 'approved',
      approved_at: { [Op.gte]: new Date(Date.now() - 30 * 24 * 60 * 60 * 1000) }
    }
  });
  
  if (recentTransfers >= 3) {
    throw new BusinessRuleError('Maximum transfer frequency exceeded (3 per month)');
  }
  
  // Rule 3: Commission retention limits
  if (transferRequest.from_keeps_percentage > 75) {
    throw new BusinessRuleError('Maximum commission retention is 75%');
  }
  
  return true;
};
```
</Accordion>
</AccordionGroup>

## Activity and communication system

The unified activity system tracks all interactions and communications across leads and deals with sophisticated filtering and reporting capabilities.

### Activity entity structure

```
activity
├── Entity Reference: entity_type, entity_id (polymorphic to leads/deals)
├── Classification: activity_type, activity_subtype, category
├── Content: subject, description, outcome
├── Timing: activity_date, duration_minutes, follow_up_date
├── Assignment: assigned_to_id, completed_by_id
├── Status: status (scheduled, completed, cancelled), completion_notes
├── Metadata: activity_data (JSONB), external_id, source
├── Communication: direction (inbound, outbound), participants JSONB
├── Priority: priority_level, is_urgent
└── Audit: organization_id, created_at, updated_at, created_by_id
```

**Activity types and subtypes:**
- `call` → missed, answered, voicemail, callback_scheduled
- `email` → sent, received, bounced, opened, clicked
- `meeting` → scheduled, completed, cancelled, no_show
- `note` → general, follow_up, internal, customer_feedback
- `task` → created, completed, overdue, delegated
- `system` → stage_change, assignment_change, transfer, conversion

## Notes system

### Note entity and polymorphic design

```
note
├── Entity Reference: entity_type, entity_id (leads, deals, contacts, persons)
├── Content: content (rich text), note_type
├── Classification: is_private, is_pinned, importance_level
├── Authorship: created_by_id, updated_by_id
├── Collaboration: shared_with_users, shared_with_teams
├── Metadata: external_id, source_system
└── Audit: organization_id, created_at, updated_at
```

**Note types:**
- `general` - Standard notes and observations
- `follow_up` - Action items and next steps
- `internal` - Team-only communication
- `customer_feedback` - Customer quotes and feedback
- `meeting_notes` - Meeting summaries and decisions
- `system` - Auto-generated system notes

## Stage history and analytics

### Stage tracking system

```
stage_history
├── Entity Reference: entity_type, entity_id
├── Stage Transition: from_stage_id, to_stage_id
├── Timing: changed_at, duration_in_previous_stage
├── User Context: changed_by_id, change_reason
├── Business Context: stage_value_snapshot, probability_snapshot
├── Metadata: change_metadata JSONB
└── Audit: organization_id
```

## Query patterns

### Optimized entity queries

<AccordionGroup>
<Accordion title="Lead and deal queries with stakeholder data">
**Efficient entity retrieval with relationships:**

```sql
-- Lead with stakeholders and current stage
SELECT 
  l.*,
  p.first_name, p.last_name, p.full_name,
  pe.email as primary_email,
  pp.phone_number as primary_phone,
  ls.name as current_stage_name, ls.stage_order,
  es.commission_percentage, es.is_primary as is_primary_stakeholder,
  u.first_name || ' ' || u.last_name as assigned_to_name
FROM leads l
LEFT JOIN persons p ON l.person_id = p.id
LEFT JOIN person_emails pe ON p.id = pe.person_id AND pe.is_primary = true
LEFT JOIN person_phones pp ON p.id = pp.person_id AND pp.is_primary = true
LEFT JOIN lead_stages ls ON l.stage_id = ls.id
LEFT JOIN entity_stakeholder es ON (es.entity_type = 'lead' AND es.entity_id = l.id AND es.user_id = :current_user_id)
LEFT JOIN users u ON l.assigned_to_id = u.id
WHERE l.organization_id = :org_id
  AND l.status = 'active'
  AND (es.user_id = :current_user_id OR l.assigned_to_id = :current_user_id OR :is_admin)
ORDER BY l.updated_at DESC;
```
</Accordion>

<Accordion title="Performance optimization indexes">
**Critical database indexes for CRM performance:**

```sql
-- Entity stakeholder lookups
CREATE INDEX CONCURRENTLY idx_entity_stakeholder_lookup 
ON entity_stakeholder (entity_type, entity_id, user_id) 
WHERE status = 'active';

-- Activity timeline queries  
CREATE INDEX CONCURRENTLY idx_activity_entity_timeline 
ON activity (entity_type, entity_id, activity_date DESC) 
WHERE status IN ('completed', 'scheduled');

-- Transfer status queries
CREATE INDEX CONCURRENTLY idx_entity_transfer_status 
ON entity_transfer (status, organization_id, entity_type) 
WHERE status IN ('pending', 'approved');

-- Commission payment processing
CREATE INDEX CONCURRENTLY idx_commission_payment_processing 
ON commission_payment (status, due_date) 
WHERE status = 'pending';

-- Stage history analytics
CREATE INDEX CONCURRENTLY idx_stage_history_analytics 
ON stage_history (entity_type, to_stage_id, changed_at);

-- Public ID lookups
CREATE UNIQUE INDEX CONCURRENTLY idx_leads_public_id ON leads (public_id);
CREATE UNIQUE INDEX CONCURRENTLY idx_deals_public_id ON deals (public_id);
CREATE UNIQUE INDEX CONCURRENTLY idx_contacts_public_id ON contacts (public_id);
CREATE UNIQUE INDEX CONCURRENTLY idx_persons_public_id ON persons (public_id);

-- Person contact information
CREATE INDEX CONCURRENTLY idx_person_emails_primary 
ON person_emails (person_id) WHERE is_primary = true;
CREATE INDEX CONCURRENTLY idx_person_phones_primary 
ON person_phones (person_id) WHERE is_primary = true;
```
</Accordion>
</AccordionGroup>

## Business rules

### Data validation and constraints

<AccordionGroup>
<Accordion title="Commission business rules">
**Commission validation logic:**

```javascript
const validateCommissionRules = async (entityType, entityId, stakeholderChanges) => {
  const rules = [];
  
  // Rule 1: Total commission cannot exceed 100%
  const currentStakeholders = await EntityStakeholder.findAll({
    where: { entity_type: entityType, entity_id: entityId, status: 'active' }
  });
  
  const newTotal = currentStakeholders
    .filter(s => !stakeholderChanges.some(c => c.stakeholder_id === s.id))
    .reduce((sum, s) => sum + s.commission_percentage, 0) +
    stakeholderChanges.reduce((sum, c) => sum + c.commission_percentage, 0);
  
  if (newTotal > 100) {
    rules.push({
      rule: 'commission_total_limit',
      violated: true,
      message: `Total commission would be ${newTotal}%, exceeding 100% limit`,
      severity: 'error'
    });
  }
  
  // Rule 2: Primary stakeholder must have >0% commission
  const primaryStakeholder = stakeholderChanges.find(c => c.is_primary) ||
    currentStakeholders.find(s => s.is_primary && !stakeholderChanges.some(c => c.stakeholder_id === s.id));
    
  if (primaryStakeholder && primaryStakeholder.commission_percentage === 0) {
    rules.push({
      rule: 'primary_stakeholder_commission',
      violated: true,
      message: 'Primary stakeholder must have commission >0%',
      severity: 'error'
    });
  }
  
  return {
    valid: rules.filter(r => r.severity === 'error').length === 0,
    rules: rules
  };
};
```
</Accordion>

<Accordion title="Entity lifecycle rules">
**Status transition constraints:**

```javascript
const validateStatusTransition = (entityType, currentStatus, newStatus) => {
  const allowedTransitions = {
    lead: {
      'new': ['working', 'disqualified', 'archived'],
      'working': ['qualified', 'disqualified', 'archived'],  
      'qualified': ['converted', 'disqualified', 'archived'],
      'disqualified': ['working', 'archived'], // Re-qualification possible
      'converted': ['archived'], // Terminal state
      'archived': [] // No transitions from archived
    },
    deal: {
      'open': ['won', 'lost', 'archived'],
      'won': ['archived'], // Terminal state
      'lost': ['open', 'archived'], // Re-opening possible
      'archived': [] // No transitions from archived
    }
  };
  
  const allowed = allowedTransitions[entityType]?.[currentStatus] || [];
  
  return {
    valid: allowed.includes(newStatus),
    allowedTransitions: allowed,
    message: !allowed.includes(newStatus) 
      ? `Cannot transition ${entityType} from ${currentStatus} to ${newStatus}`
      : null
  };
};
```
</Accordion>
</AccordionGroup>

## Entity relationship diagram

The CRM module follows a hierarchical entity relationship structure with polymorphic associations:

```
    Person (Identity)
    ├── person_email
    ├── person_phone  
    ├── person_address
    └── person_channel
         │
         ├─→ Lead (Opportunity)
         │   ├── entity_stakeholder
         │   ├── entity_transfer
         │   ├── activity
         │   ├── note
         │   └── LeadStage
         │
         └─→ Contact (Qualified)
             ├── ContactCompanyRole ←→ Company
             └── Deal (Transaction)
                 ├── entity_stakeholder
                 ├── entity_transfer  
                 ├── commission_payment
                 ├── activity
                 ├── note
                 ├── DealStage
                 └── stage_history
```

## Events and integration

### Event-driven architecture

<AccordionGroup>
<Accordion title="CRM event system">
**Event publishing for key CRM operations:**

```javascript
const CrmEventBus = {
  async publishEntityEvent(eventType, entityType, entityId, eventData, userId) {
    const event = {
      event_type: eventType,
      entity_type: entityType,
      entity_id: entityId,
      event_data: eventData,
      user_id: userId,
      organization_id: eventData.organization_id,
      timestamp: new Date().toISOString(),
      event_id: generateUUID()
    };
    
    // Internal event handling
    await this.handleInternalEvent(event);
    
    // External integrations
    await this.publishToExternalSystems(event);
    
    return event;
  },
  
  async handleInternalEvent(event) {
    switch (event.event_type) {
      case 'lead.converted':
        await this.handleLeadConversion(event);
        break;
      case 'deal.won':
        await this.handleDealWon(event);
        break;
      case 'stakeholder.assigned':
        await this.handleStakeholderAssignment(event);
        break;
      case 'transfer.approved':
        await this.handleTransferApproval(event);
        break;
    }
  }
};
```
</Accordion>

<Accordion title="External system integration">
**Event listeners for cross-module coordination:**

```javascript
// Event listeners for external systems
const setupEventListeners = () => {
  // Real Estate integration
  RealEstateEventBus.on('unit.reserved', async (event) => {
    const deal = await Deal.findOne({
      include: [{
        model: DealPropertyInterest,
        where: { inventory_unit_id: event.unit_id }
      }]
    });
    
    if (deal) {
      await CrmEventBus.publishEntityEvent('deal.unit_reserved', 'deal', deal.id, {
        inventory_unit_id: event.unit_id,
        reservation_date: event.timestamp
      });
    }
  });
  
  // Lead Capture integration  
  LeadCaptureEventBus.on('lead.captured', async (event) => {
    const lead = await Lead.findOne({
      where: { external_id: event.lead_external_id }
    });
    
    if (lead) {
      await CrmEventBus.publishEntityEvent('lead.source_attributed', 'lead', lead.id, {
        source: event.source,
        attribution_type: event.attribution_type,
        source_channel: event.source_channel
      });
    }
  });
};
```
</Accordion>
</AccordionGroup>

## Data consistency guarantees

### Transaction boundaries and atomicity

<AccordionGroup>
<Accordion title="Transfer operation consistency">
**Atomic transfer execution:**

```javascript
const executeTransfer = async (transferId) => {
  const transaction = await sequelize.transaction();
  
  try {
    // 1. Lock transfer record for update
    const transfer = await EntityTransfer.findByPk(transferId, {
      transaction,
      lock: true
    });
    
    if (transfer.status !== 'pending') {
      throw new Error('Transfer not in pending status');
    }
    
    // 2. Validate current stakeholder state
    const fromStakeholder = await EntityStakeholder.findOne({
      where: {
        entity_type: transfer.entity_type,
        entity_id: transfer.entity_id,
        user_id: transfer.from_user_id,
        status: 'active'
      },
      transaction,
      lock: true
    });
    
    if (!fromStakeholder || fromStakeholder.commission_percentage < transfer.from_commission_total) {
      throw new Error('Stakeholder commission has changed since transfer request');
    }
    
    // 3. Execute stakeholder updates atomically
    await updateStakeholderCommissions(transfer, transaction);
    
    // 4. Update transfer status
    await transfer.update({
      status: 'approved',
      approved_at: new Date(),
      approved_by: transfer.approved_by
    }, { transaction });
    
    // 5. Create audit entries
    await createTransferAuditEntries(transfer, transaction);
    
    await transaction.commit();
    
    // 6. Post-commit notifications (outside transaction)
    await sendTransferNotifications(transfer);
    
    return transfer;
    
  } catch (error) {
    await transaction.rollback();
    throw error;
  }
};
```
</Accordion>

<Accordion title="Commission calculation consistency">
**Snapshot-based commission calculations:**

```javascript
const calculateCommissionPayments = async (entityType, entityId) => {
  const transaction = await sequelize.transaction();
  
  try {
    // 1. Lock entity for value snapshot
    const entity = await getEntity(entityType, entityId, {
      transaction,
      lock: true
    });
    
    // 2. Get current stakeholders with lock
    const stakeholders = await EntityStakeholder.findAll({
      where: {
        entity_type: entityType,
        entity_id: entityId,
        status: 'active',
        commission_percentage: { [Op.gt]: 0 }
      },
      transaction,
      lock: true
    });
    
    // 3. Validate total commission doesn't exceed 100%
    const totalCommission = stakeholders.reduce(
      (sum, s) => sum + s.commission_percentage, 0
    );
    
    if (totalCommission > 100) {
      throw new Error(`Commission total exceeds 100%: ${totalCommission}%`);
    }
    
    // 4. Cancel existing pending payments
    await CommissionPayment.update(
      { status: 'cancelled', cancelled_reason: 'Recalculation' },
      {
        where: {
          source_entity_type: entityType,
          source_entity_id: entityId,
          status: 'pending'
        },
        transaction
      }
    );
    
    // 5. Create new payments with locked values
    const payments = stakeholders.map(stakeholder => ({
      stakeholder_id: stakeholder.id,
      amount: entity.value * (stakeholder.commission_percentage / 100),
      currency: entity.currency,
      source_entity_type: entityType,
      source_entity_id: entityId,
      status: 'pending',
      due_date: calculateDueDate(stakeholder.payment_schedule, entity),
      calculation_date: new Date(),
      calculation_note: `${stakeholder.commission_percentage}% of ${entity.value}`
    }));
    
    await CommissionPayment.bulkCreate(payments, { transaction });
    
    await transaction.commit();
    
    return payments;
    
  } catch (error) {
    await transaction.rollback();
    throw error;
  }
};
```
</Accordion>
</AccordionGroup>

## AI module integration

The CRM module integrates with the fully implemented and operational AI module (`AiModule`) for comprehensive automated conversations, lead processing, and workflow-assisted follow-up. CRM owns the business entities and lifecycle rules; the AI module owns agent configuration, runtime execution, queueing, LLM integration, security controls, and activity logging.

<AccordionGroup>
<Accordion title="AI agent templates and execution">
**10 pre-configured agent types:**
- Receptionist, Sales Qualification, Listing Inquiry, Off-Plan Inquiry
- Appointment Booking, After-Hours, FAQ & Support, Campaign Lead Capture
- Spam Handler, Human Handoff

**Key capabilities:**
- Queue-based execution via AiAgentExecuteWorker with retry logic and error handling
- Knowledge base integration (FAQ, SNIPPET, DOCUMENT, PAGE types) with chunking, embedding, and RAG capabilities through KnowledgeBaseService
- Credit Management: AI-credit affordability/gating via the subscription module's unified-wallet `CreditMeteringService` (shared org pool with per-user ceilings)
- Tool Registry: Extensible system through AiAgentToolRegistryService and AiAgentActionService
- OpenAI Integration: Project provisioning and encrypted key management through OpenAiProjectProvisioner and OpenAiEncryptionService
- Activity Logging: Comprehensive audit trail via AiActivityLogService with filtering and analytics
- Workflow Integration: Bidirectional integration - AI agents can trigger workflows through the `trigger_workflow` action AND workflows can activate AI agents via AI_AGENT steps
- Media Processing: Audio/image processing via AiAgentMediaProcessorService
- Optimization: Instruction optimization via AiAgentOptimizeService with protected token preservation
- Security: SSRF protection via assertNotSsrf utility and AES-GCM encryption utilities
</Accordion>

<Accordion title="CRM integration points">
**Lead source tracking:**
- Leads include `sourceConversation` for tracing originating messaging conversations
- CRM workflows can access AI conversation context for automation

**Tool execution:**
- AI agents can search and update CRM data through fixed tool catalog
- Contact field updates and lead lifecycle stage changes via CRM services

**Conversation automation:**
- AI agents can respond to messaging conversations, use CRM context, and hand off to users or teams when configured actions require human follow-up
</Accordion>
</AccordionGroup>

## CRM public IDs

All core CRM business records expose a stored `publicId` for user-facing references. The format is:

```
{ENTITY_CODE}-{ORG_PREFIX}-{SEQUENCE}
```

<Note>
Examples: `LEAD-ADNS-001`, `PERS-ADNS-001`, `CONT-ADNS-001`, `COMP-ADNS-001`, `DEAL-ADNS-001`, `COMM-ADNS-001`, `USER-ADNS-001`, `TEAM-ADNS-001`, `LSTG-ADNS-001`, `DSTG-ADNS-001`.
</Note>

<AccordionGroup>
<Accordion title="Public ID format and structure">
**Entity codes and organization prefixes:**
- Entity codes make IDs globally unique within an organization across CRM record types
- `ORG_PREFIX` is generated once from the organization's normalized alphanumeric name. Names of 4 characters or fewer use the whole normalized name (`WIK`); longer names use the first 2 and last 2 characters (`Adidas Operations` → `ADNS`, `Adidas` → `ADAS`)
- `SEQUENCE` is scoped by organization and entity type, padded to at least 3 digits, and not capped (`LEAD-ADNS-1000` is valid)
- IDs are allocated by `CrmPublicIdService` from `crm_public_id_counter` in the same transaction as entity creation
- Existing IDs do not change when the organization is renamed

**Entity codes:** Lead=`LEAD`, Person=`PERS`, Contact=`CONT`, Company=`COMP`, Deal=`DEAL`, CommissionPayment=`COMM`, organization user membership=`USER`, Team=`TEAM`, LeadStage=`LSTG`, DealStage=`DSTG`.
</Accordion>

<Accordion title="Special ID handling">
**Organization user memberships:**
- Also receive stable fallback avatar colors from the user sequence (`organization_users.avatar_bg_color` + `avatar_text_color`)
- The hue uses a golden-angle spread across a finite HSL palette
- Colors are display metadata and may repeat in very large organizations, while `public_id` remains the unique user-facing membership identifier

**Global system stages:**
- Exception to the org-sequence format: they have deterministic IDs based on `systemType`
- Examples: `LSTG-NEW`, `LSTG-DISQUALIFIED`, `DSTG-CLOSED-WON`
- Org-specific custom/override stages use the normal org-scoped sequence (`LSTG-ADNS-001`, `DSTG-ADNS-001`)
</Accordion>
</AccordionGroup>

<CardGroup cols={2}>
<Card title="Lead Management" icon="user-plus" href="/leads">
Comprehensive lead tracking and conversion workflows
</Card>
<Card title="Deal Pipeline" icon="handshake" href="/deals">  
Deal management with stage tracking and forecasting
</Card>
<Card title="Commission System" icon="dollar-sign" href="/commissions">
Automated commission calculations and payment processing
</Card>
<Card title="Activity Tracking" icon="clock" href="/activities">
Unified communication and interaction logging
</Card>
</CardGroup>