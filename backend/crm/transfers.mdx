---
title: "Transfers"
description: "Complete CRM transfer system specification including entity transfer workflows, approval processes, commission splitting, stakeholder management, and integration with core CRM entities for real estate sales operations."
---

The CRM transfer system manages formal transfers of leads and deals between agents or teams, with comprehensive approval workflows, commission splitting, and integration with the broader stakeholder management system.

## Transfer system overview

The transfer system enables formal reassignment of entity ownership with sophisticated commission splitting and approval workflows:

<Tabs>
<Tab title="Core concepts">
**Entity transfers:**
- Formal ownership transfer of leads and deals between users or teams
- Integrated approval workflow with configurable authorization
- Commission splitting with percentage-based retention for transferor
- Atomic operations ensuring data consistency during transfers

**Polymorphic design:**
- Uses `entity_type` and `entity_id` pattern for flexible entity reference
- Supports both `lead` and `deal` transfers with unified logic
- Extensible architecture for future entity types

**Key principles:**
- Snapshot-based commission calculation prevents race conditions
- Auto-cancellation protection for invalid states
- History tracking for audit compliance
- Integration with broader stakeholder management system
</Tab>

<Tab title="Transfer workflow">
<Steps>
<Step title="Transfer request">
Agent A requests transfer of Lead #1 to Agent B with 30% commission retention. System validates eligibility and creates pending transfer with commission snapshot.
</Step>

<Step title="Approval process">
Authorized approver reviews request with full context and commission impact. Business rules determine required approval level based on entity value and transfer type.
</Step>

<Step title="Atomic execution">
System performs stakeholder updates, commission redistribution, and history tracking in single transaction. Primary stakeholder status transfers based on retention percentage.
</Step>

<Step title="Integration updates">
Commission payments recalculated for closed deals, activity records created, and all parties notified of completed transfer with updated ownership structure.
</Step>
</Steps>
</Tab>

<Tab title="Commission mechanics">
**Calculation formula:**
```
to_commission = from_commission_total × (100% - from_keeps_percentage) / 100%
```

**Transfer scenarios:**
- 100% commission, 30% retention → 30% kept, 70% transferred
- 60% commission, 20% retention → 48% kept, 12% transferred  
- 50% commission, 0% retention → 0% kept, 50% transferred (primary status only)

**Primary stakeholder rules:**
- 0% retention → Primary status transfers to recipient
- >0% retention → Primary status remains with transferor (unless recipient has higher commission)
</Tab>
</Tabs>

## Entity transfer schema

The `entity_transfer` table manages transfer requests with comprehensive tracking and validation:

<AccordionGroup>
<Accordion title="Transfer entity structure">
```sql
entity_transfer
├── Entity reference:
│   ├── entity_type (lead | deal)
│   └── entity_id
├── Transfer parties:
│   ├── from_user_id (nullable), from_team_id (nullable)
│   └── to_user_id (nullable), to_team_id (nullable)
├── Commission handling:
│   ├── from_commission_total (snapshot of sender's commission at request time)
│   └── from_keeps_percentage (e.g., 30%)
├── Workflow tracking:
│   ├── status (pending, approved, rejected, cancelled)
│   ├── reason, reject_reason
│   ├── requested_by_id, requested_at
│   ├── approved_by, approved_at, rejected_by, rejected_at
│   ├── cancelled_at, cancel_reason, cancelled_by_system
├── Audit fields:
│   ├── created_at, updated_at
│   └── organization_id
└── Database constraints:
    ├── Unique partial index: (entity_type, entity_id) WHERE status = 'pending'
    ├── Check: Either from_user_id OR from_team_id (not both)
    ├── Check: Either to_user_id OR to_team_id (not both)
    ├── Check: from_keeps_percentage BETWEEN 0 AND 100
    └── Check: from_commission_total >= 0
```

**Polymorphic entity pattern:**
- `entity_type` + `entity_id` references any transferable entity
- Unified transfer logic works across leads and deals
- Future entity types (contacts, projects) can use same system
- Foreign key relationships maintained through application logic

**Key constraints:**
- Only one pending transfer per entity (enforced by partial unique index)
- Mutually exclusive user/team selection for both parties
- Commission percentage and total validation
- Organization-scoped operations
</Accordion>

<Accordion title="Transfer status lifecycle">
```mermaid
stateDiagram-v2
    [*] --> pending: Transfer requested
    pending --> approved: Approved by authorized user
    pending --> rejected: Rejected with reason
    pending --> cancelled: Auto-cancelled or manual cancellation
    approved --> [*]: Transfer completed
    rejected --> [*]: Transfer denied
    cancelled --> [*]: Transfer invalid
    
    note right of pending: Unique constraint:\nOnly one pending\nper entity
    note right of approved: Atomic stakeholder\nand commission updates
    note right of cancelled: Triggered by:\n- Stakeholder removal\n- Commission reduction\n- Entity status change
```

**Status transitions:**
- **pending → approved**: Authorized user approves with atomic stakeholder updates
- **pending → rejected**: Approver provides rejection reason and recommendations
- **pending → cancelled**: System auto-cancellation or manual cancellation
- **Auto-expiration**: Pending transfers expire after 72 hours without approval
</Accordion>

<Accordion title="Commission snapshot protection">
**Purpose:** The `from_commission_total` field captures the exact commission percentage at request time, protecting against concurrent modifications.

```javascript
const requestTransfer = async (entityType, entityId, fromUserId, toUserId, fromKeepsPercentage) => {
  // Get current stakeholder commission (snapshot)
  const fromStakeholder = await EntityStakeholder.findOne({
    where: { 
      entity_type: entityType, 
      entity_id: entityId, 
      user_id: fromUserId,
      status: 'active'
    }
  });
  
  if (!fromStakeholder) {
    throw new Error('Transferor is not a stakeholder of this entity');
  }
  
  // Create transfer with commission snapshot
  const transfer = await EntityTransfer.create({
    entity_type: entityType,
    entity_id: entityId,
    from_user_id: fromUserId,
    to_user_id: toUserId,
    from_commission_total: fromStakeholder.commission_percentage, // SNAPSHOT
    from_keeps_percentage: fromKeepsPercentage,
    status: 'pending',
    requested_by_id: currentUserId,
    reason: 'Workload redistribution'
  });
  
  return transfer;
};
```

**Protection scenarios:**
- Prevents invalidation if transferor's commission changes during pending period
- Guards against race conditions between transfer and other stakeholder operations
- Ensures transfer terms remain valid even with concurrent modifications
- Provides audit trail of commission state at request time
</Accordion>
</AccordionGroup>

## Transfer execution workflow

The transfer execution process implements atomic operations with comprehensive validation and rollback capabilities:

<Tabs>
<Tab title="Pre-execution validation">
<Warning>
**Transfer eligibility validation:**
- Cannot transfer to self (same user or team)
- Must have valid from/to targets (user or team, not both)
- Transferor must be current stakeholder with sufficient commission
- Entity must be in transferable status (active lead/deal)
- All parties must be in same organization
- No existing pending transfers for the entity
- Entity type must support transfers (leads and deals only)
</Warning>

```javascript
const validateTransferEligibility = async (transferRequest) => {
  const { entityType, entityId, fromUserId, toUserId, fromKeepsPercentage } = transferRequest;
  
  // Check self-transfer
  if (fromUserId === toUserId) {
    throw new ValidationError('Cannot transfer to yourself');
  }
  
  // Validate entity type
  if (!['lead', 'deal'].includes(entityType)) {
    throw new ValidationError('Only leads and deals can be transferred');
  }
  
  // Check existing pending transfers
  const existingPending = await EntityTransfer.findOne({
    where: { 
      entity_type: entityType, 
      entity_id: entityId, 
      status: 'pending' 
    }
  });
  
  if (existingPending) {
    throw new ValidationError('Entity already has a pending transfer');
  }
  
  // Validate stakeholder status and commission
  const fromStakeholder = await EntityStakeholder.findOne({
    where: { 
      entity_type: entityType, 
      entity_id: entityId, 
      user_id: fromUserId,
      status: 'active'
    }
  });
  
  if (!fromStakeholder || fromStakeholder.commission_percentage === 0) {
    throw new ValidationError('Transferor must have commission stake in entity');
  }
  
  // Validate entity status
  const entity = await getEntity(entityType, entityId);
  if (!entity.isTransferable()) {
    throw new ValidationError(`${entityType} is not in transferable state`);
  }
  
  return true;
};
```
</Tab>

<Tab title="Atomic execution">
**Transfer approval transaction:**

```sql
BEGIN;
  -- 1. Update transferor stakeholder
  UPDATE entity_stakeholder 
  SET 
    commission_percentage = :from_commission_total * :from_keeps_percentage / 100,
    is_primary = CASE WHEN :from_keeps_percentage = 0 THEN false ELSE is_primary END,
    updated_at = NOW()
  WHERE entity_type = :entity_type 
    AND entity_id = :entity_id 
    AND user_id = :from_user_id;
    
  -- 2. Create or update recipient stakeholder
  INSERT INTO entity_stakeholder (
    entity_type, entity_id, user_id, commission_percentage, 
    is_primary, role, status, assigned_at, assigned_by_id, organization_id
  ) VALUES (
    :entity_type, :entity_id, :to_user_id,
    :from_commission_total * (100 - :from_keeps_percentage) / 100,
    CASE WHEN :from_keeps_percentage = 0 THEN true ELSE false END,
    'owner', 'active', NOW(), :approved_by_id, :organization_id
  )
  ON CONFLICT (entity_type, entity_id, user_id) 
  DO UPDATE SET 
    commission_percentage = commission_percentage + EXCLUDED.commission_percentage,
    is_primary = CASE 
      WHEN EXCLUDED.commission_percentage > entity_stakeholder.commission_percentage 
      THEN EXCLUDED.is_primary 
      ELSE entity_stakeholder.is_primary 
    END;
    
  -- 3. Update transfer status
  UPDATE entity_transfer 
  SET 
    status = 'approved',
    approved_by = :approved_by_id,
    approved_at = NOW(),
    updated_at = NOW()
  WHERE id = :transfer_id;
  
  -- 4. Create stakeholder history records
  INSERT INTO entity_stakeholder_history (
    stakeholder_id, action, old_values, new_values, 
    changed_by, changed_at, change_reason
  ) VALUES 
    (:from_stakeholder_id, 'transfer_out', :old_from_values, :new_from_values, 
     :approved_by_id, NOW(), 'Transfer approved'),
    (:to_stakeholder_id, 'transfer_in', :old_to_values, :new_to_values, 
     :approved_by_id, NOW(), 'Transfer approved');
     
COMMIT;
```

<Info>
The atomic transaction ensures either complete success or complete rollback. No partial states are possible, maintaining data integrity even during system failures.
</Info>
</Tab>

<Tab title="Post-execution integration">
**System integration updates:**

```javascript
const executePostTransferIntegration = async (transfer) => {
  // 1. Update commission payments for closed deals
  if (transfer.entity_type === 'deal') {
    await CommissionPaymentService.recalculateForEntity('deal', transfer.entity_id, {
      reason: `Transfer approved: ${transfer.id}`,
      recalculated_by: transfer.approved_by
    });
  }
  
  // 2. Create activity record
  await Activity.create({
    entity_type: transfer.entity_type,
    entity_id: transfer.entity_id,
    activity_type: 'transfer',
    activity_subtype: 'approved',
    subject: `Ownership transferred`,
    description: `Transfer approved by ${transfer.approver.full_name}. Commission split: ${transfer.from_keeps_percentage}% retained, ${100 - transfer.from_keeps_percentage}% transferred.`,
    created_by: transfer.approved_by,
    activity_data: {
      transfer_id: transfer.id,
      commission_split: {
        from_keeps: transfer.from_keeps_percentage,
        to_receives: 100 - transfer.from_keeps_percentage,
        total_amount: transfer.from_commission_total
      }
    }
  });
  
  // 3. Update entity assignment if primary stakeholder changed
  if (transfer.from_keeps_percentage === 0) {
    const entity = await getEntity(transfer.entity_type, transfer.entity_id);
    await entity.update({
      assigned_to_id: transfer.to_user_id,
      assigned_at: new Date(),
      assigned_by_id: transfer.approved_by
    });
  }
  
  // 4. Send notifications
  await notifyTransferCompletion(transfer);
};
```
</Tab>
</Tabs>

## Transfer approval workflow

Comprehensive approval system with authorization controls and business rule validation:

<Tabs>
<Tab title="Authorization controls">
**Approval authority matrix:**

| Transfer Type | Approver Requirements | Additional Conditions |
|---------------|----------------------|----------------------|
| User → User | Team Lead or Manager | Both users in same team |
| User → Team | Team Lead | Target team in same department |
| Team → User | Department Manager | User qualified for team workload |
| Team → Team | Department Manager | Cross-team coordination required |
| High Value Deal | Senior Manager + Finance | Deal value > $100K threshold |
| Complete Transfer (0% retention) | Manager+ | Primary ownership change |

```javascript
const validateApprovalAuthority = async (transfer, approverId) => {
  const approver = await User.findById(approverId, { include: ['roles', 'teams'] });
  const entity = await getEntity(transfer.entity_type, transfer.entity_id);
  
  // Check role-based permissions
  const requiredRoles = getRequiredApprovalRoles(transfer);
  const hasRequiredRole = approver.roles.some(role => requiredRoles.includes(role.name));
  
  if (!hasRequiredRole) {
    throw new AuthorizationError('Insufficient approval authority for this transfer type');
  }
  
  // Check value-based requirements
  if (transfer.entity_type === 'deal' && entity.value > 100000) {
    const hasFinanceApproval = await checkFinanceApproval(transfer.id);
    if (!hasFinanceApproval) {
      throw new AuthorizationError('High value deals require finance approval');
    }
  }
  
  // Check complete transfer requirements (0% retention)
  if (transfer.from_keeps_percentage === 0) {
    const hasManagerRole = approver.roles.some(role => 
      ['manager', 'senior_manager', 'director'].includes(role.name)
    );
    if (!hasManagerRole) {
      throw new AuthorizationError('Complete transfers require manager approval');
    }
  }
  
  return true;
};
```
</Tab>

<Tab title="Business rules validation">
**Transfer-specific business rules:**

```javascript
const validateBusinessRules = async (transfer) => {
  const { entityType, entityId, fromUserId, toUserId, fromKeepsPercentage } = transfer;
  
  // Rule 1: Minimum stakeholder tenure (prevents churning)
  const fromStakeholder = await EntityStakeholder.findOne({
    where: { entity_type: entityType, entity_id: entityId, user_id: fromUserId }
  });
  
  const stakeholderTenureHours = (new Date() - fromStakeholder.assigned_at) / (1000 * 60 * 60);
  if (stakeholderTenureHours < 24) {
    throw new BusinessRuleError('Stakeholder must be assigned for at least 24 hours before transfer');
  }
  
  // Rule 2: Total commission limit validation
  const allStakeholders = await EntityStakeholder.findAll({
    where: { entity_type: entityType, entity_id: entityId, status: 'active' }
  });
  
  const otherCommissionTotal = allStakeholders
    .filter(s => s.user_id !== fromUserId)
    .reduce((sum, s) => sum + s.commission_percentage, 0);
  
  const newFromCommission = transfer.from_commission_total * (fromKeepsPercentage / 100);
  const newToCommission = transfer.from_commission_total * ((100 - fromKeepsPercentage) / 100);
  
  if (otherCommissionTotal + newFromCommission + newToCommission > 100) {
    throw new BusinessRuleError('Transfer would exceed 100% total commission limit');
  }
  
  // Rule 3: Recipient capacity validation
  const recipientEntityCount = await EntityStakeholder.count({
    where: { 
      user_id: toUserId, 
      status: 'active',
      is_primary: true 
    }
  });
  
  const maxEntityCapacity = await getUserMaxEntityCapacity(toUserId);
  if (recipientEntityCount >= maxEntityCapacity && fromKeepsPercentage === 0) {
    throw new BusinessRuleError('Recipient has reached maximum entity capacity');
  }
  
  return true;
};
```
</Tab>

<Tab title="Approval workflow">
**Multi-stage approval process:**

```javascript
const processTransferApproval = async (transferId, approverId, decision, notes) => {
  const transfer = await EntityTransfer.findById(transferId, {
    include: ['entity', 'fromUser', 'toUser']
  });
  
  if (transfer.status !== 'pending') {
    throw new WorkflowError('Transfer is not in pending state');
  }
  
  await db.transaction(async (t) => {
    if (decision === 'approve') {
      // Validate business rules
      await validateBusinessRules(transfer);
      await validateApprovalAuthority(transfer, approverId);
      
      // Execute transfer
      await executeTransferApproval(transfer, approverId, t);
      
      // Create approval record
      await TransferApproval.create({
        transfer_id: transferId,
        approver_id: approverId,
        decision: 'approved',
        notes: notes,
        approved_at: new Date()
      }, { transaction: t });
      
    } else if (decision === 'reject') {
      // Update transfer status
      await transfer.update({
        status: 'rejected',
        rejected_by: approverId,
        rejected_at: new Date(),
        reject_reason: notes
      }, { transaction: t });
      
      // Create rejection record
      await TransferApproval.create({
        transfer_id: transferId,
        approver_id: approverId,
        decision: 'rejected',
        notes: notes,
        rejected_at: new Date()
      }, { transaction: t });
    }
  });
  
  // Send notifications
  await notifyTransferDecision(transfer, decision, approverId, notes);
};
```
</Tab>
</Tabs>

## Commission splitting mechanics

Sophisticated commission calculation with retention options and edge case handling:

<AccordionGroup>
<Accordion title="Basic commission calculations">
**Standard transfer scenarios:**

```javascript
const calculateCommissionSplit = (fromCommissionTotal, fromKeepsPercentage) => {
  const fromKeeps = fromCommissionTotal * (fromKeepsPercentage / 100);
  const toReceives = fromCommissionTotal * ((100 - fromKeepsPercentage) / 100);
  
  return {
    fromKeeps: Number(fromKeeps.toFixed(2)),
    toReceives: Number(toReceives.toFixed(2)),
    totalTransferred: fromCommissionTotal
  };
};

// Examples
calculateCommissionSplit(100, 30); // { fromKeeps: 30, toReceives: 70, totalTransferred: 100 }
calculateCommissionSplit(60, 20);  // { fromKeeps: 48, toReceives: 12, totalTransferred: 60 }
calculateCommissionSplit(50, 0);   // { fromKeeps: 0, toReceives: 50, totalTransferred: 50 }
```

**Commission distribution rules:**
- **Zero retention (0%):** Complete transfer including primary stakeholder status
- **Partial retention:** Transferor keeps specified percentage, recipient gets remainder
- **Full retention (100%):** Invalid - would result in no actual transfer
- **Precision:** All amounts rounded to 2 decimal places for consistency
</Accordion>

<Accordion title="Multi-stakeholder scenarios">
**Complex stakeholder arrangements:**

Consider Deal #123 with multiple stakeholders:
- User A: 60% commission (primary)
- User B: 25% commission  
- User C: 15% commission

**User A transfers to User D with 40% retention:**

```javascript
// Before transfer
const beforeTransfer = {
  userA: { commission: 60, isPrimary: true },
  userB: { commission: 25, isPrimary: false },
  userC: { commission: 15, isPrimary: false }
};

// Transfer calculation
const transferAmount = 60 * (100 - 40) / 100; // 36%
const retainedAmount = 60 * (40 / 100); // 24%

// After transfer (A keeps 40% = 24%, D gets 60% = 36%)
const afterTransfer = {
  userA: { commission: 24, isPrimary: false },  // Reduced, no longer primary
  userB: { commission: 25, isPrimary: false },  // Unchanged
  userC: { commission: 15, isPrimary: false },  // Unchanged
  userD: { commission: 36, isPrimary: true }    // New primary stakeholder
};
```

**Primary stakeholder determination:**
- 0% retention → Recipient becomes primary automatically
- Partial retention → Highest commission holder becomes primary
- Tie scenarios → Most recent assignment wins (or configurable rule)
</Accordion>

<Accordion title="Commission payment integration">
**Payment recalculation workflow:**

```javascript
const recalculateCommissionPayments = async (dealId, transferId) => {
  const deal = await Deal.findById(dealId);
  const transfer = await EntityTransfer.findById(transferId);
  
  if (deal.status !== 'won') {
    // No payments to recalculate for open deals
    return;
  }
  
  // Find pending commission payments
  const pendingPayments = await CommissionPayment.findAll({
    where: { 
      source_entity_type: 'deal',
      source_entity_id: dealId,
      status: 'pending'
    }
  });
  
  for (const payment of pendingPayments) {
    const stakeholder = await EntityStakeholder.findOne({
      where: { id: payment.stakeholder_id }
    });
    
    if (!stakeholder) continue;
    
    // Recalculate payment amount based on new commission percentage
    const newAmount = deal.value * (stakeholder.commission_percentage / 100);
    
    await payment.update({
      amount: newAmount,
      recalculated_at: new Date(),
      recalculation_reason: `Transfer ${transferId} - commission split`,
      recalculated_by: transfer.approved_by
    });
  }
  
  // Create new payment for recipient if they weren't previously a stakeholder
  const recipientPayment = pendingPayments.find(p => 
    p.stakeholder_id === transfer.to_stakeholder_id
  );
  
  if (!recipientPayment && transfer.to_stakeholder_id) {
    const toStakeholder = await EntityStakeholder.findById(transfer.to_stakeholder_id);
    const paymentAmount = deal.value * (toStakeholder.commission_percentage / 100);
    
    await CommissionPayment.create({
      stakeholder_id: transfer.to_stakeholder_id,
      amount: paymentAmount,
      currency: deal.currency,
      status: 'pending',
      source_entity_type: 'deal',
      source_entity_id: dealId,
      payment_schedule: toStakeholder.payment_schedule || 'immediate'
    });
  }
};
```

<Note>
Commission payments are only recalculated for pending payments. Completed payments remain unchanged to preserve financial integrity and audit compliance.
</Note>
</Accordion>
</AccordionGroup>

## Auto-cancellation system

Automatic transfer cancellation protects against invalid states and maintains system integrity:

<Tabs>
<Tab title="Cancellation triggers">
**Automatic cancellation conditions:**

```javascript
const AUTO_CANCEL_CONDITIONS = [
  {
    trigger: 'stakeholder_removed',
    condition: 'Transferor removed from entity stakeholder list',
    action: 'Cancel all pending transfers FROM this stakeholder'
  },
  {
    trigger: 'commission_reduced',
    condition: 'Commission reduced below from_commission_total',
    action: 'Cancel transfer - insufficient commission to fulfill terms'
  },
  {
    trigger: 'entity_converted',
    condition: 'Lead converted to Contact/Deal',
    action: 'Cancel pending lead transfers - entity type changed'
  },
  {
    trigger: 'entity_closed',
    condition: 'Deal marked as won/lost/cancelled',
    action: 'Cancel pending transfers - entity no longer active'
  },
  {
    trigger: 'user_deactivated',
    condition: 'Transfer participant (from/to) deactivated',
    action: 'Cancel transfer - participant no longer available'
  },
  {
    trigger: 'team_disbanded',
    condition: 'Team participant disbanded or reorganized',
    action: 'Cancel transfer - team structure changed'
  },
  {
    trigger: 'transfer_expired',
    condition: 'Transfer pending for more than 72 hours',
    action: 'Auto-expire transfer - approval timeout'
  }
];
```

**Entity lifecycle integration:**
- Lead conversion automatically cancels pending lead transfers
- Deal closure (won/lost/cancelled) blocks new transfers and cancels pending ones
- Entity deletion triggers immediate cancellation of all related transfers
- Organization changes invalidate cross-organizational transfers
</Tab>

<Tab title="Event-driven cancellation">
**Stakeholder change monitoring:**

```javascript
// Stakeholder removal trigger
eventBus.on('stakeholder.removed', async (event) => {
  const { stakeholderId, entityType, entityId, userId, teamId } = event;
  
  const pendingTransfers = await EntityTransfer.findAll({
    where: {
      entity_type: entityType,
      entity_id: entityId,
      status: 'pending',
      [Op.or]: [
        { from_user_id: userId },
        { from_team_id: teamId },
        { to_user_id: userId },
        { to_team_id: teamId }
      ]
    }
  });
  
  for (const transfer of pendingTransfers) {
    await transfer.update({
      status: 'cancelled',
      cancel_reason: 'Stakeholder removed from entity',
      cancelled_at: new Date(),
      cancelled_by_system: true
    });
    
    await notifyTransferCancellation(transfer, 'stakeholder_removed');
  }
});

// Commission reduction trigger
eventBus.on('stakeholder.commission_changed', async (event) => {
  const { stakeholderId, oldCommission, newCommission } = event;
  
  const stakeholder = await EntityStakeholder.findById(stakeholderId);
  const pendingTransfers = await EntityTransfer.findAll({
    where: {
      entity_type: stakeholder.entity_type,
      entity_id: stakeholder.entity_id,
      from_user_id: stakeholder.user_id,
      status: 'pending',
      from_commission_total: { [Op.gt]: newCommission }
    }
  });
  
  for (const transfer of pendingTransfers) {
    await transfer.update({
      status: 'cancelled',
      cancel_reason: `Commission reduced below transfer requirement (${newCommission}% < ${transfer.from_commission_total}%)`,
      cancelled_at: new Date(),
      cancelled_by_system: true
    });
    
    await notifyTransferCancellation(transfer, 'commission_reduced');
  }
});
```
</Tab>

<Tab title="Manual cancellation">
**User-initiated cancellation:**

```javascript
const cancelTransfer = async (transferId, cancelledById, reason) => {
  const transfer = await EntityTransfer.findById(transferId);
  
  if (transfer.status !== 'pending') {
    throw new WorkflowError('Only pending transfers can be cancelled');
  }
  
  // Validate cancellation authority
  const canCancel = await validateCancellationAuthority(transfer, cancelledById);
  if (!canCancel) {
    throw new AuthorizationError('Insufficient authority to cancel this transfer');
  }
  
  await transfer.update({
    status: 'cancelled',
    cancel_reason: reason,
    cancelled_at: new Date(),
    cancelled_by: cancelledById,
    cancelled_by_system: false
  });
  
  // Create activity record
  await Activity.create({
    entity_type: transfer.entity_type,
    entity_id: transfer.entity_id,
    activity_type: 'transfer',
    activity_subtype: 'cancelled',
    subject: 'Transfer cancelled',
    description: `Transfer cancelled by user: ${reason}`,
    created_by: cancelledById
  });
  
  await notifyTransferCancellation(transfer, 'manual_cancellation');
};

const validateCancellationAuthority = async (transfer, userId) => {
  // Transfer requester can cancel
  if (transfer.requested_by_id === userId) return true;
  
  // Transferor can cancel
  if (transfer.from_user_id === userId) return true;
  
  // Managers and above can cancel
  const user = await User.findById(userId, { include: ['roles'] });
  const hasManagerRole = user.roles.some(role => 
    ['manager', 'senior_manager', 'director'].includes(role.name)
  );
  
  return hasManagerRole;
};
```
</Tab>
</Tabs>

## Transfer query patterns

Optimized query patterns for transfer operations, analytics, and reporting:

<AccordionGroup>
<Accordion title="Entity transfer history">
**Complete transfer history for an entity:**

```sql
-- Get all transfers for a specific entity with participant details
SELECT 
  et.*,
  from_user.first_name || ' ' || from_user.last_name AS from_user_name,
  to_user.first_name || ' ' || to_user.last_name AS to_user_name,
  approver.first_name || ' ' || approver.last_name AS approver_name,
  requester.first_name || ' ' || requester.last_name AS requester_name
FROM entity_transfer et
LEFT JOIN users from_user ON et.from_user_id = from_user.id
LEFT JOIN users to_user ON et.to_user_id = to_user.id  
LEFT JOIN users approver ON et.approved_by = approver.id
LEFT JOIN users requester ON et.requested_by_id = requester.id
WHERE et.entity_type = 'deal' AND et.entity_id = 123
ORDER BY et.created_at DESC;

-- Transfer analytics for entity
SELECT 
  COUNT(*) as total_transfers,
  COUNT(CASE WHEN status = 'approved' THEN 1 END) as approved_count,
  COUNT(CASE WHEN status = 'cancelled' THEN 1 END) as cancelled_count,
  AVG(CASE 
    WHEN status = 'approved' 
    THEN EXTRACT(EPOCH FROM (approved_at - requested_at))/3600 
  END) as avg_approval_time_hours,
  SUM(CASE 
    WHEN status = 'approved' 
    THEN from_commission_total * (100 - from_keeps_percentage) / 100 
  END) as total_commission_transferred
FROM entity_transfer 
WHERE entity_type = 'deal' AND entity_id = 123;
```
</Accordion>

<Accordion title="User transfer analytics">
**User-specific transfer patterns and performance:**

```sql
-- Outgoing transfers (user as transferor)
SELECT 
  et.entity_type,
  COUNT(*) as transfers_out,
  AVG(et.from_keeps_percentage) as avg_retention_rate,
  SUM(et.from_commission_total * (100 - et.from_keeps_percentage) / 100) as total_given_away
FROM entity_transfer et
WHERE et.from_user_id = 456 
  AND et.status = 'approved'
  AND et.created_at >= CURRENT_DATE - INTERVAL '6 months'
GROUP BY et.entity_type;

-- Incoming transfers (user as recipient)
SELECT 
  et.entity_type,
  COUNT(*) as transfers_in,
  SUM(et.from_commission_total * (100 - et.from_keeps_percentage) / 100) as total_received
FROM entity_transfer et
WHERE et.to_user_id = 456 
  AND et.status = 'approved'
  AND et.created_at >= CURRENT_DATE - INTERVAL '6 months'
GROUP BY et.entity_type;

-- Pending transfers requiring approval
SELECT 
  et.*,
  CASE et.entity_type 
    WHEN 'lead' THEN l.title 
    WHEN 'deal' THEN d.title 
  END as entity_title,
  EXTRACT(EPOCH FROM (CURRENT_TIMESTAMP - et.requested_at))/3600 as pending_hours
FROM entity_transfer et
LEFT JOIN leads l ON et.entity_type = 'lead' AND et.entity_id = l.id
LEFT JOIN deals d ON et.entity_type = 'deal' AND et.entity_id = d.id
WHERE et.status = 'pending'
  AND et.organization_id = 789
ORDER BY et.requested_at ASC;
```
</Accordion>

<Accordion title="Organizational transfer metrics">
**Organization-wide transfer analytics and trends:**

```sql
-- Transfer volume and approval rates by month
SELECT 
  DATE_TRUNC('month', et.created_at) as month,
  et.entity_type,
  COUNT(*) as total_requests,
  COUNT(CASE WHEN status = 'approved' THEN 1 END) as approved,
  COUNT(CASE WHEN status = 'rejected' THEN 1 END) as rejected,
  COUNT(CASE WHEN status = 'cancelled' THEN 1 END) as cancelled,
  ROUND(
    COUNT(CASE WHEN status = 'approved' THEN 1 END) * 100.0 / COUNT(*), 2
  ) as approval_rate_percent
FROM entity_transfer et
WHERE et.organization_id = 789
  AND et.created_at >= CURRENT_DATE - INTERVAL '12 months'
GROUP BY DATE_TRUNC('month', et.created_at), et.entity_type
ORDER BY month DESC, et.entity_type;

-- High-value transfer monitoring
SELECT 
  et.*,
  CASE et.entity_type 
    WHEN 'deal' THEN d.value 
    ELSE 0 
  END as entity_value,
  et.from_commission_total * (100 - et.from_keeps_percentage) / 100 as commission_transferred,
  (CASE et.entity_type WHEN 'deal' THEN d.value ELSE 0 END) * 
  (et.from_commission_total * (100 - et.from_keeps_percentage) / 100) / 100 as dollar_value_transferred
FROM entity_transfer et
LEFT JOIN deals d ON et.entity_type = 'deal' AND et.entity_id = d.id
WHERE et.status = 'approved'
  AND et.organization_id = 789
  AND (
    (et.entity_type = 'deal' AND d.value > 100000) OR
    (et.from_commission_total * (100 - et.from_keeps_percentage) / 100 > 50)
  )
ORDER BY dollar_value_transferred DESC;
```
</Accordion>
</AccordionGroup>

## Transfer API endpoints

RESTful API interface for transfer operations with comprehensive validation and error handling:

<AccordionGroup>
<Accordion title="Create transfer request">
**POST** `/api/transfers`

```javascript
// Request body
{
  "entity_type": "deal",
  "entity_id": 456,
  "to_user_id": 789,
  "from_keeps_percentage": 30,
  "reason": "Agent specializes in luxury properties"
}

// Response
{
  "id": "transfer_abc123",
  "status": "pending",
  "entity": {
    "type": "deal",
    "id": 456,
    "title": "Luxury Condo Sale - Downtown",
    "value": 850000
  },
  "from_stakeholder": {
    "user_id": 123,
    "name": "Alice Johnson",
    "current_commission": 60,
    "will_keep": 18
  },
  "to_stakeholder": {
    "user_id": 789,
    "name": "Bob Smith",
    "will_receive": 42
  },
  "approval_required": true,
  "estimated_commission_value": 35700,
  "expires_at": "2024-01-18T10:30:00Z",
  "created_at": "2024-01-15T10:30:00Z"
}
```

**Validation errors:**
- `400` - Invalid entity type or entity not found
- `400` - Cannot transfer to self
- `400` - Insufficient commission for transfer
- `409` - Pending transfer already exists for entity
- `403` - Not authorized to transfer this entity
- `422` - Business rule validation failed
</Accordion>

<Accordion title="Approve/reject transfer">
**PATCH** `/api/transfers/{transferId}/approve`

```javascript
// Approve request
{
  "action": "approve",
  "notes": "Transfer approved - workload balancing initiative"
}

// Reject request  
{
  "action": "reject",
  "reason": "Insufficient experience with luxury properties",
  "notes": "Recommend additional training before handling high-value deals"
}

// Response (approval)
{
  "id": "transfer_abc123",
  "status": "approved",
  "approved_by": {
    "user_id": 555,
    "name": "Carol Manager",
    "role": "Sales Director"
  },
  "approved_at": "2024-01-15T14:45:00Z",
  "stakeholder_changes": {
    "from_stakeholder": {
      "old_commission": 60,
      "new_commission": 18,
      "primary_status_changed": true
    },
    "to_stakeholder": {
      "old_commission": 0,
      "new_commission": 42,
      "became_primary": true
    }
  },
  "commission_payments_updated": 2,
  "activity_created": true
}
```
</Accordion>

<Accordion title="Transfer history and analytics">
**GET** `/api/transfers?entity_type={type}&entity_id={id}`

```javascript
// Response - Transfer history for entity
{
  "transfers": [
    {
      "id": "transfer_abc123",
      "status": "approved", 
      "requested_at": "2024-01-15T10:30:00Z",
      "approved_at": "2024-01-15T14:45:00Z",
      "from_user": "Alice Johnson",
      "to_user": "Bob Smith", 
      "commission_transferred": 42,
      "reason": "Agent specializes in luxury properties"
    },
    {
      "id": "transfer_def456",
      "status": "cancelled",
      "requested_at": "2024-01-10T09:15:00Z", 
      "cancelled_at": "2024-01-12T11:20:00Z",
      "from_user": "Bob Smith",
      "to_user": "Charlie Brown",
      "cancel_reason": "Original stakeholder removed",
      "cancelled_by_system": true
    }
  ],
  "analytics": {
    "total_transfers": 2,
    "approved_transfers": 1,
    "cancelled_transfers": 1,
    "avg_approval_time_hours": 4.25,
    "total_commission_transferred": 42
  },
  "current_stakeholders": [
    {
      "user_id": 123,
      "name": "Alice Johnson",
      "commission_percentage": 18,
      "is_primary": false
    },
    {
      "user_id": 789,
      "name": "Bob Smith",
      "commission_percentage": 42,
      "is_primary": true
    }
  ]
}
```

**Query parameters:**
- `entity_type` - Filter by entity type (lead/deal)
- `entity_id` - Filter by specific entity
- `user_id` - Filter by user involvement (from/to)
- `status` - Filter by transfer status
- `date_from` / `date_to` - Date range filtering
- `include_analytics` - Include analytics summary
</Accordion>
</AccordionGroup>

## Integration with CRM system

The transfer system integrates seamlessly with the broader CRM ecosystem:

<Tabs>
<Tab title="Stakeholder system integration">
**Unified stakeholder model:**

```javascript
// Transfer approval leverages existing stakeholder infrastructure
const approveTransfer = async (transferId, approverId) => {
  const transfer = await EntityTransfer.findById(transferId, { 
    include: ['fromStakeholder', 'toStakeholder'] 
  });
  
  await db.transaction(async (t) => {
    // Update transferor stakeholder using existing model
    await transfer.fromStakeholder.update({
      commission_percentage: transfer.from_commission_total * (transfer.from_keeps_percentage / 100),
      is_primary: transfer.from_keeps_percentage === 0 ? false : transfer.fromStakeholder.is_primary
    }, { transaction: t });
    
    // Create or update recipient stakeholder using findOrCreate pattern
    const [toStakeholder, created] = await EntityStakeholder.findOrCreate({
      where: {
        entity_type: transfer.entity_type,
        entity_id: transfer.entity_id, 
        user_id: transfer.to_user_id
      },
      defaults: {
        commission_percentage: transfer.from_commission_total * ((100 - transfer.from_keeps_percentage) / 100),
        is_primary: transfer.from_keeps_percentage === 0,
        role: 'owner',
        status: 'active',
        assigned_by_id: approverId
      },
      transaction: t
    });
    
    // Stakeholder history automatically created via model hooks
    // Transfer status updated
    await transfer.update({
      status: 'approved',
      approved_by: approverId,
      approved_at: new Date()
    }, { transaction: t });
  });
};
```

**Stakeholder constraints respected:**
- Total commission cannot exceed 100% across all stakeholders
- Primary stakeholder designation follows existing business rules
- Stakeholder history tracking maintained through standard audit trails
- Integration with role-based permissions and team assignments
</Tab>

<Tab title="Entity lifecycle integration">
**Lead conversion handling:**

```javascript
// Lead conversion automatically cancels pending transfers
eventBus.on('lead.converted', async (event) => {
  const { leadId, contactId, dealId } = event;
  
  // Cancel pending lead transfers
  const pendingLeadTransfers = await EntityTransfer.findAll({
    where: {
      entity_type: 'lead',
      entity_id: leadId,
      status: 'pending'
    }
  });
  
  for (const transfer of pendingLeadTransfers) {
    await transfer.update({
      status: 'cancelled',
      cancel_reason: 'Lead converted to deal',
      cancelled_at: new Date(),
      cancelled_by_system: true
    });
  }
  
  // Optionally create corresponding deal transfers if business rules allow
  if (dealId && pendingLeadTransfers.length > 0) {
    await createCorrespondingDealTransfers(pendingLeadTransfers, dealId);
  }
});

// Deal closure integration
eventBus.on('deal.status_changed', async (event) => {
  const { dealId, oldStatus, newStatus } = event;
  
  if (['won', 'lost', 'cancelled'].includes(newStatus)) {
    // Cancel pending transfers for closed deals
    await EntityTransfer.update(
      {
        status: 'cancelled',
        cancel_reason: `Deal ${newStatus}`,
        cancelled_at: new Date(),
        cancelled_by_system: true
      },
      {
        where: {
          entity_type: 'deal',
          entity_id: dealId,
          status: 'pending'
        }
      }
    );
  }
});
```
</Tab>

<Tab title="Activity and communication integration">
**Activity tracking and notifications:**

```javascript
// Transfer events create comprehensive activity records
eventBus.on('transfer.approved', async (event) => {
  const transfer = await EntityTransfer.findById(event.transferId, {
    include: ['fromUser', 'toUser', 'approver', 'entity']
  });
  
  // Create activity record using standard activity system
  await Activity.create({
    entity_type: transfer.entity_type,
    entity_id: transfer.entity_id,
    activity_type: 'transfer',
    activity_subtype: 'approved',
    subject: `Ownership transferred from ${transfer.fromUser.full_name} to ${transfer.toUser.full_name}`,
    description: `Transfer approved by ${transfer.approver.full_name}. Commission split: ${transfer.from_keeps_percentage}% retained, ${100 - transfer.from_keeps_percentage}% transferred. Reason: ${transfer.reason}`,
    created_by: transfer.approved_by,
    activity_data: {
      transfer_id: transfer.id,
      commission_split: {
        from_keeps: transfer.from_keeps_percentage,
        to_receives: 100 - transfer.from_keeps_percentage,
        total_amount: transfer.from_commission_total
      },
      stakeholder_changes: {
        primary_changed: transfer.from_keeps_percentage === 0
      }
    }
  });
  
  // Send notifications through standard notification system
  await NotificationService.sendMultiple([
    {
      user_id: transfer.from_user_id,
      type: 'transfer_completed',
      title: 'Transfer Completed',
      message: `Your transfer of ${transfer.entity.title} to ${transfer.toUser.full_name} has been approved.`,
      entity_type: transfer.entity_type,
      entity_id: transfer.entity_id
    },
    {
      user_id: transfer.to_user_id,
      type: 'transfer_received',
      title: 'New Assignment Received',
      message: `You have received ${transfer.entity.title} via approved transfer from ${transfer.fromUser.full_name}.`,
      entity_type: transfer.entity_type,
      entity_id: transfer.entity_id
    }
  ]);
});
```
</Tab>
</Tabs>

## Business rules and constraints

Comprehensive business rules ensure transfer system integrity and compliance with organizational policies:

<AccordionGroup>
<Accordion title="Transfer eligibility rules">
**Entity-level constraints:**
- Only active leads and deals can be transferred (not archived, deleted, or closed)
- Lead transfers automatically cancelled upon conversion to contact/deal
- Deal transfers blocked once deal reaches 'won', 'lost', or 'cancelled' status
- Entity must belong to same organization as all transfer participants
- Polymorphic entity validation ensures only supported entity types

**Stakeholder constraints:**
- Transferor must be current stakeholder with commission > 0%
- Cannot transfer more commission than currently held (protected by snapshot)
- Transferor must have been stakeholder for minimum 24 hours (prevents rapid churning)
- Total stakeholder commission after transfer cannot exceed 100%
- Stakeholder status must be 'active' for transfer participation

**User/team constraints:**
- All participants must be active users/teams in same organization
- Cannot transfer to/from deactivated or suspended accounts
- Team transfers require all team members to be eligible for entity type
- Cross-organizational transfers prohibited
- User capacity limits enforced for complete transfers (0% retention)
</Accordion>

<Accordion title="Commission and payment rules">
**Commission calculation rules:**
- Retention percentage must be between 0% and 100%
- 0% retention transfers primary stakeholder status to recipient
- Commission amounts rounded to 2 decimal places for consistency
- Snapshot protection prevents commission calculation drift during pending period
- Multi-stakeholder scenarios maintain total commission <= 100% constraint

**Payment system integration:**
- Only pending payments can be recalculated after transfer approval
- Completed payments remain unchanged (financial integrity)
- New payment schedules follow recipient's preferences
- Multi-currency deals maintain exchange rate consistency
- Commission payment history preserved for audit compliance

**High-value deal rules:**
- Deals > $100K require additional approval authority
- Commission payments > $50K require finance department review
- International transfers subject to tax jurisdiction validation
- Regulatory compliance checks for licensed agent requirements
- Special handling for commission-based vs. salary-based compensation models
</Accordion>

<Accordion title="Approval and workflow rules">
**Authorization matrix:**
```javascript
const getRequiredApprovalLevel = (transfer) => {
  const { entityType, entityValue, fromKeepsPercentage, organizationalLevel } = transfer;
  
  // High-value deals require senior approval
  if (entityType === 'deal' && entityValue > 100000) {
    return 'senior_manager';
  }
  
  // Complete transfers (0% retention) require manager approval
  if (fromKeepsPercentage === 0) {
    return 'manager';
  }
  
  // Cross-team transfers require team lead approval
  if (organizationalLevel === 'cross_team') {
    return 'team_lead';
  }
  
  // Standard transfers require supervisor approval
  return 'supervisor';
};
```

**Workflow constraints:**
- Maximum 72-hour pending period before auto-expiration
- Approvers cannot approve their own transfer requests
- Rejection requires detailed reason and alternative recommendations
- Emergency transfers (system failures) require dual approval
- Escalation paths for delayed approvals or disputes

**Cancellation rules:**
- System auto-cancellation triggers are comprehensive and non-bypassable
- Manual cancellation requires appropriate authority
- Cancelled transfers cannot be reactivated (must create new transfer)
- Cancellation reasons tracked for pattern analysis and improvement
</Accordion>

<Accordion title="Data integrity and audit rules">
**Audit trail requirements:**
- All transfer operations logged with full context and timestamps
- Stakeholder history maintained for compliance reporting
- Commission payment changes tracked with transfer attribution
- Failed transfers logged with detailed error information
- User actions tracked for security and compliance auditing

**Data consistency checks:**
- Periodic validation of total commission percentages across entities
- Orphaned transfer detection and cleanup procedures
- Stakeholder relationship integrity verification  
- Cross-system synchronization status monitoring
- Database constraint enforcement at multiple levels

**Rollback and recovery:**
- Transfer approval operations are atomic (complete or fail entirely)
- Failed transfers can be retried with same terms within 24 hours
- System errors during approval trigger automatic investigation workflow
- Data corruption detection triggers immediate transfer suspension
- Backup and recovery procedures maintain transfer state consistency

**Performance and monitoring:**
- Transfer operation performance metrics tracked
- Alert systems for unusual transfer patterns or volumes
- Capacity planning based on transfer load and approval times
- Integration testing covers all transfer scenarios and edge cases
</Accordion>
</AccordionGroup>

<CardGroup cols={2}>
  <Card
    title="Stakeholder API reference"
    icon="users"
    href="/backend/stakeholder-api"
  >
    Complete stakeholder management and commission tracking endpoints.
  </Card>
  <Card
    title="Transfer API reference"  
    icon="arrows-rotate"
    href="/backend/transfer-api"
  >
    Transfer operations and approval workflow endpoints.
  </Card>
  <Card
    title="Entity management"
    icon="diagram-project"
    href="/backend/entity-management"
  >
    Core entity operations including leads, contacts, and deals.
  </Card>
  <Card
    title="Commission system"
    icon="money-bill-transfer"
    href="/backend/commission-system"
  >
    Commission calculation and payment processing system.
  </Card>
</CardGroup>