---
title: "Transfers"
description: "Formal transfer of leads and deals between agents or teams — request workflow, commission splitting, approval, and auto-cancel behavior."
---

The transfer system manages formal transfers of leads and deals between agents or teams, with an approval workflow and commission splitting.

## entity_transfer

```
entity_transfer
├── entity_type (lead | deal)
├── entity_id
├── from_user_id (nullable), from_team_id (nullable)
├── to_user_id (nullable), to_team_id (nullable)
├── Commission handling:
│   ├── from_commission_total (snapshot of sender's commission at request time)
│   └── from_keeps_percentage (e.g., 30%)
├── Workflow:
│   ├── status (pending, approved, rejected, cancelled)
│   ├── reason, reject_reason
│   ├── approved_by, approved_at, rejected_by, rejected_at
├── requested_by_id, requested_at, created_at, updated_at
├── organization_id
└── Database constraints:
    └── Unique partial index: (entity_type, entity_id) WHERE status = 'pending'
```

## Transfer types

| From | To   | Description                      |
| ---- | ---- | -------------------------------- |
| User | User | Standard agent-to-agent transfer |
| User | Team | Agent transfers to a team pool   |
| Team | User | Team pool claim by a user        |
| Team | Team | Team-to-team reallocation        |

---

## Transfer workflow

<Steps>
<Step title="Transfer request">
Agent A requests transfer of Lead #1 to Agent B. An `entity_transfer` record is created with `from_commission_total` (snapshot) and `from_keeps_percentage`.

<Note>
The system validates that the transfer is valid (cannot transfer to self, must have valid from/to targets) and ensures only one pending transfer exists per entity.
</Note>
</Step>

<Step title="Pending review">
Agent B (or manager) reviews the request. They can approve, reject, or cancel.

<Warning>
**Auto-cancel:** If while a transfer is pending, any stakeholder on the entity is removed or has their commission reduced, the transfer is automatically cancelled because the commission split is now invalid.
</Warning>
</Step>

<Step title="Transfer approved">
On approval:
- Agent A's stakeholder is updated: `30%`, `isPrimary=false`
- Agent B's stakeholder is created: `isPrimary=true`, `70%`
- History records created for both agents
- Transfer status set to `approved`
</Step>

<Step title="Downstream effects">
When a new deal is created from the lead later, all lead stakeholders are auto-copied to the deal with their commission splits preserved.
</Step>
</Steps>

---

## Commission calculation

The commission split is calculated using the formula:

```
to_commission = from_commission_total × (100% - from_keeps_percentage)
```

**Example:**
- Agent A has 100% commission on a lead
- Agent A transfers to Agent B, keeping 30%
- Result: Agent A keeps 30%, Agent B gets 70%

<Tip>
The `from_commission_total` field captures the transferor's commission percentage at the time of request, ensuring the calculation remains valid even if stakeholder percentages change later.
</Tip>

---

## Auto-cancel triggers

Pending transfers are automatically cancelled when:

<AccordionGroup>
<Accordion title="Stakeholder removal">
Any stakeholder on the entity (lead/deal) is completely removed from the stakeholder list.
</Accordion>

<Accordion title="Commission reduction">
Any stakeholder has their commission percentage reduced below what was captured in `from_commission_total`.
</Accordion>

<Accordion title="Entity status change">
The underlying entity (lead/deal) changes to a status that would invalidate the transfer (e.g., lead converted to deal, deal closed).
</Accordion>
</AccordionGroup>

---

## Validation rules

| Rule                          | Description                                                     |
| ----------------------------- | --------------------------------------------------------------- |
| Cannot transfer to self       | User cannot transfer entity to themselves                       |
| Valid from/to targets         | Must specify valid user_id or team_id for both from and to     |
| One pending per entity        | Only one pending transfer allowed per entity (database constraint) |
| Organization scope            | All parties must be in same organization                        |
| Entity exists                 | Target entity (lead/deal) must exist and be accessible         |
| Stakeholder permissions       | Requestor must have permission to transfer the entity          |

## Business rules

| Rule                 | Description                                                                                            |
| -------------------- | ------------------------------------------------------------------------------------------------------ |
| Approval required    | Transfer requires target user/team approval                                                            |
| Commission preserved | `from_keeps_percentage` sets what transferor keeps                                                     |
| Commission snapshot  | `from_commission_total` snapshots sender's commission at request time                                  |
| 0% transfer          | Allowed only when target is already a stakeholder (transfers primary status without moving commission) |
| Auto-cancel          | Pending transfers auto-cancelled when a stakeholder is removed or has commission reduced               |
| Team transfers       | Supports user→user, user→team, team→user, team→team                                                    |
| One pending          | Only one pending transfer per entity (enforced by unique partial index)                                |
| History recorded     | All changes logged to `entity_stakeholder_history`                                                     |

<CardGroup cols={2}>
  <Card
    title="Stakeholder data model"
    icon="database"
    href="/backend/crm/stakeholder-model"
  >
    Stakeholder model, commission splits, and payment tracking.
  </Card>
  <Card
    title="Stakeholder API reference"
    icon="arrows-rotate"
    href="/backend/stakeholder-api"
  >
    API endpoints and detailed transfer operations.
  </Card>
</CardGroup>