---
title: "Overview"
description: "Person-centric architecture, module boundaries, entity relationships, and the end-to-end customer lifecycle from inquiry to revenue."
---

The CRM module is the core of PropWise. It manages the full lifecycle of customer relationships — from initial inquiry (lead) through qualification (contact) to revenue (deal). Every entity links back to a central **Person** identity layer.

## Architecture overview

### Design principles

The CRM module follows six core design principles:

1. **Person + Contact model** — `Person` is the hidden identity layer (single source of truth for personal details). `Contact` is the business relationship layer (qualified customers). `Lead` is the sales opportunity layer (unqualified inquiries). `Deal` links to `Contact`, not `Person` directly.
2. **Unified stakeholder model** — A single `entity_stakeholder` table manages assignment and commission across leads and deals.
3. **Polymorphic patterns** — Notes, tags, and activities use `entity_type`/`entity_id` patterns for flexible targeting.
4. **Channel separation** — The `activity` table indexes the timeline; channel tables (`call_log`, `email_log`, etc.) store full data.
5. **Modular design** — CRM core is independent. Real Estate, Marketing, and Channels are optional modules.
6. **Company via Contact** — Companies associate with `Contact` via `ContactCompanyRole`, not with `Person`.

### Module boundaries

The CRM core contains the foundational entities. External modules connect through well-defined integration points.

```
┌─────────────────────────────────────────────────────────────────┐
│                         CRM CORE                                │
│  Person, Lead, Contact, Company, Deal, DealContact             │
│  person_email, person_phone, person_address, person_channel    │
│  person_not_duplicate, contact_company_role                    │
│  entity_stakeholder, entity_transfer, commission_payment       │
│  activity, note, task, event, tag                              │
└─────────────────────────────────────────────────────────────────┘
        │                    │                    │
        ▼                    ▼                    ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│ REAL ESTATE  │    │  MARKETING   │    │   CHANNELS   │
│ development  │    │  campaign    │    │  whatsapp    │
│ unit         │    │  campaign_   │    │  instagram   │
│ site_visit   │    │  lead        │    │  (linked via │
│ lead_property│    │              │    │  person_     │
│ _interest    │    │              │    │  channel)    │
│ unit_owner-  │    │              │    │              │
│ ship→Contact │    │              │    │              │
└──────────────┘    └──────────────┘    └──────────────┘
```

### Real estate → CRM integration

Real Estate entities link to the CRM layer that owns their business context:

| Real estate entity       | Links to    | Rationale                                           |
| ------------------------ | ----------- | --------------------------------------------------- |
| `unit_ownership`         | `contact_id` | Ownership is represented by the org's owner Contact |
| `unit_transaction`       | `person_id` | Transaction party is an individual                  |
| `site_visit`             | `person_id` | Who visited the property                            |
| `lead_property_interest` | `lead_id`   | Links to lead for sales context                     |
| `deal_property_interest` | `deal_id`   | Links to deal for transaction context               |

Lead property interests remain readable when a lead is archived. Archived leads are read-only, but the CRM lead detail/property-interest endpoint must bypass the Lead `active` filter for the parent lead check so historical property interest context stays visible.

**Deal property interest workflow:**

```
// Deal created FROM Lead:
// Copy the explicitly selected LeadPropertyInterest criteria → DealPropertyInterest (1:1)
// If the lead interest has multiple InventoryUnit candidates, choose one unit for the deal.
deal.propertyInterest.originatingInterest = leadPropertyInterest

// Deal created directly (walk-in):
// Create DealPropertyInterest with no originating interest and at least one criterion
// (an inventory unit selection is sufficient on its own)
deal.propertyInterest.originatingInterest = null
```

**Access pattern for Contact:**

```typescript
// Contact page showing ownership
contact.unitOwnerships  // All ownerships
contact.unitOwnerships.filter(o => o.isActive)  // Current
contact.person  // Identity details for the owner Contact
```

---

## CRM public IDs

All core CRM business records expose a stored `publicId` for user-facing references. The format is:

```
{ENTITY_CODE}-{ORG_PREFIX}-{SEQUENCE}
```

Examples: `LEAD-ADNS-001`, `PERS-ADNS-001`, `CONT-ADNS-001`, `COMP-ADNS-001`, `DEAL-ADNS-001`, `COMM-ADNS-001`, `USER-ADNS-001`, `TEAM-ADNS-001`, `LSTG-ADNS-001`, `DSTG-ADNS-001`.

<Info>
Entity codes make IDs globally unique within an organization across CRM record types. `ORG_PREFIX` is generated once from the organization's normalized alphanumeric name. Names of 4 characters or fewer use the whole normalized name (`WIK`); longer names use the first 2 and last 2 characters (`Adidas Operations` → `ADNS`, `Adidas` → `ADAS`).
</Info>

### ID allocation rules

- `SEQUENCE` is scoped by organization and entity type, padded to at least 3 digits, and not capped (`LEAD-ADNS-1000` is valid)
- IDs are allocated by `CrmPublicIdService` from `crm_public_id_counter` in the same transaction as entity creation
- Existing IDs do not change when the organization is renamed
- Entity codes: Lead=`LEAD`, Person=`PERS`, Contact=`CONT`, Company=`COMP`, Deal=`DEAL`, CommissionPayment=`COMM`, organization user membership=`USER`, Team=`TEAM`, LeadStage=`LSTG`, DealStage=`DSTG`

### Avatar colors

Organization user memberships also receive stable fallback avatar colors from the user sequence (`organization_users.avatar_bg_color` + `avatar_text_color`). The hue uses a golden-angle spread across a finite HSL palette; colors are display metadata and may repeat in very large organizations, while `public_id` remains the unique user-facing membership identifier.

<Note>
Global system stages are the exception to the org-sequence format: they have deterministic IDs based on `systemType` (for example `LSTG-NEW`, `LSTG-DISQUALIFIED`, `DSTG-CLOSED-WON`). Org-specific custom/override stages use the normal org-scoped sequence (`LSTG-ADNS-001`, `DSTG-ADNS-001`).
</Note>

---

## Core entities

### Person (central identity)

**Purpose**: Single source of truth for human identity and preferences.

```
Person
├── public_id (coded org-scoped human ID, e.g. PERS-ADNS-001)
├── Identity: first_name, last_name, avatar_url, title
├── Demographics: date_of_birth, nationality
├── Links: links jsonb [{ type, url }] where type is website/facebook/instagram/linkedIn/twitter/tiktok
├── Preferences: timezone
├── Languages: languages (unified array with code and proficiency per entry)
├── Communication Flags: do_not_call, do_not_email
├── Source Tracking: original_source
├── Merge Tracking: merged_into_id, merged_at, merged_by
├── Computed: full_name (getter: first_name + last_name)
└── Related Tables:
    ├── person_email (multiple emails, one primary)
    ├── person_phone (multiple phones, one primary)
    ├── person_address (multiple addresses, one primary)
    ├── person_channel (WhatsApp, Instagram, etc. identities)
    └── person_not_duplicate (deduplication override pairs)
```

**Key Rules**:
- Every Lead, Contact must link to a Person
- `publicId` is generated once at creation using the shared CRM public ID allocator (`PERS-*`)
- Person preferences apply across all contexts (leads, deals, contacts)
- `nationality` is stored on Person so leads and contacts share the same identity profile value
- `original_source` is set once when person first enters system
- Languages array uses unified `UserLanguageEntry` format with code and proficiency per entry
- `links` stores web and social profile links as JSONB entries with `type` and `url`

### PersonChannel (communication channels)

**Purpose**: Stores a person's communication channel identities (WhatsApp, Instagram, etc.).

```
person_channel
├── person_id → Person
├── channel_type (whatsapp, instagram, facebook, telegram, sms, webchat)
├── channel_identifier (phone number, username, PSID, etc.)
├── display_name, avatar_url
├── channel_identity_id → WhatsAppUser.id, InstagramUser.id, etc.
├── status (active, inactive, blocked, unsubscribed)
├── is_primary
├── Opt-in: marketing_opt_in, transactional_opt_in
├── Engagement: first_contact_at, last_message_at, message_count
└── Verification: is_verified, verified_at
```

**Key Rules**:
- Similar pattern to `person_email`, `person_phone`, `person_address`
- Channel belongs to Person, not Lead (Person-centric architecture)
- Lead can reference `source_channel_id` for attribution (which channel it came through)
- `channel_identity_id` links to detailed channel entities (WhatsAppUser, InstagramUser)
- One Person can have multiple channels of same type (e.g., multiple WhatsApp numbers)

### PersonEmail, PersonPhone, PersonAddress

**Purpose**: Multiple contact methods per person with primary designation.

```
person_email
├── person_id → Person
├── email, is_primary
├── is_verified, verified_at
├── bounce_count, last_bounce_at
├── organization_id
└── Unique constraint: (email, organization_id)

person_phone
├── person_id → Person
├── phone, is_primary
├── is_verified, verified_at
├── country_code, formatted_number
├── organization_id
└── Unique constraint: (phone, organization_id)

person_address
├── person_id → Person
├── address_line1, address_line2, city, state
├── postal_code, country, is_primary
├── address_type (Home, Work, Billing, etc.)
├── latitude, longitude
├── organization_id
└── No unique constraint (same address for multiple people)
```

### PersonNotDuplicate (deduplication overrides)

**Purpose**: Records pairs of persons that have been manually confirmed as NOT duplicates.

```
person_not_duplicate
├── person1_id → Person
├── person2_id → Person
├── marked_by → User (who made the decision)
├── marked_at (when the decision was made)
├── organization_id → Organization
├── Unique constraint: (person1, person2, organization)
```

**Key Rules**:
- Symmetric: if (A, B) is marked as not-duplicate, the system treats (B, A) equivalently
- Organization-scoped: each org maintains its own override decisions
- Used by `PersonNotDuplicateService` to exclude pairs from duplicate detection

### Person merge system

**Purpose**: Consolidates duplicate persons into a single primary record, reassigning all related data.

**API Endpoint**: `POST /persons/:primaryPersonId/merge`

<Steps>
<Step title="Validation">
- Verify primary person exists and is not deleted
- Verify all secondary persons exist and are not deleted
- All persons must be in the same organization
</Step>

<Step title="Field selection">
- Accept fieldSelections: Record<string, string>
- For each field, pick the value from the specified source person
- Fields not listed default to primary person's values
</Step>

<Step title="Contact info merge">
- mergeAllEmails: boolean — reassign all secondary emails to primary
- mergeAllPhones: boolean — reassign all secondary phones to primary  
- mergeAllAddresses: boolean — reassign all secondary addresses to primary
- isPrimary flags reset to false for transferred records
</Step>

<Step title="Channel and entity reassignment">
- All person_channels reassigned to primary person
- All leads, contacts, and related CRM entities updated
- Secondary persons marked with merged_into_id and merged_at
</Step>
</Steps>

### Lead (sales opportunity layer)

**Purpose**: Represents unqualified inquiries and sales opportunities.

```
Lead
├── person_id → Person (required)
├── stage_id → LeadStage, stage_entered_at
├── Business: intent, score, temperature, budget
├── Attribution: lead_source, source_channel_id
├── Display: companyName (display only, no FK)
├── Lifecycle: converted_at, disqualified_at, archived_at
├── Tracking: is_deleted, created_at, updated_at
└── Relations: stakeholders[], activities[], propertyInterests[]
```

**Key Rules**:
- Every lead must link to exactly one Person
- Lead visibility is determined by stakeholder assignments
- Converting a lead creates Contact + Deal with stakeholder inheritance
- Multiple leads can exist for the same Person

### Contact (business relationship layer)

**Purpose**: Represents qualified customers with business context.

```
Contact
├── person_id → Person (OneToOne per organization)
├── Business: customer_since, rating, reference_number
├── Company: companyRoles[] → ContactCompanyRole
├── Deals: dealContacts[] → DealContact
├── Computed: lifetime_value (sum of won deals)
└── Visibility: Derived from person's leads
```

**Key Rules**:
- One Contact per Person per organization
- Contact visibility derived from Person's leads (via stakeholders)
- Lifetime value calculated from associated deals
- Company associations handled through ContactCompanyRole

### Company

**Purpose**: Organization profiles that associate with contacts (not persons).

```
Company
├── Identity: name, website, logo_url
├── Details: description, industry, size_category
├── Location: headquarter_country, headquarter_city
├── Social: linkedin_company_id
└── Relations: contactRoles[] → ContactCompanyRole
```

**Key Rules**:
- Companies link to Contacts, not Persons directly
- Multiple contacts can be associated with one company
- ContactCompanyRole manages the many-to-many relationship

### Deal (revenue opportunities)

**Purpose**: Represents qualified sales opportunities with revenue potential.

```
Deal
├── title, value, currency, commission_rate
├── stage_id → DealStage, probability
├── Lifecycle: is_closed, is_won, closed_at, expected_close_date
├── Source: originating_lead_id → Lead (lineage tracking)
├── Contacts: dealContacts[] → DealContact (primary + others)
├── Commission: total_commission, commissionPayments[]
└── Property: propertyInterests[] (Real Estate module)
```

**Key Rules**:
- Deals link to Contacts through DealContact junction table
- One primary contact per deal (isPrimary = true)
- Commission payments created automatically when deal closes as won
- Property interests managed through Real Estate module integration

### DealContact (junction table)

**Purpose**: Manages many-to-many relationship between deals and contacts.

```
deal_contact
├── deal_id → Deal
├── contact_id → Contact
├── role, is_primary, notes
├── created_at, created_by_id
└── organization_id
```

**Key Rules**:
- Exactly one primary contact per deal (is_primary = true)
- Primary contact cannot be removed until another is designated primary
- Role describes the contact's involvement in the deal

---

## Assignment & commission system

The CRM uses a unified stakeholder model where both leads and deals share the same assignment and commission structure.

### EntityStakeholder (unified assignment)

**Purpose**: Single table for assignment and commission across leads/deals.

```
entity_stakeholder
├── entity_type (lead | deal)
├── entity_id (UUID pointing to lead or deal)
├── user_id (nullable), team_id (nullable)
├── is_primary, access_level (READ | WRITE | FULL)
├── commission_percentage, original_team_commission
├── inherited_from_stakeholder_id, inherited_from_lead_id
├── created_at, updated_at
└── Unique constraint: (entity_type, entity_id, user_id, team_id)
```

<Warning>
**Business Rules**:
- Commission percentages must sum to exactly 0% or 100% (0.01% tolerance)
- At least one `isPrimary = true` stakeholder must exist when stakeholders are present
- Observers (0% commission) cannot be primary
- Stakeholders inherit from lead to deal during conversion
</Warning>

### Commission payments

**Purpose**: Tracks actual commission payouts when deals close.

```
commission_payment
├── deal_id → Deal
├── stakeholder_id → EntityStakeholder
├── amount (calculated from deal value × commission rate × percentage)
├── currency
├── status (pending | paid | on_hold | cancelled)
├── paid_at, payment_reference
├── hold_reason, hold_until
└── organization_id
```

**Business Rules**:
- Commission payments are created when deal closes with `is_won = true`
- Amount calculated as: `deal.value * deal.commission_rate * stakeholder.commission_percentage / 10000`
- Payments can be held for approval or legal reasons
- Payment status tracked for accounting integration

---

## Transfer system

The transfer system allows stakeholders to move leads or deals between agents with configurable commission retention.

### EntityTransfer

**Purpose**: Manages transfer requests with approval workflow.

```
entity_transfer
├── entity_type (lead | deal)
├── entity_id → Lead or Deal
├── from_user_id → User (current owner)
├── to_user_id → User (intended new owner)
├── retain_commission_percentage (0-100)
├── reason, notes
├── status (pending | approved | rejected | cancelled)
├── requested_at, approved_at, approved_by
├── completed_at
└── organization_id
```

### Transfer workflow

<Steps>
<Step title="Request transfer">
Current primary stakeholder creates transfer request specifying commission retention (0-100%).
</Step>

<Step title="Approval">
Target user or manager approves/rejects the transfer request.
</Step>

<Step title="Execute transfer">
System updates stakeholders:
- Original owner: reduced to retained percentage, `is_primary = false`
- New owner: gets remaining percentage, `is_primary = true`
- All stakeholders get inheritance tracking
</Step>

<Step title="Auto-cancel">
Pending transfers are automatically cancelled if the entity is archived or deleted.
</Step>
</Steps>

---

## Activity & communication system

The activity system provides a unified timeline across all CRM entities while keeping detailed communication data in specialized channel tables.

### Activity (timeline index)

**Purpose**: Lightweight timeline index for all entity interactions.

```
activity
├── target_entity_type (lead | deal | contact | company)
├── target_entity_id (UUID)
├── contact_id (nullable — for bubble-up visibility)
├── activity_type (call | email | meeting | site_visit | note | task)
├── reference_id → channel table ID
├── summary (max 500 chars)
├── is_pinned, is_important
├── created_at, created_by_id
└── organization_id
```

### Channel tables

Detailed communication data is stored in specialized tables:

- `call_log` — Phone call records
- `email_log` — Email communications  
- `sms_log` — SMS messages
- `meeting` — Meeting records
- `site_visit` — Property visits
- `note` — Notes and comments

**Key Rules**:
- Activity table indexes the timeline; channel tables store full data
- `reference_id` links activity to channel table record
- Channel tables can exist without Activity records (internal tracking)
- Activity records always have corresponding channel records

---

## Notes system

### Note entity

**Purpose**: Pure content storage with polymorphic targeting via Activity table.

```
note
├── title (optional), body
├── attachments[] (JSONB: {url, filename, size, mimeType})
├── is_pinned, is_private
├── organization_id, created_by_id
├── created_at, updated_at
└── No direct entity targeting (done via Activity)
```

### Entity targeting

Notes target entities through the Activity system:
1. Create `note` record
2. Create `activity` record with `activity_type = 'note'` and `reference_id = note.id`
3. Set `target_entity_type` and `target_entity_id` on activity

**Benefits**:
- Consistent timeline ordering with other activities
- Unified permissions through activity targeting
- Notes can be pinned/flagged independently of activity metadata

---

## Stage history & analytics

The CRM tracks pipeline progression for both leads and deals with detailed stage history.

### Stage configuration

```sql
lead_stage / deal_stage
├── name, color, order
├── system_type (enum: new | qualified | converted | closed | disqualified)
├── next_stage_id (nullable)
├── organization_id
└── is_active
```

### Stage history

**Purpose**: Records completed stages only (not current stage).

```sql
lead_stage_history / deal_stage_history
├── lead_id / deal_id
├── stage_id (the stage we LEFT)
├── entered_at, duration_seconds
├── next_stage_id
├── notes
└── created_by_id
```

<Info>
**Key Rules**:
- Current stage is stored on `lead.stage_id` / `deal.stage_id` with `stage_entered_at`
- History only contains stages we've LEFT, never the current stage
- Duration is calculated when leaving a stage
- Analytics aggregate history records for pipeline reporting
</Info>

### Analytics queries

Common stage analytics patterns:

```sql
-- Average time in each stage
SELECT 
  s.name,
  AVG(h.duration_seconds / 86400.0) as avg_days
FROM lead_stage_history h
JOIN lead_stage s ON h.stage_id = s.id
GROUP BY s.id, s.name;

-- Conversion rates between stages  
SELECT 
  from_stage.name as from_stage,
  to_stage.name as to_stage,
  COUNT(*) as transitions
FROM lead_stage_history h
JOIN lead_stage from_stage ON h.stage_id = from_stage.id
JOIN lead_stage to_stage ON h.next_stage_id = to_stage.id
GROUP BY from_stage.id, to_stage.id;
```

---

## Query patterns

### Person-centric queries

```sql
-- All CRM entities for a person
SELECT p.*, 
  l.id as lead_id, l.stage_id as lead_stage,
  c.id as contact_id, c.rating as contact_rating,
  d.id as deal_id, d.value as deal_value
FROM person p
LEFT JOIN lead l ON l.person_id = p.id AND l.is_deleted = false
LEFT JOIN contact c ON c.person_id = p.id AND c.is_deleted = false
LEFT JOIN deal_contact dc ON dc.contact_id = c.id
LEFT JOIN deal d ON d.id = dc.deal_id AND d.is_deleted = false
WHERE p.id = :person_id;
```

### Stakeholder visibility

```sql
-- Contacts visible to current user
SELECT DISTINCT c.* 
FROM contact c
JOIN person p ON c.person_id = p.id
JOIN lead l ON l.person_id = p.id
JOIN entity_stakeholder es ON es.entity_type = 'lead' AND es.entity_id = l.id
WHERE es.user_id = :current_user_id
  AND es.access_level IN ('WRITE', 'FULL');
```

### Commission calculations

```sql
-- Commission breakdown for closed deals
SELECT 
  d.id as deal_id,
  d.value * d.commission_rate / 100 as total_commission,
  u.first_name + ' ' + u.last_name as stakeholder,
  es.commission_percentage,
  (d.value * d.commission_rate * es.commission_percentage / 10000) as amount
FROM deal d
JOIN entity_stakeholder es ON es.entity_type = 'deal' AND es.entity_id = d.id
JOIN user u ON es.user_id = u.id
WHERE d.is_closed = true AND d.is_won = true;
```

---

## Business rules

### Data integrity rules

1. **Unique constraints**: Person emails, phone numbers, and channel identifiers must be unique within organization
2. **Soft deletes**: All entities use `is_deleted` flag; hard deletes are prohibited
3. **Cascade behavior**: Deleting person cascades to all leads/contacts; deleting lead nullifies deal references
4. **Commission validation**: Stakeholder percentages must sum to 0% or 100% (±0.01% tolerance)
5. **Primary stakeholder**: At least one `is_primary = true` stakeholder required when stakeholders exist

### Business logic rules

1. **Lead conversion**: Converting lead creates contact + deal; stakeholders inherit to deal
2. **Person merge**: Secondary persons are marked with `merged_into_id`; all related data reassigns to primary
3. **Transfer approval**: Only primary stakeholders or managers can initiate transfers
4. **Commission inheritance**: Deal stakeholders inherit from originating lead with audit trail
5. **Access control**: Users see entities based on stakeholder relationships and team hierarchies

### Validation rules

1. **Email format**: All email addresses validated against RFC 5322 standard
2. **Phone format**: Phone numbers normalized to E.164 international format
3. **Commission range**: Individual percentages must be 0-100%; total must be 0% or 100%
4. **Stage progression**: Stages must follow configured `next_stage_id` relationships
5. **Entity references**: All foreign keys must reference existing, non-deleted records

---

## Entity relationship diagram

<AccordionGroup>
<Accordion title="Core CRM entities">
```
                          PERSON
            (Hidden identity layer)
  id, public_id (PERS-ADNS-001), first_name, last_name, avatar_url, title
  timezone, do_not_call, do_not_email
  original_source, languages[], date_of_birth, nationality
  links jsonb [{ type, url }]
  merged_into_id, merged_at, merged_by
                     │
    ┌────────────────┼────────────────┬────────────────┐
    ▼                ▼                ▼                ▼
 person_email    person_phone    person_address    person_channel
 ├── email        ├── phone      ├── address_line1  ├── channel_type
 ├── is_primary   ├── is_primary ├── city, country  ├── channel_identifier
 └── is_verified  └── is_verified├── is_primary     ├── is_primary
                                 └── address_type   └── channel_identity_id
                     │
                     │ person_id
                     ▼
  LEAD (Sales Layer)                 CONTACT (Business Layer)
  person_id, stage_id                person_id (OneToOne)
  intent, score, temperature         customer_since, rating
  companyName (display, no FK)       reference_number
  budget, lead_source                companyRoles → Company
  converted_at, disqualified_at      dealContacts → DealContact
              │                               │
              │ originating_lead_id            │ via DealContact
              └───────────────────────────────▼
                          DEAL
                          title, value, currency
                          dealContacts[] → DealContact
                            └── isPrimary=true for primary
                          originating_lead_id → Lead (lineage)
                          stage_id, probability
                          is_closed, is_won, closed_at
                          commission_rate, total_commission

DEAL_CONTACT (Junction Table)
deal_id → Deal, contact_id → Contact
role, is_primary, notes

CONTACT_COMPANY_ROLE (Company Associations)
contact_id → Contact, company_id → Company
role, job_title, department
is_primary, is_decision_maker, is_active
```
</Accordion>

<Accordion title="Assignment and commission">
```
                entity_stakeholder
    Unified assignment for leads & deals

entity_type (lead | deal), entity_id
user_id (nullable), team_id (nullable)  
is_primary, access_level (READ | WRITE | FULL)
commission_percentage, original_team_commission
inherited_from_stakeholder_id, inherited_from_lead_id
                │
    ┌───────────┼───────────┬───────────────┐
    ▼           ▼           ▼               ▼
stakeholder_  entity_     commission_     entity_
history       transfer    payment         not_duplicate
│             │           │               │
├── action    ├── from_   ├── amount      ├── person1_id
├── old_vals  │   user_id ├── status      ├── person2_id  
├── new_vals  ├── to_     ├── paid_at     └── marked_by
└── reason    │   user_id └── hold_until
              ├── retain_
              │   commission
              └── status
```
</Accordion>

<Accordion title="Activity and communication">
```
                      activity
Timeline index (lightweight, polymorphic targeting)
target_entity_type (lead | deal | contact | company)
target_entity_id (UUID)
contact_id (nullable — for bubble-up visibility)
activity_type, reference_id → channel table
summary (max 500 chars)  
is_pinned, is_important
                │ reference_id
    ┌───────────┼───────────┬───────────┬───────────┐
    ▼           ▼           ▼           ▼           ▼
  call_log    email_log   sms_log    meeting    site_visit
  │           │           │          │          │
  ├── phone   ├── to[]    ├── phone  ├── title  ├── person_id
  ├── duration├── cc[]    ├── body   ├── agenda ├── property
  ├── outcome ├── subject ├── sent   ├── attendees └── outcome
  └── notes   └── body    └── status └── location

                        note
                Pure content storage
                ├── title (optional)
                ├── body, attachments[]
                ├── is_pinned, is_private
                └── created_by_id
```
</Accordion>

<Accordion title="Stage history and analytics">
```
    lead_stage                 lead_stage_history
┌─────────────────┐        ┌────────────────────────┐
│ name, color     │        │ lead_id                │
│ order, system_  │        │ stage_id (LEFT stage) │
│ type            │◄───────│ entered_at             │
│ next_stage_id   │        │ duration_seconds       │
└─────────────────┘        │ next_stage_id          │
                           │ notes, created_by_id   │
    deal_stage             └────────────────────────┘
┌─────────────────┐        ┌────────────────────────┐
│ name, color     │        │ deal_id                │
│ order, system_  │        │ stage_id (LEFT stage) │ 
│ type            │◄───────│ entered_at             │
│ next_stage_id   │        │ duration_seconds       │
└─────────────────┘        │ next_stage_id          │
                           │ notes, created_by_id   │
                           └────────────────────────┘

Note: Current stage stored on lead/deal with stage_entered_at
History only contains completed (LEFT) stages
```
</Accordion>
</AccordionGroup>

---

## Events & integration

The CRM module emits events at key lifecycle points for integration with other systems.

### Event types

<AccordionGroup>
<Accordion title="Person events">
- `person.created` — New person record created
- `person.merged` — Person merge completed  
- `person.updated` — Person information updated
- `person.channel.added` — New communication channel added
</Accordion>

<Accordion title="Lead events">
- `lead.created` — New lead inquiry received
- `lead.converted` — Lead converted to contact + deal
- `lead.stage_changed` — Lead moved to different stage
- `lead.assigned` — Lead stakeholders updated
- `lead.archived` — Lead archived or disqualified
</Accordion>

<Accordion title="Deal events">
- `deal.created` — New deal opportunity created
- `deal.closed_won` — Deal successfully closed
- `deal.closed_lost` — Deal lost/rejected
- `deal.stage_changed` — Deal moved to different stage
- `deal.commission_paid` — Commission payment processed
</Accordion>

<Accordion title="Transfer events">
- `transfer.requested` — Transfer request created
- `transfer.approved` — Transfer request approved
- `transfer.completed` — Transfer execution finished
- `transfer.cancelled` — Transfer request cancelled
</Accordion>
</AccordionGroup>

### Integration patterns

**Webhook delivery**: Events are delivered to configured webhook endpoints with retry logic and failure tracking.

**Event sourcing**: All events are stored in an event log for audit trails and system reconstruction.

**External system sync**: Events trigger synchronization with external CRM systems, accounting software, and marketing platforms.

---

## Data consistency guarantees

The CRM module enforces several invariants across all operations:

| Guarantee | Mechanism | Details |
| --- | --- | --- |
| Transactional writes | `TenantContext.executeInOrg()` | Every write runs in a database transaction. Any failure rolls back the entire operation. |
| Soft-delete everywhere | `isDeleted` flag | No entity is hard-deleted. All deletions set `isDeleted = true`. |
| Unique DB constraints | PostgreSQL unique indexes | Race conditions are caught by database-level constraints (stakeholder uniqueness, transfer uniqueness, lead deduplication). Error code 23505 is handled gracefully. |
| Commission invariant | `Math.abs(total - 100) > 0.01` | Commission percentages must sum to exactly 0% or 100% with 0.01% floating-point tolerance. Enforced on every stakeholder mutation. |
| Primary invariant | Pre-delete + post-create checks | At least one `isPrimary = true` stakeholder must exist when stakeholders are present. Observers (0% commission) cannot be primary. |
| Cascade cleanup | Archive/delete side effects | Archiving cancels pending transfers. Soft-deleting cascades to all child records. Deal `originatingLead` references are set to NULL on lead deletion. |

---

<CardGroup cols={2}>
  <Card title="Person" icon="user" href="/backend/crm/person">
    Central identity layer, channels, merge, and deduplication.
  </Card>
  <Card title="Lead" icon="bullseye" href="/backend/crm/lead">
    Sales opportunities, creation behavior, and import.
  </Card>
  <Card title="Contact" icon="address-book" href="/backend/crm/contact">
    Business relationship layer, visibility, and company roles.
  </Card>
  <Card title="Company" icon="building" href="/backend/crm/company">
    Organization profiles and contact-company associations.
  </Card>
  <Card title="Deal" icon="handshake" href="/backend/crm/deal">
    Revenue opportunities, DealContact, and property interests.
  </Card>
  <Card
    title="Stakeholder data model"
    icon="database"
    href="/backend/crm/stakeholder-model"
  >
    Stakeholder model, commission splits, and payment tracking.
  </Card>
  <Card
    title="Transfers"
    icon="arrow-right-arrow-left"
    href="/backend/crm/transfers"
  >
    Transfer requests, approval workflow, and auto-cancel.
  </Card>
  <Card title="Activities" icon="timeline" href="/backend/crm/activities">
    Activity timeline, channel tables, and notes.
  </Card>
  <Card title="Stages" icon="diagram-project" href="/backend/crm/stages">
    Lead and deal pipelines, stage history, and analytics.
  </Card>
  <Card title="Enums" icon="list" href="/backend/crm/enums">
    Complete enum reference for all CRM entities.
  </Card>
</CardGroup>