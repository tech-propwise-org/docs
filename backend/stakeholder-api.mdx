---
title: "Stakeholder API reference"
description: "REST endpoints, service functions, and access control for stakeholder assignment, transfer, commission management, and bidirectional deal-lead sync."
---

This page documents the API endpoints and service functions for managing lead/deal stakeholders, assignment, transfer, and commission handling. For table schemas, data model concepts, and invariants, see the [stakeholder data model](/backend/crm/stakeholder-model).

## Stakeholder sync service

When working with deals that originate from leads, stakeholder assignments maintain consistency automatically. The `StakeholderSyncService` syncs stakeholder assignments between deals and their originating leads.

### Design principles

1. **One-way primary flow** — Leads inherit to deals at deal creation
2. **Bidirectional sync** — Deal stakeholder changes sync back to lead
3. **Safe by default** — Multiple safety checks prevent unwanted removals
4. **Non-blocking** — Sync failures log errors but don't fail the primary operation

### Assign to deal → auto-assign to lead

When you assign a stakeholder to a deal, they are also assigned to the originating lead as an observer.

```bash
POST /deals/{dealId}/stakeholders
Content-Type: application/json

{
  "userId": "agent-b-uuid",
  "commissionPercentage": 30,
  "accessLevel": "WRITE"
}
```

**What happens:**

1. Agent B is assigned to Deal #1 (30% commission, WRITE access)
2. System checks: Does Deal #1 have an originating lead?
3. If yes AND lead exists AND lead not archived:
   - Agent B is also assigned to Lead #1 (0% commission, WRITE access, observer)
   - Returns: `{ success: true, syncedToLead: true, leadId: "lead-uuid" }`

**Sync settings:**

| Property               | Deal value                 | Lead value                |
| ---------------------- | -------------------------- | ------------------------- |
| `commissionPercentage` | As specified (e.g., 30%)   | **0%** (observer)         |
| `accessLevel`          | As specified (e.g., WRITE) | **Same as deal**          |
| `isPrimary`            | As specified               | **false** (never primary) |

**Skip conditions:**

| Condition            | Action    | Reason                         |
| -------------------- | --------- | ------------------------------ |
| No originating lead  | Skip sync | Nothing to sync to             |
| Lead is archived     | Skip sync | Can't modify archived entities |
| User already on lead | Skip sync | No duplication needed          |

### Remove from deal → auto-remove from lead

When you remove a stakeholder from a deal, the system checks whether they should also be removed from the lead.

```bash
DELETE /deals/{dealId}/stakeholders/{stakeholderId}
```

**What happens:**

1. Stakeholder removed from Deal #1
2. System checks: Should we also remove from originating lead?
3. Multiple safety checks applied (see below)
4. If all checks pass:
   - Stakeholder removed from Lead #1
   - Other lead stakeholder commissions auto-adjusted to 100%
   - Returns: `{ success: true, syncedToLead: true, leadId: "lead-uuid" }`

**Safety checks (ALL must pass):**

| #   | Check                                       | Reason                           |
| --- | ------------------------------------------- | -------------------------------- |
| 1   | Lead exists and not archived                | Can't modify archived leads      |
| 2   | User has **0% commission** on lead          | Protect earned commission rights |
| 3   | User is **NOT primary** on lead             | Protect primary ownership        |
| 4   | This is user's **ONLY deal** from that lead | Keep access if other deals exist |

If any check fails, the stakeholder is removed from the deal only — the lead is unchanged.

**Commission auto-adjustment:**

If removing would break 100% total on lead, the system auto-adjusts:

```
BEFORE:
- Agent A: 50%
- Agent B: 0% ← Removing
- Agent C: 20%
Total: 70%

AFTER (auto-adjusted to 100%):
- Agent A: 71.43% (50 × 100/70)
- Agent C: 28.57% (20 × 100/70)
Total: 100% ✓

Formula: new% = old% × (100 / current_total)
```

### Sync response format

Both create and remove operations return:

```typescript
interface StakeholderSyncResultDto {
  success: boolean; // Operation succeeded
  syncedToLead: boolean; // Whether sync occurred
  leadId?: string; // ID of lead (if applicable)
  message?: string; // Optional status message
}
```

## Team commission budget

A team's `commissionPercentage` on an entity stakeholder acts as a **live budget** that you can sub-allocate to individual team members.

### Design principles

1. **Team as budget holder** — The team stakeholder's `commissionPercentage` is the total pool available for team member allocation
2. **Sub-allocation** — Team managers assign commission from the team budget to individual members. The team's percentage decreases as members receive allocations
3. **Unallocated budget at close** — If a deal closes with remaining team budget (team stakeholder has `commissionPercentage > 0`), that amount is paid to the **team** directly, not to any individual
4. **Team pool freeze** — When a team-only stakeholder (has `team`, no `user`) holds primary status, the entity is blocked from modification until a team member claims or is assigned to it

### Budget lifecycle

```
1. Lead assigned to Team A (100% commission)
   └── Team A stakeholder: { team: "Team A", user: null, commission: 100%, isPrimary: true,
                              originalTeamCommission: 100 }

2. Team manager assigns Agent X (40% from team budget)
   ├── Team A stakeholder: { team: "Team A", user: null, commission: 60%, isPrimary: false,
   │                          originalTeamCommission: 100 }
   └── Agent X stakeholder: { user: "Agent X", team: "Team A", commission: 40%, isPrimary: true,
                               originalTeamCommission: 100 }

3. Team manager assigns Agent Y (30% from team budget)
   ├── Team A stakeholder: { commission: 30% }   ← remaining budget
   ├── Agent X stakeholder: { commission: 40%, originalTeamCommission: 100 }
   └── Agent Y stakeholder: { commission: 30%, originalTeamCommission: 100 }

4. Deal closes → Commission payments:
   ├── Agent X: 40% → paid to user
   ├── Agent Y: 30% → paid to user
   └── Team A:  30% → paid to TEAM (unallocated budget)
```

`originalTeamCommission` on each member's stakeholder records the team's total allocation (100% in this case), allowing the system to validate that the sum of team member commissions never exceeds the original team budget.

### Claim from team pool

When a team member claims a lead from the team pool, the team stakeholder is **preserved** as a budget placeholder (set to `isPrimary: false`, `commissionPercentage: 0`) rather than deleted.

```
BEFORE claim:
└── Team A: { commission: 100%, isPrimary: true, originalTeamCommission: 100 }

AFTER claim by Agent X (same team):
├── Team A: { commission: 0%, isPrimary: false, originalTeamCommission: 100 }
└── Agent X: { commission: 100%, isPrimary: true, originalTeamCommission: 100 }
```

The claiming user's stakeholder is created with `originalTeamCommission` set to the team's commission at the time of claim, enabling team budget enforcement even after subsequent reassignments.

### Commission aggregation

With the [dual-record model](/backend/crm/stakeholder-model#dual-record-model), a single user can hold multiple stakeholder rows on the same entity. Always aggregate when computing payouts:

```sql
SELECT user_id, SUM(commission_percentage) AS total_commission
FROM entity_stakeholder
WHERE entity_type = :entityType
  AND entity_id = :entityId
  AND user_id = :userId
  AND is_deleted = false
GROUP BY user_id;
```

<Warning>
  When implementing the payment module, always use the aggregation query (`SUM`
  by `user_id`) rather than assuming one row per user.
</Warning>

## Entity changes required

To support the stakeholder system features, the following entities require updates:

### Lead entity
- Add `originalTeamCommission` field for tracking team budget allocations
- Add indexes for stakeholder query performance
- Update state machine to handle team pool freeze conditions

### Deal entity  
- Add `originatingLeadId` field for sync service
- Add `originalTeamCommission` field for team budget tracking
- Update creation logic to inherit stakeholders from leads

### User entity
- Add team membership tracking for claim validation
- Update permission computation for team-based access

### Team entity
- Add commission budget tracking
- Add team pool management capabilities

## API endpoints

### Lead APIs

<Tabs>
<Tab title="GET stakeholders">

**`GET /leads/:leadId/stakeholders`** — List all stakeholders with commission summary

```json Response
{
  "stakeholders": {
    "data": [
      {
        "id": "stakeholder-uuid-1",
        "entityType": "lead",
        "entityId": "lead-uuid-123",
        "user": {
          "id": "user-uuid-1",
          "firstName": "Ahmed",
          "lastName": "Hassan",
          "email": "ahmed@company.com"
        },
        "team": { "id": "team-uuid-1", "name": "Sales Team A" },
        "isPrimary": true,
        "accessLevel": "FULL",
        "commissionPercentage": 60,
        "originalTeamCommission": 100,
        "inheritedFromStakeholderId": null,
        "inheritedFromLeadId": null,
        "assignedAt": "2026-01-15T10:30:00Z",
        "assignedBy": {
          "id": "manager-uuid",
          "firstName": "Sara",
          "lastName": "Manager"
        },
        "createdAt": "2026-01-15T10:30:00Z",
        "updatedAt": "2026-01-15T10:30:00Z"
      }
    ],
    "total": 1,
    "page": 1,
    "limit": 50,
    "totalPages": 1
  },
  "summary": {
    "totalCommission": 100,
    "availableToAllocate": 0,
    "isValid": true,
    "hasPrimary": true
  },
  "holders": ["...EntityStakeholderDto (commission > 0)..."],
  "observers": ["...EntityStakeholderDto (commission = 0)..."],
  "contributedTeams": [{ "id": "team-uuid-1", "name": "Sales Team A" }]
}
```

- `holders` — Pre-filtered stakeholders with commission > 0
- `observers` — Pre-filtered stakeholders with 0% commission
- `contributedTeams` — Teams that have any stakeholder presence on this entity

</Tab>
<Tab title="Removal preview">

**`GET /leads/:leadId/stakeholders/:stakeholderId/removal-preview`** — Preview removal adjustments

```json Redistribution required
{
  "canRemove": true,
  "stakeholderToRemove": {
    "id": "stakeholder-uuid-1",
    "user": { "id": "user-uuid-1", "firstName": "Ahmed" },
    "currentPercentage": 40
  },
  "redistributionOptions": {
    "toStakeholders": [
      {
        "id": "stakeholder-uuid-2",
        "user": { "id": "user-uuid-2", "firstName": "Omar" },
        "currentPercentage": 30,
        "suggestedPercentage": 50
      },
      {
        "id": "stakeholder-uuid-3",
        "user": { "id": "user-uuid-3", "firstName": "Fatima" },
        "currentPercentage": 30,
        "suggestedPercentage": 50
      }
    ],
    "availableToRedistribute": 40
  },
  "message": "Removing Ahmed will free 40%."
}
```

```json Observer (direct removal)
{
  "canRemove": true,
  "stakeholderToRemove": {
    "id": "stakeholder-uuid-1",
    "user": { "id": "user-uuid-1", "firstName": "Ahmed" },
    "currentPercentage": 0
  },
  "message": "Ahmed is an observer (0% commission). Can be removed without adjustments."
}
```

</Tab>
<Tab title="Add preview">

**`GET /leads/:leadId/stakeholders/add-preview`** — Preview commission pool before adding

Query params: `userId` (UUID, optional), `teamId` (UUID, optional)

```json Response
{
  "canAdd": true,
  "existingStakeholders": [
    {
      "id": "stakeholder-uuid-1",
      "user": { "id": "user-uuid-1", "firstName": "Ahmed" },
      "percentage": 60
    },
    {
      "id": "stakeholder-uuid-2",
      "user": { "id": "user-uuid-2", "firstName": "Omar" },
      "percentage": 40
    }
  ],
  "suggestedPercentage": 30,
  "adjustmentOptions": {
    "fromStakeholders": [
      {
        "id": "stakeholder-uuid-1",
        "user": { "id": "user-uuid-1", "firstName": "Ahmed" },
        "reduceBy": 15
      },
      {
        "id": "stakeholder-uuid-2",
        "user": { "id": "user-uuid-2", "firstName": "Omar" },
        "reduceBy": 15
      }
    ]
  },
  "message": "Khalid can receive up to 100%. Adjust existing stakeholders to make room."
}
```

</Tab>
<Tab title="Create stakeholder">

**`POST /leads/:leadId/stakeholders`** — Create with optional commission adjustments

`accessLevel` options: `READ` (observer), `WRITE` (can modify), `FULL` (full access). Defaults to `FULL` for primary, `READ` for non-primary.

```json Request
{
  "userId": "user-uuid-new",
  "teamId": null,
  "isPrimary": false,
  "accessLevel": "WRITE",
  "commissionPercentage": 30,
  "adjustments": [
    { "stakeholderId": "stakeholder-uuid-1", "commissionPercentage": 30 }
  ]
}
```

```json Response
{
  "created": {
    "id": "stakeholder-uuid-new",
    "user": { "id": "user-uuid-new", "firstName": "Khalid" },
    "commissionPercentage": 30,
    "isPrimary": false,
    "accessLevel": "WRITE"
  },
  "adjusted": [
    {
      "id": "stakeholder-uuid-1",
      "previousPercentage": 60,
      "newPercentage": 30
    }
  ],
  "summary": { "totalCommission": 100, "isValid": true }
}
```

</Tab>
<Tab title="Batch update">

**`PATCH /leads/:leadId/stakeholders/batch`** — Batch update with optional remove

```json Request
{
  "updates": [
    { "stakeholderId": "stakeholder-uuid-1", "commissionPercentage": 50 },
    { "stakeholderId": "stakeholder-uuid-2", "commissionPercentage": 50 }
  ],
  "removeStakeholderId": "stakeholder-uuid-3"
}
```

```json Response
{
  "updated": [
    {
      "id": "stakeholder-uuid-1",
      "changes": [{ "field": "commissionPercentage", "from": 30, "to": 50 }]
    },
    {
      "id": "stakeholder-uuid-2",
      "changes": [{ "field": "commissionPercentage", "from": 30, "to": 50 }]
    }
  ],
  "totalCommission": 100
}
```

</Tab>
<Tab title="Remove stakeholder">

**`DELETE /leads/:leadId/stakeholders/:stakeholderId`** — Remove stakeholder (auto-redistribute)

Commission is redistributed proportionally among remaining stakeholders. Any pending transfer is auto-cancelled.

```json Response
{ "success": true }
```

```json Error (removing primary)
{
  "statusCode": 400,
  "message": "Cannot remove the primary stakeholder while other stakeholders exist. Transfer primary status first, or remove all stakeholders."
}
```

</Tab>
</Tabs>

### Lead assignment endpoints

<Tabs>
<Tab title="Assign to team">

**`POST /leads/:leadId/assign`** — Assign lead from pool to a team

```json Request
{ "teamId": "team-uuid-1" }
```

```json Response
{
  "lead": {
    "id": "lead-uuid-123",
    "person": { "firstName": "John", "lastName": "Doe" },
    "stage": { "name": "New" }
  },
  "stakeholder": {
    "id": "stakeholder-uuid-team",
    "user": null,
    "team": { "id": "team-uuid-1", "name": "Sales Team A" },
    "commissionPercentage": 100,
    "isPrimary": true,
    "accessLevel": "FULL"
  },
  "message": "Lead assigned to Sales Team A"
}
```

```json Error (team already present)
{
  "statusCode": 400,
  "message": "This team already has a stakeholder presence on this lead. Use the team commission endpoint to adjust the team budget."
}
```

<Note>
  Lead `assignToTeam` only blocks if the *target team* already has a stakeholder
  presence. Stakeholders from other teams or global-pool users do not prevent
  assignment. Deal `assignToTeam` blocks if *any* stakeholders exist.
</Note>

**Permissions:** Team leader, Admin, or `team_crm.manage`

</Tab>
<Tab title="Unassign">

**`POST /leads/:leadId/unassign`** — Return lead to pool (removes all stakeholders)

```json Response
{
  "lead": { "id": "lead-uuid-123" },
  "removedStakeholders": [
    {
      "id": "stakeholder-uuid-1",
      "user": { "firstName": "Ahmed" },
      "team": null,
      "commissionPercentage": 60
    }
  ],
  "message": "Lead unassigned and returned to pool"
}
```

**Permissions:** Admin, or primary stakeholder with `crm.manage`

</Tab>
<Tab title="Team commission">

**`PATCH /leads/:leadId/team-commission`** — Update team budget (org admin only)

```json Request
{ "teamId": "team-uuid-1", "commissionPercentage": 60 }
```

```json Response
{ "success": true }
```

**Permissions:** `crm.manage` (org admin only)

</Tab>
<Tab title="Claim">

**`POST /leads/:leadId/claim`** — Claim from global pool or team pool

`teamId` is **required** when claiming from a team pool. Omit for global pool claims. A user can claim from multiple team pools independently.

```json Request (team pool)
{ "userId": "user-uuid-claiming", "teamId": "team-uuid-1" }
```

```json Response (team pool)
{
  "lead": { "id": "lead-uuid-123" },
  "claimedFrom": "team_pool",
  "stakeholder": {
    "id": "stakeholder-uuid-new",
    "user": { "id": "user-uuid-claiming", "firstName": "Nour" },
    "team": { "id": "team-uuid-1", "name": "Sales Team A" },
    "commissionPercentage": 100,
    "isPrimary": true
  },
  "previousTeamStakeholder": {
    "id": "stakeholder-uuid-team",
    "team": { "name": "Sales Team A" },
    "previousCommission": 100,
    "newCommission": 0,
    "isPrimary": false
  },
  "message": "Nour claimed lead from Sales Team A with 100% commission"
}
```

**Permissions:** Lead pool: `crm.manage` or `crm.view` | Team pool: team membership

</Tab>
</Tabs>

### Lead transfer endpoints

<Tabs>
<Tab title="Transfer preview">

**`GET /leads/:leadId/transfer/preview?toUserId=...`** — Preview transfer options

```json Response
{
  "canTransfer": true,
  "fromStakeholder": {
    "id": "stakeholder-uuid-1",
    "user": { "id": "user-uuid-1", "firstName": "Ahmed" },
    "team": null,
    "currentPercentage": 60,
    "isPrimary": true
  },
  "toUser": { "id": "user-uuid-to", "firstName": "Khalid" },
  "toTeam": null,
  "otherStakeholders": [
    {
      "id": "stakeholder-uuid-2",
      "user": { "firstName": "Omar" },
      "team": null,
      "percentage": 40
    }
  ],
  "transferOptions": {
    "fromCurrentPercentage": 60,
    "fromCanKeepMin": 0,
    "fromCanKeepMax": 60,
    "toWillReceiveMin": 0,
    "toWillReceiveMax": 60,
    "suggestedFromKeeps": 20,
    "suggestedToReceives": 40
  },
  "message": "Ahmed can transfer up to 60% to Khalid."
}
```

**Permissions:** `crm.view` OR `crm.manage` OR stakeholder access

</Tab>
<Tab title="Request transfer">

**`POST /leads/:leadId/transfer`** — Create transfer request

```json Request
{
  "toUserId": "user-uuid-to",
  "fromKeepsPercentage": 20,
  "reason": "Reassigning to specialized agent"
}
```

```json Response
{
  "transfer": {
    "id": "transfer-uuid-1",
    "entityType": "lead",
    "entityId": "lead-uuid-123",
    "fromUser": { "id": "user-uuid-1", "firstName": "Ahmed" },
    "toUser": { "id": "user-uuid-to", "firstName": "Khalid" },
    "status": "pending",
    "fromCommissionTotal": 60,
    "fromKeepsPercentage": 20,
    "reason": "Reassigning to specialized agent",
    "createdAt": "2026-01-28T10:30:00Z"
  },
  "message": "Transfer request created. Awaiting approval."
}
```

<Note>
  When `fromKeepsPercentage` equals the primary's current commission,
  `transferAmount = 0`. This is only allowed if the target is already a
  stakeholder on the entity — primary moves, no commission changes.
</Note>

**Permissions:** Primary stakeholder or member of primary team

</Tab>
<Tab title="List transfers">

**`GET /leads/:leadId/transfers`** — Get all transfers for lead (paginated)

Query params: `page` (default: 1), `limit` (default: 50)

```json Response
{
  "data": [
    {
      "id": "transfer-uuid-1",
      "entityType": "lead",
      "status": "approved",
      "fromCommissionTotal": 60,
      "fromKeepsPercentage": 20,
      "createdAt": "2026-01-28T10:30:00Z"
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 50,
  "totalPages": 1
}
```

</Tab>
</Tabs>

### Lead API summary

| #   | Method | Endpoint                                                     | Purpose                                |
| --- | ------ | ------------------------------------------------------------ | -------------------------------------- |
| 1   | GET    | `/leads/:leadId/stakeholders`                                | List stakeholders with summary         |
| 2   | GET    | `/leads/:leadId/stakeholders/:stakeholderId/removal-preview` | Preview removal adjustments            |
| 3   | GET    | `/leads/:leadId/stakeholders/add-preview`                    | Preview addition adjustments           |
| 4   | POST   | `/leads/:leadId/stakeholders`                                | Create stakeholder with adjustments    |
| 5   | PATCH  | `/leads/:leadId/stakeholders/batch`                          | Batch update stakeholders              |
| 6   | DELETE | `/leads/:leadId/stakeholders/:stakeholderId`                 | Remove stakeholder (auto-redistribute) |
| 7   | POST   | `/leads/:leadId/assign`                                      | Assign lead to team                    |
| 8   | POST   | `/leads/:leadId/unassign`                                    | Unassign lead (return to pool)         |
| 9   | PATCH  | `/leads/:leadId/team-commission`                             | Update team commission budget          |
| 10  | POST   | `/leads/:leadId/claim`                                       | Claim from pool/team                   |
| 11  | GET    | `/leads/:leadId/transfer/preview`                            | Preview transfer options               |
| 12  | POST   | `/leads/:leadId/transfer`                                    | Create transfer request                |
| 13  | GET    | `/leads/:leadId/transfers`                                   | Get all transfers for lead             |

### Deal APIs

Deal stakeholder endpoints mirror lead endpoints with `entityType: "deal"`. Key differences:

- Deal stakeholder create/remove uses **automatic lead sync** via `StakeholderSyncService`
- Deals do **not** have a `/claim` endpoint — deals are created from leads (inheriting stakeholders) or assigned directly
- Deal `assignToTeam` blocks if _any_ stakeholders exist (unlike lead which only blocks the target team)

<Tabs>
<Tab title="Create (with sync)">

**`POST /deals/:dealId/stakeholders`** — Create stakeholder with automatic lead sync

```json Response
{
  "success": true,
  "syncedToLead": true,
  "leadId": "lead-uuid-123",
  "message": "Stakeholder assigned to deal and synced to originating lead as observer"
}
```

Sync creates an observer on the originating lead (0% commission, same access level, never primary). Skips if user already on lead or lead is archived.

</Tab>
<Tab title="Remove (with sync)">

**`DELETE /deals/:dealId/stakeholders/:stakeholderId`** — Remove with automatic lead sync

```json Response (synced)
{ "success": true, "syncedToLead": true, "leadId": "lead-uuid-123" }
```

```json Response (sync skipped)
{ "success": true, "syncedToLead": false, "leadId": "lead-uuid-123" }
```

Safety checks: lead not archived, user has 0% on lead, user not primary on lead, user's only deal from that lead.

</Tab>
<Tab title="Assign to team">

**`POST /deals/:dealId/assign`** — Assign deal to a team

```json Error (already assigned)
{
  "statusCode": 400,
  "message": "Deal already has stakeholders. Use transfer to change ownership."
}
```

<Note>
  Unlike lead `assignToTeam` (which only blocks the target team), deal
  `assignToTeam` blocks if *any* stakeholders exist.
</Note>

</Tab>
</Tabs>

### Deal API summary

| #   | Method | Endpoint                                                     | Purpose                        |
| --- | ------ | ------------------------------------------------------------ | ------------------------------ |
| 1   | GET    | `/deals/:dealId/stakeholders`                                | List stakeholders with summary |
| 2   | GET    | `/deals/:dealId/stakeholders/:stakeholderId/removal-preview` | Preview removal adjustments    |
| 3   | GET    | `/deals/:dealId/stakeholders/add-preview`                    | Preview addition adjustments   |
| 4   | POST   | `/deals/:dealId/stakeholders`                                | Create stakeholder with sync   |
| 5   | PATCH  | `/deals/:dealId/stakeholders/batch`                          | Batch update stakeholders      |
| 6   | DELETE | `/deals/:dealId/stakeholders/:stakeholderId`                 | Remove stakeholder with sync   |
| 7   | POST   | `/deals/:dealId/assign`                                      | Assign deal to team            |
| 8   | POST   | `/deals/:dealId/unassign`                                    | Unassign deal                  |
| 9   | GET    | `/deals/:dealId/transfer/preview`                            | Preview transfer options       |
| 10  | POST   | `/deals/:dealId/transfer`                                    | Create transfer request        |
| 11  | GET    | `/deals/:dealId/transfers`                                   | Get all transfers for deal     |

### Entity transfer APIs

These endpoints are in the `EntityTransferController` and handle approval/rejection for both leads and deals.

<Tabs>
<Tab title="Accept">

**`POST /entity-transfers/:transferId/accept`**

```json Request
{ "notes": "Approved by team lead" }
```

```json Response
{ "success": true }
```

**Permissions:** Admin, Team Leader, or `crm.manage`

</Tab>
<Tab title="Reject">

**`POST /entity-transfers/:transferId/reject`**

```json Request
{ "rejectReason": "Lead requires specialized handling" }
```

```json Response
{ "success": true }
```

**Permissions:** Admin, Team Leader, or `crm.manage`

</Tab>
<Tab title="Cancel">

**`POST /entity-transfers/:transferId/cancel`**

```json Response
{ "success": true }
```

**Permissions:** Transfer requester or original owner

</Tab>
<Tab title="List by user">

**`GET /entity-transfers/pending`**, **`/sent`**, **`/received`**

All three accept the same query parameters:

| Parameter | Type   | Required | Description                  |
| --------- | ------ | -------- | ---------------------------- |
| `userId`  | UUID   | Yes      | User ID to filter by         |
| `page`    | number | No       | Page number (default: 1)     |
| `limit`   | number | No       | Items per page (default: 50) |

Response is paginated `EntityTransferDto[]`:

```json Response
{
  "data": [
    {
      "id": "transfer-uuid-1",
      "entityType": "lead",
      "status": "pending",
      "fromCommissionTotal": 60,
      "fromKeepsPercentage": 20,
      "createdAt": "2026-01-28T10:30:00Z"
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 50,
  "totalPages": 1
}
```

</Tab>
<Tab title="Get by ID">

**`GET /entity-transfers/:transferId`** — Get a specific transfer

```json Response
{
  "id": "transfer-uuid-1",
  "entityType": "lead",
  "entityId": "lead-uuid-123",
  "fromUser": { "id": "user-uuid-1", "firstName": "Ahmed" },
  "toUser": { "id": "user-uuid-to", "firstName": "Khalid" },
  "status": "approved",
  "fromKeepsPercentage": 20,
  "approvedBy": { "id": "approver-uuid", "firstName": "Sara" },
  "approvedAt": "2026-01-28T11:00:00Z",
  "requestedBy": { "id": "requester-uuid", "firstName": "Sara" },
  "createdAt": "2026-01-28T10:30:00Z",
  "updatedAt": "2026-01-28T11:00:00Z"
}
```

</Tab>
</Tabs>

### Me transfer API

**`GET /me/transfers`** — Get transfers for the current authenticated user

| Parameter | Type   | Required | Description                                                     |
| --------- | ------ | -------- | --------------------------------------------------------------- |
| `type`    | string | Yes      | `pending` (sent + received), `sent` (by me), `received` (to me) |
| `page`    | number | No       | Page number (default: 1)                                        |
| `limit`   | number | No       | Items per page (default: 50)                                    |

Response is the same paginated format as `/entity-transfers/pending`.

### Complete API summary

| Controller          | Count  | Endpoints                                               |
| ------------------- | ------ | ------------------------------------------------------- |
| Lead Controller     | 13     | Stakeholders, assign, claim, team-commission, transfers |
| Deal Controller     | 11     | Stakeholders, assign, sync, transfers                   |
| Transfer Controller | 7      | Accept, reject, cancel, list by user, get by ID         |
| Me Controller       | 1      | `GET /me/transfers?type=...`                            |
| **Total**           | **32** |                                                         |

## Service functions

<AccordionGroup>
<Accordion title="EntityStakeholderService">

| #   | Function                  | Returns                                 | Description                            |
| --- | ------------------------- | --------------------------------------- | -------------------------------------- |
| 1   | `findOne`                 | `EntityStakeholderDto`                  | Get stakeholder by ID                  |
| 2   | `findByEntity`            | `EntityStakeholderDto[]`                | Get all stakeholders for entity        |
| 3   | `findPrimary`             | `UserDto \| null`                       | Get primary stakeholder's user         |
| 4   | `findByUserOrTeam`        | `EntityStakeholderDto[]`                | Get stakeholders by user/team          |
| 5   | `isStakeholder`           | `boolean`                               | Check if user is stakeholder           |
| 6   | `create`                  | `EntityStakeholderDto`                  | Create stakeholder                     |
| 7   | `update`                  | `EntityStakeholderDto`                  | Update stakeholder                     |
| 8   | `remove`                  | `{ success: boolean }`                  | Soft delete (validates commission)     |
| 9   | `validateCommissionTotal` | `{ valid: boolean, total: number }`     | Validate commission sums to 100        |
| 10  | `inheritFromLeadToDeal`   | `EntityStakeholderDto[]`                | Inherit stakeholders from lead to deal |
| 11  | `getDefaultAccessLevel`   | `AccessLevel`                           | Get default access level               |
| 12  | `getRemovalPreview`       | `RemovalPreviewDto`                     | Preview removal adjustments            |
| 13  | `getAddPreview`           | `AddPreviewDto`                         | Preview addition adjustments           |
| 14  | `createWithAdjustments`   | `CreateEntityStakeholderResultDto`      | Create with commission adjustments     |
| 15  | `batchUpdate`             | `BatchEntityStakeholderUpdateResultDto` | Batch update stakeholders              |
| 16  | `getCommissionSummary`    | `CommissionSummaryDto`                  | Get commission summary for entity      |
| 17  | `removeAllForEntity`      | `RemovedStakeholdersDto`                | Remove all stakeholders (for unassign) |

</Accordion>
<Accordion title="StakeholderSyncService">

| #   | Function                     | Returns                    | Description                               |
| --- | ---------------------------- | -------------------------- | ----------------------------------------- |
| 1   | `assignToDealWithLeadSync`   | `StakeholderSyncResultDto` | Assign to deal with automatic lead sync   |
| 2   | `removeFromDealWithLeadSync` | `StakeholderSyncResultDto` | Remove from deal with automatic lead sync |

</Accordion>
<Accordion title="LeadService">

| #   | Function             | Returns                | Description                     |
| --- | -------------------- | ---------------------- | ------------------------------- |
| 1   | `assignToTeam`       | `{ success: boolean }` | Assign lead to team (from pool) |
| 2   | `unassign`           | `{ success: boolean }` | Unassign lead (return to pool)  |
| 3   | `claimLead`          | `{ success: boolean }` | Claim lead from pool or team    |
| 4   | `getTransferPreview` | `TransferPreviewDto`   | Preview transfer options        |
| 5   | `requestTransfer`    | `{ success: boolean }` | Create transfer request         |

</Accordion>
<Accordion title="DealService">

| #   | Function                    | Returns                    | Description                                 |
| --- | --------------------------- | -------------------------- | ------------------------------------------- |
| 1   | `assignToTeam`              | `{ success: boolean }`     | Assign deal to team                         |
| 2   | `unassign`                  | `{ success: boolean }`     | Unassign deal                               |
| 3   | `assignStakeholderWithSync` | `StakeholderSyncResultDto` | Assign stakeholder with automatic lead sync |
| 4   | `removeStakeholderWithSync` | `StakeholderSyncResultDto` | Remove stakeholder with automatic lead sync |
| 5   | `getTransferPreview`        | `TransferPreviewDto`       | Preview transfer options                    |
| 6   | `requestTransfer`           | `{ success: boolean }`     | Create transfer request                     |

</Accordion>
<Accordion title="EntityTransferService">

| #   | Function             | Returns                                | Description                             |
| --- | -------------------- | -------------------------------------- | --------------------------------------- |
| 1   | `getTransferPreview` | `TransferPreviewDto`                   | Preview transfer options                |
| 2   | `requestTransfer`    | `EntityTransferDto`                    | Create transfer request                 |
| 3   | `findByEntity`       | `EntityTransferDto[]`                  | Get all transfers for entity            |
| 4   | `findOne`            | `EntityTransferDto`                    | Get transfer by ID                      |
| 5   | `findPendingForUser` | `PaginatedResponse<EntityTransferDto>` | Get pending transfers (sent + received) |
| 6   | `findSentByUser`     | `PaginatedResponse<EntityTransferDto>` | Get transfers sent by user              |
| 7   | `findReceivedByUser` | `PaginatedResponse<EntityTransferDto>` | Get pending transfers received by user  |
| 8   | `approveTransfer`    | `{ success: boolean }`                 | Accept transfer and update stakeholders |
| 9   | `rejectTransfer`     | `{ success: boolean }`                 | Reject transfer                         |
| 10  | `cancelTransfer`     | `{ success: boolean }`                 | Cancel transfer (by requester)          |

</Accordion>
</AccordionGroup>

## Business rules

### Commission auto-adjustment formula

When removing a stakeholder and redistributing commission:

```
Formula: newPercentage = oldPercentage × (100 / currentTotalExcludingRemoved)

Example:
BEFORE removal:
- Agent A: 50%
- Agent B: 0% ← Removing
- Agent C: 20%
Total: 70%

AFTER (auto-adjusted to 100%):
- Agent A: 71.43% (50 × 100/70)
- Agent C: 28.57% (20 × 100/70)
Total: 100% ✓
```

### Team budget enforcement

- Team managers can only allocate commission up to `originalTeamCommission`
- Sum of individual team member commissions cannot exceed the original team budget
- Team budget decreases as members are assigned commission
- Remaining team budget at deal close is paid to the team directly

### Transfer validation rules

| Rule                             | Description                                                                               |
| -------------------------------- | ----------------------------------------------------------------------------------------- |
| `fromKeepsPercentage` ceiling    | Cannot exceed the primary stakeholder's current commission                                |
| Transfer amount ≥ 0              | 0% transfers allowed only to existing stakeholders (primary-only, no commission movement) |
| Requester must be primary        | Only the primary stakeholder (or member of primary team) can initiate                     |
| Transfer-back allowed            | Transferring back to a previous owner or team is permitted                                |
| Existing stakeholder update      | If target already has a stakeholder, it is updated (commission added) not duplicated      |
| Auto-cancel on removal/reduction | Pending transfers are cancelled when any stakeholder is removed or has commission reduced |

### Team pool freeze rules

| Rule               | Description                                                                                  |
| ------------------ | -------------------------------------------------------------------------------------------- |
| Modification block | Leads/deals cannot be modified when primary stakeholder is team-only (`team` set, no `user`) |
| Resolution         | Assign or claim the entity to a team member to unfreeze                                      |
| Exception          | Users with stakeholder access (`accessLevel: MODIFY` + `user` set) can still modify          |
| Exception          | Primary stakeholders (user-based, `isPrimary: true`) can still modify                        |

## Access control

### Permission-only access for stakeholder management

<Warning>
  Stakeholder mutation endpoints use **permission-only** access checks.
  Resource-level access (e.g., `accessLevel: MODIFY` via stakeholder access)
  does **not** grant permission to manage stakeholders.
</Warning>

```typescript
// Lead stakeholder endpoints
@CheckAccess({
  permissions: [OrgPermissionKey.CRM_MANAGE, TeamPermissionKey.TEAM_CRM_MANAGE],
})

// Deal stakeholder endpoints
@CheckAccess({
  permissions: [OrgPermissionKey.SALES_MANAGE, TeamPermissionKey.TEAM_SALES_MANAGE],
})
```

**Two-tier access:**

1. **Guard level (AccessGuard)** — Checks if user has `CRM_MANAGE` (org) OR `TEAM_CRM_MANAGE` (any team). Coarse gate.
2. **Controller level (business logic)** — For team managers, validates: team has a stakeholder on the entity, commission doesn't exceed budget, adjustments/removals target their own team, primary can only be set if their team holds primary, and assigned user is a team member.

### Permission matrix

<Tabs>
<Tab title="Lead permissions">

| Endpoint                                      | Required permission                                 |
| --------------------------------------------- | --------------------------------------------------- |
| `GET /leads/:id/stakeholders`                 | `crm.view` or stakeholder access                    |
| `GET /leads/:id/stakeholders/add-preview`     | `crm.manage` OR `team_crm.manage` (permission-only) |
| `GET /leads/:id/stakeholders/removal-preview` | `crm.manage` OR `team_crm.manage` (permission-only) |
| `POST /leads/:id/stakeholders`                | `crm.manage` OR `team_crm.manage` (permission-only) |
| `PATCH /leads/:id/stakeholders/batch`         | `crm.manage` OR `team_crm.manage` (permission-only) |
| `DELETE /leads/:id/stakeholders/:id`          | `crm.manage` OR `team_crm.manage` (permission-only) |
| `POST /leads/:id/assign`                      | `team_crm.manage`, Team Leader, or Admin            |
| `POST /leads/:id/unassign`                    | Primary stakeholder + `crm.manage`, or Admin        |
| `POST /leads/:id/claim` (lead pool)           | `crm.manage` or `crm.view`                          |
| `POST /leads/:id/claim` (team pool)           | Team membership                                     |
| `PATCH /leads/:id/team-commission`            | `crm.manage` (org admin only)                       |
| `GET /leads/:id/transfer/preview`             | `crm.view` OR `crm.manage` OR stakeholder access    |
| `POST /leads/:id/transfer`                    | Primary stakeholder or `crm.manage`                 |

</Tab>
<Tab title="Deal permissions">

| Endpoint                                      | Required permission                                     |
| --------------------------------------------- | ------------------------------------------------------- |
| `GET /deals/:id/stakeholders`                 | `sales.view` or stakeholder access                      |
| `GET /deals/:id/stakeholders/add-preview`     | `sales.manage` OR `team_sales.manage` (permission-only) |
| `GET /deals/:id/stakeholders/removal-preview` | `sales.manage` OR `team_sales.manage` (permission-only) |
| `POST /deals/:id/stakeholders`                | `sales.manage` OR `team_sales.manage` (permission-only) |
| `PATCH /deals/:id/stakeholders/batch`         | `sales.manage` OR `team_sales.manage` (permission-only) |
| `DELETE /deals/:id/stakeholders/:id`          | `sales.manage` OR `team_sales.manage` (permission-only) |

</Tab>
<Tab title="Transfer permissions">

| Endpoint                            | Required permission                 |
| ----------------------------------- | ----------------------------------- |
| `POST /entity-transfers/:id/accept` | Admin, Team Leader, or `crm.manage` |
| `POST /entity-transfers/:id/reject` | Admin, Team Leader, or `crm.manage` |

</Tab>
</Tabs>

## State transitions

| Scenario                       | Before                               | After                                                                  |
| ------------------------------ | ------------------------------------ | ---------------------------------------------------------------------- |
| Assign to team                 | No team stakeholder with `team_id=X` | Team (100%, primary, `originalTeamCommission: 100`)                    |
| Claim from lead pool           | No stakeholders                      | User (100%, primary)                                                   |
| Claim from team pool           | Team X (100%)                        | User (100%, primary, `team_id=X`), Team X (0%, non-primary, preserved) |
| Update team commission (admin) | Team-only row exists                 | Team commission updated; `originalTeamCommission` synced               |
| Unassign                       | User(s) with commission              | No stakeholders (back to pool)                                         |
| Transfer (no keep)             | A (100%)                             | B (100%)                                                               |
| Transfer (with keep)           | A (100%)                             | A (30%), B (70%)                                                       |
| Transfer 0% to existing        | A (50%), B (50%)                     | A (50%, non-primary), B (50%, primary)                                 |
| Transfer to existing user      | A (60%), B (40%)                     | A (keeps%), B (40% + transfer%, primary)                               |
| Transfer to existing team      | Team (20%), B (80%)                  | Team (20% + transfer%), B (updated)                                    |

## Examples & workflows

### Workflow 1: Team assignment and claim

<Steps>
<Step title="Admin assigns lead to Sales Team A">
```bash
POST /leads/123/assign
{ "teamId": "team-a" }
```
Creates team stakeholder with 100% commission.
</Step>

<Step title="Agent Sarah claims from team pool">
```bash
POST /leads/123/claim
{ "userId": "sarah-uuid", "teamId": "team-a" }
```
Sarah gets 100% commission, team stakeholder preserved with 0%.
</Step>

<Step title="Team manager assigns Agent Mike 40%">
```bash
POST /leads/123/stakeholders
{
  "userId": "mike-uuid",
  "commissionPercentage": 40,
  "adjustments": [
    { "stakeholderId": "sarah-stakeholder", "commissionPercentage": 60 }
  ]
}
```
Now Sarah has 60%, Mike has 40%, team has 0%.
</Step>
</Steps>

### Workflow 2: Deal sync with originating lead

<Steps>
<Step title="Deal created from lead">
Deal inherits Sarah (60%) and Mike (40%) from lead.
</Step>

<Step title="New agent added to deal">
```bash
POST /deals/456/stakeholders
{ "userId": "tom-uuid", "commissionPercentage": 20 }
```
Tom added to deal with 20% commission, also synced to lead as observer (0%).
</Step>

<Step title="Agent removed from deal">
```bash
DELETE /deals/456/stakeholders/tom-stakeholder
```
Tom removed from deal. Since he has 0% on lead and it's his only deal, he's also removed from lead.
</Step>
</Steps>

### Workflow 3: Transfer request and approval

<Steps>
<Step title="Primary agent requests transfer">
```bash
POST /leads/123/transfer
{
  "toUserId": "alice-uuid",
  "fromKeepsPercentage": 20,
  "reason": "Specialization needed"
}
```
Sarah requests to transfer 40% to Alice, keeping 20%.
</Step>

<Step title="Manager previews transfer">
```bash
GET /leads/123/transfer/preview?toUserId=alice-uuid
```
Shows transfer options and impact.
</Step>

<Step title="Manager approves transfer">
```bash
POST /entity-transfers/transfer-uuid/accept
{ "notes": "Approved for specialization" }
```
Transfer executed: Sarah (20%), Mike (40%), Alice (40%, primary).
</Step>
</Steps>

## Quick reference

### Validation rules by operation

| Operation             | Validation                                                                                                |
| --------------------- | --------------------------------------------------------------------------------------------------------- |
| Create (simple)       | `new + existing = 100` OR `all = 0`; at least one primary after creation                                  |
| CreateWithAdjustments | `new + adjusted = 100`; at least one primary; auto-cancels pending transfers if commission reduced        |
| Update                | `new total ≤ 100`; cannot remove last primary; auto-cancels pending transfers when commission reduced     |
| Remove                | Auto-redistributes proportionally; cannot remove primary while others exist; auto-cancels transfers       |
| Transfer              | `fromKeepsPercentage ≤ primary's commission`; 0% only to existing stakeholders; requester must be primary |
| Batch                 | `sum(all) = 100`; at least one primary; auto-cancels transfers if commission reduced                      |
| Team Assign           | Same as create; `originalTeamCommission` set to 100                                                       |
| Team Claim            | User gets team's commission, team → 0%; `originalTeamCommission` recorded; team preserved                 |
| Unassign              | Removes all stakeholders; auto-cancels all pending transfers                                              |

### Team manager constraints

| Rule                | Description                                                                              |
| ------------------- | ---------------------------------------------------------------------------------------- |
| Budget ceiling      | Team budget = team remaining % + team members' combined %. Assignment cannot exceed this |
| Adjustments scope   | Can only adjust own team's stakeholder and team member commissions                       |
| Primary restriction | Cannot set primary if no team stakeholder currently holds primary                        |
| Entity visibility   | Must have a team-associated stakeholder on the entity                                    |
| Assignee membership | Can only assign users who are members of their own team                                  |
| Removal scope       | Can only remove stakeholders belonging to their own team                                 |

### Performance considerations

The sync service follows patterns from the [query optimization guide](/backend/query-optimization):

<Tip>
  Use EXISTS subqueries for counting other deals instead of loading IDs. Use
  transaction composition with `*InTransaction` methods.
</Tip>

| Operation          | Without sync | With sync | Overhead |
| ------------------ | ------------ | --------- | -------- |
| Assign stakeholder | ~50ms        | ~80ms     | +30ms    |
| Remove stakeholder | ~50ms        | ~100ms    | +50ms    |

Additional queries: check originating lead (1), check lead exists/archived (1), check user on lead (1), count other deals (1, EXISTS), assign/remove on lead (1-2). Total: 4-6 queries, all optimized with indexes.

<CardGroup cols={2}>
  <Card
    title="Stakeholder data model"
    icon="database"
    href="/backend/crm/stakeholder-model"
  >
    Table schemas, dual-record model, commission payments, and invariants.
  </Card>
  <Card
    title="Access control"
    icon="shield-halved"
    href="/backend/rbac/access-control/overview"
  >
    Endpoint protection, resource permissions, and permission compute service.
  </Card>
  <Card
    title="Query optimization"
    icon="bolt"
    href="/backend/query-optimization"
  >
    EXISTS subqueries, pagination, and performance patterns.
  </Card>
  <Card title="DTO serialization" icon="code" href="/backend/dto-serialization">
    Response DTOs, serialization groups, and transformation patterns.
  </Card>
</CardGroup>