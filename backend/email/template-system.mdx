---
title: "Email Template System"
description: "React Email components with Resend integration for transactional emails"
---

## Overview

PropWise transactional emails are authored as **React Email components** and uploaded to **Resend** as managed templates. At send-time the backend calls Resend by template alias with variable values. If a template is accidentally deleted or hasn't been synced yet, the email falls back to in-app rendering (same React component, rendered server-side) so no email is ever silently dropped.

<Note>
The system is **not database-backed**: there is no active `email_template` table in the runtime schema. The source of truth is `src/emails/email-template.registry.tsx` plus the corresponding templates in Resend.
</Note>

## Architecture

The email template system follows a component-based approach with fallback rendering:

```
React Email components        Email Template Registry         Resend
(src/emails/templates/)  ──►  (email-template.registry.tsx) ──► pnpm email-templates:sync ──► Resend dashboard
                                                                                               (non-engineers edit content here)
                               at send-time
EmailService.sendTemplateEmail()  ────────────────────────────► Resend API (template by alias)
                                                                      │
                                                         if not_found ▼
                                              descriptor.renderHtml(vars) → sendEmail(html)
```

### Key Files

<AccordionGroup>
  <Accordion title="Core System Files">
    | File | Role |
    |------|------|
    | `src/emails/email-template.types.ts` | `EmailTemplateDescriptor<V>` interface |
    | `src/emails/email-template.registry.tsx` | Single source of truth — all templates with stable aliases |
    | `src/services/email.service.ts` | `sendEmail()` + `sendTemplateEmail()` + `dispatch()` |
    | `src/emails/email-template.registry.spec.ts` | CI contract test — enforces variable coverage |
  </Accordion>

  <Accordion title="Component Files">
    | File | Role |
    |------|------|
    | `src/emails/components/email-layout.tsx` | Shared wrapper (`variant`: `app` \| `developer`) |
    | `src/emails/components/email-logo-mark.tsx` | Logo tile linked to `https://propwise.com` (public HTTPS PNG) |
    | `src/emails/components/propwise-brand-text.tsx` | `PropwiseBrand` — renders "Propwise" in variant accent color |
    | `src/emails/templates/*.tsx` | Individual email components |
  </Accordion>

  <Accordion title="Configuration Files">
    | File | Role |
    |------|------|
    | `src/emails/email-assets.constants.ts` | `R2_PUBLIC_BASE_URL` + `/email-templates/*.png` logo URL resolution |
    | `src/emails/email-brand-tokens.ts` | Light-mode hex tokens for inline email CSS |
    | `src/emails/email-content-styles.ts` | Shared heading/body/CTA styles per variant |
    | `scripts/sync-email-templates.ts` | Upload logos to R2 + upload templates to Resend |
  </Accordion>

  <Accordion title="Notification System">
    | File | Role |
    |------|------|
    | `src/modules/notification/channels/email.channel.ts` | `EmailChannel` — routes notification types to templates, builds action buttons HTML |
    | `src/modules/notification/utils/entity-route.util.ts` | `resolveEntityUrl()` — maps entity type + payload to absolute deep links |
  </Accordion>
</AccordionGroup>

## Template Registry

### Auth & Invitation Templates

<CardGroup cols={2}>
  <Card title="Email Verification" icon="envelope-circle-check">
    **Key:** `VERIFY_EMAIL`  
    **Alias:** `propwise-verify-email`  
    **Subject:** Verify Your Email  
    **Variables:** `CODE`
  </Card>

  <Card title="Password Reset" icon="key">
    **Key:** `RESET_PASSWORD`  
    **Alias:** `propwise-reset-password`  
    **Subject:** Reset Your Password  
    **Variables:** `RESET_LINK`
  </Card>

  <Card title="Organization Invite" icon="users">
    **Key:** `ORG_INVITE`  
    **Alias:** `propwise-org-invite`  
    **Subject:** Join an Organization on Propwise  
    **Variables:** `INVITER_NAME`, `ORG_NAME`, `ROLE`, `INVITE_URL`, `EXPIRES_AT`
  </Card>

  <Card title="Existing User Invite" icon="user-plus">
    **Key:** `ORG_INVITE_EXISTING`  
    **Alias:** `propwise-org-invite-existing`  
    **Subject:** Join Another Organization on Propwise  
    **Variables:** `INVITEE_NAME`, `INVITER_NAME`, `ORG_NAME`, `ROLE`, `INVITE_URL`, `EXPIRES_AT`
  </Card>
</CardGroup>

#### Developer Portal Templates

| Template | Alias | Subject | Variables |
|----------|-------|---------|-----------|
| `DEV_VERIFY_EMAIL` | `propwise-dev-verify-email` | Verify Your Email – PropWise Developer Portal | `CODE`, `EXPIRY_MINUTES` |
| `DEV_CONFIRM_EMAIL` | `propwise-dev-confirm-email` | Confirm Your New Email – PropWise Developer Portal | `CODE`, `EXPIRY_MINUTES` |
| `DEV_RESET_PASSWORD` | `propwise-dev-reset-password` | Reset Your Password – PropWise Developer Portal | `RESET_LINK`, `EXPIRY_MINUTES` |

### Notification Templates

<Tabs>
  <Tab title="General Notifications">
    **Template:** `NOTIFICATION`  
    **Alias:** `propwise-notification`  
    **Usage:** All notification types **not** covered by a bespoke template  
    **Variables:** `PREVIEW`, `TITLE`, `MESSAGE`, `ACTIONS_HTML`
  </Tab>

  <Tab title="Deal Won">
    **Template:** `DEAL_WON`  
    **Alias:** `propwise-deal-won`  
    **Usage:** `deal_won` notifications  
    **Variables:** `PREVIEW`, `TITLE`, `MESSAGE`, `DEAL_NAME`, `DEAL_VALUE`, `DEAL_LINK`
  </Tab>

  <Tab title="Transfer Requests">
    **Template:** `TRANSFER_REQUEST`  
    **Alias:** `propwise-transfer-request`  
    **Usage:** `transfer_requested`, `transfer_approved`, `transfer_rejected`, `transfer_cancelled`, `transfer_accepted_team_member`  
    **Variables:** `PREVIEW`, `TITLE`, `MESSAGE`, `ENTITY_TYPE_LABEL`, `ENTITY_NAME`, `ENTITY_LINK`, `REQUESTER_NAME`
  </Tab>

  <Tab title="Commission Payments">
    **Template:** `COMMISSION_PAYMENT`  
    **Alias:** `propwise-commission-payment`  
    **Usage:** All `commission_payment_*` and `commission_feedback_*` types  
    **Variables:** `PREVIEW`, `TITLE`, `MESSAGE`, `PAYMENT_STATUS_LABEL`, `PAYMENT_STATUS_BG`, `PAYMENT_STATUS_COLOR`, `PAYMENT_AMOUNT`, `DEAL_NAME`, `PAYMENT_LINK`
  </Tab>

  <Tab title="Event Invites">
    **Template:** `EVENT_INVITE`  
    **Alias:** `propwise-event-invite`  
    **Usage:** `event_invited`, `event_invitee_rsvp_changed`, `event_reminder`  
    **Variables:** `PREVIEW`, `TITLE`, `MESSAGE`, `EVENT_NAME`, `EVENT_DATE`, `EVENT_TIME`, `EVENT_LOCATION`, `INVITER_NAME`, `EVENT_LINK`
  </Tab>
</Tabs>

## Payload Key Contracts

<Warning>
Each bespoke sender reads specific keys from the stored notification `payload`. The keys below are what listeners must emit; the email channel maps them to template variables.
</Warning>

### Deal Won Template

| Template Variable | Payload Key(s) | Notes |
|-------------------|----------------|-------|
| `DEAL_NAME` | `dealTitle` → `dealName` | Listeners emit `dealTitle` |
| `DEAL_LINK` | `entityId` → `dealId` | Listeners emit `entityId` |
| `DEAL_VALUE` | `dealValue` | Not emitted by any listener yet — renders empty until `DealWonEvent.metadata` is extended |

### Transfer Request Template

| Template Variable | Payload Key(s) | Notes |
|-------------------|----------------|-------|
| `ENTITY_NAME` | `entityTitle` → `entityName` | All transfer listeners emit `entityTitle` |
| `REQUESTER_NAME` | `requestedByName` → `approverName` → `rejecterName` → `cancelledByName` → `userName` | Coalesced across all five transfer sub-types |

### Commission Payment Template

| Template Variable | Payload Key(s) | Notes |
|-------------------|----------------|-------|
| `PAYMENT_STATUS_LABEL/BG/COLOR` | `newStatus` → type-derived fallback | Status-change listeners emit `newStatus`; `COMMISSION_PAYMENT_CREATED` derives from `event.type` |
| `PAYMENT_AMOUNT` | `amount` (number) → `paymentAmount` (string) | Listeners emit `amount` as number; channel converts to string |

## Template Sync Process

<Steps>
  <Step title="Upload assets to R2">
    The sync script uploads logo files to R2 public storage for email template use.
  </Step>

  <Step title="Create templates in Resend">
    Templates are uploaded to Resend using the registry definitions. By default, this is create-only.
  </Step>

  <Step title="Configure sync options">
    - Use `--force` flag to upsert existing templates
    - Use `--prod` flag to target production CDN
  </Step>
</Steps>

<CodeGroup>
```bash Default sync
pnpm email-templates:sync
```

```bash Force update existing
pnpm email-templates:sync --force
```

```bash Production deployment
pnpm email-templates:sync --prod
```
</CodeGroup>

## Brand Variants

The email system supports two brand variants with different color schemes:

<Tabs>
  <Tab title="App Variant">
    - **Accent Color:** `#2A54CF` (blue)
    - **Usage:** Main application emails
    - **Logo:** Standard PropWise branding
  </Tab>

  <Tab title="Developer Variant">
    - **Accent Color:** `#316337` (green)
    - **Usage:** Developer portal emails
    - **Logo:** Developer-specific branding
  </Tab>
</Tabs>

<Tip>
The `PropwiseBrand` component automatically renders "Propwise" in the appropriate variant accent color based on the layout context.
</Tip>

## Fallback Rendering

<Info>
If a template is accidentally deleted from Resend or hasn't been synced yet, the system automatically falls back to server-side rendering of the same React component, ensuring no emails are silently dropped.
</Info>

The fallback mechanism works by:

1. Attempting to send via Resend template API
2. On template not found error, rendering the React component server-side
3. Sending the rendered HTML via standard email service

This ensures 100% email delivery reliability even during template management issues.