---
title: "PropWise Labs API Specification"
description: "Complete API reference for external consumers including authentication, entity endpoints, lookup endpoints, direct mutations, change requests, and market analytics."
---

## Overview

The PropWise Labs API provides comprehensive access to real estate data and analytics for external consumers. All endpoints are prefixed with `/api`.

<Info>
Base URL: `https://your-domain.com/api`
</Info>

### Authentication Methods

The API supports two authentication methods:

<Tabs>
  <Tab title="API Client Token">
    **For external consumers:**
    1. Obtain an API key and secret from an admin
    2. Exchange them for a short-lived Bearer token via `POST /api/auth/token`
    3. Include the token on every request: `Authorization: Bearer <token>`
  </Tab>
  <Tab title="JWT">
    **For internal/dashboard users:**
    - Obtained via `POST /api/auth/login` (admin dashboard)
    - Sent as `Authorization: Bearer <token>` or via the `access_token` httpOnly cookie
  </Tab>
</Tabs>

### Access Levels

| Level | Permissions |
|-------|-------------|
| `basic` | Read-only access to all entity, lookup, analytics, and change-request endpoints |
| `super` | All of `basic` plus direct entity mutations (PATCH projects, PATCH buildings, POST/PATCH/DELETE units) |

### Response Format

All list endpoints return paginated responses:

```json
{
  "data": [...],
  "total": 1234,
  "page": 1,
  "limit": 20
}
```

### Pagination Parameters

| Parameter | Type | Default | Constraints |
|-----------|------|---------|-------------|
| `page` | int | `1` | >= 1 |
| `limit` | int | `20` | 1-100 |
| `sortBy` | string | _(per-entity)_ | allowlisted fields |
| `sortOrder` | string | `asc` | `asc`, `desc` |

### Include Parameter

Most entity GET endpoints support an optional `include` query parameter:

| Value | Behavior |
|-------|----------|
| _(omitted)_ | Default relations populated (backward-compatible) |
| `none` | No relations or stats -- only scalar fields |
| `all` | All allowed relations and stats |
| `field1,field2` | Only the specified relations/stats |

<Warning>
Invalid include values return `400` with the list of allowed options.
</Warning>

### Error Responses

<Tabs>
  <Tab title="Standard Errors">
    ```json
    {
      "statusCode": 404,
      "message": "Project not found",
      "error": "Not Found"
    }
    ```
  </Tab>
  <Tab title="Validation Errors">
    ```json
    {
      "statusCode": 400,
      "message": [
        "limit must not be greater than 100",
        "sortBy must be one of the following values: id, name, area, status, createdAt"
      ],
      "error": "Bad Request"
    }
    ```
  </Tab>
  <Tab title="Change Request Errors">
    ```json
    {
      "message": "Validation failed",
      "errors": [
        {
          "path": "projects[0]",
          "field": "name",
          "message": "name should not be empty"
        }
      ]
    }
    ```
  </Tab>
</Tabs>

### HTTP Status Codes

| Status | Meaning |
|--------|---------|
| `400` | Validation error, bad request |
| `401` | Missing or invalid authentication |
| `403` | Insufficient permissions |
| `404` | Entity not found |
| `409` | Conflict (stale update in change requests) |

## Authentication

### Get Access Token

<CodeGroup>
```bash cURL
curl -X POST https://your-domain.com/api/auth/token \
  -H "Content-Type: application/json" \
  -d '{
    "apiKey": "your-api-key",
    "apiSecret": "your-api-secret"
  }'
```

```javascript JavaScript
const response = await fetch('/api/auth/token', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    apiKey: 'your-api-key',
    apiSecret: 'your-api-secret'
  })
});

const { access_token } = await response.json();
```

```python Python
import requests

response = requests.post('/api/auth/token', json={
    'apiKey': 'your-api-key',
    'apiSecret': 'your-api-secret'
})

token = response.json()['access_token']
```
</CodeGroup>

**Request Body**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `apiKey` | string | yes | Your API key |
| `apiSecret` | string | yes | Your API secret |

**Response**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "Bearer",
  "expires_in": 1800
}
```

| Field | Type | Description |
|-------|------|-------------|
| `access_token` | string | JWT token for Authorization header |
| `token_type` | string | Always "Bearer" |
| `expires_in` | number | Token lifetime in seconds (default 1800 = 30 min) |

<Note>
Store the token securely and include it in the Authorization header for all subsequent requests: `Authorization: Bearer <token>`
</Note>

## Entity Endpoints

All entity endpoints require authentication and support the paginated response format.

### Developers

#### List Developers

`GET /api/developers`

**Query Parameters**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `page` | int | no | `1` | Page number |
| `limit` | int | no | `20` | Items per page (1-100) |
| `sortBy` | string | no | `nameEn` | Sort field: `id`, `nameEn`, `nameAr`, `developerNumber`, `createdAt` |
| `sortOrder` | string | no | `asc` | Sort order: `asc`, `desc` |
| `nameEn` | string | no | -- | Filter by English name (substring) |
| `nameAr` | string | no | -- | Filter by Arabic name (substring) |
| `search` | string | no | -- | Search across `nameEn`, `nameAr` |
| `include` | string | no | -- | Include relations: `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats` |

<CodeGroup>
```bash cURL
curl -X GET "https://your-domain.com/api/developers?page=1&limit=20" \
  -H "Authorization: Bearer <your-token>"
```

```javascript JavaScript
const response = await fetch('/api/developers?page=1&limit=20', {
  headers: {
    'Authorization': 'Bearer <your-token>'
  }
});

const data = await response.json();
```
</CodeGroup>

**Response**

```json
{
  "data": [
    {
      "id": 1,
      "nameEn": "Emaar Properties",
      "nameAr": "إعمار العقارية",
      "developerNumber": "DEV-001",
      "logo": "https://example.com/logo.png",
      "logoDark": "https://example.com/logo-dark.png",
      "licenseUrl": "https://example.com/license.pdf"
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 20
}
```

#### Get Developer

`GET /api/developers/:id`

**Path Parameters**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `id` | int | yes | Developer ID |

**Query Parameters**

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `include` | string | no | Include relations (same as list endpoint) |

**Default includes:** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Response** (extends list item with additional fields)

```json
{
  "id": 1,
  "nameEn": "Emaar Properties",
  "nameAr": "إعمار العقارية",
  "developerNumber": "DEV-001",
  "sourceDeveloperId": "EMR-001",
  "logo": "https://example.com/logo.png",
  "logoDark": "https://example.com/logo-dark.png",
  "licenseUrl": "https://example.com/license.pdf",
  "licenseSource": { "id": 1, "nameEn": "DLD" },
  "licenseType": { "id": 1, "nameEn": "Commercial" },
  "legalStatus": { "id": 1, "nameEn": "Active" },
  "parentDeveloper": { "id": 2, "nameEn": "Parent Corp" },
  "childDevelopers": [
    { "id": 3, "nameEn": "Subsidiary A" }
  ],
  "stats": {
    "projectsCount": 25,
    "masterProjectsCount": 5
  }
}
```

#### Search Developers

`GET /api/developers/search`

Fuzzy search for developers using PostgreSQL `pg_trgm` similarity.

**Query Parameters**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `q` | string | yes | -- | Search query (min length 1) |
| `limit` | int | no | `10` | Max results (1-50) |

<CodeGroup>
```bash cURL
curl -X GET "https://your-domain.com/api/developers/search?q=emaar&limit=10" \
  -H "Authorization: Bearer <your-token>"
```

```javascript JavaScript
const response = await fetch('/api/developers/search?q=emaar&limit=10', {
  headers: {
    'Authorization': 'Bearer <your-token>'
  }
});

const results = await response.json();
```
</CodeGroup>

**How it works:**
1. Trims query and runs similarity against both `nameEn` and `nameAr`
2. Filters rows where either similarity score > 0.15
3. Orders by highest similarity score (descending)

**Response**

```json
[
  {
    "id": 42,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية",
    "developerNumber": "DEV-001",
    "logo": "https://example.com/logo.png",
    "logoDark": "https://example.com/logo-dark.png",
    "licenseUrl": "https://example.com/license.pdf",
    "similarity": 0.85
  }
]
```

### Cities

#### List Cities

`GET /api/cities`

**Query Parameters**

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `page` | int | no | `1` | Page number |
| `limit` | int | no | `20` | Items per page (1-100) |
| `sortBy` | string | no | `nameEn` | Sort field: `id`, `nameEn`, `nameAr`, `state`, `createdAt` |
| `sortOrder` | string | no | `asc` | Sort order: `asc`, `desc` |
| `nameEn` | string | no | -- | Filter by English name (substring) |
| `nameAr` | string | no | -- | Filter by Arabic name (substring) |
| `search` | string | no | -- | Search across `nameEn`, `nameAr`, `state.nameEn` |
| `stateId` | int | no | -- | Filter by state ID |
| `include` | string | no | -- | Include relations: `state`, `stats` |

**Default includes:** `state`

**Response**

```json
{
  "data": [
    {
      "id": 1,
      "nameEn": "Dubai",
      "nameAr": "دبي",
      "state": {
        "id": 1,
        "nameEn": "Dubai",
        "nameAr": "دبي"
      }
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 20
}
```

#### Get City

`GET /api/cities/:id`

Similar to developer detail endpoint with default includes: `state`, `stats`

### Projects

#### List Projects

`GET /api/projects`

**Query Parameters**

<AccordionGroup>
  <Accordion title="Pagination & Sorting">
    | Parameter | Type | Default | Description |
    |-----------|------|---------|-------------|
    | `page` | int | `1` | Page number |
    | `limit` | int | `20` | Items per page (1-100) |
    | `sortBy` | string | `nameEn` | Sort field: `id`, `nameEn`, `nameAr`, `area`, `status`, `createdAt` |
    | `sortOrder` | string | `asc` | Sort order |
  </Accordion>
  
  <Accordion title="Filters">
    | Parameter | Type | Description |
    |-----------|------|-------------|
    | `nameEn` | string | Filter by English name |
    | `nameAr` | string | Filter by Arabic name |
    | `search` | string | Search across multiple fields |
    | `developerId` | int | Filter by developer |
    | `cityId` | int | Filter by city |
    | `areaIds` | string | Comma-separated area IDs |
    | `status` | string | Filter by status |
    | `minArea` | number | Minimum area filter |
    | `maxArea` | number | Maximum area filter |
  </Accordion>
  
  <Accordion title="Includes">
    Allowed includes: `developer`, `city`, `areas`, `buildings`, `stats`
    
    Default includes: `developer`, `city`, `areas`
  </Accordion>
</AccordionGroup>

### Buildings

#### List Buildings

`GET /api/buildings`

Similar structure to projects with building-specific filters and includes.

### Units

#### List Units

`GET /api/units`

**Query Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `projectId` | int | Filter by project |
| `buildingId` | int | Filter by building |
| `unitType` | string | Filter by unit type |
| `minBedrooms` | int | Minimum bedrooms |
| `maxBedrooms` | int | Maximum bedrooms |
| `minPrice` | number | Minimum price |
| `maxPrice` | number | Maximum price |

#### Create Unit

`POST /api/units`

<Warning>
Requires `super` access level
</Warning>

**Request Body**

```json
{
  "buildingId": 1,
  "unitNumber": "A101",
  "unitType": "apartment",
  "bedrooms": 2,
  "bathrooms": 2,
  "area": 1200,
  "price": 850000,
  "status": "available"
}
```

#### Update Unit

`PATCH /api/units/:id`

<Warning>
Requires `super` access level
</Warning>

#### Delete Unit

`DELETE /api/units/:id`

<Warning>
Requires `super` access level
</Warning>

## Lookup Endpoints

These endpoints provide reference data for dropdowns and filters.

### Areas

`GET /api/lookup/areas`

**Query Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `cityId` | int | Filter by city |
| `search` | string | Search by name |

### Unit Types

`GET /api/lookup/unit-types`

Returns available unit types:

```json
[
  { "id": 1, "nameEn": "Apartment", "nameAr": "شقة" },
  { "id": 2, "nameEn": "Villa", "nameAr": "فيلا" },
  { "id": 3, "nameEn": "Townhouse", "nameAr": "تاون هاوس" }
]
```

### Project Statuses

`GET /api/lookup/project-statuses`

### Building Types

`GET /api/lookup/building-types`

### Developer License Sources

`GET /api/lookup/developer-license-sources`

### Developer License Types

`GET /api/lookup/developer-license-types`

## Direct Entity Mutations

<Warning>
These endpoints require `super` access level
</Warning>

### Update Project

`PATCH /api/projects/:id`

**Request Body**

```json
{
  "nameEn": "Updated Project Name",
  "nameAr": "اسم المشروع المحدث",
  "description": "Updated description",
  "area": 15000,
  "status": "active"
}
```

### Update Building

`PATCH /api/buildings/:id`

**Request Body**

```json
{
  "nameEn": "Updated Building Name",
  "floors": 25,
  "unitsCount": 200,
  "status": "completed"
}
```

## Change Requests

Change requests provide a workflow-based approach to data modifications with approval processes.

### Create Change Request

`POST /api/change-requests`

**Request Body**

```json
{
  "title": "Update Emaar Downtown Project",
  "description": "Updating project details and adding new buildings",
  "changes": {
    "projects": [
      {
        "action": "update",
        "id": 123,
        "data": {
          "nameEn": "Downtown Views II",
          "area": 25000
        }
      }
    ],
    "buildings": [
      {
        "action": "create",
        "data": {
          "projectId": 123,
          "nameEn": "Tower C",
          "floors": 30
        }
      }
    ]
  }
}
```

### List Change Requests

`GET /api/change-requests`

**Query Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `status` | string | Filter by status: `pending`, `approved`, `rejected` |
| `createdBy` | int | Filter by creator |

### Get Change Request

`GET /api/change-requests/:id`

### Update Change Request Status

`PATCH /api/change-requests/:id/status`

**Request Body**

```json
{
  "status": "approved",
  "reviewNotes": "Changes look good, approved for implementation"
}
```

## Market Analytics

### Get Market Overview

`GET /api/analytics/market-overview`

**Query Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `cityId` | int | Filter by city |
| `areaIds` | string | Comma-separated area IDs |
| `timeframe` | string | `1m`, `3m`, `6m`, `1y`, `all` |

**Response**

```json
{
  "totalProjects": 156,
  "totalUnits": 12453,
  "averagePrice": 875000,
  "priceGrowth": {
    "percentage": 5.2,
    "period": "3m"
  },
  "topDevelopers": [
    {
      "id": 1,
      "nameEn": "Emaar Properties",
      "projectsCount": 25,
      "marketShare": 18.5
    }
  ]
}
```

### Get Price Trends

`GET /api/analytics/price-trends`

**Query Parameters**

| Parameter | Type | Description |
|-----------|------|-------------|
| `cityId` | int | Filter by city |
| `unitType` | string | Filter by unit type |
| `bedrooms` | int | Filter by bedrooms |
| `period` | string | `daily`, `weekly`, `monthly` |
| `timeframe` | string | `1m`, `3m`, `6m`, `1y` |

**Response**

```json
{
  "data": [
    {
      "date": "2024-01-01",
      "averagePrice": 850000,
      "medianPrice": 820000,
      "transactionCount": 45
    }
  ],
  "summary": {
    "currentAverage": 875000,
    "priceChange": 25000,
    "priceChangePercentage": 2.9
  }
}
```

### Get Area Analytics

`GET /api/analytics/areas/:id`

**Response**

```json
{
  "area": {
    "id": 1,
    "nameEn": "Downtown Dubai",
    "nameAr": "وسط مدينة دبي"
  },
  "stats": {
    "projectsCount": 15,
    "unitsCount": 2340,
    "averagePrice": 1200000,
    "pricePerSqFt": 1100
  },
  "priceRanges": {
    "studio": { "min": 450000, "max": 650000, "average": 550000 },
    "1bed": { "min": 650000, "max": 950000, "average": 800000 },
    "2bed": { "min": 950000, "max": 1500000, "average": 1200000 }
  }
}
```

## Appendix: Enum Reference

<AccordionGroup>
  <Accordion title="Project Status">
    - `planning`
    - `approved`
    - `under_construction`
    - `completed`
    - `on_hold`
    - `cancelled`
  </Accordion>

  <Accordion title="Unit Types">
    - `studio`
    - `apartment`
    - `penthouse`
    - `villa`
    - `townhouse`
    - `duplex`
    - `loft`
    - `office`
    - `retail`
    - `warehouse`
  </Accordion>

  <Accordion title="Unit Status">
    - `available`
    - `reserved`
    - `sold`
    - `rented`
    - `off_market`
  </Accordion>

  <Accordion title="Building Types">
    - `residential`
    - `commercial`
    - `mixed_use`
    - `hospitality`
    - `industrial`
  </Accordion>
</AccordionGroup>

<Tip>
For the most up-to-date enum values, use the lookup endpoints which return the current valid options.
</Tip>