---
title: "PropWise Labs API Documentation"
description: "Complete API reference for external consumers including authentication, entity endpoints, lookup endpoints, mutations, and change requests."
---

# PropWise Labs API Documentation

Complete API reference for external consumers (non-admin endpoints).

## Overview

### Base URL

All endpoints are prefixed with `/api`. Example:

```
https://your-domain.com/api/projects
```

### Authentication

The API supports two authentication methods:

**API Client Token (external consumers):**

<Steps>
<Step title="Obtain credentials">
Get an API key and secret from an admin.
</Step>
<Step title="Exchange for token">
Exchange them for a short-lived Bearer token via `POST /api/auth/token`.
</Step>
<Step title="Include in requests">
Include the token on every request: `Authorization: Bearer <token>`.
</Step>
</Steps>

**JWT (internal/dashboard users):**

- Obtained via `POST /api/auth/login` (admin dashboard)
- Sent as `Authorization: Bearer <token>` or via the `access_token` httpOnly cookie

<Note>
Both methods are accepted on all external-facing endpoints. The API tries JWT first, then falls back to API key token.
</Note>

**Access levels for API clients:**

| Level   | Permissions                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------ |
| `basic` | Read-only access to all entity, lookup, analytics, and change-request endpoints                        |
| `super` | All of `basic` plus direct entity mutations (PATCH projects, PATCH buildings, POST/PATCH/DELETE units) |

### Paginated Response Envelope

All list endpoints return:

```json
{
  "data": [ ... ],
  "total": 1234,
  "page": 1,
  "limit": 20
}
```

Standard pagination query parameters:

| Param       | Type   | Default        | Constraints        |
| ----------- | ------ | -------------- | ------------------ |
| `page`      | int    | `1`            | >= 1               |
| `limit`     | int    | `20`           | 1--100             |
| `sortBy`    | string | _(per-entity)_ | allowlisted fields |
| `sortOrder` | string | `asc`          | `asc`, `desc`      |

### Configurable Includes

<Info>
Most entity GET endpoints support an optional `include` query parameter that controls which relations and computed fields are returned.
</Info>

| Value           | Behavior                                          |
| --------------- | ------------------------------------------------- |
| _(omitted)_     | Default relations populated (backward-compatible) |
| `none`          | No relations or stats -- only scalar fields       |
| `all`           | All allowed relations and stats                   |
| `field1,field2` | Only the specified relations/stats                |

Invalid values return `400` with the list of allowed options. Virtual includes like `stats` and `buildingAreas` control expensive aggregate queries rather than ORM relations.

### Error Response Format

<CodeGroup>

```json Standard errors
{
  "statusCode": 404,
  "message": "Project not found",
  "error": "Not Found"
}
```

```json Validation errors (400)
{
  "statusCode": 400,
  "message": [
    "limit must not be greater than 100",
    "sortBy must be one of the following values: id, name, area, status, createdAt"
  ],
  "error": "Bad Request"
}
```

```json Change request validation errors (400)
{
  "message": "Validation failed",
  "errors": [{ "path": "projects[0]", "field": "name", "message": "name should not be empty" }]
}
```

</CodeGroup>

| Status | Meaning                                                              |
| ------ | -------------------------------------------------------------------- |
| `400`  | Validation error, bad request                                        |
| `401`  | Missing or invalid authentication                                    |
| `403`  | Insufficient permissions (e.g. `basic` client attempting a mutation) |
| `404`  | Entity not found                                                     |
| `409`  | Conflict (stale update in change requests)                           |

### Global Request Rules

<Check>
- All request bodies are JSON (`Content-Type: application/json`)
- Unknown fields in request bodies and query params are rejected (`forbidNonWhitelisted`)
- Query params are auto-coerced to their declared types (e.g. `"1"` becomes `1` for int fields)
- Comma-separated ID lists (e.g. `areaIds=1,2,3`) are parsed into integer arrays
- Soft-deleted records are excluded from all results
</Check>

## Authentication

### Exchange API credentials for token

<CodeGroup>

```bash cURL
curl -X POST https://your-domain.com/api/auth/token \
  -H "Content-Type: application/json" \
  -d '{
    "apiKey": "your-api-key",
    "apiSecret": "your-api-secret"
  }'
```

```javascript JavaScript
const response = await fetch('/api/auth/token', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    apiKey: 'your-api-key',
    apiSecret: 'your-api-secret'
  })
});

const { access_token } = await response.json();
```

```python Python
import requests

response = requests.post('/api/auth/token', json={
    'apiKey': 'your-api-key',
    'apiSecret': 'your-api-secret'
})

token = response.json()['access_token']
```

</CodeGroup>

**Auth:** None (public endpoint)

**Request body:**

| Field       | Type   | Required | Constraints |
| ----------- | ------ | -------- | ----------- |
| `apiKey`    | string | yes      | non-empty   |
| `apiSecret` | string | yes      | non-empty   |

**Response `200 OK`:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "Bearer",
  "expires_in": 1800
}
```

| Field          | Type   | Description                                        |
| -------------- | ------ | -------------------------------------------------- |
| `access_token` | string | JWT token to use in `Authorization: Bearer` header |
| `token_type`   | string | Always `"Bearer"`                                  |
| `expires_in`   | number | Token lifetime in seconds (default 1800 = 30 min)  |

**Error responses:**

| Status | Condition                 |
| ------ | ------------------------- |
| `401`  | Invalid API key or secret |
| `401`  | API client is disabled    |

## Entity Endpoints

<Warning>
All entity endpoints require authentication via `ApiAuthGuard` (JWT or API client token).
</Warning>

All list endpoints return the paginated envelope format.

### Developers

#### List developers

<CodeGroup>

```bash cURL
curl -X GET "https://your-domain.com/api/developers?page=1&limit=20&search=emaar" \
  -H "Authorization: Bearer <your-token>"
```

```javascript JavaScript
const response = await fetch('/api/developers?page=1&limit=20&search=emaar', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});

const { data, total } = await response.json();
```

</CodeGroup>

**Query Parameters:**

| Param       | Type   | Required | Default  | Constraints                                                                                           |
| ----------- | ------ | -------- | -------- | ----------------------------------------------------------------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                                                                                  |
| `limit`     | int    | no       | `20`     | 1--100                                                                                                |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `developerNumber`, `createdAt`                                              |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                                                                         |
| `nameEn`    | string | no       | --       | substring filter                                                                                      |
| `nameAr`    | string | no       | --       | substring filter                                                                                      |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`                                                                           |
| `include`   | string | no       | --       | allowed: `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats` |

**Default includes (list):** _(none)_

**Response item:**

| Field             | Type   | Always present |
| ----------------- | ------ | -------------- |
| `id`              | number | yes            |
| `nameEn`          | string | --             |
| `nameAr`          | string | --             |
| `developerNumber` | string | --             |
| `logo`            | string | --             |
| `logoDark`        | string | --             |
| `licenseUrl`      | string | --             |

#### Get developer by ID

```bash
GET /api/developers/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`
**Allowed includes:** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Response (extends list item):**

| Field               | Type             | Notes                                                                |
| ------------------- | ---------------- | -------------------------------------------------------------------- |
| `sourceDeveloperId` | string           | always present                                                       |
| `licenseSource`     | `{ id, nameEn }` | if included                                                          |
| `licenseType`       | `{ id, nameEn }` | if included                                                          |
| `legalStatus`       | `{ id, nameEn }` | if included                                                          |
| `parentDeveloper`   | `{ id, nameEn }` | if included                                                          |
| `childDevelopers`   | array            | `[{ id, nameEn }]` -- if included                                    |
| `stats`             | object           | `{ projectsCount, masterProjectsCount }` -- only if `stats` included |

#### Search developers

```bash
GET /api/developers/search
```

Fuzzy search for developers by name using PostgreSQL `pg_trgm` similarity.

| Param   | Type   | Required | Default | Constraints  |
| ------- | ------ | -------- | ------- | ------------ |
| `q`     | string | yes      | --      | min length 1 |
| `limit` | int    | no       | `10`    | 1--50        |

<Info>
**How it works:**

1. Trims `q`, then runs `similarity()` against both `name_en` and `name_ar` columns
2. Filters rows where either similarity score > 0.15
3. Orders by the highest of the two scores (descending)
4. Returns up to `limit` results
</Info>

**Response `200 OK`:**

```json
[
  {
    "id": 42,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية",
    "developerNumber": "DEV-001",
    "logo": "https://example.com/logo.png",
    "logoDark": "https://example.com/logo-dark.png",
    "licenseUrl": "https://example.com/license.pdf",
    "similarity": 0.85
  }
]
```

<Note>
All fields except `id` and `similarity` are nullable.
</Note>

### Cities

#### List cities

```bash
GET /api/cities
```

| Param       | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `state`, `createdAt` |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `state.nameEn`    |
| `stateId`   | int    | no       | --       | filter by state                                |
| `include`   | string | no       | --       | allowed: `state`, `stats`                      |

**Default includes (list):** `state`

**Response item:**

| Field    | Type                   | Notes       |
| -------- | ---------------------- | ----------- |
| `id`     | number                 | yes         |
| `nameEn` | string                 | --          |
| `nameAr` | string                 | --          |
| `state`  | `{ id, nameEn, code }` | if included |

#### Get city by ID

```bash
GET /api/cities/:id
```

**Default includes (detail):** `state`, `stats`
**Allowed includes:** `state`, `stats`

**Response (extends list item):**

| Field   | Type   | Notes                                |
| ------- | ------ | ------------------------------------ |
| `stats` | object | `{ projectsCount }` -- if included   |

### Areas

#### List areas

```bash
GET /api/areas
```

| Param       | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `createdAt`          |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `city.nameEn`     |
| `cityId`    | int    | no       | --       | filter by city                                 |
| `stateId`   | int    | no       | --       | filter by state (via city)                     |
| `include`   | string | no       | --       | allowed: `city`, `stats`                       |

**Default includes (list):** `city`

**Response item:**

| Field    | Type                   | Notes       |
| -------- | ---------------------- | ----------- |
| `id`     | number                 | yes         |
| `nameEn` | string                 | --          |
| `nameAr` | string                 | --          |
| `city`   | `{ id, nameEn }`       | if included |

#### Get area by ID

```bash
GET /api/areas/:id
```

**Default includes (detail):** `city`, `stats`
**Allowed includes:** `city`, `stats`

**Response (extends list item):**

| Field   | Type   | Notes                              |
| ------- | ------ | ---------------------------------- |
| `stats` | object | `{ projectsCount }` -- if included |

### Projects

#### List projects

```bash
GET /api/projects
```

<Tabs>
<Tab title="Query Parameters">

| Param         | Type   | Required | Default     | Constraints                                                      |
| ------------- | ------ | -------- | ----------- | ---------------------------------------------------------------- |
| `page`        | int    | no       | `1`         | >= 1                                                             |
| `limit`       | int    | no       | `20`        | 1--100                                                           |
| `sortBy`      | string | no       | `createdAt` | `id`, `name`, `area`, `status`, `createdAt`, `lastUpdate`        |
| `sortOrder`   | string | no       | `desc`      | `asc`, `desc`                                                    |
| `name`        | string | no       | --          | substring filter                                                 |
| `search`      | string | no       | --          | searches across multiple fields                                  |
| `developerId` | int    | no       | --          | filter by developer                                              |
| `cityId`      | int    | no       | --          | filter by city                                                   |
| `stateId`     | int    | no       | --          | filter by state                                                  |
| `areaIds`     | string | no       | --          | comma-separated area IDs                                         |
| `statuses`    | string | no       | --          | comma-separated status values                                    |
| `include`     | string | no       | --          | allowed: `developer`, `city`, `areas`, `buildings`, `stats`      |

</Tab>
<Tab title="Filter Examples">

```bash
# Search by name
GET /api/projects?search=downtown

# Filter by multiple areas
GET /api/projects?areaIds=1,2,3

# Filter by status
GET /api/projects?statuses=ACTIVE,COMPLETED

# Include related data
GET /api/projects?include=developer,city,stats
```

</Tab>
</Tabs>

**Default includes (list):** `developer`, `city`

**Response item:**

| Field        | Type                | Notes                    |
| ------------ | ------------------- | ------------------------ |
| `id`         | number              | yes                      |
| `name`       | string              | yes                      |
| `area`       | number              | area in square meters    |
| `status`     | string              | enum value               |
| `createdAt`  | string              | ISO 8601 timestamp       |
| `lastUpdate` | string              | ISO 8601 timestamp       |
| `developer`  | `{ id, nameEn }`    | if included              |
| `city`       | `{ id, nameEn }`    | if included              |

#### Get project by ID

```bash
GET /api/projects/:id
```

**Default includes (detail):** `developer`, `city`, `areas`, `buildings`, `stats`
**Allowed includes:** `developer`, `city`, `areas`, `buildings`, `stats`

**Additional response fields:**

| Field       | Type     | Notes                                   |
| ----------- | -------- | --------------------------------------- |
| `areas`     | array    | `[{ id, nameEn }]` -- if included       |
| `buildings` | array    | building objects -- if included         |
| `stats`     | object   | project statistics -- if included       |

### Buildings

#### List buildings

```bash
GET /api/buildings
```

| Param       | Type   | Required | Default     | Constraints                                         |
| ----------- | ------ | -------- | ----------- | --------------------------------------------------- |
| `page`      | int    | no       | `1`         | >= 1                                                |
| `limit`     | int    | no       | `20`        | 1--100                                              |
| `sortBy`    | string | no       | `createdAt` | `id`, `name`, `floors`, `status`, `createdAt`       |
| `sortOrder` | string | no       | `desc`      | `asc`, `desc`                                       |
| `name`      | string | no       | --          | substring filter                                    |
| `search`    | string | no       | --          | searches name and project name                      |
| `projectId` | int    | no       | --          | filter by project                                   |
| `statuses`  | string | no       | --          | comma-separated status values                       |
| `include`   | string | no       | --          | allowed: `project`, `units`, `stats`                |

**Default includes (list):** `project`

**Response item:**

| Field       | Type             | Notes                 |
| ----------- | ---------------- | --------------------- |
| `id`        | number           | yes                   |
| `name`      | string           | yes                   |
| `floors`    | number           | --                    |
| `status`    | string           | enum value            |
| `createdAt` | string           | ISO 8601 timestamp    |
| `project`   | `{ id, name }`   | if included           |

#### Get building by ID

```bash
GET /api/buildings/:id
```

**Default includes (detail):** `project`, `units`, `stats`
**Allowed includes:** `project`, `units`, `stats`

### Units

#### List units

```bash
GET /api/units
```

<Tabs>
<Tab title="Basic Parameters">

| Param        | Type   | Required | Default     | Constraints                                    |
| ------------ | ------ | -------- | ----------- | ---------------------------------------------- |
| `page`       | int    | no       | `1`         | >= 1                                           |
| `limit`      | int    | no       | `20`        | 1--100                                         |
| `sortBy`     | string | no       | `createdAt` | `id`, `unitNumber`, `bedrooms`, `area`, etc.   |
| `sortOrder`  | string | no       | `desc`      | `asc`, `desc`                                  |

</Tab>
<Tab title="Filters">

| Param         | Type   | Description                |
| ------------- | ------ | -------------------------- |
| `unitNumber`  | string | substring filter           |
| `search`      | string | searches multiple fields   |
| `projectId`   | int    | filter by project          |
| `buildingId`  | int    | filter by building         |
| `bedrooms`    | string | comma-separated values     |
| `bathrooms`   | string | comma-separated values     |
| `unitTypes`   | string | comma-separated enum values|
| `statuses`    | string | comma-separated enum values|

</Tab>
<Tab title="Includes">

| Value                     | Description                    |
| ------------------------- | ------------------------------ |
| `project`                 | project info                   |
| `building`                | building info                  |
| `latestTransaction`       | most recent transaction        |
| `stats`                   | unit statistics                |

</Tab>
</Tabs>

**Default includes (list):** `project`, `building`

**Response item:**

| Field        | Type               | Notes              |
| ------------ | ------------------ | ------------------ |
| `id`         | number             | yes                |
| `unitNumber` | string             | --                 |
| `bedrooms`   | number             | --                 |
| `bathrooms`  | number             | --                 |
| `area`       | number             | square meters      |
| `unitType`   | string             | enum value         |
| `status`     | string             | enum value         |
| `project`    | `{ id, name }`     | if included        |
| `building`   | `{ id, name }`     | if included        |

#### Get unit by ID

```bash
GET /api/units/:id
```

**Default includes (detail):** `project`, `building`, `latestTransaction`, `stats`

## Lookup Endpoints

<Info>
Lookup endpoints provide reference data for dropdowns and filters. All are paginated and support basic search.
</Info>

### Unit types

```bash
GET /api/lookup/unit-types
```

**Response:**

```json
{
  "data": [
    {
      "id": 1,
      "nameEn": "Apartment",
      "nameAr": "شقة",
      "createdAt": "2024-01-01T00:00:00.000Z"
    }
  ],
  "total": 5,
  "page": 1,
  "limit": 20
}
```

### Building types

```bash
GET /api/lookup/building-types
```

### Project statuses

```bash
GET /api/lookup/project-statuses
```

### Building statuses

```bash
GET /api/lookup/building-statuses
```

### Unit statuses

```bash
GET /api/lookup/unit-statuses
```

### Payment methods

```bash
GET /api/lookup/payment-methods
```

### States

```bash
GET /api/lookup/states
```

All lookup endpoints support:

| Param       | Type   | Default  | Constraints                        |
| ----------- | ------ | -------- | ---------------------------------- |
| `page`      | int    | `1`      | >= 1                               |
| `limit`     | int    | `20`     | 1--100                             |
| `sortBy`    | string | `nameEn` | `id`, `nameEn`, `nameAr`, `createdAt` |
| `sortOrder` | string | `asc`    | `asc`, `desc`                      |
| `nameEn`    | string | --       | substring filter                   |
| `nameAr`    | string | --       | substring filter                   |
| `search`    | string | --       | searches `nameEn`, `nameAr`        |

## Direct Entity Mutations

<Warning>
These endpoints require `super` level API client access.
</Warning>

### Update project

```bash
PATCH /api/projects/:id
```

**Request body:**

```json
{
  "name": "Updated Project Name",
  "area": 5000.5,
  "status": "ACTIVE"
}
```

**Response:** Updated project object with default includes.

### Update building

```bash
PATCH /api/buildings/:id
```

**Request body:**

```json
{
  "name": "Updated Building Name",
  "floors": 25,
  "status": "ACTIVE"
}
```

### Unit operations

<Tabs>
<Tab title="Create Unit">

```bash
POST /api/units
```

**Request body:**

```json
{
  "projectId": 1,
  "buildingId": 2,
  "unitNumber": "A-101",
  "bedrooms": 2,
  "bathrooms": 2,
  "area": 85.5,
  "unitType": "APARTMENT",
  "status": "AVAILABLE"
}
```

</Tab>
<Tab title="Update Unit">

```bash
PATCH /api/units/:id
```

**Request body:**

```json
{
  "unitNumber": "A-102",
  "bedrooms": 3,
  "area": 95.5
}
```

</Tab>
<Tab title="Delete Unit">

```bash
DELETE /api/units/:id
```

**Response:** `204 No Content` on success.

</Tab>
</Tabs>

## Change Requests

<Info>
Change requests provide a workflow for proposing and reviewing bulk changes to entities before applying them to the live database.
</Info>

### Create change request

```bash
POST /api/change-requests
```

**Request body:**

```json
{
  "title": "Q4 2024 Project Updates",
  "description": "Quarterly update of project statuses and areas",
  "projects": [
    {
      "id": 1,
      "name": "Updated Project Name",
      "area": 5000.5,
      "status": "COMPLETED"
    }
  ],
  "buildings": [
    {
      "id": 2,
      "floors": 30
    }
  ],
  "units": [
    {
      "id": 3,
      "bedrooms": 3,
      "area": 120.0
    }
  ]
}
```

**Response:** Created change request with status `PENDING`.

### List change requests

```bash
GET /api/change-requests
```

| Param       | Type   | Constraints                                |
| ----------- | ------ | ------------------------------------------ |
| `status`    | string | `PENDING`, `APPROVED`, `REJECTED`, `APPLIED` |
| `createdBy` | string | filter by creator                          |
| `search`    | string | searches title and description             |

### Get change request by ID

```bash
GET /api/change-requests/:id
```

**Response includes:**
- Change request metadata
- All proposed changes for projects, buildings, and units
- Approval workflow status

### Update change request

```bash
PATCH /api/change-requests/:id
```

<Warning>
Only allowed if status is `PENDING`. Once approved or rejected, change requests become immutable.
</Warning>

### Approve change request

```bash
POST /api/change-requests/:id/approve
```

**Auth:** Admin users only

**Response:** Updates status to `APPROVED` and queues for application.

### Reject change request

```bash
POST /api/change-requests/:id/reject
```

**Auth:** Admin users only

**Request body:**

```json
{
  "reason": "Data validation failed for project #1"
}
```

## Market Analytics

### Price trends

```bash
GET /api/analytics/price-trends
```

**Query parameters:**

| Param        | Type   | Required | Description                    |
| ------------ | ------ | -------- | ------------------------------ |
| `areaId`     | int    | no       | filter by area                 |
| `cityId`     | int    | no       | filter by city                 |
| `unitType`   | string | no       | filter by unit type            |
| `bedrooms`   | int    | no       | filter by bedroom count        |
| `startDate`  | string | no       | ISO date (YYYY-MM-DD)          |
| `endDate`    | string | no       | ISO date (YYYY-MM-DD)          |
| `interval`   | string | no       | `month`, `quarter`, `year`     |

**Response:**

```json
{
  "trends": [
    {
      "period": "2024-01",
      "averagePrice": 1250000,
      "medianPrice": 1200000,
      "transactionCount": 45,
      "pricePerSqm": 12500
    }
  ],
  "summary": {
    "totalTransactions": 180,
    "averageGrowth": 3.2,
    "highestPrice": 2500000,
    "lowestPrice": 800000
  }
}
```

### Area comparison

```bash
GET /api/analytics/area-comparison
```

**Query parameters:**

| Param      | Type   | Required | Description                |
| ---------- | ------ | -------- | -------------------------- |
| `areaIds`  | string | yes      | comma-separated area IDs   |
| `unitType` | string | no       | filter by unit type        |
| `period`   | string | no       | `3m`, `6m`, `1y`, `2y`     |

**Response:**

```json
{
  "areas": [
    {
      "id": 1,
      "name": "Downtown",
      "averagePrice": 1350000,
      "medianPrice": 1300000,
      "pricePerSqm": 13500,
      "transactionCount": 67,
      "growth": 5.2
    }
  ]
}
```

### Market insights

```bash
GET /api/analytics/market-insights
```

Provides AI-powered market analysis and recommendations.

**Query parameters:**

| Param       | Type   | Description                     |
| ----------- | ------ | ------------------------------- |
| `cityId`    | int    | focus on specific city          |
| `unitType`  | string | filter by unit type             |
| `budget`    | int    | budget range for recommendations |

**Response:**

```json
{
  "insights": [
    {
      "type": "price_trend",
      "title": "Rising Prices in Downtown",
      "description": "Prices have increased 8% in the last quarter",
      "confidence": 0.85,
      "impact": "high"
    }
  ],
  "recommendations": [
    {
      "type": "investment_opportunity",
      "area": "Marina District",
      "reasoning": "Strong rental yields and upcoming infrastructure",
      "expectedReturn": 12.5
    }
  ]
}
```

## Unit Evaluation (AI Valuation)

### Get unit valuation

```bash
POST /api/ai/valuate-unit
```

**Request body:**

```json
{
  "projectId": 1,
  "buildingId": 2,
  "bedrooms": 2,
  "bathrooms": 2,
  "area": 95.5,
  "floor": 15,
  "unitType": "APARTMENT",
  "features": ["balcony", "parking", "storage"]
}
```

**Response:**

```json
{
  "valuation": {
    "estimatedPrice": 1425000,
    "priceRange": {
      "min": 1350000,
      "max": 1500000
    },
    "pricePerSqm": 14921,
    "confidence": 0.78
  },
  "factors": [
    {
      "factor": "location",
      "impact": 15.2,
      "description": "Prime location with metro access"
    },
    {
      "factor": "size",
      "impact": 8.5,
      "description": "Optimal size for 2BR units in the area"
    }
  ],
  "comparables": [
    {
      "unitId": 456,
      "similarity": 0.92,
      "price": 1380000,
      "area": 93.0,
      "bedrooms": 2
    }
  ]
}
```

### Batch valuation

```bash
POST /api/ai/valuate-units
```

**Request body:**

```json
{
  "units": [
    {
      "projectId": 1,
      "buildingId": 2,
      "bedrooms": 2,
      "area": 95.5
    }
  ]
}
```

**Response:** Array of valuation objects with same structure as single valuation.

<Note>
Batch endpoint supports up to 50 units per request for performance optimization.
</Note>

## Appendix A: Enum Reference

<AccordionGroup>

<Accordion title="Project Status">

| Value          | Description                    |
| -------------- | ------------------------------ |
| `PLANNING`     | In planning phase              |
| `APPROVED`     | Approved but not started       |
| `UNDER_CONSTRUCTION` | Currently under construction |
| `COMPLETED`    | Construction completed         |
| `DELIVERED`    | Handed over to buyers          |
| `ON_HOLD`      | Temporarily suspended          |
| `CANCELLED`    | Project cancelled              |

</Accordion>

<Accordion title="Building Status">

| Value             | Description                    |
| ----------------- | ------------------------------ |
| `PLANNED`         | In planning phase              |
| `UNDER_CONSTRUCTION` | Currently under construction |
| `COMPLETED`       | Construction completed         |
| `DELIVERED`       | Ready for occupancy            |
| `MAINTENANCE`     | Under maintenance              |

</Accordion>

<Accordion title="Unit Status">

| Value         | Description                    |
| ------------- | ------------------------------ |
| `AVAILABLE`   | Available for sale/rent        |
| `RESERVED`    | Reserved by buyer              |
| `SOLD`        | Sold to buyer                  |
| `RENTED`      | Currently rented               |
| `MAINTENANCE` | Under maintenance              |

</Accordion>

<Accordion title="Unit Type">

| Value        | Description                    |
| ------------ | ------------------------------ |
| `APARTMENT`  | Standard apartment             |
| `VILLA`      | Standalone villa               |
| `TOWNHOUSE`  | Townhouse unit                 |
| `PENTHOUSE`  | Penthouse apartment            |
| `STUDIO`     | Studio apartment               |
| `DUPLEX`     | Duplex apartment               |
| `OFFICE`     | Commercial office space        |
| `RETAIL`     | Retail/shop space              |

</Accordion>

<Accordion title="Payment Method">

| Value           | Description                    |
| --------------- | ------------------------------ |
| `CASH`          | Cash payment                   |
| `MORTGAGE`      | Bank mortgage                  |
| `INSTALLMENT`   | Developer installment plan     |
| `MIXED`         | Combination of methods         |

</Accordion>

</AccordionGroup>

## Appendix B: Entity Relationships

<CardGroup cols={2}>

<Card title="Developer" icon="building">
**Relations:**
- Has many Projects
- Belongs to License Source
- Belongs to License Type
- Belongs to Legal Status
- Self-referential parent/child

**Key Fields:**
- `nameEn`, `nameAr`
- `developerNumber`
- `logo`, `logoDark`
</Card>

<Card title="Project" icon="blueprint">
**Relations:**
- Belongs to Developer
- Belongs to City
- Has many Areas (M:M)
- Has many Buildings
- Has many Units (through Buildings)

**Key Fields:**
- `name`, `area`, `status`
- Geographic coordinates
- Timeline dates
</Card>

<Card title="Building" icon="building-columns">
**Relations:**
- Belongs to Project
- Has many Units
- Belongs to Building Type

**Key Fields:**
- `name`, `floors`, `status`
- Construction details
- Facility information
</Card>

<Card title="Unit" icon="home">
**Relations:**
- Belongs to Building
- Belongs to Project (through Building)
- Has many Transactions
- Belongs to Unit Type

**Key Fields:**
- `unitNumber`, `bedrooms`, `bathrooms`
- `area`, `status`, `unitType`
- Price and valuation data
</Card>

</CardGroup>

<Tip>
Use the `include` parameter strategically to fetch only the relations you need, as some relationships involve expensive aggregate queries.
</Tip>