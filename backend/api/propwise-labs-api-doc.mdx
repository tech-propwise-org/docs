---
title: "PropWise Labs API Documentation"
description: "Complete API reference for external consumers including authentication, entity endpoints, lookups, mutations, change requests, and market analytics"
---

Complete API reference for external consumers (non-admin endpoints).

## Overview

### Base URL

All endpoints are prefixed with `/api`. Example:

```
https://your-domain.com/api/projects
```

### Authentication

The API supports two authentication methods:

<Tabs>
  <Tab title="API Client Token">
    **For external consumers:**

    1. Obtain an API key and secret from an admin
    2. Exchange them for a short-lived Bearer token via `POST /api/auth/token`
    3. Include the token on every request: `Authorization: Bearer <token>`
  </Tab>
  <Tab title="JWT">
    **For internal/dashboard users:**

    - Obtained via `POST /api/auth/login` (admin dashboard)
    - Sent as `Authorization: Bearer <token>` or via the `access_token` httpOnly cookie
  </Tab>
</Tabs>

Both methods are accepted on all external-facing endpoints. The API tries JWT first, then falls back to API key token.

#### Access Levels for API Clients

| Level   | Permissions                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------ |
| `basic` | Read-only access to all entity, lookup, analytics, and change-request endpoints                        |
| `super` | All of `basic` plus direct entity mutations (PATCH projects, PATCH buildings, POST/PATCH/DELETE units) |

### Paginated Response Envelope

All list endpoints return:

```json
{
  "data": [ ... ],
  "total": 1234,
  "page": 1,
  "limit": 20
}
```

Standard pagination query parameters:

| Param       | Type   | Default        | Constraints        |
| ----------- | ------ | -------------- | ------------------ |
| `page`      | int    | `1`            | >= 1               |
| `limit`     | int    | `20`           | 1--100             |
| `sortBy`    | string | _(per-entity)_ | allowlisted fields |
| `sortOrder` | string | `asc`          | `asc`, `desc`      |

### Configurable Includes

Most entity GET endpoints support an optional `include` query parameter that controls which relations and computed fields are returned:

| Value           | Behavior                                          |
| --------------- | ------------------------------------------------- |
| _(omitted)_     | Default relations populated (backward-compatible) |
| `none`          | No relations or stats -- only scalar fields       |
| `all`           | All allowed relations and stats                   |
| `field1,field2` | Only the specified relations/stats                |

<Info>Invalid values return `400` with the list of allowed options. Virtual includes like `stats` and `buildingAreas` control expensive aggregate queries rather than ORM relations.</Info>

### Error Response Format

<Tabs>
  <Tab title="Standard Errors">
    ```json
    {
      "statusCode": 404,
      "message": "Project not found",
      "error": "Not Found"
    }
    ```
  </Tab>
  <Tab title="Validation Errors">
    ```json
    {
      "statusCode": 400,
      "message": [
        "limit must not be greater than 100",
        "sortBy must be one of the following values: id, name, area, status, createdAt"
      ],
      "error": "Bad Request"
    }
    ```
  </Tab>
  <Tab title="Change Request Validation">
    ```json
    {
      "message": "Validation failed",
      "errors": [{ "path": "projects[0]", "field": "name", "message": "name should not be empty" }]
    }
    ```
  </Tab>
</Tabs>

#### HTTP Status Codes

| Status | Meaning                                                              |
| ------ | -------------------------------------------------------------------- |
| `400`  | Validation error, bad request                                        |
| `401`  | Missing or invalid authentication                                    |
| `403`  | Insufficient permissions (e.g. `basic` client attempting a mutation) |
| `404`  | Entity not found                                                     |
| `409`  | Conflict (stale update in change requests)                           |

### Global Request Rules

<Note>
- All request bodies are JSON (`Content-Type: application/json`)
- Unknown fields in request bodies and query params are rejected (`forbidNonWhitelisted`)
- Query params are auto-coerced to their declared types (e.g. `"1"` becomes `1` for int fields)
- Comma-separated ID lists (e.g. `areaIds=1,2,3`) are parsed into integer arrays
- Soft-deleted records are excluded from all results
</Note>

## Authentication

### Exchange API Credentials for Token

<CodeGroup>
```bash cURL
curl -X POST https://your-domain.com/api/auth/token \
  -H "Content-Type: application/json" \
  -d '{
    "apiKey": "your-api-key",
    "apiSecret": "your-api-secret"
  }'
```

```javascript JavaScript
const response = await fetch('/api/auth/token', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    apiKey: 'your-api-key',
    apiSecret: 'your-api-secret'
  })
});

const data = await response.json();
```

```python Python
import requests

response = requests.post('https://your-domain.com/api/auth/token', 
  json={
    'apiKey': 'your-api-key',
    'apiSecret': 'your-api-secret'
  }
)

data = response.json()
```
</CodeGroup>

**Auth:** None (public endpoint)

**Request body:**

| Field       | Type   | Required | Constraints |
| ----------- | ------ | -------- | ----------- |
| `apiKey`    | string | yes      | non-empty   |
| `apiSecret` | string | yes      | non-empty   |

**Response `200 OK`:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "Bearer",
  "expires_in": 1800
}
```

| Field          | Type   | Description                                        |
| -------------- | ------ | -------------------------------------------------- |
| `access_token` | string | JWT token to use in `Authorization: Bearer` header |
| `token_type`   | string | Always `"Bearer"`                                  |
| `expires_in`   | number | Token lifetime in seconds (default 1800 = 30 min)  |

**Error responses:**

| Status | Condition                 |
| ------ | ------------------------- |
| `401`  | Invalid API key or secret |
| `401`  | API client is disabled    |

## Entity Endpoints

<Warning>All entity endpoints require authentication via `ApiAuthGuard` (JWT or API client token).</Warning>

All list endpoints return the [paginated envelope](#paginated-response-envelope).

### Developers

#### List Developers

```http
GET /api/developers
```

| Param       | Type   | Required | Default  | Constraints                                                                                           |
| ----------- | ------ | -------- | -------- | ----------------------------------------------------------------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                                                                                  |
| `limit`     | int    | no       | `20`     | 1--100                                                                                                |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `developerNumber`, `createdAt`                                              |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                                                                         |
| `nameEn`    | string | no       | --       | substring filter                                                                                      |
| `nameAr`    | string | no       | --       | substring filter                                                                                      |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`                                                                           |
| `include`   | string | no       | --       | allowed: `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats` |

**Default includes (list):** _(none)_

**Response item:**

| Field             | Type   | Always present |
| ----------------- | ------ | -------------- |
| `id`              | number | yes            |
| `nameEn`          | string | --             |
| `nameAr`          | string | --             |
| `developerNumber` | string | --             |
| `logo`            | string | --             |
| `logoDark`        | string | --             |
| `licenseUrl`      | string | --             |

#### Get Developer by ID

```http
GET /api/developers/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Allowed includes:** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Response (extends list item):**

| Field               | Type             | Notes                                                                |
| ------------------- | ---------------- | -------------------------------------------------------------------- |
| `sourceDeveloperId` | string           | always present                                                       |
| `licenseSource`     | `{ id, nameEn }` | if included                                                          |
| `licenseType`       | `{ id, nameEn }` | if included                                                          |
| `legalStatus`       | `{ id, nameEn }` | if included                                                          |
| `parentDeveloper`   | `{ id, nameEn }` | if included                                                          |
| `childDevelopers`   | array            | `[{ id, nameEn }]` -- if included                                    |
| `stats`             | object           | `{ projectsCount, masterProjectsCount }` -- only if `stats` included |

#### Search Developers

```http
GET /api/developers/search
```

Fuzzy search for developers by name using PostgreSQL `pg_trgm` similarity.

| Param   | Type   | Required | Default | Constraints  |
| ------- | ------ | -------- | ------- | ------------ |
| `q`     | string | yes      | --      | min length 1 |
| `limit` | int    | no       | `10`    | 1--50        |

<Info>
**How it works:**

1. Trims `q`, then runs `similarity()` against both `name_en` and `name_ar` columns
2. Filters rows where either similarity score > 0.15
3. Orders by the highest of the two scores (descending)
4. Returns up to `limit` results
</Info>

**Response `200 OK`:**

```json
[
  {
    "id": 42,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية",
    "developerNumber": "DEV-001",
    "logo": "https://example.com/logo.png",
    "logoDark": "https://example.com/logo-dark.png",
    "licenseUrl": "https://example.com/license.pdf",
    "similarity": 0.85
  }
]
```

<Note>All fields except `id` and `similarity` are nullable.</Note>

### Cities

#### List Cities

```http
GET /api/cities
```

| Param       | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `state`, `createdAt` |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `state.nameEn`    |
| `stateId`   | int    | no       | --       | filter by state                                |
| `include`   | string | no       | --       | allowed: `state`, `stats`                      |

**Default includes (list):** `state`

**Response item:**

| Field    | Type                   | Notes       |
| -------- | ---------------------- | ----------- |
| `id`     | number                 | always      |
| `nameEn` | string                 | --          |
| `nameAr` | string                 | --          |
| `state`  | `{ id, nameEn, code }` | if included |

#### Get City by ID

```http
GET /api/cities/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `state`, `stats`

**Allowed includes:** `state`, `stats`

**Response (extends list item):**

| Field   | Type   | Notes                                                |
| ------- | ------ | ---------------------------------------------------- |
| `stats` | object | `{ projectsCount, masterProjectsCount, areasCount }` |

### States

#### List States

```http
GET /api/states
```

| Param       | Type   | Required | Default  | Constraints                                  |
| ----------- | ------ | -------- | -------- | -------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                         |
| `limit`     | int    | no       | `20`     | 1--100                                       |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `code`, `createdAt` |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                |
| `nameEn`    | string | no       | --       | substring filter                             |
| `nameAr`    | string | no       | --       | substring filter                             |
| `code`      | string | no       | --       | exact match filter                           |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `code`          |
| `include`   | string | no       | --       | allowed: `stats`                             |

**Default includes (list):** _(none)_

**Response item:**

| Field    | Type   | Always present |
| -------- | ------ | -------------- |
| `id`     | number | yes            |
| `nameEn` | string | --             |
| `nameAr` | string | --             |
| `code`   | string | --             |

#### Get State by ID

```http
GET /api/states/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `stats`

**Allowed includes:** `stats`

**Response (extends list item):**

| Field   | Type   | Notes                                                          |
| ------- | ------ | -------------------------------------------------------------- |
| `stats` | object | `{ projectsCount, masterProjectsCount, citiesCount, areasCount }` |

### Areas

#### List Areas

```http
GET /api/areas
```

| Param       | Type   | Required | Default  | Constraints                                              |
| ----------- | ------ | -------- | -------- | -------------------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                                     |
| `limit`     | int    | no       | `20`     | 1--100                                                   |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `city`, `state`, `createdAt`   |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                            |
| `nameEn`    | string | no       | --       | substring filter                                         |
| `nameAr`    | string | no       | --       | substring filter                                         |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `city.nameEn`, `state.nameEn` |
| `cityId`    | int    | no       | --       | filter by city                                           |
| `stateId`   | int    | no       | --       | filter by state                                          |
| `include`   | string | no       | --       | allowed: `city`, `state`, `stats`                        |

**Default includes (list):** `city`, `state`

**Response item:**

| Field    | Type                   | Notes       |
| -------- | ---------------------- | ----------- |
| `id`     | number                 | always      |
| `nameEn` | string                 | --          |
| `nameAr` | string                 | --          |
| `city`   | `{ id, nameEn }`       | if included |
| `state`  | `{ id, nameEn, code }` | if included |

#### Get Area by ID

```http
GET /api/areas/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `city`, `state`, `stats`

**Allowed includes:** `city`, `state`, `stats`

**Response (extends list item):**

| Field   | Type   | Notes                                          |
| ------- | ------ | ---------------------------------------------- |
| `stats` | object | `{ projectsCount, masterProjectsCount }` |

### Projects

#### List Projects

```http
GET /api/projects
```

<AccordionGroup>
  <Accordion title="Query Parameters">
    | Param           | Type     | Required | Default  | Constraints                                                                    |
    | --------------- | -------- | -------- | -------- | ------------------------------------------------------------------------------ |
    | `page`          | int      | no       | `1`      | >= 1                                                                           |
    | `limit`         | int      | no       | `20`     | 1--100                                                                         |
    | `sortBy`        | string   | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `area`, `status`, `createdAt`                        |
    | `sortOrder`     | string   | no       | `asc`    | `asc`, `desc`                                                                  |
    | `nameEn`        | string   | no       | --       | substring filter                                                               |
    | `nameAr`        | string   | no       | --       | substring filter                                                               |
    | `search`        | string   | no       | --       | searches `nameEn`, `nameAr`, `developer.nameEn`, `area.nameEn`                 |
    | `developerId`   | int      | no       | --       | filter by developer                                                            |
    | `developerIds`  | int[]    | no       | --       | filter by multiple developers                                                  |
    | `areaId`        | int      | no       | --       | filter by area                                                                 |
    | `areaIds`       | int[]    | no       | --       | filter by multiple areas                                                       |
    | `cityId`        | int      | no       | --       | filter by city                                                                 |
    | `cityIds`       | int[]    | no       | --       | filter by multiple cities                                                      |
    | `stateId`       | int      | no       | --       | filter by state                                                                |
    | `stateIds`      | int[]    | no       | --       | filter by multiple states                                                      |
    | `status`        | string   | no       | --       | exact match (enum: `PLANNING`, `UNDER_CONSTRUCTION`, `COMPLETED`, `ON_HOLD`)   |
    | `include`       | string   | no       | --       | allowed: `developer`, `masterProject`, `area`, `city`, `state`, `buildings`, `stats` |
  </Accordion>
</AccordionGroup>

**Default includes (list):** `developer`, `area`, `city`, `state`

**Response item:**

| Field         | Type                   | Notes       |
| ------------- | ---------------------- | ----------- |
| `id`          | number                 | always      |
| `nameEn`      | string                 | --          |
| `nameAr`      | string                 | --          |
| `status`      | string                 | enum value  |
| `launchDate`  | string (ISO date)      | --          |
| `completionDate` | string (ISO date)   | --          |
| `developer`   | `{ id, nameEn }`       | if included |
| `area`        | `{ id, nameEn }`       | if included |
| `city`        | `{ id, nameEn }`       | if included |
| `state`       | `{ id, nameEn, code }` | if included |

#### Get Project by ID

```http
GET /api/projects/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `developer`, `masterProject`, `area`, `city`, `state`, `buildings`, `stats`

**Allowed includes:** `developer`, `masterProject`, `area`, `city`, `state`, `buildings`, `stats`

**Response (extends list item):**

| Field           | Type   | Notes                                                        |
| --------------- | ------ | ------------------------------------------------------------ |
| `masterProject` | object | `{ id, nameEn, nameAr }` -- if included                      |
| `buildings`     | array  | `[{ id, nameEn, nameAr, status }]` -- if included            |
| `stats`         | object | `{ buildingsCount, unitsCount, totalBuiltUpArea }` -- if included |

### Buildings

#### List Buildings

```http
GET /api/buildings
```

<AccordionGroup>
  <Accordion title="Query Parameters">
    | Param         | Type   | Required | Default  | Constraints                                                                      |
    | ------------- | ------ | -------- | -------- | -------------------------------------------------------------------------------- |
    | `page`        | int    | no       | `1`      | >= 1                                                                             |
    | `limit`       | int    | no       | `20`     | 1--100                                                                           |
    | `sortBy`      | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `project`, `status`, `floors`, `createdAt`             |
    | `sortOrder`   | string | no       | `asc`    | `asc`, `desc`                                                                    |
    | `nameEn`      | string | no       | --       | substring filter                                                                 |
    | `nameAr`      | string | no       | --       | substring filter                                                                 |
    | `search`      | string | no       | --       | searches `nameEn`, `nameAr`, `project.nameEn`                                   |
    | `projectId`   | int    | no       | --       | filter by project                                                                |
    | `projectIds`  | int[]  | no       | --       | filter by multiple projects                                                      |
    | `status`      | string | no       | --       | exact match (enum: `PLANNING`, `UNDER_CONSTRUCTION`, `COMPLETED`, `ON_HOLD`)     |
    | `include`     | string | no       | --       | allowed: `project`, `developer`, `area`, `city`, `state`, `units`, `buildingAreas`, `stats` |
  </Accordion>
</AccordionGroup>

**Default includes (list):** `project`, `developer`, `area`, `city`, `state`

**Response item:**

| Field         | Type                   | Notes       |
| ------------- | ---------------------- | ----------- |
| `id`          | number                 | always      |
| `nameEn`      | string                 | --          |
| `nameAr`      | string                 | --          |
| `status`      | string                 | enum value  |
| `floors`      | number                 | --          |
| `project`     | `{ id, nameEn }`       | if included |
| `developer`   | `{ id, nameEn }`       | if included |
| `area`        | `{ id, nameEn }`       | if included |
| `city`        | `{ id, nameEn }`       | if included |
| `state`       | `{ id, nameEn, code }` | if included |

#### Get Building by ID

```http
GET /api/buildings/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `project`, `developer`, `area`, `city`, `state`, `units`, `buildingAreas`, `stats`

**Allowed includes:** `project`, `developer`, `area`, `city`, `state`, `units`, `buildingAreas`, `stats`

**Response (extends list item):**

| Field           | Type   | Notes                                                          |
| --------------- | ------ | -------------------------------------------------------------- |
| `units`         | array  | `[{ id, unitNumber, unitType, ... }]` -- if included           |
| `buildingAreas` | array  | `[{ unitType, count, totalBuiltUpArea }]` -- if included       |
| `stats`         | object | `{ unitsCount, totalBuiltUpArea, avgPricePerSqFt }` -- if included |

### Units

#### List Units

```http
GET /api/units
```

<AccordionGroup>
  <Accordion title="Query Parameters">
    | Param         | Type   | Required | Default    | Constraints                                                                                |
    | ------------- | ------ | -------- | ---------- | ------------------------------------------------------------------------------------------ |
    | `page`        | int    | no       | `1`        | >= 1                                                                                       |
    | `limit`       | int    | no       | `20`       | 1--100                                                                                     |
    | `sortBy`      | string | no       | `unitNumber` | `id`, `unitNumber`, `unitType`, `builtUpArea`, `price`, `building`, `createdAt`          |
    | `sortOrder`   | string | no       | `asc`      | `asc`, `desc`                                                                              |
    | `unitNumber`  | string | no       | --         | substring filter                                                                           |
    | `search`      | string | no       | --         | searches `unitNumber`, `building.nameEn`, `project.nameEn`                                |
    | `buildingId`  | int    | no       | --         | filter by building                                                                         |
    | `buildingIds` | int[]  | no       | --         | filter by multiple buildings                                                               |
    | `projectId`   | int    | no       | --         | filter by project                                                                          |
    | `projectIds`  | int[]  | no       | --         | filter by multiple projects                                                                |
    | `unitType`    | string | no       | --         | exact match (enum: `STUDIO`, `1BR`, `2BR`, `3BR`, `4BR`, `5BR`, `PENTHOUSE`, `TOWNHOUSE`) |
    | `minPrice`    | int    | no       | --         | minimum price filter                                                                       |
    | `maxPrice`    | int    | no       | --         | maximum price filter                                                                       |
    | `minArea`     | int    | no       | --         | minimum built-up area filter                                                              |
    | `maxArea`     | int    | no       | --         | maximum built-up area filter                                                              |
    | `include`     | string | no       | --         | allowed: `building`, `project`, `developer`, `area`, `city`, `state`                      |
  </Accordion>
</AccordionGroup>

**Default includes (list):** `building`, `project`, `developer`, `area`, `city`, `state`

**Response item:**

| Field         | Type                   | Notes       |
| ------------- | ---------------------- | ----------- |
| `id`          | number                 | always      |
| `unitNumber`  | string                 | --          |
| `unitType`    | string                 | enum value  |
| `builtUpArea` | number                 | --          |
| `price`       | number                 | --          |
| `pricePerSqFt` | number                | calculated  |
| `building`    | `{ id, nameEn }`       | if included |
| `project`     | `{ id, nameEn }`       | if included |
| `developer`   | `{ id, nameEn }`       | if included |
| `area`        | `{ id, nameEn }`       | if included |
| `city`        | `{ id, nameEn }`       | if included |
| `state`       | `{ id, nameEn, code }` | if included |

#### Get Unit by ID

```http
GET /api/units/:id
```

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes (detail):** `building`, `project`, `developer`, `area`, `city`, `state`

**Allowed includes:** `building`, `project`, `developer`, `area`, `city`, `state`

**Response:** Same as list item with all scalar fields always present.

## Lookup Endpoints

Lightweight endpoints for dropdown/search functionality.

### Developer Lookups

#### `GET /api/lookups/developers`

Simple list of developers for dropdowns.

| Param    | Type   | Required | Default | Constraints  |
| -------- | ------ | -------- | ------- | ------------ |
| `search` | string | no       | --      | substring on names |
| `limit`  | int    | no       | `50`    | 1--200       |

**Response:**

```json
[
  {
    "id": 1,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية"
  }
]
```

### Location Lookups

#### `GET /api/lookups/states`

| Param    | Type   | Required | Default | Constraints |
| -------- | ------ | -------- | ------- | ----------- |
| `search` | string | no       | --      | substring   |
| `limit`  | int    | no       | `50`    | 1--200      |

**Response:**

```json
[
  {
    "id": 1,
    "nameEn": "Dubai",
    "nameAr": "دبي",
    "code": "DU"
  }
]
```

#### `GET /api/lookups/cities`

| Param     | Type   | Required | Default | Constraints |
| --------- | ------ | -------- | ------- | ----------- |
| `search`  | string | no       | --      | substring   |
| `stateId` | int    | no       | --      | filter      |
| `limit`   | int    | no       | `50`    | 1--200      |

**Response:**

```json
[
  {
    "id": 1,
    "nameEn": "Dubai",
    "nameAr": "دبي",
    "stateId": 1
  }
]
```

#### `GET /api/lookups/areas`

| Param     | Type   | Required | Default | Constraints |
| --------- | ------ | -------- | ------- | ----------- |
| `search`  | string | no       | --      | substring   |
| `cityId`  | int    | no       | --      | filter      |
| `stateId` | int    | no       | --      | filter      |
| `limit`   | int    | no       | `50`    | 1--200      |

**Response:**

```json
[
  {
    "id": 1,
    "nameEn": "Downtown Dubai",
    "nameAr": "وسط مدينة دبي",
    "cityId": 1,
    "stateId": 1
  }
]
```

## Direct Entity Mutations

<Warning>Requires `super` access level for API clients.</Warning>

### Projects

#### Update Project

```http
PATCH /api/projects/:id
```

| Field           | Type              | Required | Constraints                                      |
| --------------- | ----------------- | -------- | ------------------------------------------------ |
| `nameEn`        | string            | no       | --                                               |
| `nameAr`        | string            | no       | --                                               |
| `status`        | string            | no       | enum: `PLANNING`, `UNDER_CONSTRUCTION`, etc.     |
| `launchDate`    | string (ISO date) | no       | --                                               |
| `completionDate`| string (ISO date) | no       | --                                               |

**Response:** Updated project with default includes.

### Buildings

#### Update Building

```http
PATCH /api/buildings/:id
```

| Field     | Type   | Required | Constraints                                  |
| --------- | ------ | -------- | -------------------------------------------- |
| `nameEn`  | string | no       | --                                           |
| `nameAr`  | string | no       | --                                           |
| `status`  | string | no       | enum: `PLANNING`, `UNDER_CONSTRUCTION`, etc. |
| `floors`  | number | no       | positive integer                             |

**Response:** Updated building with default includes.

### Units

#### Create Unit

```http
POST /api/units
```

| Field        | Type   | Required | Constraints                                           |
| ------------ | ------ | -------- | ----------------------------------------------------- |
| `unitNumber` | string | yes      | unique within building                                |
| `unitType`   | string | yes      | enum: `STUDIO`, `1BR`, `2BR`, etc.                    |
| `builtUpArea`| number | yes      | positive                                              |
| `price`      | number | yes      | positive                                              |
| `buildingId` | number | yes      | must exist                                            |

**Response:** Created unit with default includes.

#### Update Unit

```http
PATCH /api/units/:id
```

| Field        | Type   | Required | Constraints                        |
| ------------ | ------ | -------- | ---------------------------------- |
| `unitNumber` | string | no       | unique within building             |
| `unitType`   | string | no       | enum: `STUDIO`, `1BR`, `2BR`, etc. |
| `builtUpArea`| number | no       | positive                           |
| `price`      | number | no       | positive                           |

**Response:** Updated unit with default includes.

#### Delete Unit

```http
DELETE /api/units/:id
```

**Response:** `204 No Content`

## Change Requests

Bulk operations system allowing multiple entity changes in a single request with approval workflow.

### Submit Change Request

```http
POST /api/change-requests
```

<CodeGroup>
```json Example Request
{
  "title": "Q4 Portfolio Update",
  "description": "Update project statuses and add new units for Q4 launch",
  "projects": [
    {
      "action": "update",
      "id": 123,
      "data": {
        "status": "COMPLETED",
        "completionDate": "2024-12-31"
      }
    }
  ],
  "buildings": [
    {
      "action": "update", 
      "id": 456,
      "data": {
        "floors": 25
      }
    }
  ],
  "units": [
    {
      "action": "create",
      "data": {
        "unitNumber": "1001",
        "unitType": "2BR", 
        "builtUpArea": 1200,
        "price": 850000,
        "buildingId": 456
      }
    },
    {
      "action": "update",
      "id": 789,
      "data": {
        "price": 900000
      }
    }
  ]
}
```

```javascript JavaScript
const changeRequest = await fetch('/api/change-requests', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    title: 'Q4 Portfolio Update',
    description: 'Update project statuses and add new units',
    projects: [{
      action: 'update',
      id: 123,
      data: { status: 'COMPLETED' }
    }],
    units: [{
      action: 'create',
      data: {
        unitNumber: '1001',
        unitType: '2BR',
        builtUpArea: 1200,
        price: 850000,
        buildingId: 456
      }
    }]
  })
});
```
</CodeGroup>

**Request body:**

| Field         | Type   | Required | Constraints |
| ------------- | ------ | -------- | ----------- |
| `title`       | string | yes      | non-empty   |
| `description` | string | no       | --          |
| `projects`    | array  | no       | operations  |
| `buildings`   | array  | no       | operations  |
| `units`       | array  | no       | operations  |

**Operation format:**

| Field    | Type   | Required | Notes                        |
| -------- | ------ | -------- | ---------------------------- |
| `action` | string | yes      | `create`, `update`, `delete` |
| `id`     | number | no       | required for update/delete   |
| `data`   | object | no       | required for create/update   |

**Response `201 Created`:**

```json
{
  "id": 42,
  "title": "Q4 Portfolio Update",
  "description": "Update project statuses...", 
  "status": "PENDING",
  "submittedBy": "api-client-name",
  "submittedAt": "2024-01-15T10:00:00Z",
  "totalOperations": 3,
  "operations": {
    "projects": 1,
    "buildings": 1, 
    "units": 1
  }
}
```

### List Change Requests

```http
GET /api/change-requests
```

| Param       | Type   | Required | Default      | Constraints                              |
| ----------- | ------ | -------- | ------------ | ---------------------------------------- |
| `page`      | int    | no       | `1`          | >= 1                                     |
| `limit`     | int    | no       | `20`         | 1--100                                   |
| `sortBy`    | string | no       | `submittedAt`| `id`, `title`, `status`, `submittedAt`   |
| `sortOrder` | string | no       | `desc`       | `asc`, `desc`                            |
| `status`    | string | no       | --           | `PENDING`, `APPROVED`, `REJECTED`        |
| `submittedBy` | string | no     | --           | substring filter                         |

**Response:**

```json
{
  "data": [
    {
      "id": 42,
      "title": "Q4 Portfolio Update",
      "status": "PENDING",
      "submittedBy": "api-client-name",
      "submittedAt": "2024-01-15T10:00:00Z",
      "totalOperations": 3
    }
  ],
  "total": 1,
  "page": 1,
  "limit": 20
}
```

### Get Change Request Details

```http
GET /api/change-requests/:id
```

**Response:** Full change request with all operations included.

## Market Analytics

### Price Trends

```http
GET /api/analytics/price-trends
```

Analyze price trends across different dimensions.

<AccordionGroup>
  <Accordion title="Query Parameters">
    | Param         | Type     | Required | Default | Constraints                                    |
    | ------------- | -------- | -------- | ------- | ---------------------------------------------- |
    | `groupBy`     | string   | no       | `month` | `month`, `quarter`, `year`                     |
    | `startDate`   | string   | no       | --      | ISO date (YYYY-MM-DD)                          |
    | `endDate`     | string   | no       | --      | ISO date (YYYY-MM-DD)                          |
    | `unitType`    | string   | no       | --      | enum: `STUDIO`, `1BR`, `2BR`, etc.             |
    | `areaIds`     | int[]    | no       | --      | filter by areas                                |
    | `cityIds`     | int[]    | no       | --      | filter by cities                               |
    | `stateIds`    | int[]    | no       | --      | filter by states                               |
    | `developerIds`| int[]    | no       | --      | filter by developers                           |
  </Accordion>
</AccordionGroup>

**Response:**

```json
{
  "periods": [
    {
      "period": "2024-01",
      "avgPrice": 875000,
      "avgPricePerSqFt": 950,
      "medianPrice": 825000,
      "unitsCount": 142,
      "minPrice": 650000,
      "maxPrice": 1200000
    }
  ],
  "summary": {
    "totalPeriods": 12,
    "overallAvgPrice": 862000,
    "overallAvgPricePerSqFt": 945,
    "priceChangePercent": 8.5,
    "trendDirection": "up"
  }
}
```

### Market Comparison

```http
GET /api/analytics/market-comparison
```

Compare market metrics across different areas or developers.

| Param      | Type   | Required | Default | Constraints                  |
| ---------- | ------ | -------- | ------- | ---------------------------- |
| `compareBy`| string | yes      | --      | `area`, `city`, `state`, `developer` |
| `ids`      | int[]  | yes      | --      | entities to compare (2-10)   |
| `unitType` | string | no       | --      | enum: `STUDIO`, `1BR`, etc.  |

**Response:**

```json
{
  "comparison": [
    {
      "id": 1,
      "name": "Downtown Dubai",
      "avgPrice": 1250000,
      "avgPricePerSqFt": 1100,
      "unitsCount": 245,
      "projectsCount": 8,
      "priceRange": {
        "min": 850000,
        "max": 2100000
      }
    }
  ],
  "insights": [
    "Downtown Dubai has 23% higher average price than Dubai Marina",
    "Business Bay offers the best value at 890 AED/sqft"
  ]
}
```

### Inventory Analysis

```http
GET /api/analytics/inventory
```

Analyze unit type distribution and availability.

| Param         | Type   | Required | Default | Constraints                |
| ------------- | ------ | -------- | ------- | -------------------------- |
| `areaIds`     | int[]  | no       | --      | filter by areas            |
| `cityIds`     | int[]  | no       | --      | filter by cities           |
| `stateIds`    | int[]  | no       | --      | filter by states           |
| `developerIds`| int[]  | no       | --      | filter by developers       |

**Response:**

```json
{
  "byUnitType": [
    {
      "unitType": "1BR",
      "count": 342,
      "percentage": 28.5,
      "avgPrice": 785000,
      "avgArea": 650
    }
  ],
  "byPriceRange": [
    {
      "range": "500K-750K",
      "count": 156,
      "percentage": 18.2
    }
  ],
  "summary": {
    "totalUnits": 1205,
    "avgPrice": 892000,
    "avgArea": 875,
    "mostCommonType": "1BR"
  }
}
```

### Developer Performance

```http
GET /api/analytics/developer-performance
```

Analyze developer metrics and rankings.

| Param     | Type   | Required | Default | Constraints |
| --------- | ------ | -------- | ------- | ----------- |
| `metric`  | string | no       | `units` | `units`, `projects`, `avgPrice`, `volume` |
| `period`  | string | no       | `year`  | `month`, `quarter`, `year`, `all` |
| `limit`   | int    | no       | `20`    | 1--100      |

**Response:**

```json
{
  "rankings": [
    {
      "developerId": 1,
      "developerName": "Emaar Properties", 
      "unitsCount": 1842,
      "projectsCount": 23,
      "avgPrice": 1150000,
      "totalVolume": 2118300000,
      "marketShare": 15.2
    }
  ],
  "insights": [
    "Emaar leads with 15.2% market share",
    "Average price increased 12% vs last period"
  ]
}
```

## Unit Evaluation (AI Valuation)

AI-powered property valuation based on comparable sales and market data.

### Request Unit Evaluation

```http
POST /api/unit-evaluation
```

<CodeGroup>
```json Example Request
{
  "unitType": "2BR",
  "builtUpArea": 1200,
  "areaId": 15,
  "buildingId": 456,
  "features": {
    "floor": 12,
    "view": "sea",
    "parking": 1,
    "balcony": true,
    "furnished": false
  }
}
```

```javascript JavaScript
const evaluation = await fetch('/api/unit-evaluation', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${token}`,
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    unitType: '2BR',
    builtUpArea: 1200,
    areaId: 15,
    features: {
      floor: 12,
      view: 'sea',
      parking: 1
    }
  })
});

const result = await evaluation.json();
```
</CodeGroup>

**Request body:**

| Field        | Type   | Required | Constraints                        |
| ------------ | ------ | -------- | ---------------------------------- |
| `unitType`   | string | yes      | enum: `STUDIO`, `1BR`, `2BR`, etc. |
| `builtUpArea`| number | yes      | positive (square feet)             |
| `areaId`     | number | no       | must exist                         |
| `buildingId` | number | no       | must exist (overrides areaId)      |
| `features`   | object | no       | additional property features       |

**Features object:**

| Field       | Type    | Required | Notes                               |
| ----------- | ------- | -------- | ----------------------------------- |
| `floor`     | number  | no       | floor number                        |
| `view`      | string  | no       | `sea`, `city`, `garden`, `street`   |
| `parking`   | number  | no       | number of parking spaces            |
| `balcony`   | boolean | no       | has balcony                         |
| `furnished` | boolean | no       | is furnished                        |

**Response `200 OK`:**

```json
{
  "estimatedPrice": 1125000,
  "priceRange": {
    "min": 1050000,
    "max": 1200000
  },
  "pricePerSqFt": 937,
  "confidence": 0.85,
  "comparableUnits": [
    {
      "id": 789,
      "unitNumber": "901",
      "price": 1100000,
      "builtUpArea": 1180,
      "similarity": 0.92,
      "building": "Marina Heights",
      "project": "Dubai Marina Complex"
    }
  ],
  "marketInsights": [
    "2BR units in this area average 950 AED/sqft",
    "Sea view adds approximately 15% premium",
    "High floor (12+) adds 8-12% premium"
  ],
  "methodology": {
    "algorithm": "weighted_comparison",
    "factorsConsidered": [
      "location", "unit_type", "area_size", "building_quality", "view", "floor"
    ],
    "comparableUnitsCount": 23,
    "dataFreshness": "updated_today"
  }
}
```

**Response fields:**

| Field             | Type   | Description                                    |
| ----------------- | ------ | ---------------------------------------------- |
| `estimatedPrice`  | number | AI-estimated market value                      |
| `priceRange`      | object | Confidence interval (min/max)                 |
| `pricePerSqFt`    | number | Calculated price per square foot              |
| `confidence`      | number | Algorithm confidence score (0-1)              |
| `comparableUnits` | array  | Similar units used for comparison              |
| `marketInsights`  | array  | Human-readable insights about the valuation   |
| `methodology`     | object | Details about the valuation approach          |

**Error responses:**

| Status | Condition                                |
| ------ | ---------------------------------------- |
| `400`  | Invalid unit type or area                |
| `404`  | Area/building not found                  |
| `422`  | Insufficient comparable data for valuation |

<Tip>
The AI evaluation considers multiple factors including location, recent sales, building quality, floor level, view type, and market trends. For best results, provide as much detail as possible in the `features` object.
</Tip>

## Appendix A: Enum Reference

### Project Status
- `PLANNING` - Project in planning phase
- `UNDER_CONSTRUCTION` - Currently under construction  
- `COMPLETED` - Construction completed
- `ON_HOLD` - Project temporarily halted

### Building Status
- `PLANNING` - Building in planning phase
- `UNDER_CONSTRUCTION` - Currently under construction
- `COMPLETED` - Construction completed  
- `ON_HOLD` - Construction temporarily halted

### Unit Types
- `STUDIO` - Studio apartment
- `1BR` - One bedroom
- `2BR` - Two bedroom
- `3BR` - Three bedroom
- `4BR` - Four bedroom
- `5BR` - Five bedroom
- `PENTHOUSE` - Penthouse unit
- `TOWNHOUSE` - Townhouse

### Change Request Status
- `PENDING` - Awaiting admin review
- `APPROVED` - Approved and applied
- `REJECTED` - Rejected by admin

### API Client Access Levels
- `basic` - Read-only access
- `super` - Read-write access

## Appendix B: Entity Relationships

<CardGroup cols={2}>
  <Card title="Geographic Hierarchy" icon="map">
    State → City → Area → Project → Building → Unit
  </Card>
  <Card title="Development Hierarchy" icon="building">
    Developer → Master Project → Project → Building → Unit
  </Card>
</CardGroup>

### Key Relationships

<Steps>
  <Step title="States contain Cities">
    Each city belongs to exactly one state
  </Step>
  <Step title="Cities contain Areas">
    Each area belongs to exactly one city
  </Step>
  <Step title="Areas contain Projects">
    Each project belongs to exactly one area
  </Step>
  <Step title="Projects contain Buildings">
    Each building belongs to exactly one project
  </Step>
  <Step title="Buildings contain Units">
    Each unit belongs to exactly one building
  </Step>
</Steps>

### Additional Relationships

- **Developers** can have parent-child relationships (corporate structure)
- **Master Projects** can group multiple related projects
- **Projects** are developed by exactly one developer
- **License sources/types** categorize developers by regulatory status

<Note>
All entities support soft deletion, meaning deleted records are hidden from API results but preserved in the database for audit purposes.
</Note>