---
title: "PropWise Labs API Reference"
description: "Complete API reference for external consumers including authentication, entity endpoints, lookups, mutations, change requests, market analytics, and AI valuation"
---

# PropWise Labs API Reference

Complete API reference for external consumers (non-admin endpoints).

## Overview

### Base URL

All endpoints are prefixed with `/api`. Example:

```
https://your-domain.com/api/projects
```

### Authentication

The API supports two authentication methods:

<Tabs>
  <Tab title="API Client Token">
    **For external consumers:**
    
    1. Obtain an API key and secret from an admin
    2. Exchange them for a short-lived Bearer token via `POST /api/auth/token`
    3. Include the token on every request: `Authorization: Bearer <token>`
  </Tab>
  <Tab title="JWT Token">
    **For internal/dashboard users:**
    
    - Obtained via `POST /api/auth/login` (admin dashboard)
    - Sent as `Authorization: Bearer <token>` or via the `access_token` httpOnly cookie
  </Tab>
</Tabs>

<Note>
Both methods are accepted on all external-facing endpoints. The API tries JWT first, then falls back to API key token.
</Note>

**Access levels for API clients:**

| Level   | Permissions                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------ |
| `basic` | Read-only access to all entity, lookup, analytics, and change-request endpoints                        |
| `super` | All of `basic` plus direct entity mutations (PATCH projects, PATCH buildings, POST/PATCH/DELETE units) |

### Paginated Response Envelope

All list endpoints return:

```json
{
  "data": [ ... ],
  "total": 1234,
  "page": 1,
  "limit": 20
}
```

Standard pagination query parameters:

| Param       | Type   | Default        | Constraints        |
| ----------- | ------ | -------------- | ------------------ |
| `page`      | int    | `1`            | >= 1               |
| `limit`     | int    | `20`           | 1--100             |
| `sortBy`    | string | _(per-entity)_ | allowlisted fields |
| `sortOrder` | string | `asc`          | `asc`, `desc`      |

### Configurable Includes

<Info>
Most entity GET endpoints support an optional `include` query parameter that controls which relations and computed fields are returned.
</Info>

| Value           | Behavior                                          |
| --------------- | ------------------------------------------------- |
| _(omitted)_     | Default relations populated (backward-compatible) |
| `none`          | No relations or stats -- only scalar fields       |
| `all`           | All allowed relations and stats                   |
| `field1,field2` | Only the specified relations/stats                |

Invalid values return `400` with the list of allowed options. Virtual includes like `stats` and `buildingAreas` control expensive aggregate queries rather than ORM relations.

### Error Response Format

<CodeGroup>
```json Standard errors
{
  "statusCode": 404,
  "message": "Project not found",
  "error": "Not Found"
}
```

```json Validation errors (400)
{
  "statusCode": 400,
  "message": [
    "limit must not be greater than 100",
    "sortBy must be one of the following values: id, name, area, status, createdAt"
  ],
  "error": "Bad Request"
}
```

```json Change request validation errors (400)
{
  "message": "Validation failed",
  "errors": [{ "path": "projects[0]", "field": "name", "message": "name should not be empty" }]
}
```
</CodeGroup>

| Status | Meaning                                                              |
| ------ | -------------------------------------------------------------------- |
| `400`  | Validation error, bad request                                        |
| `401`  | Missing or invalid authentication                                    |
| `403`  | Insufficient permissions (e.g. `basic` client attempting a mutation) |
| `404`  | Entity not found                                                     |
| `409`  | Conflict (stale update in change requests)                           |

### Global Request Rules

<Check>
- All request bodies are JSON (`Content-Type: application/json`)
- Unknown fields in request bodies and query params are rejected (`forbidNonWhitelisted`)
- Query params are auto-coerced to their declared types (e.g. `"1"` becomes `1` for int fields)
- Comma-separated ID lists (e.g. `areaIds=1,2,3`) are parsed into integer arrays
- Soft-deleted records are excluded from all results
</Check>

## Authentication

### Exchange API Credentials for Token

<CodeGroup>
```bash cURL
curl -X POST https://your-domain.com/api/auth/token \
  -H "Content-Type: application/json" \
  -d '{
    "apiKey": "your-api-key",
    "apiSecret": "your-api-secret"
  }'
```

```javascript JavaScript
const response = await fetch('/api/auth/token', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    apiKey: 'your-api-key',
    apiSecret: 'your-api-secret'
  })
});
const data = await response.json();
```

```python Python
import requests

response = requests.post('/api/auth/token', json={
    'apiKey': 'your-api-key',
    'apiSecret': 'your-api-secret'
})
data = response.json()
```
</CodeGroup>

**Request body:**

| Field       | Type   | Required | Constraints |
| ----------- | ------ | -------- | ----------- |
| `apiKey`    | string | yes      | non-empty   |
| `apiSecret` | string | yes      | non-empty   |

**Response `200 OK`:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "Bearer",
  "expires_in": 1800
}
```

| Field          | Type   | Description                                        |
| -------------- | ------ | -------------------------------------------------- |
| `access_token` | string | JWT token to use in `Authorization: Bearer` header |
| `token_type`   | string | Always `"Bearer"`                                  |
| `expires_in`   | number | Token lifetime in seconds (default 1800 = 30 min)  |

<Warning>
**Error responses:**
- `401`: Invalid API key or secret
- `401`: API client is disabled
</Warning>

## Entity Endpoints

<Note>
All entity endpoints require authentication via `ApiAuthGuard` (JWT or API client token).
All list endpoints return the paginated envelope format.
</Note>

### Developers

#### List Developers

**GET** `/api/developers`

| Param       | Type   | Required | Default  | Constraints                                                                                           |
| ----------- | ------ | -------- | -------- | ----------------------------------------------------------------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                                                                                  |
| `limit`     | int    | no       | `20`     | 1--100                                                                                                |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `developerNumber`, `createdAt`                                              |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                                                                         |
| `nameEn`    | string | no       | --       | substring filter                                                                                      |
| `nameAr`    | string | no       | --       | substring filter                                                                                      |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`                                                                           |
| `include`   | string | no       | --       | allowed: `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats` |

**Response item:**

| Field             | Type   | Always present |
| ----------------- | ------ | -------------- |
| `id`              | number | yes            |
| `nameEn`          | string | --             |
| `nameAr`          | string | --             |
| `developerNumber` | string | --             |
| `logo`            | string | --             |
| `logoDark`        | string | --             |
| `licenseUrl`      | string | --             |

#### Get Developer Details

**GET** `/api/developers/:id`

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes:** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Response (extends list item):**

| Field               | Type             | Notes                                                                |
| ------------------- | ---------------- | -------------------------------------------------------------------- |
| `sourceDeveloperId` | string           | always present                                                       |
| `licenseSource`     | `{ id, nameEn }` | if included                                                          |
| `licenseType`       | `{ id, nameEn }` | if included                                                          |
| `legalStatus`       | `{ id, nameEn }` | if included                                                          |
| `parentDeveloper`   | `{ id, nameEn }` | if included                                                          |
| `childDevelopers`   | array            | `[{ id, nameEn }]` -- if included                                    |
| `stats`             | object           | `{ projectsCount, masterProjectsCount }` -- only if `stats` included |

#### Search Developers

**GET** `/api/developers/search`

Fuzzy search for developers by name using PostgreSQL `pg_trgm` similarity.

| Param   | Type   | Required | Default | Constraints  |
| ------- | ------ | -------- | ------- | ------------ |
| `q`     | string | yes      | --      | min length 1 |
| `limit` | int    | no       | `10`    | 1--50        |

<Info>
**How it works:**
1. Trims `q`, then runs `similarity()` against both `name_en` and `name_ar` columns
2. Filters rows where either similarity score > 0.15
3. Orders by the highest of the two scores (descending)
4. Returns up to `limit` results
</Info>

**Response:**

```json
[
  {
    "id": 42,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية",
    "developerNumber": "DEV-001",
    "logo": "https://example.com/logo.png",
    "logoDark": "https://example.com/logo-dark.png",
    "licenseUrl": "https://example.com/license.pdf",
    "similarity": 0.85
  }
]
```

### Cities

#### List Cities

**GET** `/api/cities`

| Param       | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `state`, `createdAt` |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `state.nameEn`    |
| `stateId`   | int    | no       | --       | filter by state                                |
| `include`   | string | no       | --       | allowed: `state`, `stats`                      |

**Default includes:** `state`

**Response item:**

| Field    | Type                   | Notes                           |
| -------- | ---------------------- | ------------------------------- |
| `id`     | number                 | yes                             |
| `nameEn` | string                 | --                              |
| `nameAr` | string                 | --                              |
| `state`  | `{ id, nameEn, code }` | if included (default for list)  |
| `stats`  | object                 | `{ projectsCount }` if included |

### Projects

<AccordionGroup>
<Accordion title="List Projects - GET /api/projects">
**Parameters:**

| Param          | Type   | Required | Default    | Constraints                                                               |
| -------------- | ------ | -------- | ---------- | ------------------------------------------------------------------------- |
| `page`         | int    | no       | `1`        | >= 1                                                                      |
| `limit`        | int    | no       | `20`       | 1--100                                                                    |
| `sortBy`       | string | no       | `nameEn`   | `id`, `nameEn`, `nameAr`, `area`, `status`, `launchDate`, `createdAt`     |
| `sortOrder`    | string | no       | `asc`      | `asc`, `desc`                                                             |
| `nameEn`       | string | no       | --         | substring filter                                                          |
| `nameAr`       | string | no       | --         | substring filter                                                          |
| `search`       | string | no       | --         | searches across multiple fields                                           |
| `developerId`  | int    | no       | --         | filter by developer                                                       |
| `cityId`       | int    | no       | --         | filter by city                                                            |
| `areaIds`      | string | no       | --         | comma-separated area IDs                                                  |
| `status`       | string | no       | --         | enum value                                                                |
| `include`      | string | no       | --         | allowed: `developer`, `city`, `areas`, `images`, `buildingAreas`, `stats` |

**Default includes:** `developer`, `city`, `areas`, `images`
</Accordion>

<Accordion title="Get Project Details - GET /api/projects/:id">
**Parameters:**

| Param     | Type           | Required |
| --------- | -------------- | -------- |
| `id`      | int (path)     | yes      |
| `include` | string (query) | no       |

**Default includes:** `developer`, `city`, `areas`, `images`, `buildingAreas`, `stats`
</Accordion>
</AccordionGroup>

### Buildings

#### List Buildings

**GET** `/api/buildings`

Similar structure to projects with building-specific fields and filters.

#### Get Building Details  

**GET** `/api/buildings/:id`

Returns detailed building information with configurable includes.

### Units

<Tabs>
<Tab title="List Units">
**GET** `/api/units`

| Param         | Type   | Required | Default    | Constraints                                                    |
| ------------- | ------ | -------- | ---------- | -------------------------------------------------------------- |
| `page`        | int    | no       | `1`        | >= 1                                                           |
| `limit`       | int    | no       | `20`       | 1--100                                                         |
| `sortBy`      | string | no       | `id`       | `id`, `unitNumber`, `area`, `price`, `pricePerSqft`, `status`  |
| `sortOrder`   | string | no       | `asc`      | `asc`, `desc`                                                  |
| `unitNumber`  | string | no       | --         | substring filter                                               |
| `buildingId`  | int    | no       | --         | filter by building                                             |
| `projectId`   | int    | no       | --         | filter by project                                              |
| `unitTypeId`  | int    | no       | --         | filter by unit type                                            |
| `status`      | string | no       | --         | enum value                                                     |
| `include`     | string | no       | --         | allowed: `building`, `project`, `unitType`, `images`, `stats`  |

**Default includes:** `building`, `project`, `unitType`, `images`
</Tab>

<Tab title="Get Unit Details">
**GET** `/api/units/:id`

Returns comprehensive unit information including all related entities and computed statistics.

**Default includes:** `building`, `project`, `unitType`, `images`, `stats`
</Tab>
</Tabs>

## Lookup Endpoints

<Info>
Lookup endpoints provide reference data for dropdowns, filters, and validation. All require authentication.
</Info>

### States

**GET** `/api/lookups/states`

Returns all states/provinces with basic information:

```json
[
  {
    "id": 1,
    "nameEn": "Dubai",
    "nameAr": "دبي",
    "code": "DU"
  }
]
```

### Unit Types

**GET** `/api/lookups/unit-types`

Returns available unit types (studio, 1BR, 2BR, etc.):

```json
[
  {
    "id": 1,
    "nameEn": "Studio",
    "nameAr": "استوديو",
    "shortName": "ST"
  }
]
```

### Additional Lookups

<CardGroup cols={2}>
<Card title="Areas" href="#areas">
Get areas within specific cities or states
</Card>
<Card title="Project Statuses" href="#project-statuses">  
Available project status values
</Card>
<Card title="Unit Statuses" href="#unit-statuses">
Available unit status values  
</Card>
<Card title="Developer Sources" href="#developer-sources">
License and legal status references
</Card>
</CardGroup>

## Direct Entity Mutations

<Warning>
Direct mutations require API clients with `super` access level. `basic` level clients will receive `403 Forbidden`.
</Warning>

### Update Project

**PATCH** `/api/projects/:id`

<Steps>
<Step title="Prepare Request">
Include only the fields you want to update in the request body.
</Step>

<Step title="Send Update">
```json
{
  "nameEn": "Updated Project Name",
  "description": "New project description",
  "launchDate": "2024-01-15"
}
```
</Step>

<Step title="Handle Response">
Returns the updated project with all current field values.
</Step>
</Steps>

### Update Building

**PATCH** `/api/buildings/:id`

Similar to project updates but with building-specific fields.

### Unit Operations

<Tabs>
<Tab title="Create Unit">
**POST** `/api/units`

```json
{
  "unitNumber": "A-101",
  "buildingId": 123,
  "unitTypeId": 2,
  "area": 850.5,
  "price": 750000,
  "status": "available"
}
```
</Tab>

<Tab title="Update Unit">
**PATCH** `/api/units/:id`

Partial updates supported. Only include changed fields.
</Tab>

<Tab title="Delete Unit">
**DELETE** `/api/units/:id`

Soft-delete operation. Unit will be excluded from future queries.
</Tab>
</Tabs>

## Change Requests

<Note>
Change requests provide a workflow-based approach to data modifications with approval processes and audit trails.
</Note>

### Submit Change Request

**POST** `/api/change-requests`

```json
{
  "requestType": "bulk_update",
  "description": "Update pricing for Building A units",
  "changes": {
    "units": [
      {
        "id": 1001,
        "price": 825000,
        "pricePerSqft": 970.59
      },
      {
        "id": 1002,
        "price": 750000,
        "pricePerSqft": 925.00
      }
    ]
  }
}
```

**Request Types:**

| Type          | Description                           |
| ------------- | ------------------------------------- |
| `bulk_update` | Update multiple entities              |
| `data_import` | Import new entities from external source |
| `correction`  | Fix data errors or inconsistencies    |
| `enhancement` | Add new fields or improve existing data |

### List Change Requests

**GET** `/api/change-requests`

| Param      | Type   | Default | Constraints                                          |
| ---------- | ------ | ------- | ---------------------------------------------------- |
| `status`   | string | --      | `pending`, `approved`, `rejected`, `in_review`       |
| `type`     | string | --      | request type filter                                  |
| `fromDate` | string | --      | ISO date string (YYYY-MM-DD)                        |
| `toDate`   | string | --      | ISO date string (YYYY-MM-DD)                        |

### Get Change Request Details

**GET** `/api/change-requests/:id`

Returns detailed information including:
- Original request data
- Current status and approval workflow
- Applied changes (if approved)
- Rejection reason (if rejected)
- Full audit trail

## Market Analytics

<Info>
Market analytics endpoints provide aggregated data and insights for market analysis and reporting.
</Info>

### Market Overview

**GET** `/api/analytics/market-overview`

| Param      | Type   | Required | Description                |
| ---------- | ------ | -------- | -------------------------- |
| `cityId`   | int    | no       | Filter by specific city    |
| `areaIds`  | string | no       | Comma-separated area IDs   |
| `fromDate` | string | no       | Start date (YYYY-MM-DD)    |
| `toDate`   | string | no       | End date (YYYY-MM-DD)      |

**Response:**

```json
{
  "totalProjects": 1250,
  "activeProjects": 890,
  "totalUnits": 45630,
  "availableUnits": 12450,
  "averagePrice": 1250000,
  "averagePricePerSqft": 1150,
  "priceDistribution": {
    "under500k": 15,
    "500k_1m": 35,
    "1m_2m": 30,
    "2m_5m": 15,
    "over5m": 5
  },
  "topAreas": [
    {
      "areaId": 101,
      "nameEn": "Downtown",
      "projectCount": 45,
      "averagePrice": 1850000
    }
  ]
}
```

### Price Trends

**GET** `/api/analytics/price-trends`

Returns historical price data for trend analysis:

```json
{
  "period": "monthly",
  "data": [
    {
      "date": "2024-01-01",
      "averagePrice": 1200000,
      "averagePricePerSqft": 1100,
      "salesVolume": 125,
      "newListings": 89
    }
  ]
}
```

### Comparative Analysis  

**GET** `/api/analytics/comparative`

Compare metrics across different areas, developers, or time periods.

## Unit Evaluation (AI Valuation)

<Note>
The AI valuation service provides automated property valuations using machine learning models trained on market data.
</Note>

### Get Unit Valuation

**POST** `/api/evaluation/units/:id`

```json
{
  "evaluationDate": "2024-01-15",
  "includeComparables": true,
  "confidenceLevel": "high"
}
```

**Response:**

```json
{
  "unitId": 1001,
  "evaluationDate": "2024-01-15T10:30:00Z",
  "valuation": {
    "estimatedValue": 1250000,
    "pricePerSqft": 1176,
    "confidenceScore": 0.87,
    "valueRange": {
      "low": 1180000,
      "high": 1320000
    }
  },
  "factors": [
    {
      "name": "Location Score",
      "impact": 0.25,
      "description": "Prime downtown location"
    },
    {
      "name": "Building Quality",
      "impact": 0.18,
      "description": "Premium finishes and amenities"
    }
  ],
  "comparables": [
    {
      "unitId": 2001,
      "similarity": 0.92,
      "price": 1280000,
      "pricePerSqft": 1205,
      "saleDate": "2024-01-10"
    }
  ],
  "marketTrends": {
    "areaGrowth": 0.08,
    "projectGrowth": 0.12,
    "marketVolatility": "low"
  }
}
```

### Bulk Valuation

**POST** `/api/evaluation/bulk`

Request valuations for multiple units:

```json
{
  "unitIds": [1001, 1002, 1003],
  "evaluationDate": "2024-01-15",
  "options": {
    "includeComparables": false,
    "confidenceLevel": "standard"
  }
}
```

## Appendix A: Enum Reference

<AccordionGroup>
<Accordion title="Project Status Values">
- `planned` - Project in planning phase
- `launched` - Project launched and selling
- `under_construction` - Construction in progress  
- `completed` - Construction completed
- `delivered` - Units delivered to buyers
- `cancelled` - Project cancelled
- `on_hold` - Project temporarily suspended
</Accordion>

<Accordion title="Unit Status Values">
- `available` - Available for sale
- `reserved` - Reserved by buyer
- `sold` - Sold to buyer
- `blocked` - Blocked from sale
- `rented` - Currently rented
- `owner_occupied` - Occupied by owner
</Accordion>

<Accordion title="Change Request Status Values">
- `pending` - Awaiting review
- `in_review` - Under review by admin
- `approved` - Approved and applied
- `rejected` - Rejected with reason
- `cancelled` - Cancelled by requester
</Accordion>
</AccordionGroup>

## Appendix B: Entity Relationships

<Tip>
Understanding the relationship hierarchy helps with efficient API usage and proper include parameters.
</Tip>

```
State
├── Cities
    ├── Areas
        └── Projects
            ├── Buildings
            │   └── Units
            └── Direct Units (no building)

Developer
├── Projects (many-to-many)
└── Child Developers

Unit Types
└── Units (one-to-many)
```

**Key Relationships:**

- Projects can have multiple buildings or direct units
- Units belong to either a building or directly to a project
- Projects can have multiple developers (joint ventures)
- Areas belong to cities, cities belong to states
- All entities support soft deletion and audit trails