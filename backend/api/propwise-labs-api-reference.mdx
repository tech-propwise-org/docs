---
title: "PropWise Labs API Reference"
description: "Complete API reference for external consumers including authentication, entity endpoints, change requests, and market analytics"
---

## Overview

### Base URL

All endpoints are prefixed with `/api`. Example:

```
https://your-domain.com/api/projects
```

### Authentication Methods

The API supports two authentication methods:

<Tabs>
  <Tab title="API Client Token">
    **For external consumers:**
    
    1. Obtain an API key and secret from an admin
    2. Exchange them for a short-lived Bearer token via `POST /api/auth/token`
    3. Include the token on every request: `Authorization: Bearer <token>`
  </Tab>
  <Tab title="JWT">
    **For internal/dashboard users:**
    
    - Obtained via `POST /api/auth/login` (admin dashboard)
    - Sent as `Authorization: Bearer <token>` or via the `access_token` httpOnly cookie
  </Tab>
</Tabs>

<Note>
Both methods are accepted on all external-facing endpoints. The API tries JWT first, then falls back to API key token.
</Note>

### Access Levels for API Clients

| Level   | Permissions                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------ |
| `basic` | Read-only access to all entity, lookup, analytics, and change-request endpoints                        |
| `super` | All of `basic` plus direct entity mutations (PATCH projects, PATCH buildings, POST/PATCH/DELETE units) |

### Paginated Response Envelope

All list endpoints return:

```json
{
  "data": [ ... ],
  "total": 1234,
  "page": 1,
  "limit": 20
}
```

**Standard pagination query parameters:**

| Parameter   | Type   | Default        | Constraints        |
| ----------- | ------ | -------------- | ------------------ |
| `page`      | int    | `1`            | >= 1               |
| `limit`     | int    | `20`           | 1--100             |
| `sortBy`    | string | _(per-entity)_ | allowlisted fields |
| `sortOrder` | string | `asc`          | `asc`, `desc`      |

### Configurable Includes

Most entity GET endpoints support an optional `include` query parameter that controls which relations and computed fields are returned:

| Value           | Behavior                                          |
| --------------- | ------------------------------------------------- |
| _(omitted)_     | Default relations populated (backward-compatible) |
| `none`          | No relations or stats -- only scalar fields       |
| `all`           | All allowed relations and stats                   |
| `field1,field2` | Only the specified relations/stats                |

<Warning>
Invalid values return `400` with the list of allowed options. Virtual includes like `stats` and `buildingAreas` control expensive aggregate queries rather than ORM relations.
</Warning>

### Error Response Format

<Tabs>
  <Tab title="Standard Errors">
    ```json
    {
      "statusCode": 404,
      "message": "Project not found",
      "error": "Not Found"
    }
    ```
  </Tab>
  <Tab title="Validation Errors">
    ```json
    {
      "statusCode": 400,
      "message": [
        "limit must not be greater than 100",
        "sortBy must be one of the following values: id, name, area, status, createdAt"
      ],
      "error": "Bad Request"
    }
    ```
  </Tab>
  <Tab title="Change Request Validation">
    ```json
    {
      "message": "Validation failed",
      "errors": [
        {
          "path": "projects[0]",
          "field": "name",
          "message": "name should not be empty"
        }
      ]
    }
    ```
  </Tab>
</Tabs>

**HTTP Status Codes:**

| Status | Meaning                                                              |
| ------ | -------------------------------------------------------------------- |
| `400`  | Validation error, bad request                                        |
| `401`  | Missing or invalid authentication                                    |
| `403`  | Insufficient permissions (e.g. `basic` client attempting a mutation) |
| `404`  | Entity not found                                                     |
| `409`  | Conflict (stale update in change requests)                           |

### Global Request Rules

<Check>All request bodies are JSON (`Content-Type: application/json`)</Check>
<Check>Unknown fields in request bodies and query params are rejected</Check>
<Check>Query params are auto-coerced to their declared types</Check>
<Check>Comma-separated ID lists (e.g. `areaIds=1,2,3`) are parsed into integer arrays</Check>
<Check>Soft-deleted records are excluded from all results</Check>

---

## Authentication

### Exchange API Credentials for Token

<Warning>
This is a public endpoint that does not require authentication.
</Warning>

```http
POST /api/auth/token
```

Exchange API client credentials for a short-lived Bearer token.

**Request Body:**

```json
{
  "apiKey": "your-api-key",
  "apiSecret": "your-api-secret"
}

```

| Field       | Type   | Required | Constraints |
| ----------- | ------ | -------- | ----------- |
| `apiKey`    | string | yes      | non-empty   |
| `apiSecret` | string | yes      | non-empty   |

**Response `200 OK`:**

```json
{
  "access_token": "eyJhbGciOiJIUzI1NiIs...",
  "token_type": "Bearer",
  "expires_in": 1800
}
```

| Field          | Type   | Description                                        |
| -------------- | ------ | -------------------------------------------------- |
| `access_token` | string | JWT token to use in `Authorization: Bearer` header |
| `token_type`   | string | Always `"Bearer"`                                  |
| `expires_in`   | number | Token lifetime in seconds (default 1800 = 30 min)  |

**Error Responses:**

| Status | Condition                 |
| ------ | ------------------------- |
| `401`  | Invalid API key or secret |
| `401`  | API client is disabled    |

---

## Entity Endpoints

<Note>
All entity endpoints require authentication via `ApiAuthGuard` (JWT or API client token).

All list endpoints return the paginated envelope format.
</Note>

### Developers

#### List Developers

```http
GET /api/developers
```

**Query Parameters:**

| Parameter   | Type   | Required | Default  | Constraints                                                                                           |
| ----------- | ------ | -------- | -------- | ----------------------------------------------------------------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                                                                                  |
| `limit`     | int    | no       | `20`     | 1--100                                                                                                |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `developerNumber`, `createdAt`                                              |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                                                                         |
| `nameEn`    | string | no       | --       | substring filter                                                                                      |
| `nameAr`    | string | no       | --       | substring filter                                                                                      |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`                                                                           |
| `include`   | string | no       | --       | allowed: `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats` |

**Default includes (list):** _(none)_

**Response Item Fields:**

| Field             | Type   | Always Present |
| ----------------- | ------ | -------------- |
| `id`              | number | yes            |
| `nameEn`          | string | --             |
| `nameAr`          | string | --             |
| `developerNumber` | string | --             |
| `logo`            | string | --             |
| `logoDark`        | string | --             |
| `licenseUrl`      | string | --             |

#### Get Developer by ID

```http
GET /api/developers/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Allowed includes:** `licenseSource`, `licenseType`, `legalStatus`, `parentDeveloper`, `childDevelopers`, `stats`

**Response (extends list item):**

| Field               | Type             | Notes                                                                |
| ------------------- | ---------------- | -------------------------------------------------------------------- |
| `sourceDeveloperId` | string           | always present                                                       |
| `licenseSource`     | `{ id, nameEn }` | if included                                                          |
| `licenseType`       | `{ id, nameEn }` | if included                                                          |
| `legalStatus`       | `{ id, nameEn }` | if included                                                          |
| `parentDeveloper`   | `{ id, nameEn }` | if included                                                          |
| `childDevelopers`   | array            | `[{ id, nameEn }]` -- if included                                    |
| `stats`             | object           | `{ projectsCount, masterProjectsCount }` -- only if `stats` included |

#### Search Developers

```http
GET /api/developers/search
```

Fuzzy search for developers by name using PostgreSQL `pg_trgm` similarity.

**Query Parameters:**

| Parameter | Type   | Required | Default | Constraints  |
| --------- | ------ | -------- | ------- | ------------ |
| `q`       | string | yes      | --      | min length 1 |
| `limit`   | int    | no       | `10`    | 1--50        |

<Info>
**How it works:**

1. Trims `q`, then runs `similarity()` against both `name_en` and `name_ar` columns
2. Filters rows where either similarity score > 0.15
3. Orders by the highest of the two scores (descending)
4. Returns up to `limit` results
</Info>

**Response `200 OK`:**

```json
[
  {
    "id": 42,
    "nameEn": "Emaar Properties",
    "nameAr": "إعمار العقارية",
    "developerNumber": "DEV-001",
    "logo": "https://example.com/logo.png",
    "logoDark": "https://example.com/logo-dark.png",
    "licenseUrl": "https://example.com/license.pdf",
    "similarity": 0.85
  }
]
```

<Note>
All fields except `id` and `similarity` are nullable.
</Note>

---

### Cities

#### List Cities

```http
GET /api/cities
```

**Query Parameters:**

| Parameter   | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `state`, `createdAt` |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `state.nameEn`    |
| `stateId`   | int    | no       | --       | filter by state                                |
| `include`   | string | no       | --       | allowed: `state`, `stats`                      |

**Default includes (list):** `state`

**Response Item Fields:**

| Field    | Type                   | Notes              |
| -------- | ---------------------- | ------------------ |
| `id`     | number                 | yes                |
| `nameEn` | string                 | --                 |
| `nameAr` | string                 | --                 |
| `state`  | `{ id, nameEn, code }` | if included        |
| `stats`  | object                 | `{ areasCount }` if included |

#### Get City by ID

```http
GET /api/cities/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `state`, `stats`

**Allowed includes:** `state`, `stats`

---

### Areas

#### List Areas

```http
GET /api/areas
```

**Query Parameters:**

| Parameter   | Type   | Required | Default  | Constraints                                    |
| ----------- | ------ | -------- | -------- | ---------------------------------------------- |
| `page`      | int    | no       | `1`      | >= 1                                           |
| `limit`     | int    | no       | `20`     | 1--100                                         |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `city`, `createdAt`  |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                  |
| `nameEn`    | string | no       | --       | substring filter                               |
| `nameAr`    | string | no       | --       | substring filter                               |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `city.nameEn`     |
| `cityId`    | int    | no       | --       | filter by city                                 |
| `stateId`   | int    | no       | --       | filter by state (via city)                     |
| `include`   | string | no       | --       | allowed: `city`, `stats`                       |

**Default includes (list):** `city`

**Response Item Fields:**

| Field    | Type                   | Notes                        |
| -------- | ---------------------- | ---------------------------- |
| `id`     | number                 | yes                          |
| `nameEn` | string                 | --                           |
| `nameAr` | string                 | --                           |
| `city`   | `{ id, nameEn, state }` | if included (state nested)  |
| `stats`  | object                 | `{ projectsCount, subAreasCount }` if included |

#### Get Area by ID

```http
GET /api/areas/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `city`, `stats`

**Allowed includes:** `city`, `stats`

---

### Sub-Areas

#### List Sub-Areas

```http
GET /api/sub-areas
```

**Query Parameters:**

| Parameter   | Type   | Required | Default  | Constraints                                      |
| ----------- | ------ | -------- | -------- | ------------------------------------------------ |
| `page`      | int    | no       | `1`      | >= 1                                             |
| `limit`     | int    | no       | `20`     | 1--100                                           |
| `sortBy`    | string | no       | `nameEn` | `id`, `nameEn`, `nameAr`, `area`, `createdAt`    |
| `sortOrder` | string | no       | `asc`    | `asc`, `desc`                                    |
| `nameEn`    | string | no       | --       | substring filter                                 |
| `nameAr`    | string | no       | --       | substring filter                                 |
| `search`    | string | no       | --       | searches `nameEn`, `nameAr`, `area.nameEn`       |
| `areaId`    | int    | no       | --       | filter by area                                   |
| `cityId`    | int    | no       | --       | filter by city (via area)                        |
| `stateId`   | int    | no       | --       | filter by state (via area → city)                |
| `include`   | string | no       | --       | allowed: `area`, `stats`                         |

**Default includes (list):** `area`

**Response Item Fields:**

| Field    | Type                         | Notes                                     |
| -------- | ---------------------------- | ----------------------------------------- |
| `id`     | number                       | yes                                       |
| `nameEn` | string                       | --                                        |
| `nameAr` | string                       | --                                        |
| `area`   | `{ id, nameEn, city }`       | if included (city nested)                 |
| `stats`  | object                       | `{ projectsCount }` if included           |

#### Get Sub-Area by ID

```http
GET /api/sub-areas/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `area`, `stats`

**Allowed includes:** `area`, `stats`

---

### Projects

#### List Projects

```http
GET /api/projects
```

**Query Parameters:**

| Parameter           | Type   | Required | Default     | Constraints                                                                                                                                                                                |
| ------------------- | ------ | -------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `page`              | int    | no       | `1`         | >= 1                                                                                                                                                                                       |
| `limit`             | int    | no       | `20`        | 1--100                                                                                                                                                                                     |
| `sortBy`            | string | no       | `createdAt` | `id`, `name`, `area`, `status`, `createdAt`                                                                                                                                                |
| `sortOrder`         | string | no       | `desc`      | `asc`, `desc`                                                                                                                                                                              |
| `name`              | string | no       | --          | substring filter                                                                                                                                                                           |
| `search`            | string | no       | --          | searches `name`, `area.nameEn`, `developer.nameEn`                                                                                                                                         |
| `areaIds`           | string | no       | --          | comma-separated list (e.g. `1,2,3`)                                                                                                                                                        |
| `cityIds`           | string | no       | --          | comma-separated list                                                                                                                                                                       |
| `stateIds`          | string | no       | --          | comma-separated list                                                                                                                                                                       |
| `subAreaIds`        | string | no       | --          | comma-separated list                                                                                                                                                                       |
| `developerIds`      | string | no       | --          | comma-separated list                                                                                                                                                                       |
| `status`            | string | no       | --          | enum: `Planned`, `ConstructionUnderway`, `Completed`, `OnHold`, `Cancelled`                                                                                                                |
| `projectType`       | string | no       | --          | enum: `Residential`, `Commercial`, `MixedUse`, `Industrial`, `Hospitality`, `Retail`, `Office`, `Healthcare`, `Education`, `Infrastructure`, `Leisure`, `Agricultural`, `Technology`, `Other` |
| `isPublished`       | bool   | no       | --          | filter by published status                                                                                                                                                                 |
| `isMasterPlan`      | bool   | no       | --          | filter by master plan status                                                                                                                                                               |
| `masterPlanProject` | int    | no       | --          | filter by master plan project ID                                                                                                                                                           |
| `include`           | string | no       | --          | allowed: `area`, `developer`, `masterPlanProject`, `childProjects`, `projectStatus`, `projectType`, `stats`                                                                                |

**Default includes (list):** `area`, `developer`, `projectStatus`, `projectType`

**Response Item Fields:**

| Field                | Type                       | Notes                                                          |
| -------------------- | -------------------------- | -------------------------------------------------------------- |
| `id`                 | number                     | yes                                                            |
| `name`               | string                     | yes                                                            |
| `area`               | `{ id, nameEn, city }`     | if included (city nested with state)                           |
| `developer`          | `{ id, nameEn }`           | if included                                                    |
| `projectStatus`      | `{ id, nameEn }`           | if included                                                    |
| `projectType`        | `{ id, nameEn }`           | if included                                                    |
| `masterPlanProject`  | `{ id, name }`             | if included                                                    |
| `childProjects`      | array                      | `[{ id, name }]` if included                                   |
| `stats`              | object                     | `{ buildingsCount, unitsCount, plotsCount }` if included       |
| `isMasterPlan`       | boolean                    | yes                                                            |
| `isPublished`        | boolean                    | yes                                                            |
| `deliveryDate`       | string (ISO date)          | nullable                                                       |
| `sourceProjectId`    | string                     | yes                                                            |

#### Get Project by ID

```http
GET /api/projects/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `area`, `subArea`, `developer`, `masterPlanProject`, `childProjects`, `projectStatus`, `projectType`, `stats`

**Allowed includes:** `area`, `subArea`, `developer`, `masterPlanProject`, `childProjects`, `projectStatus`, `projectType`, `stats`

**Response (extends list item):**

Additional fields include:

| Field             | Type                  | Notes                                 |
| ----------------- | --------------------- | ------------------------------------- |
| `subArea`         | `{ id, nameEn }`      | if included                           |
| `description`     | string                | nullable                              |
| `bannerImage`     | string                | nullable                              |
| `thumbnailImage`  | string                | nullable                              |
| `latitude`        | number                | nullable                              |
| `longitude`       | number                | nullable                              |
| `amenities`       | array                 | `[{ id, nameEn }]`                    |
| `nearbyLandmarks` | array                 | `[{ id, nameEn }]`                    |

---

### Buildings

#### List Buildings

```http
GET /api/buildings
```

**Query Parameters:**

| Parameter      | Type   | Required | Default     | Constraints                                                                                |
| -------------- | ------ | -------- | ----------- | ------------------------------------------------------------------------------------------ |
| `page`         | int    | no       | `1`         | >= 1                                                                                       |
| `limit`        | int    | no       | `20`        | 1--100                                                                                     |
| `sortBy`       | string | no       | `createdAt` | `id`, `name`, `buildingType`, `status`, `createdAt`                                        |
| `sortOrder`    | string | no       | `desc`      | `asc`, `desc`                                                                              |
| `name`         | string | no       | --          | substring filter                                                                           |
| `search`       | string | no       | --          | searches `name`, `project.name`                                                            |
| `projectId`    | int    | no       | --          | filter by project                                                                          |
| `buildingType` | string | no       | --          | enum: `Residential`, `Commercial`, `MixedUse`, `Villa`, `Townhouse`, `Apartment`, `Other`  |
| `status`       | string | no       | --          | enum: `Planned`, `UnderConstruction`, `Completed`, `OnHold`, `Cancelled`                   |
| `include`      | string | no       | --          | allowed: `project`, `buildingType`, `buildingStatus`, `buildingAreas`, `stats`             |

**Default includes (list):** `project`, `buildingType`, `buildingStatus`

**Response Item Fields:**

| Field            | Type                   | Notes                                                       |
| ---------------- | ---------------------- | ----------------------------------------------------------- |
| `id`             | number                 | yes                                                         |
| `name`           | string                 | yes                                                         |
| `project`        | `{ id, name }`         | if included                                                 |
| `buildingType`   | `{ id, nameEn }`       | if included                                                 |
| `buildingStatus` | `{ id, nameEn }`       | if included                                                 |
| `buildingAreas`  | array                  | `[{ buildingAreaType: {...}, value, unit }]` if included    |
| `stats`          | object                 | `{ unitsCount }` if included                                |
| `deliveryDate`   | string (ISO date)      | nullable                                                    |

#### Get Building by ID

```http
GET /api/buildings/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `project`, `buildingType`, `buildingStatus`, `buildingAreas`, `stats`

**Allowed includes:** `project`, `buildingType`, `buildingStatus`, `buildingAreas`, `stats`

---

### Units

#### List Units

```http
GET /api/units
```

**Query Parameters:**

| Parameter      | Type   | Required | Default     | Constraints                                                                          |
| -------------- | ------ | -------- | ----------- | ------------------------------------------------------------------------------------ |
| `page`         | int    | no       | `1`         | >= 1                                                                                 |
| `limit`        | int    | no       | `20`        | 1--100                                                                               |
| `sortBy`       | string | no       | `createdAt` | `id`, `unitNumber`, `unitType`, `status`, `price`, `createdAt`                       |
| `sortOrder`    | string | no       | `desc`      | `asc`, `desc`                                                                        |
| `unitNumber`   | string | no       | --          | substring filter                                                                     |
| `search`       | string | no       | --          | searches `unitNumber`, `building.name`, `project.name`                               |
| `projectId`    | int    | no       | --          | filter by project                                                                    |
| `buildingId`   | int    | no       | --          | filter by building                                                                   |
| `unitType`     | string | no       | --          | enum: `Apartment`, `Villa`, `Townhouse`, `Penthouse`, `Studio`, `Shop`, `Office`, `Other` |
| `status`       | string | no       | --          | enum: `Available`, `Reserved`, `Sold`, `Rented`, `OffMarket`                         |
| `minPrice`     | number | no       | --          | filter by minimum price                                                              |
| `maxPrice`     | number | no       | --          | filter by maximum price                                                              |
| `minBedrooms`  | int    | no       | --          | filter by minimum bedrooms                                                           |
| `maxBedrooms`  | int    | no       | --          | filter by maximum bedrooms                                                           |
| `minBathrooms` | int    | no       | --          | filter by minimum bathrooms                                                          |
| `maxBathrooms` | int    | no       | --          | filter by maximum bathrooms                                                          |
| `include`      | string | no       | --          | allowed: `building`, `project`, `unitType`, `unitStatus`, `unitAreas`, `unitPricing` |

**Default includes (list):** `building`, `project`, `unitType`, `unitStatus`

**Response Item Fields:**

| Field          | Type                   | Notes                                                    |
| -------------- | ---------------------- | -------------------------------------------------------- |
| `id`           | number                 | yes                                                      |
| `unitNumber`   | string                 | yes                                                      |
| `building`     | `{ id, name }`         | if included                                              |
| `project`      | `{ id, name }`         | if included                                              |
| `unitType`     | `{ id, nameEn }`       | if included                                              |
| `unitStatus`   | `{ id, nameEn }`       | if included                                              |
| `bedrooms`     | number                 | nullable                                                 |
| `bathrooms`    | number                 | nullable                                                 |
| `unitAreas`    | array                  | `[{ unitAreaType: {...}, value, unit }]` if included     |
| `unitPricing`  | array                  | `[{ priceType: {...}, value, currency }]` if included    |

#### Get Unit by ID

```http
GET /api/units/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `building`, `project`, `unitType`, `unitStatus`, `unitAreas`, `unitPricing`

**Allowed includes:** `building`, `project`, `unitType`, `unitStatus`, `unitAreas`, `unitPricing`

---

### Plots

#### List Plots

```http
GET /api/plots
```

**Query Parameters:**

| Parameter     | Type   | Required | Default     | Constraints                                                 |
| ------------- | ------ | -------- | ----------- | ----------------------------------------------------------- |
| `page`        | int    | no       | `1`         | >= 1                                                        |
| `limit`       | int    | no       | `20`        | 1--100                                                      |
| `sortBy`      | string | no       | `createdAt` | `id`, `plotNumber`, `status`, `price`, `createdAt`          |
| `sortOrder`   | string | no       | `desc`      | `asc`, `desc`                                               |
| `plotNumber`  | string | no       | --          | substring filter                                            |
| `search`      | string | no       | --          | searches `plotNumber`, `project.name`                       |
| `projectId`   | int    | no       | --          | filter by project                                           |
| `status`      | string | no       | --          | enum: `Available`, `Reserved`, `Sold`, `OffMarket`          |
| `minPrice`    | number | no       | --          | filter by minimum price                                     |
| `maxPrice`    | number | no       | --          | filter by maximum price                                     |
| `minArea`     | number | no       | --          | filter by minimum area                                      |
| `maxArea`     | number | no       | --          | filter by maximum area                                      |
| `include`     | string | no       | --          | allowed: `project`, `plotStatus`, `plotType`, `plotPricing` |

**Default includes (list):** `project`, `plotStatus`, `plotType`

**Response Item Fields:**

| Field         | Type                   | Notes                                                |
| ------------- | ---------------------- | ---------------------------------------------------- |
| `id`          | number                 | yes                                                  |
| `plotNumber`  | string                 | yes                                                  |
| `project`     | `{ id, name }`         | if included                                          |
| `plotStatus`  | `{ id, nameEn }`       | if included                                          |
| `plotType`    | `{ id, nameEn }`       | if included                                          |
| `area`        | number                 | nullable                                             |
| `areaUnit`    | string                 | nullable                                             |
| `plotPricing` | array                  | `[{ priceType: {...}, value, currency }]` if included |

#### Get Plot by ID

```http
GET /api/plots/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Query Parameters:**

| Parameter | Type   | Required |
| --------- | ------ | -------- |
| `include` | string | no       |

**Default includes (detail):** `project`, `plotStatus`, `plotType`, `plotPricing`

**Allowed includes:** `project`, `plotStatus`, `plotType`, `plotPricing`

---

## Lookup Endpoints

<Info>
Lookup endpoints provide access to enumeration and reference data. All require authentication.
</Info>

### Developer License Sources

```http
GET /api/lookups/developer-license-sources
```

Returns all developer license sources (e.g., DLD, RERA).

**Response:**

```json
{
  "data": [
    { "id": 1, "nameEn": "DLD", "nameAr": "دائرة الأراضي والأملاك" }
  ]
}
```

### Developer License Types

```http
GET /api/lookups/developer-license-types
```

Returns all developer license types.

### Developer Legal Statuses

```http
GET /api/lookups/developer-legal-statuses
```

Returns all developer legal statuses.

### Project Statuses

```http
GET /api/lookups/project-statuses
```

Returns all project statuses.

### Project Types

```http
GET /api/lookups/project-types
```

Returns all project types.

### Building Types

```http
GET /api/lookups/building-types
```

Returns all building types.

### Building Statuses

```http
GET /api/lookups/building-statuses
```

Returns all building statuses.

### Building Area Types

```http
GET /api/lookups/building-area-types
```

Returns all building area types (e.g., Built-up Area, Plot Area).

### Unit Types

```http
GET /api/lookups/unit-types
```

Returns all unit types.

### Unit Statuses

```http
GET /api/lookups/unit-statuses
```

Returns all unit statuses.

### Unit Area Types

```http
GET /api/lookups/unit-area-types
```

Returns all unit area types.

### Price Types

```http
GET /api/lookups/price-types
```

Returns all price types (used for both units and plots).

### Plot Types

```http
GET /api/lookups/plot-types
```

Returns all plot types.

### Plot Statuses

```http
GET /api/lookups/plot-statuses
```

Returns all plot statuses.

### Amenities

```http
GET /api/lookups/amenities
```

Returns all amenities (e.g., Swimming Pool, Gym).

### Nearby Landmarks

```http
GET /api/lookups/nearby-landmarks
```

Returns all nearby landmarks (e.g., Schools, Hospitals, Metro Stations).

---

## Direct Entity Mutations

<Warning>
**Permission required:** `super` access level

Direct mutations bypass change requests and apply changes immediately to the database.
</Warning>

### Update Project

```http
PATCH /api/projects/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Request Body:**

```json
{
  "name": "Updated Project Name",
  "description": "New description",
  "areaId": 5,
  "subAreaId": 10,
  "developerId": 3,
  "projectStatusId": 2,
  "projectTypeId": 1,
  "deliveryDate": "2025-12-31",
  "bannerImage": "https://example.com/banner.jpg",
  "thumbnailImage": "https://example.com/thumb.jpg",
  "latitude": 25.2048,
  "longitude": 55.2708,
  "isMasterPlan": false,
  "masterPlanProjectId": null,
  "isPublished": true,
  "amenities": [1, 2, 3],
  "nearbyLandmarks": [4, 5]
}
```

<Note>
All fields are optional. Only provided fields will be updated.
</Note>

**Response `200 OK`:**

Returns the updated project object (with default includes).

### Update Building

```http
PATCH /api/buildings/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Request Body:**

```json
{
  "name": "Updated Building Name",
  "buildingTypeId": 2,
  "buildingStatusId": 3,
  "deliveryDate": "2026-06-30",
  "buildingAreas": [
    {
      "buildingAreaTypeId": 1,
      "value": 5000,
      "unit": "sqm"
    }
  ]
}
```

**Response `200 OK`:**

Returns the updated building object (with default includes).

### Create Unit

```http
POST /api/units
```

**Request Body:**

```json
{
  "unitNumber": "A-101",
  "buildingId": 15,
  "unitTypeId": 1,
  "unitStatusId": 1,
  "bedrooms": 2,
  "bathrooms": 2,
  "unitAreas": [
    {
      "unitAreaTypeId": 1,
      "value": 1200,
      "unit": "sqft"
    }
  ],
  "unitPricing": [
    {
      "priceTypeId": 1,
      "value": 850000,
      "currency": "AED"
    }
  ]
}
```

**Response `201 Created`:**

Returns the created unit object (with default includes).

### Update Unit

```http
PATCH /api/units/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Request Body:**

Same structure as create, all fields optional.

**Response `200 OK`:**

Returns the updated unit object.

### Delete Unit

```http
DELETE /api/units/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Response `204 No Content`:**

Soft-deletes the unit (sets `deletedAt` timestamp).

---

## Change Requests

<Info>
Change requests provide a workflow for proposing and reviewing changes before they are applied to entities.

**Available to:** All authenticated API clients (`basic` and `super`)
</Info>

### Workflow States

| State       | Description                                                     |
| ----------- | --------------------------------------------------------------- |
| `pending`   | Awaiting admin review                                           |
| `approved`  | Admin approved; changes applied to entities                     |
| `rejected`  | Admin rejected; no changes applied                              |

### Submit Change Request

```http
POST /api/change-requests
```

Submit a batch of proposed changes for one or more entities.

**Request Body:**

```json
{
  "projects": [
    {
      "id": 10,
      "changes": {
        "name": "New Project Name",
        "description": "Updated description"
      }
    }
  ],
  "buildings": [
    {
      "id": 25,
      "changes": {
        "buildingStatusId": 3
      }
    }
  ],
  "units": [
    {
      "action": "create",
      "data": {
        "unitNumber": "B-202",
        "buildingId": 25,
        "unitTypeId": 1
      }
    },
    {
      "action": "update",
      "id": 100,
      "changes": {
        "bedrooms": 3
      }
    },
    {
      "action": "delete",
      "id": 101
    }
  ],
  "plots": [
    {
      "id": 5,
      "changes": {
        "plotStatusId": 2
      }
    }
  ]
}
```

<Accordion title="Request Body Schema">
**Top-level structure:**

| Field       | Type  | Required | Description                          |
| ----------- | ----- | -------- | ------------------------------------ |
| `projects`  | array | no       | Array of project change objects      |
| `buildings` | array | no       | Array of building change objects     |
| `units`     | array | no       | Array of unit change objects         |
| `plots`     | array | no       | Array of plot change objects         |

**Project/Building/Plot change object:**

| Field     | Type   | Required | Description                    |
| --------- | ------ | -------- | ------------------------------ |
| `id`      | int    | yes      | Entity ID to update            |
| `changes` | object | yes      | Key-value pairs of field changes |

**Unit change object (create):**

| Field    | Type   | Required | Description                        |
| -------- | ------ | -------- | ---------------------------------- |
| `action` | string | yes      | Must be `"create"`                 |
| `data`   | object | yes      | Full unit creation payload         |

**Unit change object (update):**

| Field     | Type   | Required | Description                    |
| --------- | ------ | -------- | ------------------------------ |
| `action`  | string | yes      | Must be `"update"`             |
| `id`      | int    | yes      | Unit ID to update              |
| `changes` | object | yes      | Key-value pairs of field changes |

**Unit change object (delete):**

| Field    | Type   | Required | Description            |
| -------- | ------ | -------- | ---------------------- |
| `action` | string | yes      | Must be `"delete"`     |
| `id`     | int    | yes      | Unit ID to delete      |
</Accordion>

**Response `201 Created`:**

```json
{
  "id": 42,
  "status": "pending",
  "submittedBy": { "id": 5, "name": "API Client Name" },
  "submittedAt": "2024-01-15T10:30:00Z",
  "projects": [
    {
      "id": 10,
      "changes": { "name": "New Project Name", "description": "Updated description" }
    }
  ],
  "buildings": [...],
  "units": [...],
  "plots": [...]
}
```

### List Change Requests

```http
GET /api/change-requests
```

**Query Parameters:**

| Parameter   | Type   | Required | Default        | Constraints                       |
| ----------- | ------ | -------- | -------------- | --------------------------------- |
| `page`      | int    | no       | `1`            | >= 1                              |
| `limit`     | int    | no       | `20`           | 1--100                            |
| `sortBy`    | string | no       | `submittedAt`  | `id`, `status`, `submittedAt`     |
| `sortOrder` | string | no       | `desc`         | `asc`, `desc`                     |
| `status`    | string | no       | --             | `pending`, `approved`, `rejected` |

**Response:**

```json
{
  "data": [
    {
      "id": 42,
      "status": "pending",
      "submittedBy": { "id": 5, "name": "API Client Name" },
      "submittedAt": "2024-01-15T10:30:00Z",
      "reviewedBy": null,
      "reviewedAt": null,
      "rejectionReason": null,
      "projects": [...],
      "buildings": [...],
      "units": [...],
      "plots": [...]
    }
  ],
  "total": 100,
  "page": 1,
  "limit": 20
}
```

### Get Change Request by ID

```http
GET /api/change-requests/:id
```

**Path Parameters:**

| Parameter | Type | Required |
| --------- | ---- | -------- |
| `id`      | int  | yes      |

**Response `200 OK`:**

Same structure as list item.

**Error Responses:**

| Status | Condition                |
| ------ | ------------------------ |
| `404`  | Change request not found |

---

## Market Analytics

<Info>
Market analytics endpoints provide aggregated statistics and insights about real estate market trends.

**Available to:** All authenticated API clients
</Info>

### Average Price by Area

```http
GET /api/analytics/avg-price-by-area
```

Returns average unit prices grouped by area.

**Query Parameters:**

| Parameter  | Type   | Required | Description                               |
| ---------- | ------ | -------- | ----------------------------------------- |
| `cityId`   | int    | no       | Filter by city                            |
| `stateId`  | int    | no       | Filter by state                           |
| `unitType` | string | no       | Filter by unit type enum                  |
| `limit`    | int    | no       | Max results (default 20, max 100)         |

**Response `200 OK`:**

```json
[
  {
    "areaId": 5,
    "areaNameEn": "Dubai Marina",
    "areaNameAr": "مارينا دبي",
    "avgPrice": 1250000,
    "currency": "AED",
    "unitCount": 450
  }
]
```

### Project Completion Rate by Developer

```http
GET /api/analytics/project-completion-rate
```

Returns project completion statistics grouped by developer.

**Query Parameters:**

| Parameter | Type | Required | Description                       |
| --------- | ---- | -------- | --------------------------------- |
| `limit`   | int  | no       | Max results (default 20, max 100) |

**Response `200 OK`:**

```json
[
  {
    "developerId": 3,
    "developerNameEn": "Emaar Properties",
    "developerNameAr": "إعمار العقارية",
    "totalProjects": 50,
    "completedProjects": 42,
    "completionRate": 84.0
  }
]
```

### Units Distribution by Type

```http
GET /api/analytics/units-distribution
```

Returns unit count distribution by unit type.

**Query Parameters:**

| Parameter   | Type | Required | Description        |
| ----------- | ---- | -------- | ------------------ |
| `projectId` | int  | no       | Filter by project  |
| `areaId`    | int  | no       | Filter by area     |
| `cityId`    | int  | no       | Filter by city     |

**Response `200 OK`:**

```json
[
  {
    "unitTypeId": 1,
    "unitTypeNameEn": "Apartment",
    "unitTypeNameAr": "شقة",
    "count": 1250
  },
  {
    "unitTypeId": 2,
    "unitTypeNameEn": "Villa",
    "unitTypeNameAr": "فيلا",
    "count": 320
  }
]
```

### Market Trends Over Time

```http
GET /api/analytics/market-trends
```

Returns time-series data for average prices and project counts.

**Query Parameters:**

| Parameter   | Type   | Required | Description                               |
| ----------- | ------ | -------- | ----------------------------------------- |
| `startDate` | string | yes      | ISO date (e.g., `2024-01-01`)             |
| `endDate`   | string | yes      | ISO date                                  |
| `granularity` | string | no     | `month` or `quarter` (default: `month`)   |
| `areaId`    | int    | no       | Filter by area                            |
| `cityId`    | int    | no       | Filter by city                            |

**Response `200 OK`:**

```json
[
  {
    "period": "2024-01",
    "avgPrice": 1150000,
    "projectCount": 15,
    "unitCount": 450
  },
  {
    "period": "2024-02",
    "avgPrice": 1180000,
    "projectCount": 18,
    "unitCount": 520
  }
]
```

---

## Unit Evaluation (AI Valuation)

<Info>
The unit evaluation endpoint provides AI-powered property valuation based on comparable market data.

**Available to:** All authenticated API clients
</Info>

### Evaluate Unit Value

```http
POST /api/unit-evaluation
```

Request an AI-generated valuation for a unit based on its attributes and comparable properties.

**Request Body:**

```json
{
  "unitId": 100,
  "bedrooms": 2,
  "bathrooms": 2,
  "builtUpArea": 1200,
  "areaUnit": "sqft",
  "areaId": 5,
  "unitTypeId": 1,
  "buildingId": 25,
  "projectId": 10
}
```

<Accordion title="Request Body Schema">
| Field         | Type   | Required | Description                                    |
| ------------- | ------ | -------- | ---------------------------------------------- |
| `unitId`      | int    | no       | Existing unit ID (for re-evaluation)           |
| `bedrooms`    | int    | yes      | Number of bedrooms                             |
| `bathrooms`   | int    | yes      | Number of bathrooms                            |
| `builtUpArea` | number | yes      | Unit built-up area                             |
| `areaUnit`    | string | yes      | Area unit (e.g., `sqft`, `sqm`)                |
| `areaId`      | int    | yes      | Area ID for location context                   |
| `unitTypeId`  | int    | yes      | Unit type ID                                   |
| `buildingId`  | int    | no       | Building ID (for building-specific factors)    |
| `projectId`   | int    | no       | Project ID (for project-specific factors)      |
</Accordion>

**Response `200 OK`:**

```json
{
  "estimatedValue": 1250000,
  "currency": "AED",
  "pricePerSqft": 1042,
  "confidence": "high",
  "comparables": [
    {
      "unitId": 95,
      "unitNumber": "A-105",
      "price": 1220000,
      "bedrooms": 2,
      "bathrooms": 2,
      "builtUpArea": 1180,
      "similarity": 0.92
    }
  ],
  "marketFactors": {
    "areaAvgPrice": 1180000,
    "areaMedianPrice": 1150000,
    "projectAvgPrice": 1300000,
    "recentTrend": "increasing"
  },
  "evaluatedAt": "2024-01-15T14:30:00Z"
}
```

<Accordion title="Response Schema">
| Field             | Type   | Description                                           |
| ----------------- | ------ | ----------------------------------------------------- |
| `estimatedValue`  | number | AI-estimated property value                           |
| `currency`        | string | Currency code (e.g., `AED`)                           |
| `pricePerSqft`    | number | Price per square foot                                 |
| `confidence`      | string | Confidence level: `high`, `medium`, `low`             |
| `comparables`     | array  | List of comparable units used for valuation           |
| `marketFactors`   | object | Market statistics and trends                          |
| `evaluatedAt`     | string | ISO timestamp of evaluation                           |

**Comparable object:**

| Field         | Type   | Description                              |
| ------------- | ------ | ---------------------------------------- |
| `unitId`      | int    | Comparable unit ID                       |
| `unitNumber`  | string | Unit number                              |
| `price`       | number | Listed/sold price                        |
| `bedrooms`    | int    | Number of bedrooms                       |
| `bathrooms`   | int    | Number of bathrooms                      |
| `builtUpArea` | number | Built-up area                            |
| `similarity`  | number | Similarity score (0-1)                   |

**Market factors object:**

| Field              | Type   | Description                        |
| ------------------ | ------ | ---------------------------------- |
| `areaAvgPrice`     | number | Average price in the area          |
| `areaMedianPrice`  | number | Median price in the area           |
| `projectAvgPrice`  | number | Average price in the project       |
| `recentTrend`      | string | `increasing`, `stable`, `decreasing` |
</Accordion>

**Error Responses:**

| Status | Condition                                    |
| ------ | -------------------------------------------- |
| `400`  | Invalid input (missing required fields)      |
| `404`  | Referenced entities not found                |
| `503`  | AI service unavailable                       |

---

## Appendix A: Enum Reference

<AccordionGroup>
  <Accordion title="Project Status">
    - `Planned`
    - `ConstructionUnderway`
    - `Completed`
    - `OnHold`
    - `Cancelled`
  </Accordion>

  <Accordion title="Project Type">
    - `Residential`
    - `Commercial`
    - `MixedUse`
    - `Industrial`
    - `Hospitality`
    - `Retail`
    - `Office`
    - `Healthcare`
    - `Education`
    - `Infrastructure`
    - `Leisure`
    - `Agricultural`
    - `Technology`
    - `Other`
  </Accordion>

  <Accordion title="Building Type">
    - `Residential`
    - `Commercial`
    - `MixedUse`
    - `Villa`
    - `Townhouse`
    - `Apartment`
    - `Other`
  </Accordion>

  <Accordion title="Building Status">
    - `Planned`
    - `UnderConstruction`
    - `Completed`
    - `OnHold`
    - `Cancelled`
  </Accordion>

  <Accordion title="Unit Type">
    - `Apartment`
    - `Villa`
    - `Townhouse`
    - `Penthouse`
    - `Studio`
    - `Shop`
    - `Office`
    - `Other`
  </Accordion>

  <Accordion title="Unit Status">
    - `Available`
    - `Reserved`
    - `Sold`
    - `Rented`
    - `OffMarket`
  </Accordion>

  <Accordion title="Plot Status">
    - `Available`
    - `Reserved`
    - `Sold`
    - `OffMarket`
  </Accordion>
</AccordionGroup>

---

## Appendix B: Entity Relationships

<CardGroup cols={2}>
  <Card title="Project Hierarchy" icon="sitemap">
    - **Project** (can be a master plan)
      - Child Projects (for master plans)
      - Buildings
        - Units
      - Plots
  </Card>

  <Card title="Location Hierarchy" icon="location-dot">
    - **State**
      - Cities
        - Areas
          - Sub-Areas
  </Card>

  <Card title="Developer Relationships" icon="building">
    - **Developer**
      - Parent Developer (optional)
      - Child Developers (subsidiaries)
      - Projects
  </Card>

  <Card title="Lookup Relationships" icon="link">
    - Projects use: ProjectStatus, ProjectType
    - Buildings use: BuildingType, BuildingStatus, BuildingAreaTypes
    - Units use: UnitType, UnitStatus, UnitAreaTypes, PriceTypes
    - Plots use: PlotType, PlotStatus, PriceTypes
  </Card>
</CardGroup>

---

<Note>
**Need help?** Contact the API support team for assistance with integration, authentication, or data questions.
</Note>