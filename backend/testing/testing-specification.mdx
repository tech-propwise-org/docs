---
title: "Testing Specification"
description: "Architecture, conventions, and run guide for the PropWise CRM backend test suite"
---

The test suite focuses on **RBAC (Role-Based Access Control) E2E testing** — verifying that every API endpoint enforces the correct permissions for every user role.

**Stack:** Jest + supertest + `@nestjs/testing`

<Note>
**Key design decisions:**
- **Singleton app context** — the NestJS app boots once and is shared across all test files
- **Matrix-driven tests** — a declarative permission matrix defines expected HTTP status codes per persona × endpoint
- **API-driven seeding** — test data is created through actual API endpoints so all business logic executes naturally
</Note>

## Directory Structure

```
test/
├── jest-e2e.json                        # Jest E2E config
├── TESTING_METHODOLOGY.md               # How to analyze controllers/DTOs before writing tests
├── INTEGRATION_TEST_SCENARIOS.md        # Manual scenario catalog (LC-*, LOP-*, etc.)
├── mocks/
│   └── pg-boss.mock.js                  # pg-boss stub for Jest (moduleNameMapper)
├── helpers/
│   ├── rbac-personas.ts                 # Persona definitions, TestUser interface, constants
│   └── email-spy.ts                     # DB code reader — reads verification/reset codes, cleanup
├── setup/
│   ├── seed-test-users.ts               # DB seeder — creates 5 users + org + team (run once)
│   ├── auth-context.ts                  # Auth test context — boots app via @nestjs/testing
│   ├── cleanup-test-users.ts            # Deletes @propwise-test.com users from DB
│   ├── rbac-context.ts                  # Singleton — boots app, authenticates all personas, resolves teamId
│   ├── rbac-test-data.ts                # Creates leads/deals/contacts/companies via API (cached)
│   ├── rbac-matrix.ts                   # Barrel re-export of all matrices
│   ├── rbac-matrix-types.ts             # Shared types (EndpointExpectation, StatusExpectation)
│   ├── rbac-runner.ts                   # Iterates matrix → generates parameterized Jest tests
│   └── matrices/
│       ├── index.ts                     # Barrel export
│       ├── lead-matrix.ts              # Lead permission matrices
│       ├── deal-matrix.ts              # Deal permission matrices
│       ├── contact-matrix.ts           # Contact permission matrices
│       └── company-matrix.ts           # Company permission matrices
└── e2e/
    ├── auth/
    │   ├── auth-lifecycle.e2e-spec.ts   # Auth lifecycle (signup→verify→login→reset→org create→org select)
    │   └── org-lifecycle.e2e-spec.ts    # Org CRUD (optional fields, multi-org, switching)
    └── rbac/
        ├── lead-rbac.e2e-spec.ts        # Lead access control
        ├── deal-rbac.e2e-spec.ts        # Deal access control
        ├── contact-rbac.e2e-spec.ts     # Contact access control
        └── company-rbac.e2e-spec.ts     # Company access control
```

## Architecture

### Singleton Context

The app boots once for the entire test run. All test files share the same NestJS app instance, authenticated tokens, and resolved `teamId`.

<CodeGroup>

```typescript Flow Diagram
Jest starts
  → lead-rbac.e2e-spec.ts calls getContext()
    → First call: boots NestJS app (~3-5s), logs in 5 personas, resolves teamId
    → Returns { app, personas, teamId }
  → deal-rbac.e2e-spec.ts calls getContext()
    → Returns cached instance instantly (0ms)
  → ... same for contact and company files
  → Last file's afterAll() calls closeContext()
    → Reference count hits 0 → app shuts down
```

```typescript Implementation
import { getContext, closeContext, RbacTestContext } from '../../setup/rbac-context';

let ctx: RbacTestContext;
beforeAll(async () => { ctx = await getContext(); }, 60000);
afterAll(async () => { await closeContext(); });
```

</CodeGroup>

<Info>
The context replicates production config from `main.ts`:
- `ValidationPipe` (whitelist, transform, implicit conversion)
- URI versioning (`/v1/...`)
- Cookie parser
</Info>

### Matrix Runner

Instead of writing individual `it()` blocks, you define expectations declaratively:

```typescript
{
  method: 'GET',
  path: '/v1/leads/:leadByBasicId',
  description: 'View lead detail',
  expectations: {
    OWNER: 200,
    ADMIN: 200,
    TEAM_LEADER: 403,
    AGENT: 403,
    BASIC: 200,
  },
}
```

<Steps>
<Step title="Iterate endpoints">
The runner iterates every endpoint in the matrix
</Step>
<Step title="Test each persona">
For each endpoint, iterates all 5 personas
</Step>
<Step title="Resolve placeholders">
Resolves path placeholders (`:leadByBasicId` → actual UUID)
</Step>
<Step title="Make request">
Makes the HTTP request with the persona's tenant token
</Step>
<Step title="Assert response">
Asserts the response status matches the expected value and produces clear failure messages
</Step>
</Steps>

<Tip>
One matrix entry = 5 generated test cases automatically.
</Tip>

## RBAC Personas

Defined in `test/helpers/rbac-personas.ts`. All personas share a single source of truth.

| Key | Email | Org Role | Team | Description |
|-----|-------|----------|------|-------------|
| `OWNER` | owner@propwise.com | Owner | Any | All permissions including `system.owner` |
| `ADMIN` | admin@propwise.com | Admin | Any | All non-sensitive permissions (includes `system.admin`) |
| `TEAM_LEADER` | teamleader@propwise.com | Basic | Team Leader in Test Sales Team | All team permissions (`team_crm.*`, `team_sales.*`, `team.admin`) |
| `AGENT` | agent@propwise.com | Basic | Agent in Test Sales Team | No team permissions by default |
| `BASIC` | basic@propwise.com | Basic | None | No permissions — relies entirely on stakeholder access |

### Permission Profiles

<AccordionGroup>
<Accordion title="OWNER / ADMIN">
Bypass resource-level checks via org permissions (`crm.view`, `crm.manage`, `sales.view`, `sales.manage`, etc.). Can access any entity in the org.
</Accordion>

<Accordion title="TEAM_LEADER">
No org permissions, but has `team_crm.manage` and `team_sales.manage` in the team. Can access entities where the team is a stakeholder.
</Accordion>

<Accordion title="AGENT">
No org permissions, no team permissions. Can only access entities where they are personally a stakeholder. Used to test access level differentiation (READ vs WRITE vs FULL).
</Accordion>

<Accordion title="BASIC">
Same as AGENT but not a team member at all. Access is purely stakeholder-based.
</Accordion>
</AccordionGroup>

### Login Flow

The context performs a two-step login for each persona:

<Steps>
<Step title="Identity login">
`POST /v1/auth/login` → identity token
</Step>
<Step title="Organization selection">
`POST /v1/auth/select-organization` → tenant token (org-scoped JWT)
</Step>
</Steps>

<Note>
All subsequent API calls use the tenant token in the `Authorization: Bearer` header.
</Note>

## Permission Matrix

Matrices are split into per-entity files under `test/setup/matrices/`. The barrel at `test/setup/rbac-matrix.ts` re-exports everything.

### Lead Matrices

| Matrix | Entity | Who Can Access | Tests |
|--------|--------|----------------|-------|
| `LEAD_BY_BASIC_MATRIX` | Lead created by BASIC | OWNER, ADMIN (via `crm.*`), BASIC (via FULL stakeholder) | View, update, stakeholders, activities, property interests |
| `LEAD_ON_TEAM_MATRIX` | Lead with team as stakeholder | OWNER, ADMIN, TEAM_LEADER (via `team_crm.*`) | View, update, stakeholders, activities |
| `LEAD_AGENT_READ_MATRIX` | Lead where AGENT has READ access | OWNER, ADMIN, AGENT (view only — modify denied) | View allowed, update denied, access level enforcement |
| `LEAD_IN_POOL_MATRIX` | Lead with no stakeholders | OWNER, ADMIN only (via `crm.*`) | View, update |
| `LEAD_ADMIN_ONLY_MATRIX` | Lead for admin-only ops | OWNER, ADMIN (via `system.admin`) | Delete |

### Deal Matrices

| Matrix | Entity | Who Can Access |
|--------|--------|----------------|
| `DEAL_BY_OWNER_MATRIX` | Deal created by OWNER | OWNER, ADMIN only (via `sales.*`) |

### Contact Matrices

| Matrix | Entity | Who Can Access |
|--------|--------|----------------|
| `CONTACT_BY_BASIC_MATRIX` | Contact created by BASIC | OWNER, ADMIN, BASIC |
| `CONTACT_BY_OWNER_MATRIX` | Contact created by OWNER | OWNER, ADMIN only |

### Company Matrices

| Matrix | Entity | Who Can Access |
|--------|--------|----------------|
| `COMPANY_BY_BASIC_MATRIX` | Company created by BASIC | OWNER, ADMIN, BASIC |
| `COMPANY_BY_OWNER_MATRIX` | Company created by OWNER | OWNER, ADMIN only |

### Access Mode Reference

Your `@CheckAccess` decorator uses two modes:

<Tabs>
<Tab title="OR Mode (default)">
User has the required permission **OR** has stakeholder-level access on the resource → allowed.
</Tab>
<Tab title="AND Mode">
User must have the permission **AND** stakeholder access → allowed.
</Tab>
</Tabs>

<Info>
**Resource action hierarchy:**
- `ACCESS` — granted by `READ`, `WRITE`, or `FULL` access level
- `MODIFY` — granted by `WRITE` or `FULL`
- `DELETE` — granted by `FULL` only
</Info>

## Test Data Seeding

### Step 1: Seed Users

<Warning>
This is a one-time setup before running tests.
</Warning>

```bash
pnpm test:seed-users
```

This runs `test/setup/seed-test-users.ts` which:

<Steps>
<Step title="Create test users">
Creates the 5 test users in the database (idempotent — skips if they exist)
</Step>
<Step title="Create organization">
Creates "Test Organization" with owner
</Step>
<Step title="Create team">
Creates "Test Sales Team" with team leader and agent
</Step>
<Step title="Assign roles">
Assigns org roles (Owner, Admin, Basic) and team roles (Team Leader, Agent)
</Step>
<Step title="Bypass RLS">
Uses RLS bypass to write directly via MikroORM
</Step>
</Steps>

### Step 2: Seed Test Data

Each test file calls `seedRbacTestData(app, personas, teamId)` in its `beforeAll`. Results are cached across files. This creates entities through the API:

<AccordionGroup>
<Accordion title="Leads">
- One by BASIC (auto-assigned as primary stakeholder with FULL access)
- One by OWNER (for admin-only tests)
- One in pool (empty stakeholders)
- One with team as stakeholder (OWNER creates, assigns team — TEAM_LEADER gets access)
- One with AGENT as READ-only stakeholder (OWNER creates, adds AGENT with `accessLevel: read`)
</Accordion>

<Accordion title="Contacts">
- One by BASIC
- One by OWNER
</Accordion>

<Accordion title="Companies">
- One by BASIC
- One by OWNER
</Accordion>

<Accordion title="Deals">
- One by OWNER (from lead + contact)
</Accordion>
</AccordionGroup>

<Tip>
Because seeding goes through the real API, all guards, auto-assignment rules, and stakeholder creation run. The test data matches what a real user would produce.
</Tip>

## How to Run

### Prerequisites

<Check>
1. Database must be running with migrations applied
2. Test users must be seeded: `pnpm test:seed-users`
3. `.env.local` must have `DATABASE_URL`, `JWT_TENANT_SECRET`, `META_TOKEN_ENCRYPTION_KEY`, `META_WEBHOOK_VERIFY_TOKEN`
</Check>

### Commands

<CodeGroup>

```bash Run all RBAC tests
pnpm test:e2e -- --testPathPattern=rbac
```

```bash Run specific entity
pnpm test:e2e -- --testPathPattern=lead-rbac
pnpm test:e2e -- --testPathPattern=deal-rbac
pnpm test:e2e -- --testPathPattern=contact-rbac
pnpm test:e2e -- --testPathPattern=company-rbac
```

```bash Verbose output
pnpm test:e2e -- --testPathPattern=rbac --verbose
```

```bash With coverage
pnpm test:e2e:cov -- --testPathPattern=rbac
```

</CodeGroup>

### Expected Output

```
Lead RBAC (E2E)
  Lead owned by BASIC user
    GET /v1/leads/:id — View lead detail
      ✓ OWNER → 200
      ✓ ADMIN → 200
      ✓ TEAM_LEADER → 403
      ✓ AGENT → 403
      ✓ BASIC → 200
  Lead on team (team stakeholder)
    GET /v1/leads/:id — View team lead detail
      ✓ OWNER → 200
      ✓ ADMIN → 200
      ✓ TEAM_LEADER → 200          ← team access works!
      ✓ AGENT → 403
      ✓ BASIC → 403
  Lead with AGENT READ access
    GET /v1/leads/:id — View lead (AGENT has read access)
      ✓ AGENT → 200                ← READ grants view
    PATCH /v1/leads/:id — Update lead (AGENT has read — should be denied)
      ✓ AGENT → 403                ← READ blocks modify
  Admin-only operations
    DELETE /v1/leads/:id — Delete lead (system.admin only)
      ✓ OWNER → 200
      ✓ ADMIN → 200
      ✓ TEAM_LEADER → 403
      ✓ AGENT → 403
      ✓ BASIC → 403
```

## How to Extend

### Adding New Endpoints

<Steps>
<Step title="Update matrix file">
Add the endpoint to the appropriate matrix file in `test/setup/matrices/`
</Step>
<Step title="Define expectations">
Specify expected status codes for each persona
</Step>
<Step title="Handle placeholders">
If the endpoint uses path parameters, ensure placeholders are defined in the test data
</Step>
<Step title="Run tests">
The matrix runner will automatically generate test cases
</Step>
</Steps>

### Adding New Personas

<Steps>
<Step title="Define persona">
Add to `test/helpers/rbac-personas.ts`
</Step>
<Step title="Update seeder">
Modify `test/setup/seed-test-users.ts` to create the new user
</Step>
<Step title="Update context">
Add authentication logic in `rbac-context.ts`
</Step>
<Step title="Update matrices">
Add expected status codes for the new persona in all relevant matrices
</Step>
</Steps>

### Adding New Entities

<Steps>
<Step title="Create matrix file">
Add new matrix file in `test/setup/matrices/`
</Step>
<Step title="Update test data">
Modify `rbac-test-data.ts` to seed entities via API
</Step>
<Step title="Create test file">
Add new E2E test file in `test/e2e/rbac/`
</Step>
<Step title="Export matrices">
Update barrel exports in `test/setup/matrices/index.ts`
</Step>
</Steps>

## Methodology

<Info>
See `test/TESTING_METHODOLOGY.md` for detailed guidance on analyzing controllers and DTOs before writing tests.
</Info>

The testing approach follows these principles:

1. **Permission-first design** — every endpoint must declare its required permissions
2. **Stakeholder-aware testing** — verify both permission-based and stakeholder-based access
3. **Real API usage** — test data creation goes through actual endpoints
4. **Declarative expectations** — matrices define what should happen, not how to test it

## CI/CD Integration

The test suite is designed for CI/CD environments:

- **Fast startup** — singleton context reduces app boot overhead
- **Isolated data** — all test users have `@propwise-test.com` emails
- **Deterministic results** — seeded data ensures consistent test outcomes
- **Clear failures** — detailed error messages with actual response bodies

<Warning>
Ensure test database is isolated from production data. The cleanup script removes all `@propwise-test.com` users.
</Warning>