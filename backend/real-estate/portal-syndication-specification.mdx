---
title: "Portal Syndication Specification"
description: "Complete specification for the Portal Syndication Module - architectural design with three-tier Listing system for publishing properties to UAE portals and ingesting leads back to CRM"
---

## Module Overview

The Portal Syndication Module allows real estate agents to publish property listings to three UAE property portals directly from PropWise CRM, and automatically receive leads back into the CRM pipeline.

### Three-Tier Architecture

<Note>
The module uses a three-tier data model that separates inventory, marketing, and portal-specific concerns:
</Note>

```
InventoryUnit  →  Listing  →  ListingPortalSync
  (inventory)     (marketing)   (per-portal state)
```

- **`InventoryUnit`** — what the unit *is* (rooms, area, price, physical attributes). Unchanged by portal syndication logic.
- **`Listing`** — how the unit is *marketed* (title, descriptions, permit number, portal classifications, marketing media). Created by an agent from a `InventoryUnit`.
- **`ListingPortalSync`** — where the listing is *published* and its current state on each portal.

<Tip>
This separation ensures `InventoryUnit` stays a clean inventory record and the `Listing` layer can eventually support off-plan units (`refUnitId`) without any structural change to the sync system.
</Tip>

### Integration Model Per Portal

| Portal | Listing Syndication | Lead Ingestion | Listing Timing |
|---|---|---|---|
| Property Finder | REST API Push (JSON) | Webhook push (primary) + REST poll fallback (15 min) | Real-time (seconds) |
| Bayut | XML Feed Pull (unified) | Pull API polling — scheduled every 15 min | 30 min – 2 hr delay |
| dubizzle | XML Feed Pull (same as Bayut) | Pull API polling — same API + endpoint as Bayut | 30 min – 2 hr delay |

<Warning>
Bayut and dubizzle share one API endpoint and one Bearer token (per agency). The `source` field in each lead response (`"bayut"` or `"dubizzle"`) determines which `LeadSource` enum value is used when the CRM lead is created.
</Warning>

### Data Flow Rules

- Listings flow **one direction only**: CRM → portals (CRM always wins)
- Leads flow **one direction only**: portals → CRM
- Portal data **never** overwrites CRM data
- `Listing` is the **single source of truth** for listing (marketing) content
- `InventoryUnit` is the **single source of truth** for unit inventory data

### Module Location

```
src/modules/real-estate/portal-syndication/
```

Imported in `src/modules/real-estate/real-estate.module.ts`.

## Folder Structure

```
src/modules/real-estate/portal-syndication/
├── portal-syndication.module.ts
├── index.ts
│
├── enums/
│   ├── portal.enum.ts
│   ├── listing-status.enum.ts            # draft | active | archived
│   ├── listing-sync-status.enum.ts       # per-portal state
│   ├── furnished-status.enum.ts
│   ├── rental-period.enum.ts
│   └── finishing-type.enum.ts
│
├── entities/
│   ├── listing.entity.ts
│   ├── portal-configuration.entity.ts
│   ├── listing-portal-sync.entity.ts
│   ├── pf-agent-mapping.entity.ts
│   └── pf-location-mapping.entity.ts
│
├── dto/
│   ├── listing.dto.ts
│   ├── portal-configuration.dto.ts
│   ├── listing-portal-sync.dto.ts
│   └── portal-dashboard.dto.ts
│
├── services/
│   ├── listing.service.ts
│   ├── syndication.service.ts
│   ├── portal-configuration.service.ts
│   ├── listing-sync.service.ts
│   ├── portal-lead-worker.service.ts
│   ├── portal-validation.service.ts
│   ├── listing-image.service.ts
│   ├── feed-cache.service.ts
│   ├── api-key-expiration-check.service.ts
│   ├── syndication-worker.service.ts
│   ├── sync-reconciliation.service.ts
│   ├── bayut/
│   │   ├── bayut-lead-poller.service.ts
│   │   └── bayut-lead-parser.service.ts
│   └── pf/
│       ├── pf-token.service.ts
│       ├── pf-location-mapping.service.ts
│       ├── pf-agent-mapping.service.ts
│       ├── pf-compliance.service.ts
│       ├── pf-credit.service.ts
│       └── pf-webhook-subscription.service.ts
│
├── adapters/
│   ├── push-portal-adapter.interface.ts
│   ├── feed-portal-adapter.interface.ts
│   ├── property-finder.adapter.ts
│   └── bayut-dubizzle-feed.adapter.ts
│
└── controllers/
    ├── listing.controller.ts
    ├── portal-configuration.controller.ts
    ├── listing-sync.controller.ts
    ├── portal-feed.controller.ts
    ├── portal-webhook.controller.ts
    └── portal-dashboard.controller.ts
```

## Enums

### Portal Types

```typescript
export enum Portal {
  PROPERTY_FINDER = 'property_finder',
  BAYUT = 'bayut',
  DUBIZZLE = 'dubizzle',
}
```

### Listing Sync Status

<AccordionGroup>
<Accordion title="Nine-state sync lifecycle">
The sync status covers the full Property Finder async lifecycle and feed-based portal model:

```typescript
export enum ListingSyncStatus {
  DRAFT = 'draft',                           // Not yet publishable
  PENDING = 'pending',                       // Job queued, adapter call in progress
  AWAITING_CONFIRMATION = 'awaiting_confirmation', // PF: waiting for webhook
  PUBLISHED = 'published',                   // Live on portal
  REJECTED = 'rejected',                     // PF rejected listing
  COMPLIANCE_ACTION = 'compliance_action',   // PF flagged listing
  ERROR = 'error',                           // Sync failed after retries
  INSUFFICIENT_CREDITS = 'insufficient_credits', // PF: balance < publish cost
  REMOVED = 'removed',                       // Taken down
}
```

<Note>
For Bayut/dubizzle: Status goes directly `draft → published` on enable (no `awaiting_confirmation`), and `published → removed` on disable. Feed portals have no rejection mechanism.
</Note>
</Accordion>
</AccordionGroup>

### Property Characteristics

<Tabs>
<Tab title="Furnished Status">
```typescript
export enum FurnishedStatus {
  FURNISHED = 'furnished',
  UNFURNISHED = 'unfurnished',
  PARTLY_FURNISHED = 'partly_furnished',
}
```
</Tab>

<Tab title="Rental Period">
```typescript
export enum RentalPeriod {
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
  YEARLY = 'yearly',
}
```
</Tab>

<Tab title="Finishing Type">
```typescript
export enum FinishingType {
  FULLY_FINISHED = 'fully_finished',
  SEMI_FINISHED = 'semi_finished',
  UNFINISHED = 'unfinished',
}
```

**Property Finder transform:** 
- `FULLY_FINISHED` → `"fully-finished"`
- `SEMI_FINISHED` → `"semi-finished"` 
- `UNFINISHED` → `"unfinished"`
</Tab>
</Tabs>

### Listing Status

Three-state lifecycle for the `Listing` entity (distinct from `ListingSyncStatus` which is per-portal):

```typescript
export enum ListingStatus {
  DRAFT = 'draft',       // Marketing content being prepared
  ACTIVE = 'active',     // Ready for portal syndication
  ARCHIVED = 'archived', // No longer available; triggers removal
}
```

## Entity Definitions

### Architecture Note - Three-Tier Data Model

<Info>
The three-tier model separates concerns:
- **InventoryUnit**: Physical properties and inventory data
- **Listing**: Marketing presentation and portal-specific fields
- **ListingPortalSync**: Per-portal state and synchronization status
</Info>

```
InventoryUnit  ──1:many──▶  Listing  ──1:many──▶  ListingPortalSync
  (inventory)                (marketing)             (per-portal state)
```

<Check>
**Why a separate `Listing` entity?**
1. Keeps `InventoryUnit` as a clean inventory record
2. Future-proofs off-plan support with optional `refUnitId`
3. Provides natural home for admin approval workflows
</Check>

### Portal Configuration

Per-organization portal credentials and settings. One row per (org, portal).

```typescript
@Entity()
@Filter({ name: 'isDeleted', default: true, cond: { isDeleted: false } })
@Unique({ properties: ['organization', 'portal'] })
export class PortalConfiguration {
  @PrimaryKey({ type: UuidType })
  id: string = v4();

  @ManyToOne(() => Organization)
  @Index()
  organization!: Organization;

  @Enum(() => Portal)
  @Property({ type: 'text' })
  portal!: Portal;

  @Property({ type: 'boolean', default: false })
  isEnabled: boolean = false;

  // PF credentials (encrypted at application layer)
  @Property({ type: 'text', nullable: true })
  apiKey?: string;

  @Property({ type: 'text', nullable: true })
  apiSecret?: string;

  // Auto-generated on creation for Bayut/dubizzle
  @Property({ nullable: true })
  feedUrl?: string;

  // PF webhook HMAC secret
  @Property({ nullable: true })
  webhookSecret?: string;

  // Feed URL HMAC secret
  @Property({ nullable: true })
  feedSecret?: string;

  // PF API key expiry date
  @Property({ type: 'timestamptz', nullable: true })
  apiKeyExpiresAt?: Date;

  // Last successful Bayut/dubizzle lead poll timestamp
  @Property({ type: 'timestamptz', nullable: true })
  lastLeadPollAt?: Date;

  @Property({ type: 'timestamptz', default: () => new Date() })
  createdAt: Date = new Date();

  @Property({ 
    type: 'timestamptz', 
    default: () => new Date(),
    onUpdate: () => new Date() 
  })
  updatedAt: Date = new Date();

  @Property({ type: 'boolean', default: false })
  isDeleted: boolean = false;
}
```

### Listing Entity

The marketing layer between `InventoryUnit` and portals.

```typescript
@Entity()
@Filter({ name: 'isDeleted', default: true, cond: { isDeleted: false } })
export class Listing {
  @PrimaryKey({ type: UuidType })
  id: string = v4();

  @ManyToOne(() => Organization)
  @Index()
  organization!: Organization;

  // Core relationship to inventory
  @ManyToOne(() => InventoryUnit)
  @Index()
  inventoryUnit!: InventoryUnit;

  // Optional reference unit ID for off-plan support
  @Property({ type: 'integer', nullable: true })
  @Index()
  refUnitId?: number;

  @Enum(() => ListingStatus)
  @Property({ type: 'text', default: ListingStatus.DRAFT })
  @Index()
  status: ListingStatus = ListingStatus.DRAFT;

  // Marketing content
  @Property({ type: 'text' })
  title!: string;

  @Property({ type: 'text', nullable: true })
  titleArabic?: string;

  @Property({ type: 'text', nullable: true })
  description?: string;

  @Property({ type: 'text', nullable: true })
  descriptionArabic?: string;

  // Portal-specific fields
  @Property({ type: 'text', nullable: true })
  permitNumber?: string;

  @Enum(() => FurnishedStatus)
  @Property({ type: 'text', nullable: true })
  furnished?: FurnishedStatus;

  @Enum(() => RentalPeriod)
  @Property({ type: 'text', nullable: true })
  rentalPeriod?: RentalPeriod;

  @Enum(() => FinishingType)
  @Property({ type: 'text', nullable: true })
  finishingType?: FinishingType;

  // Media
  @Property({ type: 'json', nullable: true })
  imageUrls?: string[];

  @Property({ type: 'json', nullable: true })
  videoUrls?: string[];

  @Property({ type: 'json', nullable: true })
  virtualTourUrls?: string[];

  @Property({ type: 'timestamptz', default: () => new Date() })
  createdAt: Date = new Date();

  @Property({ 
    type: 'timestamptz', 
    default: () => new Date(),
    onUpdate: () => new Date() 
  })
  updatedAt: Date = new Date();

  @Property({ type: 'boolean', default: false })
  isDeleted: boolean = false;

  // Relations
  @OneToMany(() => ListingPortalSync, sync => sync.listing, { cascade: [Cascade.ALL] })
  portalSyncs = new Collection<ListingPortalSync>(this);
}
```

### Listing Portal Sync

Per-portal synchronization state.

```typescript
@Entity()
@Filter({ name: 'isDeleted', default: true, cond: { isDeleted: false } })
@Unique({ properties: ['listing', 'portal'] })
export class ListingPortalSync {
  @PrimaryKey({ type: UuidType })
  id: string = v4();

  @ManyToOne(() => Listing)
  @Index()
  listing!: Listing;

  @Enum(() => Portal)
  @Property({ type: 'text' })
  @Index()
  portal!: Portal;

  @Enum(() => ListingSyncStatus)
  @Property({ type: 'text', default: ListingSyncStatus.DRAFT })
  @Index()
  status: ListingSyncStatus = ListingSyncStatus.DRAFT;

  // Portal-specific identifiers
  @Property({ type: 'text', nullable: true })
  @Index()
  portalListingId?: string;

  @Property({ type: 'text', nullable: true })
  portalReferenceId?: string;

  // Sync metadata
  @Property({ type: 'integer', default: 0 })
  retryCount: number = 0;

  @Property({ type: 'text', nullable: true })
  lastError?: string;

  @Property({ type: 'timestamptz', nullable: true })
  lastSyncAt?: Date;

  @Property({ type: 'timestamptz', nullable: true })
  nextRetryAt?: Date;

  @Property({ type: 'timestamptz', default: () => new Date() })
  createdAt: Date = new Date();

  @Property({ 
    type: 'timestamptz', 
    default: () => new Date(),
    onUpdate: () => new Date() 
  })
  updatedAt: Date = new Date();

  @Property({ type: 'boolean', default: false })
  isDeleted: boolean = false;
}
```

## Core Services

### Listing Service

<Steps>
<Step title="Create listing from inventory unit">
```typescript
async createFromInventoryUnit(
  inventoryUnitId: string,
  createDto: CreateListingDto,
  organizationId: string
): Promise<Listing>
```
</Step>

<Step title="Update marketing content">
```typescript
async update(
  listingId: string,
  updateDto: UpdateListingDto,
  organizationId: string
): Promise<Listing>
```
</Step>

<Step title="Archive listing">
```typescript
async archive(
  listingId: string,
  organizationId: string
): Promise<void>
```
</Step>
</Steps>

### Syndication Service

Hub orchestrator for portal publishing operations.

<CodeGroup>
```typescript Main Operations
async publishToPortal(
  listingId: string,
  portal: Portal,
  organizationId: string
): Promise<ListingPortalSync>

async unpublishFromPortal(
  listingId: string,
  portal: Portal,
  organizationId: string
): Promise<void>

async publishToAllEnabledPortals(
  listingId: string,
  organizationId: string
): Promise<ListingPortalSync[]>
```

```typescript Validation
async validateForPortal(
  listing: Listing,
  portal: Portal
): Promise<ValidationResult>
```
</CodeGroup>

### Portal Configuration Service

<Tabs>
<Tab title="CRUD Operations">
```typescript
async create(dto: CreatePortalConfigurationDto): Promise<PortalConfiguration>
async update(id: string, dto: UpdatePortalConfigurationDto): Promise<PortalConfiguration>
async findByOrganizationAndPortal(orgId: string, portal: Portal): Promise<PortalConfiguration>
async getEnabledPortals(organizationId: string): Promise<Portal[]>
```
</Tab>

<Tab title="Security Operations">
```typescript
async rotateWebhookSecret(id: string): Promise<string>
async rotateFeedSecret(id: string): Promise<string>
async encryptCredentials(apiKey: string, apiSecret: string): Promise<{ apiKey: string, apiSecret: string }>
async decryptCredentials(config: PortalConfiguration): Promise<{ apiKey: string, apiSecret: string }>
```
</Tab>
</Tabs>

## Portal Adapters

### Property Finder Adapter

REST API push adapter for real-time synchronization.

```typescript
export class PropertyFinderAdapter implements PushPortalAdapter {
  async createListing(listing: Listing, config: PortalConfiguration): Promise<CreateListingResult>
  async updateListing(listing: Listing, sync: ListingPortalSync, config: PortalConfiguration): Promise<UpdateListingResult>
  async publishListing(portalListingId: string, config: PortalConfiguration): Promise<PublishListingResult>
  async unpublishListing(portalListingId: string, config: PortalConfiguration): Promise<void>
  async deleteListing(portalListingId: string, config: PortalConfiguration): Promise<void>
}
```

<Warning>
Property Finder uses an asynchronous workflow: create → publish → await webhook confirmation. The listing is not live until the webhook confirms successful publication.
</Warning>

### Bayut/dubizzle Feed Adapter

Unified XML feed adapter for both Bayut and dubizzle portals.

```typescript
export class BayutDubizzleFeedAdapter implements FeedPortalAdapter {
  async generateFeed(
    organization: Organization,
    config: PortalConfiguration,
    enabledPortals: Portal[]
  ): Promise<string>
  
  async validateFeedStructure(xmlContent: string): Promise<ValidationResult>
  
  async getListingsForFeed(
    organizationId: string,
    enabledPortals: Portal[]
  ): Promise<Listing[]>
}
```

## Lead Ingestion

### Property Finder Webhook

<Steps>
<Step title="Webhook endpoint receives leads">
```typescript
@Post('/webhook/property-finder')
async handlePropertyFinderWebhook(
  @Body() payload: any,
  @Headers('x-signature') signature: string
): Promise<void>
```
</Step>

<Step title="HMAC signature verification">
```typescript
private verifyWebhookSignature(
  payload: string,
  signature: string,
  secret: string
): boolean
```
</Step>

<Step title="Queue lead for processing">
```typescript
await this.queueService.add('portal-lead-ingestion', {
  source: 'property_finder',
  payload,
  organizationId
});
```
</Step>
</Steps>

### Bayut/dubizzle Polling

<Note>
Bayut and dubizzle share a single API endpoint and Bearer token per agency. The `source` field in responses determines the lead source.
</Note>

```typescript
@Cron('*/15 * * * *') // Every 15 minutes
async pollBayutDubizzleLeads(): Promise<void> {
  const orgs = await this.getOrgsWithBayutDubizzleEnabled();
  
  for (const org of orgs) {
    const config = await this.getPortalConfig(org.id, Portal.BAYUT);
    const lastPoll = config.lastLeadPollAt || new Date(Date.now() - 24 * 60 * 60 * 1000);
    
    const leads = await this.pullLeads(config.apiKey, lastPoll);
    
    for (const lead of leads) {
      await this.queueService.add('portal-lead-ingestion', {
        source: lead.source, // 'bayut' or 'dubizzle'
        payload: lead,
        organizationId: org.id
      });
    }
    
    await this.updateLastPollTimestamp(config.id);
  }
}
```

### Lead Processing Worker

```typescript
@Process('portal-lead-ingestion')
async processPortalLead(job: Job<PortalLeadPayload>): Promise<void> {
  const { source, payload, organizationId } = job.data;
  
  // Transform portal lead to CRM format
  const leadData = await this.transformPortalLead(source, payload);
  
  // Create CRM lead with deduplication
  await this.leadService.createFromPortal(leadData, organizationId);
}
```

## Validation and Compliance

### Portal-Specific Validation

<AccordionGroup>
<Accordion title="Property Finder Requirements">
- Title: Required, 10-150 characters
- Description: Required, 50-2000 characters
- Permit number: Required for sales listings
- Price: Required, > 0
- Images: Minimum 3, maximum 20
- Agent mapping: Required valid PF agent ID
- Location mapping: Required valid PF location ID
</Accordion>

<Accordion title="Bayut/dubizzle Requirements">
- Title: Required, 10-100 characters
- Description: Optional
- Price: Required, > 0
- Images: Minimum 1, maximum 15
- Standard area/room validations
</Accordion>
</AccordionGroup>

### DLD/ADREC Compliance (Property Finder)

```typescript
export class PfComplianceService {
  async validateDldPermit(permitNumber: string, propertyType: string): Promise<boolean>
  async validateAdrecPermit(permitNumber: string, propertyType: string): Promise<boolean>
  async getRequiredPermitType(listing: Listing): Promise<'dld' | 'adrec' | null>
}
```

<Warning>
Sales listings in Dubai require valid DLD permit numbers. Abu Dhabi sales require ADREC permits. The system validates permit format but not real-time permit status.
</Warning>

## Queue System & Workers

### Queue Configuration

<Tabs>
<Tab title="Listing Sync Queue">
```typescript
// Queue: listing-portal-sync
interface ListingSyncJob {
  listingId: string;
  portal: Portal;
  operation: 'create' | 'update' | 'publish' | 'unpublish' | 'delete';
  organizationId: string;
  retryCount?: number;
}
```
</Tab>

<Tab title="Lead Ingestion Queue">
```typescript
// Queue: portal-lead-ingestion
interface PortalLeadJob {
  source: 'property_finder' | 'bayut' | 'dubizzle';
  payload: any;
  organizationId: string;
}
```
</Tab>

<Tab title="Image Processing Queue">
```typescript
// Queue: listing-image-processing
interface ImageProcessingJob {
  listingId: string;
  imageUrls: string[];
  portal: Portal;
  organizationId: string;
}
```
</Tab>
</Tabs>

### Worker Services

<Steps>
<Step title="Syndication Worker">
Processes listing sync jobs with exponential backoff retry logic.
```typescript
@Process('listing-portal-sync')
async processSyncJob(job: Job<ListingSyncJob>): Promise<void>
```
</Step>

<Step title="Lead Worker">
Converts portal leads to CRM leads with deduplication.
```typescript
@Process('portal-lead-ingestion')
async processLeadJob(job: Job<PortalLeadJob>): Promise<void>
```
</Step>

<Step title="Image Worker">
Validates, optimizes, and re-uploads images for portal compliance.
```typescript
@Process('listing-image-processing')
async processImageJob(job: Job<ImageProcessingJob>): Promise<void>
```
</Step>
</Steps>

## Security & Encryption

### Credential Security

<Warning>
All API credentials are encrypted at the application layer before database storage using AES-256-GCM.
</Warning>

<Steps>
<Step title="Encryption on storage">
```typescript
const encryptedKey = await this.encryptionService.encrypt(apiKey);
const encryptedSecret = await this.encryptionService.encrypt(apiSecret);
```
</Step>

<Step title="Decryption on use">
```typescript
const { apiKey, apiSecret } = await this.decryptCredentials(config);
```
</Step>

<Step title="HMAC secret management">
```typescript
// Separate secrets for webhook verification and feed URL generation
webhookSecret: crypto.randomBytes(32).toString('hex');
feedSecret: crypto.randomBytes(32).toString('hex');
```
</Step>
</Steps>

### Feed URL Security

Bayut/dubizzle feed URLs include HMAC tokens for authentication:

```typescript
const feedUrl = `${BASE_URL}/api/portal-syndication/feed/${organizationId}?token=${hmacToken}`;
```

## API Endpoints

### Listing Management

<CodeGroup>
```typescript Create Listing
POST /api/listings
{
  "inventoryUnitId": "uuid",
  "title": "string",
  "description": "string",
  "permitNumber": "string",
  "furnished": "furnished",
  "finishingType": "fully_finished"
}
```

```typescript Update Listing
PATCH /api/listings/:id
{
  "title": "string",
  "description": "string",
  "imageUrls": ["string"]
}
```

```typescript Publish to Portal
POST /api/listings/:id/sync/:portal/publish
```

```typescript Unpublish from Portal
POST /api/listings/:id/sync/:portal/unpublish
```
</CodeGroup>

### Portal Configuration

<CodeGroup>
```typescript Create Portal Config
POST /api/portal-configurations
{
  "portal": "property_finder",
  "apiKey": "string",
  "apiSecret": "string",
  "isEnabled": true
}
```

```typescript Update Configuration
PATCH /api/portal-configurations/:id
{
  "isEnabled": false,
  "apiKeyExpiresAt": "2024-12-31T23:59:59Z"
}
```
</CodeGroup>

### Public Endpoints

<CodeGroup>
```typescript XML Feed (Public)
GET /api/portal-syndication/feed/:organizationId?token=hmac_token
Content-Type: application/xml
```

```typescript Property Finder Webhook (Public)
POST /api/portal-syndication/webhook/property-finder
X-Signature: sha256=hmac_signature
```
</CodeGroup>

## Monitoring & Analytics

### Dashboard Metrics

<CardGroup cols={2}>
<Card title="Listing Performance" icon="chart-line">
- Active listings per portal
- Successful vs. failed syncs
- Average time to publish
</Card>

<Card title="Lead Analytics" icon="users">
- Leads by portal source
- Conversion rates
- Lead response times
</Card>

<Card title="System Health" icon="heartbeat">
- Queue depths
- Error rates
- API response times
</Card>

<Card title="Compliance Status" icon="shield-check">
- Permit validation rates
- Image compliance issues
- Credit balance alerts
</Card>
</CardGroup>

### Reconciliation Service

<Steps>
<Step title="Daily sync verification">
Compare CRM listing states with portal API responses
</Step>

<Step title="Missed lead detection">
Cross-reference portal lead IDs with CRM lead records
</Step>

<Step title="Credit monitoring">
Track Property Finder credit consumption and alert on low balances
</Step>

<Step title="API health checks">
Verify portal API connectivity and authentication
</Step>
</Steps>

## Error Handling & Recovery

### Retry Strategies

<Tabs>
<Tab title="Exponential Backoff">
```typescript
const retryDelays = [30, 60, 300, 900, 3600]; // seconds
const nextRetry = new Date(Date.now() + retryDelays[retryCount] * 1000);
```
</Tab>

<Tab title="Portal-Specific Limits">
- **Property Finder**: 5 retries over 2 hours
- **Bayut/dubizzle**: 3 retries over 1 hour (feed-based)
</Tab>
</Tabs>

### Error Categories

<AccordionGroup>
<Accordion title="Validation Errors">
- Missing required fields
- Invalid data formats
- Compliance violations
- **Resolution**: Fix data and retry
</Accordion>

<Accordion title="API Errors">
- Authentication failures
- Rate limiting
- Portal downtime
- **Resolution**: Automatic retry with backoff
</Accordion>

<Accordion title="Business Logic Errors">
- Insufficient credits
- Duplicate listings
- Rejected content
- **Resolution**: Manual intervention required
</Accordion>
</AccordionGroup>

### Health Monitoring

```typescript
@Cron('0 */6 * * *') // Every 6 hours
async performHealthChecks(): Promise<void> {
  // Check portal API connectivity
  // Verify webhook endpoints
  // Monitor queue depths
  // Alert on high error rates
}
```

<Info>
The module includes comprehensive error tracking, automatic recovery mechanisms, and detailed logging for troubleshooting portal integration issues.
</Info>