---
title: "Portal Syndication Specification"
description: "Complete technical specification for the Portal Syndication Module, including architecture, entities, services, and integration patterns for Property Finder, Bayut, and dubizzle portals."
---

## Module Overview

The Portal Syndication Module allows real estate agents to publish property listings to three UAE property portals directly from PropWise CRM, and automatically receive leads back into the CRM pipeline.

<Info>
**Version 3.0** — Architectural revision introducing the `Listing` entity as the marketing layer between `InventoryUnit` (inventory) and `ListingPortalSync` (per-portal state).
</Info>

### Three-Tier Architecture

```
InventoryUnit  →  Listing  →  ListingPortalSync
  (inventory)     (marketing)   (per-portal state)
```

- **`InventoryUnit`** — what the unit *is* (rooms, area, price, physical attributes). Unchanged by portal syndication logic.
- **`Listing`** — how the unit is *marketed* (title, descriptions, permit number, portal classifications, marketing media). Created by an agent from an `InventoryUnit`.
- **`ListingPortalSync`** — where the listing is *published* and its current state on each portal.

<Note>
This separation ensures `InventoryUnit` stays a clean inventory record and the `Listing` layer can eventually support off-plan units (`refUnitId`) without any structural change to the sync system.
</Note>

### Integration Model Per Portal

| Portal | Listing Syndication | Lead Ingestion | Listing Timing |
|---|---|---|---|
| Property Finder | REST API Push (JSON) | Webhook push (primary) + REST poll fallback (15 min) | Real-time (seconds) |
| Bayut | XML Feed Pull (unified) | Pull API polling — scheduled every 15 min | 30 min – 2 hr delay |
| dubizzle | XML Feed Pull (same as Bayut) | Pull API polling — same API + endpoint as Bayut | 30 min – 2 hr delay |

<Warning>
**Bayut / dubizzle lead ingestion note:** Bayut and dubizzle share one API endpoint and one Bearer token (per agency). The `source` field in each lead response (`"bayut"` or `"dubizzle"`) determines which `LeadSource` enum value is used when the CRM lead is created.
</Warning>

### Data Flow Rules

- Listings flow **one direction only**: CRM → portals (CRM always wins)
- Leads flow **one direction only**: portals → CRM
- Portal data **never** overwrites CRM data
- `Listing` is the **single source of truth** for listing (marketing) content
- `InventoryUnit` is the **single source of truth** for unit inventory data

### Module Location

```
src/modules/real-estate/portal-syndication/
```

Imported in `src/modules/real-estate/real-estate.module.ts`.

## Folder Structure

```
src/modules/real-estate/portal-syndication/
├── portal-syndication.module.ts
├── index.ts
│
├── enums/
│   ├── portal.enum.ts
│   ├── listing-status.enum.ts            # draft | active | archived (Listing lifecycle)
│   ├── listing-sync-status.enum.ts       # draft | pending | awaiting_confirmation | ... (per-portal state)
│   ├── furnished-status.enum.ts
│   ├── rental-period.enum.ts
│   └── finishing-type.enum.ts            # fully_finished | semi_finished | unfinished (PF: finishingType)
│
├── entities/
│   ├── listing.entity.ts                 # Marketing layer between InventoryUnit and portals
│   ├── portal-configuration.entity.ts
│   ├── listing-portal-sync.entity.ts
│   ├── pf-agent-mapping.entity.ts
│   └── pf-location-mapping.entity.ts
│
├── dto/
│   ├── listing.dto.ts                    # Create, Update, Response DTOs for Listing
│   ├── portal-configuration.dto.ts
│   ├── listing-portal-sync.dto.ts
│   └── portal-dashboard.dto.ts
│
├── services/
│   ├── listing.service.ts                # CRUD for Listing (create from unit, update, archive)
│   ├── syndication.service.ts            # Hub orchestrator
│   ├── portal-configuration.service.ts   # CRUD for org portal configs
│   ├── listing-sync.service.ts           # State machine + CRUD
│   ├── portal-lead-worker.service.ts     # Portal lead ingestion worker (pg-boss queue consumer)
│   ├── portal-validation.service.ts      # Per-portal field validation
│   ├── listing-image.service.ts          # Image process-on-publish (validate + auto-fix + re-upload)
│   ├── feed-cache.service.ts             # Redis-backed XML feed cache
│   ├── api-key-expiration-check.service.ts # Daily cron: PF API key expiry alerting + auto-disable
│   ├── syndication-worker.service.ts      # pg-boss worker wiring for listing sync jobs
│   ├── sync-reconciliation.service.ts     # Daily reconciliation + missed-lead polling fallback
│   ├── bayut/
│   │   ├── bayut-lead-poller.service.ts  # @Cron every 15 min — polls Bayut Pull API per org
│   │   └── bayut-lead-parser.service.ts  # Normalizes 7 lead types × 3 targets into common shape
│   └── pf/
│       ├── pf-token.service.ts           # JWT lifecycle management
│       ├── pf-location-mapping.service.ts
│       ├── pf-agent-mapping.service.ts
│       ├── pf-compliance.service.ts      # DLD/ADREC checks
│       ├── pf-credit.service.ts          # Credit balance + per-listing publish price checks
│       └── pf-webhook-subscription.service.ts
│
├── adapters/
│   ├── push-portal-adapter.interface.ts
│   ├── feed-portal-adapter.interface.ts
│   ├── property-finder.adapter.ts        # REST push adapter
│   └── bayut-dubizzle-feed.adapter.ts    # Unified XML feed adapter
│
├── controllers/
│   ├── listing.controller.ts             # Listing CRUD (create from unit, update, validate)
│   ├── portal-configuration.controller.ts
│   ├── listing-sync.controller.ts        # Publish/unpublish/retry per listing
│   ├── portal-feed.controller.ts         # Public XML feed endpoint
│   ├── portal-webhook.controller.ts      # Public webhook endpoint
│   └── portal-dashboard.controller.ts
│
├── events/
│   └── portal-syndication.events.ts
│
└── listeners/
    └── portal-syndication-event.listener.ts
```

## Enums

### Portal Types

```typescript portal.enum.ts
export enum Portal {
  PROPERTY_FINDER = 'property_finder',
  BAYUT = 'bayut',
  DUBIZZLE = 'dubizzle',
}
```

### Listing Sync Status

Nine states covering the full Property Finder async lifecycle and feed-based portal model:

```typescript listing-sync-status.enum.ts
export enum ListingSyncStatus {
  DRAFT = 'draft',                           // Not yet publishable (validation failed or not requested)
  PENDING = 'pending',                       // Job queued, adapter call in progress
  AWAITING_CONFIRMATION = 'awaiting_confirmation', // PF: create+publish called, waiting for webhook
  PUBLISHED = 'published',                   // Live on portal
  REJECTED = 'rejected',                     // PF rejected listing (compliance/quality)
  COMPLIANCE_ACTION = 'compliance_action',   // PF flagged listing for action
  ERROR = 'error',                           // Sync failed after retries
  INSUFFICIENT_CREDITS = 'insufficient_credits', // PF: balance < publish cost; agent retries after top-up
  REMOVED = 'removed',                       // Taken down (disabled, archived, deleted)
}
```

<Note>
**For Bayut/dubizzle:** Status goes directly `draft → published` on enable (no `awaiting_confirmation`), and `published → removed` on disable. Feed portals have no rejection mechanism.
</Note>

### Property Attributes

<Tabs>
  <Tab title="Furnished Status">
    ```typescript furnished-status.enum.ts
    export enum FurnishedStatus {
      FURNISHED = 'furnished',
      UNFURNISHED = 'unfurnished',
      PARTLY_FURNISHED = 'partly_furnished',
    }
    ```
  </Tab>
  <Tab title="Rental Period">
    ```typescript rental-period.enum.ts
    export enum RentalPeriod {
      DAILY = 'daily',
      WEEKLY = 'weekly',
      MONTHLY = 'monthly',
      YEARLY = 'yearly',
    }
    ```
  </Tab>
  <Tab title="Finishing Type">
    ```typescript finishing-type.enum.ts
    export enum FinishingType {
      FULLY_FINISHED = 'fully_finished',
      SEMI_FINISHED = 'semi_finished',
      UNFINISHED = 'unfinished',
    }
    ```
    
    <Info>
    **PF transform:** `FULLY_FINISHED` → `"fully-finished"`; `SEMI_FINISHED` → `"semi-finished"`; `UNFINISHED` → `"unfinished"`.
    </Info>
  </Tab>
</Tabs>

### Listing Status

Three-state lifecycle for the `Listing` entity (distinct from `ListingSyncStatus` which is per-portal):

```typescript listing-status.enum.ts
export enum ListingStatus {
  DRAFT = 'draft',       // Marketing content being prepared; not yet published to any portal
  ACTIVE = 'active',     // Ready for portal syndication
  ARCHIVED = 'archived', // No longer available; triggers removal from all portals
}
```

## Entity Definitions

### Architecture Overview

<Info>
**Three-Tier Data Model:**

```
InventoryUnit  ──1:many──▶  Listing  ──1:many──▶  ListingPortalSync
  (inventory)                (marketing)             (per-portal state)
```

- Fields describing **what the unit is** (rooms, area, price, bathrooms, location coords) live on `InventoryUnit`.
- Fields describing **how the unit is advertised** (title, descriptions, permit number, portal classifications, marketing media) live on `Listing`.
- Per-portal state (publish status, portal listing ID, retry count) lives on `ListingPortalSync`.
</Info>

<Warning>
**Portal lead ingestion:** There is no `PortalLead` staging entity. Inbound leads from portal **webhooks (PF) and scheduled Pull API polling (Bayut/dubizzle)** are queued directly via pg-boss and converted immediately to CRM `Lead` entities by the worker.
</Warning>

### Portal Configuration

Per-organization portal credentials and settings. One row per (org, portal).

```typescript portal-configuration.entity.ts
@Entity()
@Filter({ name: 'isDeleted', default: true, cond: { isDeleted: false } })
@Unique({ properties: ['organization', 'portal'] })
export class PortalConfiguration {
  @PrimaryKey({ type: UuidType })
  id: string = v4();

  @ManyToOne(() => Organization)
  @Index()
  organization!: Organization;

  @Enum(() => Portal)
  @Property({ type: 'text' })
  portal!: Portal;

  @Property({ type: 'boolean', default: false })
  isEnabled: boolean = false;

  // PF credentials (encrypted at application layer)
  @Property({ type: 'text', nullable: true })
  apiKey?: string;

  @Property({ type: 'text', nullable: true })
  apiSecret?: string;

  // Auto-generated on creation for Bayut/dubizzle
  @Property({ nullable: true })
  feedUrl?: string;

  // PF webhook HMAC secret — used only for verifying X-Signature on PF webhook payloads
  @Property({ nullable: true })
  webhookSecret?: string;

  // Feed URL HMAC secret — used only for generating/validating the Bayut/dubizzle feed URL token
  @Property({ nullable: true })
  feedSecret?: string;

  // PF API key expiry date — visible in PF Expert when the key is generated (max 365 days)
  @Property({ type: 'timestamptz', nullable: true })
  apiKeyExpiresAt?: Date;

  // Tracks the timestamp of the last successful Bayut/dubizzle lead poll
  @Property({ type: 'timestamptz', nullable: true })
  lastLeadPoll?: Date;

  @Property({ type: 'timestamptz', default: 'now()' })
  createdAt: Date = new Date();

  @Property({ type: 'timestamptz', onUpdate: () => new Date() })
  updatedAt: Date = new Date();

  @Property({ type: 'boolean', default: false })
  isDeleted: boolean = false;
}
```

### Listing Entity

Marketing layer between `InventoryUnit` and portals. Contains all portal-specific marketing content.

<AccordionGroup>
  <Accordion title="Core Listing Fields">
    ```typescript listing.entity.ts
    @Entity()
    @Filter({ name: 'isDeleted', default: true, cond: { isDeleted: false } })
    export class Listing {
      @PrimaryKey({ type: UuidType })
      id: string = v4();

      @ManyToOne(() => Organization)
      @Index()
      organization!: Organization;

      // Main unit reference (always present for inventory units)
      @ManyToOne(() => InventoryUnit, { nullable: true })
      @Index()
      inventoryUnit?: InventoryUnit;

      // Off-plan unit reference (future use)
      @Property({ type: 'integer', nullable: true })
      @Index()
      refUnitId?: number;

      @Enum(() => ListingStatus)
      @Property({ type: 'text', default: ListingStatus.DRAFT })
      @Index()
      status: ListingStatus = ListingStatus.DRAFT;

      @ManyToOne(() => User)
      createdBy!: User;
    ```
  </Accordion>

  <Accordion title="Marketing Content Fields">
    ```typescript
    // Marketing content
    @Property({ type: 'text' })
    title!: string;

    @Property({ type: 'text', nullable: true })
    description?: string;

    @Property({ type: 'text', nullable: true })
    descriptionAr?: string;

    // Permit numbers
    @Property({ type: 'text', nullable: true })
    @Index()
    permitNumber?: string;

    @Property({ type: 'text', nullable: true })
    @Index()
    dldPermitNumber?: string;

    @Property({ type: 'text', nullable: true })
    @Index()
    reraPermitNumber?: string;
    ```
  </Accordion>

  <Accordion title="Portal Classification Fields">
    ```typescript
    // Portal classifications
    @Property({ type: 'text', nullable: true })
    pfCategoryId?: string;

    @Property({ type: 'text', nullable: true })
    pfLocationId?: string;

    @Property({ type: 'text', nullable: true })
    bayutLocationPath?: string;

    @Property({ type: 'json', nullable: true })
    bayutAmenityIds?: number[];

    // Property attributes for portals
    @Enum(() => FurnishedStatus)
    @Property({ type: 'text', nullable: true })
    furnishedStatus?: FurnishedStatus;

    @Enum(() => RentalPeriod)
    @Property({ type: 'text', nullable: true })
    rentalPeriod?: RentalPeriod;

    @Enum(() => FinishingType)
    @Property({ type: 'text', nullable: true })
    finishingType?: FinishingType;
    ```
  </Accordion>

  <Accordion title="Media and Timestamps">
    ```typescript
    // Media
    @Property({ type: 'json', nullable: true })
    imageUrls?: string[];

    @Property({ type: 'text', nullable: true })
    videoUrl?: string;

    @Property({ type: 'text', nullable: true })
    virtualTourUrl?: string;

    // Timestamps
    @Property({ type: 'timestamptz', default: 'now()' })
    createdAt: Date = new Date();

    @Property({ type: 'timestamptz', onUpdate: () => new Date() })
    updatedAt: Date = new Date();

    @Property({ type: 'boolean', default: false })
    isDeleted: boolean = false;

    // Relations
    @OneToMany(() => ListingPortalSync, sync => sync.listing)
    portalSyncs = new Collection<ListingPortalSync>(this);
    ```
  </Accordion>
</AccordionGroup>

### Listing Portal Sync

Per-portal state tracking for each listing. One row per (listing, portal).

```typescript listing-portal-sync.entity.ts
@Entity()
@Unique({ properties: ['listing', 'portal'] })
export class ListingPortalSync {
  @PrimaryKey({ type: UuidType })
  id: string = v4();

  @ManyToOne(() => Listing)
  @Index()
  listing!: Listing;

  @Enum(() => Portal)
  @Property({ type: 'text' })
  portal!: Portal;

  @Enum(() => ListingSyncStatus)
  @Property({ type: 'text', default: ListingSyncStatus.DRAFT })
  @Index()
  status: ListingSyncStatus = ListingSyncStatus.DRAFT;

  @Property({ type: 'text', nullable: true })
  portalListingId?: string;

  @Property({ type: 'integer', default: 0 })
  retryCount: number = 0;

  @Property({ type: 'text', nullable: true })
  lastError?: string;

  @Property({ type: 'timestamptz', nullable: true })
  lastSyncAttempt?: Date;

  @Property({ type: 'timestamptz', nullable: true })
  lastSuccessfulSync?: Date;

  @Property({ type: 'timestamptz', default: 'now()' })
  createdAt: Date = new Date();

  @Property({ type: 'timestamptz', onUpdate: () => new Date() })
  updatedAt: Date = new Date();
}
```

## Services Architecture

### Core Services

<CardGroup cols={2}>
  <Card title="Listing Service" icon="list">
    CRUD for Listing entities - create from unit, update, archive
  </Card>
  <Card title="Syndication Service" icon="hub">
    Hub orchestrator for portal operations
  </Card>
  <Card title="Listing Sync Service" icon="sync">
    State machine and CRUD for portal sync records
  </Card>
  <Card title="Portal Configuration Service" icon="gear">
    CRUD for organization portal configurations
  </Card>
</CardGroup>

### Specialized Services

<Tabs>
  <Tab title="Lead Processing">
    - **Portal Lead Worker Service** - pg-boss queue consumer for lead ingestion
    - **Bayut Lead Poller Service** - @Cron every 15 min, polls Bayut Pull API
    - **Bayut Lead Parser Service** - Normalizes 7 lead types × 3 targets into common shape
  </Tab>
  <Tab title="Validation & Media">
    - **Portal Validation Service** - Per-portal field validation
    - **Listing Image Service** - Image process-on-publish (validate + auto-fix + re-upload)
    - **Feed Cache Service** - Redis-backed XML feed cache
  </Tab>
  <Tab title="Property Finder Specific">
    - **PF Token Service** - JWT lifecycle management
    - **PF Location Mapping Service** - Location ID mapping
    - **PF Agent Mapping Service** - Agent ID mapping
    - **PF Compliance Service** - DLD/ADREC checks
    - **PF Credit Service** - Credit balance + per-listing publish price checks
    - **PF Webhook Subscription Service** - Webhook management
  </Tab>
  <Tab title="Monitoring & Maintenance">
    - **API Key Expiration Check Service** - Daily cron for PF API key expiry alerting
    - **Syndication Worker Service** - pg-boss worker wiring for listing sync jobs
    - **Sync Reconciliation Service** - Daily reconciliation + missed-lead polling fallback
  </Tab>
</Tabs>

## Portal Adapters

### Adapter Interfaces

<CodeGroup>
```typescript push-portal-adapter.interface.ts
export interface PushPortalAdapter {
  publishListing(listing: Listing, config: PortalConfiguration): Promise<string>;
  updateListing(listing: Listing, config: PortalConfiguration): Promise<void>;
  unpublishListing(portalListingId: string, config: PortalConfiguration): Promise<void>;
  validateCredentials(config: PortalConfiguration): Promise<boolean>;
}
```

```typescript feed-portal-adapter.interface.ts
export interface FeedPortalAdapter {
  generateListingXml(listings: Listing[]): Promise<string>;
  validateFeedUrl(feedUrl: string, feedSecret: string): boolean;
}
```
</CodeGroup>

### Implementation

<Steps>
  <Step title="Property Finder Adapter">
    REST push adapter implementing `PushPortalAdapter` interface for real-time listing syndication
  </Step>
  <Step title="Bayut/dubizzle Feed Adapter">
    Unified XML feed adapter implementing `FeedPortalAdapter` interface for both Bayut and dubizzle portals
  </Step>
</Steps>

## Controllers

### API Endpoints

<AccordionGroup>
  <Accordion title="Listing Management">
    **Listing Controller** - `/listings`
    - `POST /listings/from-unit/:unitId` - Create listing from inventory unit
    - `PUT /listings/:id` - Update listing content
    - `POST /listings/:id/validate` - Validate listing for portal requirements
    - `GET /listings` - List listings with filters
  </Accordion>

  <Accordion title="Portal Configuration">
    **Portal Configuration Controller** - `/portal-config`
    - `GET /portal-config` - Get organization portal configurations
    - `PUT /portal-config/:portal` - Update portal configuration
    - `POST /portal-config/:portal/test` - Test portal credentials
  </Accordion>

  <Accordion title="Sync Management">
    **Listing Sync Controller** - `/listing-sync`
    - `POST /listings/:id/sync/:portal/publish` - Publish to portal
    - `POST /listings/:id/sync/:portal/unpublish` - Remove from portal
    - `POST /listings/:id/sync/:portal/retry` - Retry failed sync
    - `GET /listings/:id/sync` - Get sync status across all portals
  </Accordion>

  <Accordion title="Public Endpoints">
    **Portal Feed Controller** - `/portal-feed`
    - `GET /portal-feed/:orgId/bayut-dubizzle` - Public XML feed endpoint

    **Portal Webhook Controller** - `/portal-webhook`
    - `POST /portal-webhook/property-finder` - Property Finder webhook receiver
  </Accordion>

  <Accordion title="Dashboard">
    **Portal Dashboard Controller** - `/portal-dashboard`
    - `GET /portal-dashboard/stats` - Portal performance statistics
    - `GET /portal-dashboard/listings` - Listings overview across portals
    - `GET /portal-dashboard/leads` - Lead ingestion summary
  </Accordion>
</AccordionGroup>

## Queue Jobs and Workers

### Job Types

The portal syndication module uses **pg-boss** for async job processing:

<Tabs>
  <Tab title="Listing Sync Jobs">
    ```typescript
    // Queue names
    const QUEUE_NAMES = {
      LISTING_SYNC: 'listing-sync',
      PORTAL_LEAD_INGESTION: 'portal-lead-ingestion',
      IMAGE_PROCESSING: 'image-processing',
    };

    // Job payloads
    interface ListingSyncJob {
      listingId: string;
      portal: Portal;
      action: 'publish' | 'update' | 'unpublish';
      retryCount?: number;
    }
    ```
  </Tab>
  <Tab title="Lead Ingestion Jobs">
    ```typescript
    interface PortalLeadIngestionJob {
      portal: Portal;
      leadData: any; // Raw portal lead data
      organizationId: string;
      source: 'webhook' | 'poll';
    }
    ```
  </Tab>
  <Tab title="Image Processing Jobs">
    ```typescript
    interface ImageProcessingJob {
      listingId: string;
      imageUrls: string[];
      action: 'validate_and_process';
    }
    ```
  </Tab>
</Tabs>

### Worker Configuration

<Steps>
  <Step title="Job Queue Setup">
    Configure pg-boss with appropriate retry policies and error handling
  </Step>
  <Step title="Worker Registration">
    Register workers in the syndication worker service for each job type
  </Step>
  <Step title="Error Handling">
    Implement exponential backoff and dead letter queue handling
  </Step>
  <Step title="Monitoring">
    Set up job monitoring and alerting for failed jobs
  </Step>
</Steps>

## Lead Ingestion System

### Property Finder Lead Ingestion

<Warning>
Property Finder uses **webhooks** as the primary lead delivery method with **REST API polling** as a 15-minute fallback.
</Warning>

<Steps>
  <Step title="Webhook Reception">
    Receive lead webhooks at `/portal-webhook/property-finder` with HMAC signature verification
  </Step>
  <Step title="Queue Processing">
    Queue lead data via pg-boss for async processing
  </Step>
  <Step title="Lead Conversion">
    Convert portal lead data to CRM `Lead` entity with deduplication
  </Step>
  <Step title="Fallback Polling">
    Poll REST API every 15 minutes for any missed webhook deliveries
  </Step>
</Steps>

### Bayut/dubizzle Lead Ingestion

<Info>
Bayut and dubizzle share a single API endpoint with the same Bearer token. The `source` field in responses determines the portal origin.
</Info>

<Steps>
  <Step title="Scheduled Polling">
    Run @Cron job every 15 minutes to poll the unified Bayut/dubizzle API
  </Step>
  <Step title="Lead Parsing">
    Parse 7 different lead types across 3 target types into normalized format
  </Step>
  <Step title="Queue Processing">
    Queue normalized lead data for CRM conversion
  </Step>
  <Step title="Timestamp Tracking">
    Update `lastLeadPoll` timestamp for incremental polling
  </Step>
</Steps>

## Data Validation and Security

### Field Validation

<Tabs>
  <Tab title="Portal-Specific Validation">
    Each portal has different requirements:
    - **Property Finder**: DLD permit numbers, category/location IDs, minimum image requirements
    - **Bayut**: Location paths, amenity IDs, Arabic descriptions
    - **dubizzle**: Similar to Bayut but with different validation rules
  </Tab>
  <Tab title="Image Processing">
    - Validate image URLs are accessible
    - Check image dimensions and file sizes
    - Auto-fix common image issues
    - Re-upload to CDN if necessary
  </Tab>
  <Tab title="Compliance Checks">
    - DLD/ADREC permit validation for Property Finder
    - Price range validation per portal
    - Required field validation per portal
  </Tab>
</Tabs>

### Security Measures

<Steps>
  <Step title="Credential Encryption">
    All API keys and secrets are encrypted at the application layer before database storage
  </Step>
  <Step title="HMAC Verification">
    Webhook payloads are verified using HMAC signatures
  </Step>
  <Step title="Feed URL Security">
    Bayut/dubizzle feed URLs include HMAC tokens for secure access
  </Step>
  <Step title="API Key Monitoring">
    Daily monitoring of Property Finder API key expiration with automated alerts
  </Step>
</Steps>

## Error Handling and Monitoring

### Retry Logic

<AccordionGroup>
  <Accordion title="Exponential Backoff">
    Failed sync jobs use exponential backoff with maximum retry limits:
    - Initial delay: 1 minute
    - Maximum delay: 60 minutes
    - Maximum retries: 5 attempts
  </Accordion>
  
  <Accordion title="Status Transitions">
    ```
    DRAFT → PENDING → AWAITING_CONFIRMATION → PUBLISHED
                ↓
              ERROR (after retries)
                ↓
            INSUFFICIENT_CREDITS (PF specific)
    ```
  </Accordion>
  
  <Accordion title="Error Categories">
    - **Validation errors**: Immediate failure, no retry
    - **API errors**: Retry with backoff
    - **Network errors**: Retry with backoff
    - **Credit errors**: Stop retrying, notify user
  </Accordion>
</AccordionGroup>

### Reconciliation Services

<Steps>
  <Step title="Daily Sync Reconciliation">
    Compare CRM listing states with portal states to detect discrepancies
  </Step>
  <Step title="Missed Lead Detection">
    Check for leads that should have been received but weren't
  </Step>
  <Step title="API Key Expiration Alerts">
    Send notifications 30, 7, and 1 days before Property Finder API keys expire
  </Step>
  <Step title="Performance Monitoring">
    Track sync success rates, lead ingestion rates, and error frequencies
  </Step>
</Steps>

<Note>
The reconciliation system runs daily and helps maintain data consistency across all integrated portals while providing early warning of potential issues.
</Note>