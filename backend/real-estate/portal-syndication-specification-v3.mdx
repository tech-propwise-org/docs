---
title: "Portal Syndication Specification v3.0"
description: "Complete architectural specification for the Portal Syndication Module with three-tier architecture (InventoryUnit → Listing → ListingPortalSync) and integration with Property Finder, Bayut, and dubizzle portals."
---

# Portal Syndication Specification v3.0

<Info>
**Version:** 3.0 — Architectural revision: introduced `Listing` entity as the marketing layer between `InventoryUnit` (inventory) and `ListingPortalSync` (per-portal state). Consolidated from system design plan, PF API guide, Bayut/dubizzle XML guide, and gap analysis.

**Status:** Authoritative design document. The `.cursor/plans/portal_syndication_system_design` plan tracks implementation progress against this spec.
</Info>

## Module Overview

The Portal Syndication Module allows real estate agents to publish property listings to three UAE property portals directly from PropWise CRM, and automatically receive leads back into the CRM pipeline.

### Three-Tier Architecture

```
InventoryUnit  →  Listing  →  ListingPortalSync
  (inventory)     (marketing)   (per-portal state)
```

- **`InventoryUnit`** — what the unit *is* (rooms, area, price, physical attributes). Unchanged by portal syndication logic.
- **`Listing`** — how the unit is *marketed* (title, descriptions, permit number, portal classifications, marketing media). Created by an agent from a `InventoryUnit`.
- **`ListingPortalSync`** — where the listing is *published* and its current state on each portal.

<Note>
This separation ensures `InventoryUnit` stays a clean inventory record and the `Listing` layer can eventually support off-plan units (`refUnitId`) without any structural change to the sync system.
</Note>

### Integration Model Per Portal

| Portal | Listing Syndication | Lead Ingestion | Listing Timing |
|---|---|---|---|
| Property Finder | REST API Push (JSON) | Webhook push (primary) + REST poll fallback (15 min) | Real-time (seconds) |
| Bayut | XML Feed Pull (unified) | Pull API polling — scheduled every 15 min | 30 min – 2 hr delay |
| dubizzle | XML Feed Pull (same as Bayut) | Pull API polling — same API + endpoint as Bayut | 30 min – 2 hr delay |

<Warning>
**Bayut / dubizzle lead ingestion note:** Bayut and dubizzle share one API endpoint and one Bearer token (per agency). The `source` field in each lead response (`"bayut"` or `"dubizzle"`) determines which `LeadSource` enum value is used when the CRM lead is created. The Bearer token is stored encrypted in the existing `apiKey` field of the Bayut `PortalConfiguration` row — no new credential columns are needed.
</Warning>

### Data Flow Rules

- Listings flow **one direction only**: CRM → portals (CRM always wins)
- Leads flow **one direction only**: portals → CRM
- Portal data **never** overwrites CRM data
- `Listing` is the **single source of truth** for listing (marketing) content
- `InventoryUnit` is the **single source of truth** for unit inventory data

### Module Location

```
src/modules/real-estate/portal-syndication/
```

Imported in `src/modules/real-estate/real-estate.module.ts`.

## As-Built Implementation Status

This section reconciles the spec with the shipped implementation. Where the spec and the build diverge, **the build below is authoritative**.

### Phase A — Bayut/dubizzle outbound (XML feed) — BUILT

<Tabs>
<Tab title="Self-contained Listing">
Every field any portal needs now lives on `Listing` (snapshotted from the unit in linked mode via `ListingService.copyUnitToListing`, or entered directly in manual mode). Adapters + `PortalValidationService` read ONLY `listing.X` — never `listing.inventoryUnit.X`. 

Key features:
- `inventoryUnit` FK is **nullable** (manual listings have none)
- New `ListingPurpose` enum (`Sale`/`Rent`)
- New columns added by `Migration20260531120000_SelfContainedListingFields`
</Tab>

<Tab title="Creation Modes">
Two creation modes converge on `ListingService.create(dto, userId, orgId)`:
- **Linked mode**: snapshots then applies DTO overrides
- **Manual mode**: direct entry without unit reference
- `refreshFromUnit`: re-pulls snapshot fields while preserving marketing content + agent overrides
</Tab>
</Tabs>

### Centralized Value Transforms

Located in `src/modules/shared/portal-value-map.ts`:

<CodeGroup>
```typescript Purpose Mapping
purposeToBayut(purpose: ListingPurpose): string
purposeToPfPriceType(purpose: ListingPurpose): string
```

```typescript Property Features
furnishedToBayut(furnished: boolean): string
furnishedToPf(furnished: boolean): string
bedroomsToBayut(bedrooms: number): string
bedroomsToPf(bedrooms: number): number
bathroomsToBayut(bathrooms: number): string
bathroomsToPf(bathrooms: number): number
```

```typescript Location & Compliance
emirateToPfCompliance(emirate: string): string
emirateToPfUaeEmirate(emirate: string): string
finishingToPf(finishing: string): string
rentalPeriodToBayut(period: string): string
```
</CodeGroup>

### BayutDubizzleFeedAdapter

<Steps>
<Step title="XML Feed Generation">
Uses `BayutDubizzleFeedAdapter` with CDATA XML serializer (`utils/bayut-xml.serializer.ts`)
</Step>

<Step title="Property Reference Format">
`Property_Ref_No = UNIT-{orgShortCode}-{listing.id}` with null-`orgShortCode` fallback
</Step>

<Step title="Public Feed Endpoint">
`GET /portal-syndication/feeds/:orgId?token=` (`@PublicEndpoint`, `PortalFeedController`)
</Step>

<Step title="Live Feed Generation">
Built via `executeWithBypass`, includes:
- Published rows as `Property_Status=live`
- Recently-removed rows as `deleted` for ≥48h (F2) for prompt portal delisting
</Step>
</Steps>

### Sync State Machine

<Note>
`DRAFT → PUBLISHED` ADDED to `VALID_TRANSITIONS` for feed portals (mirrors the existing `REMOVED → PUBLISHED`). `PENDING → PUBLISHED` remains invalid.
</Note>

### Publish Authorization

<AccordionGroup>
<Accordion title="Gate A - Permission Check">
`SyndicationService.publish` checks `real_estate.listing.publish` permission:
- **With permission**: Direct publish to target portals
- **Without permission**: Listing goes to `ListingStatus.PENDING_APPROVAL`
- Requires `real_estate.manage` user to approve/reject via `POST /:listingId/approve|reject`
</Accordion>

<Accordion title="Approval Workflow">
**Reject Process:**
- Moves listing to `ListingStatus.REJECTED`
- Persists approver's note (`rejectionReason` + `rejectedAt`/`rejectedBy`)
- Submitter can edit + resubmit or delete

**Approve Process:**
- Honors submitter's publish intent (`publishOnApproval`)
- Auto-publishes to enabled portal targets (→ ACTIVE)
- Stamps `Listing.approvedAt` / `approvedBy` (set-once, never cleared)
</Accordion>

<Accordion title="Owner Self-Manage Bypass">
Post-approval, the listing **owner** can publish/unpublish/toggle portals directly:
- Applies when `approvedAt != null && isOwner`
- Skips Gate A for approved listings
- Owner defined as: publisher (`createdBy`) / `agent` / linked-unit `unitManager`
</Accordion>
</AccordionGroup>

### Listing Approval Notifications

<CardGroup cols={2}>
<Card title="Submit for Approval" icon="paper-plane">
`LISTING_APPROVAL_REQUESTED` to every `real_estate.manage` approver
</Card>
<Card title="Approve" icon="check-circle">
`LISTING_APPROVED` to publisher with `published` status indicator
</Card>
<Card title="Reject" icon="x-circle">
`LISTING_REJECTED` to publisher with rejection reason
</Card>
<Card title="Delete" icon="trash">
`LISTING_DELETED` to publisher (only when deleter ≠ publisher)
</Card>
</CardGroup>

### Inventory Cascade (User Choice on Delete)

<Steps>
<Step title="Delete Modal Choice">
User selects behavior via `removeLinkedListings` flag in delete modal
</Step>

<Step title="Remove Linked Listings (Default)">
`removeLinkedListings = true`:
- Remove unit's listings from all portals
- Archive listings with audit attribution
</Step>

<Step title="Keep Listings Independent">
`removeLinkedListings = false`:
- Sever unit link (`inventoryUnit = null`)
- Convert to self-contained manual listings
- User can continue editing/publishing
</Step>

<Step title="Event-Driven Processing">
Handled via `inventory-unit.deleted` event to avoid circular dependencies
</Step>
</Steps>

## Phase A.5 — Unified Inbound Lead Capture — BUILT

### Lead Capture Module

New module `src/modules/crm/lead-capture/` provides:

<Tabs>
<Tab title="Core Services">
- `LeadCaptureService.capture()` - Main capture orchestration
- `CapturedLeadInput` contract - Standardized input format
- `LeadCaptureSourceRegistry` - Source registration system
- `CapturedLead` idempotency ledger - Duplicate prevention
</Tab>

<Tab title="Processing Queue">
- Source-agnostic `lead-ingestion` pg-boss queue
- `LeadIngestionWorker` for async processing
- Org-default `LeadCaptureSettings` configuration
</Tab>
</Tabs>

### Bayut Lead Integration

<Steps>
<Step title="Lead Parsing">
`BayutLeadParserService` handles 7 lead shapes → `NormalizedBayutLead`
</Step>

<Step title="Lead Capture Adapter">
`BayutLeadCaptureAdapter` processes normalized leads into CRM format
</Step>

<Step title="Polling Service">
`BayutLeadPollerService` runs every 15 minutes:
- Selects Bayut configs with `leadIngestionEnabled = true` + token
- Decrypts Bearer token from `PortalConfiguration.apiKey`
- Polls 7 API combinations
- Filters dubizzle leads based on dubizzle config settings
- Enqueues to `lead-ingestion` queue
</Step>

<Step title="Error Handling">
On 401 authentication errors, does NOT advance `lastLeadPollAt` timestamp
</Step>
</Steps>

## Phase B — Property Finder (REST Push) — BUILT

### Core Services

<AccordionGroup>
<Accordion title="Authentication & Caching">
- **`PfTokenService`**: 30-min token cache with invalidate-on-401
- **`PfLocationMappingService`**: 24h location data cache
- **`PfAgentMappingService`**: 24h agent mapping cache + refresh functionality
</Accordion>

<Accordion title="Compliance & Media">
- **`PfComplianceService`**: Handles PF compliance requirements
- **`PfCreditService`**: Manages PF credit system
- **`ListingImageService`**: Sharp validation/auto-fix with `processedMedia` cache
</Accordion>

<Accordion title="Syndication & Webhooks">
- **`PropertyFinderAdapter`**: 6-step publish process
- **`PfWebhookSubscriptionService`**: Webhook management
- **`PortalWebhookController`**: HMAC verification over raw body
- **`PfSyndicationWorker`**: Handles `pf-syndication` queue
</Accordion>

<Accordion title="Monitoring & Reconciliation">
- **`SyncReconciliationService`**: Scheduled reconciliation cron job
- **`ApiKeyExpirationCheckService`**: Token expiration monitoring
</Accordion>
</AccordionGroup>

### Technical Implementation

<CodeGroup>
```typescript Configuration
// app.propertyFinder.apiBaseUrl configuration
// All PF/Bayut HTTP uses plain axios (codebase convention)
```

```typescript Lead Processing
// PfLeadCaptureAdapter integrates with unified lead capture system
// Webhook-first approach with polling fallback every 15 minutes
```
</CodeGroup>

<Warning>
Property Finder contract fixes have been applied and verified against PF_API.md specifications.
</Warning>

## Portal Integration Summary

<Tabs>
<Tab title="Property Finder">
- **Method**: REST API Push (JSON)
- **Authentication**: OAuth token with refresh
- **Lead Ingestion**: Webhook primary, polling fallback
- **Timing**: Real-time (seconds)
- **Status**: Phase B - BUILT
</Tab>

<Tab title="Bayut">
- **Method**: XML Feed Pull
- **Authentication**: API token
- **Lead Ingestion**: Pull API every 15 minutes
- **Timing**: 30 min – 2 hr delay
- **Status**: Phase A - BUILT
</Tab>

<Tab title="dubizzle">
- **Method**: XML Feed Pull (shared with Bayut)
- **Authentication**: Shared Bayut token
- **Lead Ingestion**: Shared Bayut API endpoint
- **Timing**: 30 min – 2 hr delay
- **Status**: Phase A - BUILT
</Tab>
</Tabs>

<Check>
All three portal integrations are now fully operational with comprehensive lead capture and listing syndication capabilities.
</Check>