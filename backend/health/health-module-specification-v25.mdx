---
title: "Health Module Specification v25"
description: "Complete specification for the health monitoring module including liveness/readiness probes, data integrity auditing, and PgBoss monitoring"
---

## Overview

The health module provides comprehensive application health monitoring through standardized health check endpoints. Built on NestJS Terminus, it implements a two-endpoint strategy distinguishing between liveness and readiness probes for container orchestration platforms like Kubernetes.

<Note>
The module monitors critical application dependencies including database connectivity, memory usage, and background job queue status through custom health indicators.
</Note>

The module is registered in `app.module.ts` and provides dedicated health check endpoints separate from the main application controller.

## Architecture

### Design Decisions

<CardGroup cols={2}>
  <Card title="Two-endpoint Strategy" icon="heart-pulse">
    Separate liveness (`/health/live`) and readiness (`/health/ready`) endpoints following cloud-native best practices
  </Card>
  <Card title="Custom Health Indicators" icon="stethoscope">
    Extended Terminus framework with PgBoss job queue monitoring
  </Card>
  <Card title="Public Endpoints" icon="globe">
    Health checks bypass authentication for load balancer and orchestration access
  </Card>
  <Card title="Rate Limited" icon="shield">
    Health checks use the app-wide throttler to reduce abuse risk while remaining reachable by probes
  </Card>
</CardGroup>

### Dependencies

The health module relies on several core dependencies:

- `@nestjs/terminus` - Core health check framework
- `PgBossService` - Background job queue monitoring  
- `MikroORM` - Database connectivity checks
- Memory monitoring for resource usage

### Data Integrity Audit Enhancements

<Info>
The `DataIntegrityAuditService` now includes `INFORMATIONAL_COUNT_FIELDS` alongside orphan and creator reference counts.
</Info>

**Informational count tracking includes:**
- `stakeholdersWithoutActiveUserOrgRoleCount` - tracks stakeholder rows where the user no longer has an active UserOrgRole (expected after org removal)
- `totalInformationalCounts` - sum of membership drift metrics (not unhealthy orphans)

<Warning>
Classification requirement: new audit counts must be explicitly categorized as orphan, creator reference, or informational
</Warning>

## API Endpoints

### GET /health/live

**Purpose:** Liveness probe - indicates if the process can handle requests

```typescript
@Get('live')
@PublicEndpoint()
@HealthCheck()
liveness(): Promise<HealthCheckResult>
```

**Response:**

```json
{
  "status": "ok",
  "info": {},
  "error": {},
  "details": {}
}
```

**Use Cases:**
- Container orchestration restart decisions
- Load balancer routing decisions
- Basic process health verification

### GET /health/ready

**Purpose:** Readiness probe - indicates if the application can serve traffic

```typescript
@Get('ready')
@PublicEndpoint()
@HealthCheck()
readiness(): Promise<HealthCheckResult>
```

<Tabs>
  <Tab title="Health Checks Performed">
    - Database connectivity (MikroORM)
    - PgBoss job queue status
    - Search provider (`SearchProviderHealthIndicator` — Phase 1A)
  </Tab>
  <Tab title="Response Format">
    ```json
    {
      "status": "ok|error",
      "info": {
        "database": { "status": "up" },
        "pgboss": { "status": "up" },
        "search": { "status": "up" }
      },
      "error": {},
      "details": {
        "database": { "status": "up" },
        "pgboss": { "status": "up" },
        "search": { "status": "up" }
      }
    }
    ```
  </Tab>
</Tabs>

## Scheduled Data-Integrity Audit

**Purpose:** Detect orphan rows resulting from soft-delete cascade gaps without exposing an unauthenticated, high-cost HTTP route.

<Warning>
The audit is **not wired into `/health/ready`** and has no public `/health/data-integrity` endpoint.
</Warning>

The daily queued job runs at `30 3 * * *` by default (override via `DATA_INTEGRITY_AUDIT_CRON`) through `DataIntegrityAuditHandler`. It logs every run, emits a warn-level application log when unhealthy orphans are found, and sends a grouped Sentry error event when `totalOrphans > 0`.

### Audit Checks Performed

The audit runs ~74 count queries grouped into module buckets. Each bucket emits parallel `Promise.all` queries.

<AccordionGroup>
  <Accordion title="Core (12 checks)">
    Lead/Deal/Contact/Company/Listing with deleted persons/orgs/stages/creators (the original 12 fixed relations from `SOFT_DELETE_FILTER_STANDARD.md` §6).
  </Accordion>
  
  <Accordion title="Person & Company sub-entities (10 checks)">
    - Person.organization, Person.createdBy
    - Person{Email,Phone,Address,Channel}.person
    - PersonNotDuplicate (either side)
    - Company{Email,Phone,Address}.company
  </Accordion>
  
  <Accordion title="Activity stack + Reminder (10 checks)">
    - Activity, Note, CallLog, EmailLog, SmsLog, Meeting, SiteVisit organization refs
    - Activity.performedBy and Note.createdBy creator refs
    - Reminder.user
  </Accordion>
  
  <Accordion title="Tasks + Events + Tag (10 checks)">
    - Tag.organization
    - Task.organization, Task.createdBy
    - TaskComment.task, TaskAssignee.task, TaskTag.tag
    - Event.organization, Event.createdBy
    - EventInvitee.event, EventTag.event
  </Accordion>
  
  <Accordion title="Additional checks (31 total)">
    Including stakeholder transfers, RBAC junctions, auth sessions, distribution rules, escalation tracking, and notifications.
  </Accordion>
</AccordionGroup>

<Tip>
Soft-deleted creator refs are informational only and do **not** contribute to `totalOrphans`. User churn is normal, and historical records should still display the deleted creator when needed.
</Tip>

### Why Separate from Readiness

Orphan rows do not affect whether the instance can serve traffic — they are a data-integrity concern, not a service-availability concern. Wiring this into the readiness probe would block rollouts unrelated to the symptom.

### Companion Infrastructure

<Steps>
  <Step title="Sentry Alert Event">
    Emitted at `error` level with tags `subsystem=health`, `check=data-integrity`, `queue=data-integrity-audit`
  </Step>
  <Step title="One-off Repair Script">
    `pnpm repair:orphaned-lead-persons --dry-run` then without the flag — re-points orphaned leads to their final merge survivor
  </Step>
  <Step title="Documentation References">
    See `QUERY_OPTIMIZATION_PATTERNS.md` §5.5 for root-cause mechanics and `CRM_MODULE_SPECIFICATION.md` §2.1.3 for mergePersons contract
  </Step>
</Steps>

## Health Indicators

### PgBossHealthIndicator

Custom health indicator for the PgBoss background job service. Uses the `HealthIndicatorService` API (NestJS Terminus v11+) instead of the deprecated `HealthIndicator` base class.

```typescript
@Injectable()
export class PgBossHealthIndicator {
  constructor(
    @Inject(QUEUE_SERVICE) private readonly queueService: IQueueService,
    private readonly healthIndicatorService: HealthIndicatorService,
  ) {}

  async isHealthy(key: string) {
    const indicator = this.healthIndicatorService.check(key);
    const result = await this.queueService.checkHealth();
    if (!result.healthy) {
      return indicator.down({ reason: result.reason ?? 'queue backend unhealthy' });
    }
    return indicator.up({ latencyMs: result.latencyMs });
  }
}
```

#### Implementation Notes

<Note>
Uses `HealthIndicatorService.check(key)` with `.up()` / `.down()` instead of the legacy `extends HealthIndicator` + `HealthCheckError` pattern.
</Note>

Delegates to `IQueueService.checkHealth()` for an **active** liveness probe rather than reading the `isStarted` boolean.

#### Why Active Probing Over isStarted Flag

The previous implementation reported healthy whenever `isStarted === true`. That flag is set once after `boss.start()` resolves and only flips back on `stop()` — so if pg-boss loses its connection or its supervisor loop stalls after startup, the flag stays `true` and a **dead queue keeps reporting healthy**.

`checkHealth()` closes this gap by round-tripping a trivial `SELECT 1` through pg-boss's own connection pool (bounded by `QUEUE_HEALTH_PROBE_TIMEOUT_MS`, default 2000ms).

**Down states cover:**

| Condition | Reason returned |
|-----------|----------------|
| `boss.start()` not yet complete | `boss.start() has not completed` |
| Probe query rejects (dropped connection) | the underlying driver error message |
| Probe exceeds timeout (stalled/hung queue) | `queue health probe timed out after <n>ms` |

<Warning>
**Pool coverage:** the probe uses pg-boss's connection (the superuser `DATABASE_URL` pool), which is distinct from the ORM connection (`RUNTIME_DATABASE_URL`) that the readiness `database` check pings. The two checks therefore exercise different pools.
</Warning>

### SearchProviderHealthIndicator

Custom readiness indicator for the active `SearchProvider` implementation (`PostgresSearchProvider` in Phase 1). Mirrors the `PgBossHealthIndicator` pattern: a thin Terminus wrapper in `src/modules/health/` that delegates to the provider interface so `HealthModule` does not import `SearchModule` internals beyond the `SEARCH_PROVIDER` token.

#### Contract (SearchProvider.healthCheck())

| Phase | Behavior |
|-------|----------|
| **1B Steps 1–2 (current)** | Probes `information_schema` for `search_document` table existence via `executeReadOnlyWithBypass`. Returns `up` when the table is reachable; read path (`search()`) and write paths (`upsert` / `delete` + ACL diff) are live. `reindexOrg` still throws `SearchProviderNotImplementedError`. |
| **1B+ (target)** | Optional row-count sanity, pg-boss `search.reindex` queue depth threshold, and report `down` when indexer backlog exceeds SLO. |